# Plan: no-files fixture — implements #101

Uses `work-issue/scripts/check-plan.py` conceptually, but the task below
never records which files it owns, and the plan carries no `## Waves`
table to carry ownership instead.

### T1 `orphan-task`

This task never says which files it owns.
