---
id: T-006
title: Scope-signal eval fixture
spec: .agent-guild/state/spec.md
clauses: [C-1, C-3, C-7, C-11]
executor: worker-craft
executor_model: opus
checker: checker-judgment
check_method: >-
  C-1: .agent-guild/scripts/check-diff-scope.py
  _maintenance/inbox-to-memory/EVALS.md _maintenance/inbox-to-memory/eval-scope.sh
  tests/fixtures/inbox-to-memory/evals/ — the script reads the whole working tree
  and passes when every changed path is inside the allowlist. Sibling tasks write
  into that same allowlist, so their files in the diff are a pass.
  C-3 (fixture half; the citation half is checked again on T-004): checker-judgment:
  confirm the delivered fixture is opted in — `_inbox/` plus `_memory/` — and that
  its project subdirectories are themselves opted in where the tier requires it.
  C-7 (fixture half; T-004 checks the document against this fixture, including
  that each scenario's expected token matches the input it is paired with, and
  that half is not delegated back here): checker-judgment: read each of the four
  inputs against references/scope-decisions.md as a whole, tiebreak at :21-25
  included, and confirm the reference routes it to the tier the input was built to
  elicit — project, client, journal, and one two-tier input resolving to the
  narrower token. Where the cross-project recurrence signal is used, confirm a
  second project directory exists and that its notes actually carry the subject.
  Fixture well-formedness, as the instrument for C-7's "signal is actually there",
  in two parts because one does not cover the other:
  (a) bash inbox-to-memory/scripts/lint-scope.sh
  tests/fixtures/inbox-to-memory/evals/<the delivered directory> — must exit 0,
  and run it over each opted-in subdirectory too.
  (b) Token grammar, by hand, because the lint does not do it on a v1 fixture:
  lint-scope.sh:421-424 puts only `schema: 2` files into the array its token
  checks iterate, so a v1 file's tokens are never read and the lint exits 0 on a
  token that does not exist. Verified by probe — `[memory candidate: journal]`,
  the exact trap this task's excerpt names, passes the lint on a v1 file. So
  extract this fixture's tokens the way the linter does, which is four steps and
  not two: skip `CLAUDE.md` and `README.md` (lint-scope.sh:418-419 excludes them,
  and this task ships a client-root `CLAUDE.md` from a template that carries bare
  `[memory candidate]` and `[topic]` illustrations — reading those as tokens would
  fail a correct fixture); for every other note take only the body `extract_body`
  yields (:208-216 — everything after the closing frontmatter `---`, stopping at
  `## Raw Content`, HTML comments stripped); strip `[[...]]` wiki links (:218-222);
  then read the bracketed tokens from what remains. `related: [extends::<id>]` in
  frontmatter, flow-style `topics:`/`tags:`, and brackets inside Raw Content are
  likewise legal and none is a token. Match what survives against the ten-row
  grammar table at references/machine-contracts.md whatever the file's schema,
  including the enumerated values inside a row's form.
  C-11: bash tests/inbox-to-memory-smoke.sh && bash tests/file-issue-smoke.sh &&
  bash tests/handoff-smoke.sh
status: complete
retries: 0
max_retries: 2
deps: [T-005]
escalations: []
artifacts:
  - tests/fixtures/inbox-to-memory/evals/scope-tiers/CLAUDE.md
  - tests/fixtures/inbox-to-memory/evals/scope-tiers/_inbox/2026-05-11-calderwood-governance-sync.md
  - tests/fixtures/inbox-to-memory/evals/scope-tiers/_memory/context/kestrel-batch-window-rWFFzufjXj.md
  - tests/fixtures/inbox-to-memory/evals/scope-tiers/notes/2026-03-05-calderwood-portfolio-review-ZKqcj5UDKS.md
  - tests/fixtures/inbox-to-memory/evals/scope-tiers/notes/2026-04-16-calderwood-kestrel-forum-FUItKUpxH6.md
  - tests/fixtures/inbox-to-memory/evals/scope-tiers/projects/claims-intake/_inbox/2026-05-07-fnol-without-policy-number.md
  - tests/fixtures/inbox-to-memory/evals/scope-tiers/projects/claims-intake/_inbox/2026-05-12-fieldnote-cutover-date.md
  - tests/fixtures/inbox-to-memory/evals/scope-tiers/projects/claims-intake/_memory/decisions/fieldnote-reads-policy-from-extract-yvOXPdadL8.md
  - tests/fixtures/inbox-to-memory/evals/scope-tiers/projects/claims-intake/notes/2026-02-19-claims-intake-discovery-9wCOavZA2T.md
  - tests/fixtures/inbox-to-memory/evals/scope-tiers/projects/claims-intake/notes/2026-04-14-claims-intake-integration-review-gyDyAWNuLt.md
  - tests/fixtures/inbox-to-memory/evals/scope-tiers/pursuits/underwriting-assist/_inbox/2026-05-08-discovery-retro-source-system-owner.md
  - tests/fixtures/inbox-to-memory/evals/scope-tiers/pursuits/underwriting-assist/_memory/context/lantern-scores-declined-book-yZCQWC3yhR.md
  - tests/fixtures/inbox-to-memory/evals/scope-tiers/pursuits/underwriting-assist/notes/2026-03-24-underwriting-assist-scoping-DibesvlEcG.md
  - tests/fixtures/inbox-to-memory/evals/scope-tiers/pursuits/underwriting-assist/notes/2026-04-28-underwriting-assist-data-walkthrough-oVYv2RYrA0.md
---

## Spec excerpt

Build one fixture for the inbox-to-memory eval suite: the scope that exercises **scope-tier proposal**, at all three tiers plus the tiebreak. You are building a fixture only. The doc that grades against it is a separate task; write no prose about it.

Own exactly one new directory tree under `tests/fixtures/inbox-to-memory/evals/`, named for what it holds. Sibling tasks are building other fixtures under that same path — do not touch theirs.

Read `inbox-to-memory/references/scope-decisions.md` in full before planting anything, and `SKILL.md` phases 1 and 3. This is the fixture where the layout matters: a project sits at `pursuits/<name>/` or `projects/<name>/` inside a client root that is itself opted in, and a client root is the directory containing those. Build a client root with at least two project directories under it.

**What to plant.** Four `_inbox/` inputs, each one that `references/scope-decisions.md` routes to a specific tier:

1. one the reference routes to **project** — a fact bound to a single project's deliverable, contract, or technical decision
2. one it routes to **client** — a stakeholder relationship, environmental fact, or operating rhythm holding across the client's projects
3. one it routes to **journal** — a pattern that generalizes beyond this client, or content in the reference's generalizing register
4. one carrying signals at **two tiers at once**, which Conflict Resolution at `scope-decisions.md:21-25` resolves to the narrower token

That fourth input is the one the whole scenario turns on. "Project is the floor" is the rule being tested, so it has to be a genuine conflict — an input a careless reader would route to client, that the tiebreak keeps at project.

**Read each input back against the signal table before calling it done.** An input mentioning a stakeholder's reporting line is a client-tier signal whatever you intended it to be, and an input that "generalizes" in tone without actually generalizing beyond this client is not a journal signal. The reference also says what never goes to journal: single-instance observations, anything bound to a specific person or deal, anything that would not make sense read 18 months later. A journal input that trips one of those is built wrong.

**If you use the recurrence signal** — "subject entity has already been referenced in 2+ projects' notes at this client" — the fixture has to actually hold those references, in distinct project directories, with the subject named in the notes there. It is the one signal in the table that is not readable off the input alone, and the one place a well-formed fixture can still grade nothing.

Where the four inputs live is your call, as long as each is somewhere the skill's phase 1 walk resolves to a scope that makes sense for it. Note that journal candidates take no scope token and route to the cross-client journal regardless of which scope the input was groomed in.

**Constraints.**

- `bash inbox-to-memory/scripts/lint-scope.sh <scope>` must exit 0 for the client root and for each opted-in project subdirectory. Run it on each before you finish.
- Check your tokens by hand as well, because the lint will not do it for you. Its token-grammar pass runs only over files carrying `schema: 2`; on a v1 file it reads no tokens at all and exits 0 on a token that does not exist. `[memory candidate: journal]` — the trap named below — passes the lint clean on a v1 file. Match every token against the ten-row grammar table at `references/machine-contracts.md` yourself. The rule is scoped to note bodies above `## Raw Content`; brackets in frontmatter, brackets inside raw content, and the illustrative `[memory candidate]` / `[topic]` examples the `CLAUDE.md` templates carry are all legal and none is a token.
- No `MEMORY.md`, `INDEX.md`, or `index.md` anywhere under a `_memory/` path. The smoke suite walks the whole fixtures tree for those.
- Every inline token must be a shape registered in the grammar table at `references/machine-contracts.md`. `[memory candidate: journal]` is not a token and never was — journal candidates carry no scope token.
- A client root wants a `CLAUDE.md` declaring its memory mode, since the mode decides which `memory_type` vocabulary applies. Follow the shape in `inbox-to-memory/assets/claude-md/`.
- Content should read like real consulting notes across two engagements at one client. The tier signals have to arise from the content, not from labels stuck on it.

Touch nothing outside your one new directory tree. Do not update `tests/fixtures/inbox-to-memory/README.md` — its staleness is a declared non-goal and editing it fails C-1. Do not commit.

## Rework diagnosis

## Courier comparison

```yaml
clause_types_sampled: mixed  # C-1,C-11, and well-formedness part (a) deterministic; C-3 close to mechanical; the crossing's whole weight was on C-7's fixture half, judgment
brief_framing: unknown
courier_outcome: pass
agreement_on_outcome: agree
disagreement_kind: null   # substantive | evidence_packet | null
unique_courier:
  defect: 0
  inference: 0
  evidence_quality: 0
  coverage: 0
unique_checker: 0
unique_checker_access_derived: 0   # of the above, needing repo access the lane cannot have
overlap: 1  # C-7's fixture half — vendor assigned all four unlabelled, reordered inputs to their intended tiers blind, including spotting the unflagged two-tier tiebreak case unprompted
changed_verdict: no
cost:
  wall_s: null
  tokens_in: null
  tokens_cached: null
  tokens_out: null
  brief_tokens: null
  vendor_calls: unknown
retrofit_note: >-
  No log/ directory survives in this archive, so cost fields are left null
  per rule 2 despite the prose citing "21,963 in / 2,044 out" — not
  reconstructed here. brief_framing unknown — briefs/T-006.md is a clause
  dump with no confirm/attack/open instruction surviving.
```

**Crossing outcome: pass.** Codex lane, `gpt-5.6-terra`, 21,963 in / 2,044 out. Ledger line appended. Identity validation held first try, same fix as T-002 and T-005.

**Cited clauses, by kind.** C-1, C-11, and part (a) of the well-formedness instrument are deterministic; they crossed as pre-run output and agree by construction. C-3 is close to mechanical. The crossing's whole weight was on C-7's fixture half, which is the most subjective judgment in this job.

**Findings only the courier raised: none.**

**Findings only the checker of record raised: none.**

**Overlap: C-7's fixture half, tested blind, which is the best evidence any crossing in this job has produced.** The brief gave the vendor the signal table, Conflict Resolution, the never-journal list, and all four inputs in full — with the inputs unlabelled, in randomized order, with no indication that any of them was a tiebreak case. It was asked to assign a tier from the reference alone.

It reproduced the intended split exactly: governance-sync to client, notice-without-policy to project, discovery-retro to journal, cutover-date to project. And it identified cutover-date as carrying signals at two tiers unprompted, then applied narrowest-wins to land on project. Nothing in the brief told it a two-tier input existed.

That matters more than the zero unique findings suggests. C-7 is the clause where a fixture can be word-perfect and still grade correct behavior as failure, because the routing lives in a nine-row table read against prose. Two independent readers reaching the same four-way assignment, one of them blind to the intended answer, is the strongest available evidence that the fixture routes as built rather than as labelled.

**For #34.** Zero unique findings, third crossing running. Running tally across four: one unique finding (T-001's C-13, an ambiguous clause), three confirmations. But the confirmations are not all worth the same — this one was a blind reproduction of a four-way judgment, which is evidence about the artifact rather than mere agreement about it. If #34 is weighing whether the lane earns its cost, the shape to record is that the crossing's value tracks how the brief is constructed, not just which clause is sampled. A blind classification bought real evidence for ~24k tokens; a "do you agree" crossing would have bought a nod.
