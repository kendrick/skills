---
id: T-002
title: Contradiction eval fixture
spec: .agent-guild/state/spec.md
clauses: [C-1, C-3, C-5, C-11]
executor: worker-craft
executor_model: opus
checker: checker-judgment
check_method: >-
  C-1: .agent-guild/scripts/check-diff-scope.py
  _maintenance/inbox-to-memory/EVALS.md _maintenance/inbox-to-memory/eval-scope.sh
  tests/fixtures/inbox-to-memory/evals/ — the script reads the whole working tree
  and passes when every changed path is inside the allowlist. Sibling tasks write
  into that same allowlist, so their files in the diff are a pass. What fails is a
  path outside it.
  C-3 (fixture half; the citation half is checked again on T-004): checker-judgment:
  confirm the delivered fixture is opted in — `_inbox/` plus `_memory/` — so the
  skill would operate on it at all.
  C-5 (fixture half; T-004 checks the document against this fixture and that half
  is not delegated back here): checker-judgment: confirm the fixture holds a
  `status: accepted` record and an `_inbox/` input that genuinely contradicts its
  claim rather than merely naming its subject, and that the record is one the
  amend outcome fits — right but incomplete, not simply wrong. Confirm any decoy
  records carry a status other than `accepted`.
  Fixture well-formedness, as the instrument for C-5's "would actually elicit the
  behavior", in two parts because one does not cover the other:
  (a) bash inbox-to-memory/scripts/lint-scope.sh
  tests/fixtures/inbox-to-memory/evals/<the delivered directory> — must exit 0.
  (b) Token grammar, by hand, because the lint does not do it on a v1 fixture:
  lint-scope.sh:421-424 puts only `schema: 2` files into the array its token
  checks iterate, so a v1 file's tokens are never read and the lint exits 0 on a
  token that does not exist. Verified by probe. So extract this fixture's tokens
  the way the linter does, which is four steps and not two: skip `CLAUDE.md` and
  `README.md` (lint-scope.sh:418-419 excludes them); for every other note take
  only the body `extract_body` yields (:208-216 — everything after the closing
  frontmatter `---`, stopping at `## Raw Content`, HTML comments stripped); strip
  `[[...]]` wiki links (:218-222); then read the bracketed tokens from what
  remains. That scoping is the check, not a detail: `related: [extends::<id>]` in
  frontmatter, flow-style `topics:`/`tags:`, and brackets inside Raw Content are
  all legal and none is a token, so a checker reading them as tokens fails a
  correct fixture. Match what survives against the ten-row grammar table at
  references/machine-contracts.md, whatever the file's schema, and report any
  shape that is not registered and any enumerated value outside its row's set.
  C-11: bash tests/inbox-to-memory-smoke.sh && bash tests/file-issue-smoke.sh &&
  bash tests/handoff-smoke.sh
status: complete
retries: 0
max_retries: 2
deps: [T-001]
escalations: []
artifacts:
  - tests/fixtures/inbox-to-memory/evals/contradiction-amend/_inbox/2026-04-21-118-service-desk-walkthrough.md
  - tests/fixtures/inbox-to-memory/evals/contradiction-amend/_memory/rules/refund-approval-code-ypAes5QNas.md
  - tests/fixtures/inbox-to-memory/evals/contradiction-amend/_memory/context/receipt-barcode-lookup-Qj2vTy_2pZ.md
  - tests/fixtures/inbox-to-memory/evals/contradiction-amend/_memory/decisions/self-checkout-refunds-DPtlHN4Ut4.md
  - tests/fixtures/inbox-to-memory/evals/contradiction-amend/notes/2026-02-24-refund-controls-workshop-vC8nfl6HWd.md
  - tests/fixtures/inbox-to-memory/evals/contradiction-amend/notes/2026-03-31-rowan-pilot-readiness-bxIfbmryhM.md
---

## Spec excerpt

Build one fixture for the inbox-to-memory eval suite: the scope that exercises **contradiction detection against accepted memory**. You are building a fixture only. The doc that grades against it is a separate task; write no prose about it.

Own exactly one new directory under `tests/fixtures/inbox-to-memory/evals/`, named for what it holds. Sibling tasks are building other fixtures under that same path — do not touch theirs, and do not build anything but the contradiction scope here. Keeping one behavior per fixture is deliberate: the skill's detection reads whole transcripts, so unrelated content planted in the same scope can silently trip or suppress a neighbouring scenario.

A fixture is an opted-in scope the skill can run in process mode: a directory holding `_inbox/` plus `_memory/`. Read `inbox-to-memory/SKILL.md` phases 1 through 3, phase 2.5 especially, before planting anything. The existing scopes under `tests/fixtures/inbox-to-memory/` (`old-only`, `mixed`) show the file shapes; match their conventions, including nanoid-suffixed filenames.

**What to plant.** One `_memory/` record with `status: accepted` making a specific, checkable claim, and one `_inbox/` input that contradicts that claim head-on.

The contradiction has to be real. An input that merely mentions the record's subject produces no flag, and a fixture built that way grades correct behavior as a failure. Only `accepted` records produce flags at all — a `proposed` record hasn't been agreed to yet and a `superseded` one is already known wrong — so if you plant decoy records to make detection non-trivial, give them one of those statuses and expect no flag from them.

The scenario built on this fixture will follow the contradiction through phase 5 sign-off into the **amend** outcome, so the record needs to be one amending makes sense for: right but incomplete, rather than flatly wrong. A record that supersession fits better than amendment is the wrong plant.

Phase 2.5 normalizes entities through the scope's alias table before any lookup, and reads at most five record bodies per input. Keep the scope small enough that the budget is not the reason detection succeeds or fails.

**Constraints.**

- `bash inbox-to-memory/scripts/lint-scope.sh <your fixture>` must exit 0. Run it before you finish.
- Check your tokens by hand as well, because the lint will not do it for you. Its token-grammar pass runs only over files carrying `schema: 2`; on a v1 file it reads no tokens at all and exits 0 on a token that does not exist. So a misspelled `[open question: ...]` or an invented `[memory candidate: journal]` ships green and silently disables the behavior the scenario grades. Match every inline token you write against the ten-row grammar table at `references/machine-contracts.md` yourself. The rule is scoped to note bodies above `## Raw Content` — brackets in frontmatter and brackets inside raw content are legal and are not tokens.
- No `MEMORY.md`, `INDEX.md`, or `index.md` anywhere under a `_memory/` path. The smoke suite walks the whole fixtures tree for those and fails on any hit.
- v1 files (no `schema` key) are legal forever and the lint never flags them, so v1 is a fine choice. If you write v2 frontmatter it has to be correct, in the key order at `SKILL.md`'s frontmatter quick reference.
- Content should read like real consulting notes. The skill's extraction heuristics need something to bite on, and a fixture of filler makes the eval it feeds unconvincing.

Touch nothing outside your one new directory. In particular, do not update `tests/fixtures/inbox-to-memory/README.md` — its staleness is a declared non-goal, and editing it fails C-1. Do not commit.

## Rework diagnosis

## Courier comparison

```yaml
repo: skills
job: 2026-08-08-issue-17
task: T-002
verdict_pair: [{of_record: T-002-judgment-r0.json, lane: T-002-judgment-r0-codex.json}]
clause_types_sampled: mixed  # C-1,C-11 deterministic cited pre-run; weight was on C-5's fixture half, judgment. C-3 not mentioned in the crossing's cited-clauses paragraph.
brief_framing: unknown
courier_outcome: pass
agreement_on_outcome: agree
disagreement_kind: null   # substantive | evidence_packet | null
unique_courier:
  defect: 0
  inference: 0
  evidence_quality: 0
  coverage: 0
unique_checker: 0
unique_checker_access_derived: 0   # of the above, needing repo access the lane cannot have
overlap: 1  # C-5's fixture half — independent three-way convergence (contradiction genuine, amend fits, supersede/dismiss closed)
changed_verdict: no
cost:
  wall_s: null
  tokens_in: null
  tokens_cached: null
  tokens_out: null
  brief_tokens: null
  vendor_calls: unknown
retrofit_note: >-
  No log/ directory survives in this archive, so cost fields are left null
  per rule 2 despite the prose citing "3s, 19,141 in / 874 out" — not
  reconstructed here. No positive record of more than one call for this
  task, unlike T-001's. brief_framing unknown — briefs/T-002.md is a clause
  dump with no confirm/attack/open instruction surviving.
```

**Crossing outcome: pass.** Codex lane, `gpt-5.6-terra`, 3s, 19,141 in / 874 out. Ledger line appended. The identity-validation failure that blocked T-001's crossing did not recur — the fix was to state the required `checker` and `model` values in the brief and instruct the vendor to echo them verbatim, and it worked first try. That is a workaround, not a repair; the lane still does not supply its own identity.

**Cited clauses, by kind.** C-1 and C-11 are deterministic script checks and crossed as pre-run output, so they agree by construction and count for nothing. The crossing's weight was on C-5's fixture half, a judgment rubric, which is the only live question on this task.

**Findings only the courier raised: none.**

**Findings only the checker of record raised: none.**

**Overlap: C-5's fixture half, and the convergence is worth more than the count suggests.** Both reached the same three-way call — contradiction genuine, amend fits, supersede and dismiss closed — and both did it by citing the same evidence without either seeing the other's reasoning. The courier was told neither how the checker of record ruled nor that the fixture had passed. Both landed on the accepted record's store-wide no-exceptions wording against the $912 desk credit, and both found the dismiss reading closed by the same plant in the prior note, the workshop where "refund" and "what the POS calls a refund" were used interchangeably and nobody separated them. Independent agreement on a judgment where three outcomes were available is stronger evidence than agreement on a binary.

**For #34.** Zero unique findings on this task, against one on T-001. Cost: 3s and ~20k tokens, an order of magnitude cheaper than T-001's crossing. The signal so far is that the lane earns its keep where a clause is ambiguous (T-001's C-13) and adds nothing where the artifact is unambiguous and the in-family checker did its job.
