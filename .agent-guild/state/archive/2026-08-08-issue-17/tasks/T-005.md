---
id: T-005
title: Unacknowledged-tension eval fixture
spec: .agent-guild/state/spec.md
clauses: [C-1, C-3, C-6, C-11]
executor: worker-craft
executor_model: opus
checker: checker-judgment
check_method: >-
  C-1: .agent-guild/scripts/check-diff-scope.py
  _maintenance/inbox-to-memory/EVALS.md _maintenance/inbox-to-memory/eval-scope.sh
  tests/fixtures/inbox-to-memory/evals/ — the script reads the whole working tree
  and passes when every changed path is inside the allowlist. Sibling tasks write
  into that same allowlist, so their files in the diff are a pass.
  C-3 (fixture half; the citation half is checked again on T-004): checker-judgment:
  confirm the delivered fixture is opted in — `_inbox/` plus `_memory/`.
  C-6 (fixture half; T-004 checks the document against this fixture, including
  that the doc's positive and negative rows are matched to the right slug, and
  that half is not delegated back here): checker-judgment: read the fixture
  against SKILL.md:122. Count each slug's occurrences in files under `notes/` —
  both must reach three or more, so the negative case turns on acknowledgement and
  not on the count. Then confirm the acknowledgement for the negative slug sits in
  the `_inbox/` transcript under grooming and nowhere else, and that no `_inbox/`
  transcript names the positive slug's question aloud. That last one is the
  interference check: the rule reads the transcript, not a designated file, so any
  passage naming the positive question silently kills the positive case.
  Fixture well-formedness, as the instrument for C-6's recurrence count, in two
  parts because one does not cover the other:
  (a) bash inbox-to-memory/scripts/lint-scope.sh
  tests/fixtures/inbox-to-memory/evals/<the delivered directory> — must exit 0.
  (b) Token grammar, by hand, because the lint does not do it on a v1 fixture:
  lint-scope.sh:421-424 puts only `schema: 2` files into the array its token
  checks iterate, so a v1 file's tokens are never read and the lint exits 0 on a
  token that does not exist. Verified by probe. This clause is the one that hole
  hurts most: a misspelled `[open-question: <slug>]` in a prior note is invisible
  to the recurrence grep, the count never reaches three, and the scenario grades
  nothing while every other check here passes. So extract this fixture's tokens
  the way the linter does, which is four steps and not two: skip `CLAUDE.md` and
  `README.md` (lint-scope.sh:418-419 excludes them); for every other note take
  only the body `extract_body` yields (:208-216 — everything after the closing
  frontmatter `---`, stopping at `## Raw Content`, HTML comments stripped); strip
  `[[...]]` wiki links (:218-222); then read the bracketed tokens from what
  remains. That scoping is the check, not a detail: `related: [extends::<id>]` in
  frontmatter, flow-style `topics:`/`tags:`, and brackets inside Raw Content are
  all legal and none is a token, so a checker reading them as tokens fails a
  correct fixture. Match what survives against the ten-row grammar table at
  references/machine-contracts.md whatever the file's schema, and confirm the slug
  is spelled identically in every note body carrying it.
  C-11: bash tests/inbox-to-memory-smoke.sh && bash tests/file-issue-smoke.sh &&
  bash tests/handoff-smoke.sh
status: complete
retries: 0
max_retries: 2
deps: [T-002]
escalations: []
artifacts:
  - tests/fixtures/inbox-to-memory/evals/unacknowledged-tension/_inbox/2026-04-22-bexley-carrier-review.md
  - tests/fixtures/inbox-to-memory/evals/unacknowledged-tension/notes/2026-02-11-dockside-bexley-walkthrough-0Z0zEXm85o.md
  - tests/fixtures/inbox-to-memory/evals/unacknowledged-tension/notes/2026-02-25-dockside-slot-rules-workshop-dyFsn16vEe.md
  - tests/fixtures/inbox-to-memory/evals/unacknowledged-tension/notes/2026-03-18-dockside-steerco-7DOpSdJ8As.md
  - tests/fixtures/inbox-to-memory/evals/unacknowledged-tension/notes/2026-04-08-dockside-pilot-checkpoint-xBDWkniOgN.md
  - tests/fixtures/inbox-to-memory/evals/unacknowledged-tension/_memory/rules/appointment-window-tolerance-AJ8Ia-Z8Xg.md
  - tests/fixtures/inbox-to-memory/evals/unacknowledged-tension/_memory/context/bexley-dock-capacity-MH-2ub4mwK.md
  - tests/fixtures/inbox-to-memory/evals/unacknowledged-tension/_memory/decisions/bexley-pilots-first-LvJedcverX.md
---

## Spec excerpt

Build one fixture for the inbox-to-memory eval suite: the scope that exercises **unacknowledged-tension detection**, both branches of it. You are building a fixture only. The doc that grades against it is a separate task; write no prose about it.

Own exactly one new directory under `tests/fixtures/inbox-to-memory/evals/`, named for what it holds. Sibling tasks are building other fixtures under that same path — do not touch theirs, and plant nothing in yours but what this behavior needs.

A fixture is an opted-in scope the skill can run in process mode: `_inbox/` plus `_memory/`. Read `inbox-to-memory/SKILL.md` phase 3, and its "Open Question Slugs" subsection in particular, before planting anything. The scopes under `tests/fixtures/inbox-to-memory/` show the file shapes; match their conventions, including nanoid-suffixed filenames.

**The rule, at `SKILL.md:122`.** Flag a slug open across three or more notes as `[tension: unacknowledged]`, unless the transcript shows someone naming it out loud.

The two halves of that rule live in different files, and getting this wrong is the single likeliest way to build a fixture that grades correct behavior as a failure:

- The **count** is over prior notes under `<scope-root>/notes/`. That is where the skill's recurrence grep reads.
- The **acknowledgement** is in the `_inbox/` transcript being groomed. Process mode never edits or re-reads a prior note for it, so an acknowledgement planted in a prior note is planted where the rule does not look.

**What to plant.** Two open-question slugs, each carried across three or more prior notes under `notes/`:

- a **positive** slug, where no `_inbox/` transcript in the fixture names its question aloud, so the skill should flag it
- a **negative** slug, where an `_inbox/` transcript does name its question aloud, so the skill should not flag it

The negative case has to turn on the acknowledgement. If it goes unflagged because only two notes carry it, the fixture tests nothing it claims to and the eval built on it is worthless.

**The interference trap.** The rule reads the transcript, not a designated passage in it. A transcript that names the positive slug's question in passing — a stray "we still haven't pinned down who owns the freeze date" — suppresses the flag the positive case exists to produce, and every other check here still passes. Read every `_inbox/` file back against both slugs before you finish, and confirm the positive question is genuinely unspoken in all of them.

Reuse the same slug text that already appears in the notes rather than minting a variant. The skill greps for slugs already in play precisely so a question does not fragment across spellings.

**Constraints.**

- `bash inbox-to-memory/scripts/lint-scope.sh <your fixture>` must exit 0. Run it before you finish.
- Check your tokens by hand as well, because the lint will not do it for you. Its token-grammar pass runs only over files carrying `schema: 2`; on a v1 file it reads no tokens at all and exits 0 on a token that does not exist. This is the failure mode that would quietly ruin *this* fixture in particular: write `[open-question: freeze-owner]` instead of `[open question: freeze-owner]` and the lint stays green, the recurrence grep never matches, the count never reaches three, and the scenario built on it grades nothing. Match every token against the ten-row grammar table at `references/machine-contracts.md` yourself, and check that the slug is spelled identically in every note carrying it. The rule is scoped to note bodies above `## Raw Content` — brackets in frontmatter and brackets inside raw content are legal and are not tokens, so the slug has to appear above the fence to count.
- No `MEMORY.md`, `INDEX.md`, or `index.md` anywhere under a `_memory/` path. The smoke suite walks the whole fixtures tree for those.
- v1 files (no `schema` key) are legal forever and the lint never flags them. If you write v2 frontmatter it has to be correct, including the `open_questions` derived count.
- The notes should read like a real engagement where a question kept not getting answered across weeks. That arc is the thing being detected, and a fixture that just repeats a token three times does not produce it.

Touch nothing outside your one new directory. Do not update `tests/fixtures/inbox-to-memory/README.md` — its staleness is a declared non-goal and editing it fails C-1. Do not commit.

## Rework diagnosis

## Courier comparison

```yaml
clause_types_sampled: mixed  # C-1,C-11 deterministic; C-3's fixture half close to mechanical; weight was on C-6's fixture half, judgment
brief_framing: unknown
courier_outcome: pass
agreement_on_outcome: agree
unique_courier:
  defect: 0
  inference: 0
  evidence_quality: 0
  coverage: 0
unique_checker: 0
overlap: 1  # C-6's fixture half — vendor derived the positive/negative slug assignment blind (unaware of intent, checker's ruling, or pass status) and matched the fixture's intent and the checker of record's independent reading
changed_verdict: no
cost:
  wall_s: null
  tokens_in: null
  tokens_cached: null
  tokens_out: null
  brief_tokens: null
  vendor_calls: unknown
retrofit_note: >-
  No log/ directory survives in this archive, so cost fields are left null
  per rule 2 despite the prose citing "18,672 in / 989 out" — not
  reconstructed here. brief_framing unknown — briefs/T-005.md is a clause
  dump with no confirm/attack/open instruction surviving.
```

**Crossing outcome: pass.** Codex lane, `gpt-5.6-terra`, 18,672 in / 989 out. Ledger line appended. Identity validation held on the first attempt, using the same fix that unblocked T-002 — required `checker` and `model` stated in the brief and echoed verbatim.

**Cited clauses, by kind.** C-1 and C-11 are deterministic and crossed as pre-run output; they agree by construction and count for nothing. C-3's fixture half is close to mechanical. The crossing's weight was on the one genuinely subjective question in C-6's fixture half: whether the `_inbox/` transcript names the positive slug's question anywhere, in any paraphrase. A grep cannot settle that; only reading can.

**Findings only the courier raised: none.**

**Findings only the checker of record raised: none.**

**Overlap: C-6's fixture half, under the strongest test this crossing has run.** The vendor was given both slugs and told neither which the worker intended as positive, nor how the checker of record ruled, nor that the fixture had passed. It read the transcript and derived the assignment itself: `noshow-charge-authority` named aloud and therefore not to be flagged, `after-hours-gate-cover` unmentioned and therefore to be flagged. That matches the fixture's intent and the checker of record's independent reading.

Both also converged on the same reason the negative case is genuinely acknowledged rather than merely mentioned — Terry naming the question and its run of non-answers, not just touching its subject. The interference check is the one an in-family checker could plausibly get wrong by grepping instead of reading, and two independent readings finding the positive question unspoken is worth more here than on a clause a script could settle.

**For #34.** Zero unique findings, second task running. Cost: ~20k tokens, one crossing, no retry. Running tally across three crossings: one unique finding (T-001's C-13, on an ambiguous clause), two crossings of pure confirmation on clauses that were not ambiguous. The pattern holds — the lane pays where the clause text admits two readings, not where the artifact is simply good or bad.
