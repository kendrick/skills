---
id: T-007
title: Correct the Kestrel provenance sentence in EVALS.md
spec: .agent-guild/state/spec.md
clauses: [C-1, C-8, C-11, C-13]
executor: worker-standard
executor_model: sonnet
checker: checker-judgment
check_method: >-
  C-1: .agent-guild/scripts/check-diff-scope.py
  _maintenance/inbox-to-memory/EVALS.md _maintenance/inbox-to-memory/eval-scope.sh
  tests/fixtures/inbox-to-memory/evals/ — the only path this task may change is
  EVALS.md, and within it only the sentence named below.
  C-8: checker-judgment: the corrected sentence must be true of the fixture on
  disk. Verify each place it claims `Kestrel` appears by opening that file, and
  confirm it claims no place `Kestrel` does not appear. Then confirm the row's
  grading condition is unchanged and still decidable against the extract that
  `--tier2-extract` actually emits — run it. The row passed C-8 before this
  correction and must still pass after; a repair that breaks the grading
  condition is worse than the inaccuracy it fixes.
  C-13: checker-judgment: the corrected sentence reads as the rest of the
  document does. It is one sentence in a finished doc, so judge it against its
  neighbours rather than in isolation.
  C-11: bash tests/inbox-to-memory-smoke.sh && bash tests/file-issue-smoke.sh &&
  bash tests/handoff-smoke.sh
status: complete
retries: 0
max_retries: 2
deps: [T-004]
escalations: []
artifacts: [_maintenance/inbox-to-memory/EVALS.md]
---

## Spec excerpt

`_maintenance/inbox-to-memory/EVALS.md` is finished and passed its check. One sentence in it is factually wrong about the fixture it describes, and this task fixes exactly that sentence and nothing else.

At `EVALS.md:110`, in the Tier 2 scenario, a sentence explains why `Kestrel` is a tempting entity for a model to propose. It currently claims the name "sits in the note's `tags`, in both record titles, and in the fixture's own name."

Two of those three are false:

- Neither record in `tests/fixtures/inbox-to-memory/evals/tier2-summary-entities/_memory/` has `Kestrel` in its `title`. The name appears in their `applies_to` and `tags`, and in one body line.
- The fixture is named `tier2-summary-entities`, not anything containing `Kestrel`.

The `tags` claim is correct.

**What to do.** Open the fixture, find every place `Kestrel` genuinely appears, and rewrite the sentence so it names only those. The sentence's job is unchanged: it explains why a model would be drawn to propose this entity, which is what makes it a good trap. Keep that job. A correct sentence that no longer explains the temptation is not an improvement.

**What not to touch.** The row's grading condition — that `Kestrel` is not proposed, and appears nowhere in the extract — is correct and was verified against the emitted extract by two independent checks. Leave it exactly as it is. This task changes an explanatory sentence, not a pass condition.

Do not rewrite neighbouring prose, do not restructure the scenario, and do not touch any other file. Match the register of the sentences around it; this is one repair inside a finished document, not a revision pass. Do not commit.

## Rework diagnosis

## Courier comparison

**Crossing outcome: pass.** Codex lane, `gpt-5.6-terra`. Ledger line appended. Deliberately the smallest brief of the job.

**Cited clauses, by kind.** C-1 and C-11 are deterministic. C-8's half of this task is a claim about repository content, which the no-paths rule puts structurally out of the far side's reach — the T-004 comparison established that boundary and it applies here in full. That left C-13 as the only clause the vendor could genuinely judge from inlined text, so the brief was scoped to it alone.

**Findings only the courier raised: none.**

**Findings only the checker of record raised: none.**

**Overlap: C-13.** Both read the repaired sentence as still doing its job — explaining why a model would be tempted by `Kestrel`, which is what makes rejecting it a real trap rather than an arbitrary negative — and both judged it better grounded than the original, on the same reasoning: four locations a model actually meets beat two that never existed. Both also read it as matching its neighbours' register rather than as a visible patch.

**For #34, and this crossing was run as a test of a prediction rather than as a check.** The T-004 comparison predicted a crossing would add little on a task of this shape. The brief was built to give that prediction a fair chance of being wrong — the ground truth was handed over as established fact so the vendor's attention fell entirely on the judgment it could actually make. It returned a sound, well-reasoned confirmation and nothing new.

That is the prediction holding, and it sharpens the rule the T-004 comparison drew. The lane's value does not track clause severity or task importance. It tracks whether the question can be *answered* from a self-contained brief. A question about reasoning travels; a question about the repository does not. On this task the only travelling question was a small one, and the crossing returned proportionally.
