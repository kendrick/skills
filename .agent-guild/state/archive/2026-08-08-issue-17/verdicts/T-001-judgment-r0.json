{
  "task_id": "T-001",
  "checker": "checker-judgment",
  "vendor": "anthropic",
  "model": "claude-opus-5[1m]",
  "verdict": "pass",
  "findings": [
    {
      "clause_id": "C-1",
      "severity": "blocker",
      "description": "The working tree touches exactly one path, the artifact itself, and it is inside the allowlist; nothing under inbox-to-memory/ or any other path changed.",
      "evidence": "$ .agent-guild/scripts/check-diff-scope.py _maintenance/inbox-to-memory/EVALS.md _maintenance/inbox-to-memory/eval-scope.sh tests/fixtures/inbox-to-memory/evals/\nOK: 1 path(s) in scope\nEXIT=0\n\nConfirmed independently that the one path is the artifact and not a collapsed directory entry (the script itself passes --untracked-files=all, per .agent-guild/scripts/check-diff-scope.py:86-95):\n$ git status --porcelain -uall\n?? _maintenance/inbox-to-memory/eval-scope.sh\n$ git ls-files --others --exclude-standard\n_maintenance/inbox-to-memory/eval-scope.sh"
    },
    {
      "clause_id": "C-10",
      "severity": "blocker",
      "description": "Script half only (the run-procedure half belongs to T-004, unrun). Staged a probe fixture and ran the script against it: it printed the staged path as its sole stdout line, the staged tree diffed clean against the source, and git status --porcelain was byte-identical before and after. The copy is verified positively by diff, not by the script's own report.",
      "evidence": "Probe fixture staged by the checker at tests/fixtures/inbox-to-memory/evals/checker-probe (copy of tests/fixtures/inbox-to-memory/mixed; 8 files incl. the dotfile _inbox/.gitkeep), then removed.\n\n$ git status --porcelain   # BEFORE run\n?? _maintenance/inbox-to-memory/\n?? tests/fixtures/inbox-to-memory/evals/\n\n$ bash _maintenance/inbox-to-memory/eval-scope.sh checker-probe\nexit=0\nstdout=[/private/tmp/i2m-eval-scope.jY2bZF]\nstderr=[]\n$ bash _maintenance/inbox-to-memory/eval-scope.sh checker-probe 2>/dev/null | wc -l\n       1\n\nStaged path is outside the repo: /private/tmp/i2m-eval-scope.jY2bZF (mktemp -d, eval-scope.sh:30).\n\n$ diff -r tests/fixtures/inbox-to-memory/evals/checker-probe /private/tmp/i2m-eval-scope.jY2bZF\ndiff exit=0    # no output, tree faithful including the dotfile\n\n$ git status --porcelain   # AFTER run\n?? _maintenance/inbox-to-memory/\n?? tests/fixtures/inbox-to-memory/evals/    # identical to BEFORE\n\nPer-run staging (not per-fixture): two consecutive calls minted distinct directories and both survived the script's return, so no cleanup trap pulls the ground out from under the caller:\na=/private/tmp/i2m-eval-scope.vsF0bL\nb=/private/tmp/i2m-eval-scope.V3jR0p\nDISTINCT / a exists / b exists\n\nMissing fixture (the judgment call the worker flagged) exits nonzero with a stderr message, which is what the task requires; the task states 'nonzero', not a specific code, and C-10's text sets no exit-code contract at all, so exit 1 satisfies the requirement as written:\n$ bash _maintenance/inbox-to-memory/eval-scope.sh no-such-fixture\nexit=1\nstdout=[]\nstderr=[no such eval fixture: /Users/karnett/repos/skills/tests/fixtures/inbox-to-memory/evals/no-such-fixture]\nA missing argument takes the usage code:\nexit=2 / stderr=[usage: eval-scope.sh <fixture-name>]\nNon-blocking observation only: the house precedent inbox-to-memory/scripts/verify-migration.sh:58-60 exits 2 for a missing directory. No clause reaches that, so it is not a defect.\n\nCwd-independent (resolves the repo from BASH_SOURCE, eval-scope.sh:22): run from /private/tmp it printed /private/tmp/i2m-eval-scope.fe6gB6.\n\nChecker contamination removed; tree restored to its pre-check state:\n$ rm -rf tests/fixtures/inbox-to-memory/evals && git status --porcelain | diff <baseline> -\nIDENTICAL to baseline (?? _maintenance/inbox-to-memory/)"
    },
    {
      "clause_id": "C-11",
      "severity": "blocker",
      "description": "All three smoke suites exit 0 with the artifact in the tree, both individually and as the chained command the check_method specifies.",
      "evidence": "$ bash tests/inbox-to-memory-smoke.sh\ni2m exit=0\ninbox-to-memory smoke: ok\n$ bash tests/file-issue-smoke.sh\nfile-issue exit=0\n$ bash tests/handoff-smoke.sh\nhandoff exit=0\nGenerated skill is current: /Users/karnett/repos/skills/handoff/SKILL.md\n$ bash tests/inbox-to-memory-smoke.sh && bash tests/file-issue-smoke.sh && bash tests/handoff-smoke.sh\nchain exit=0\n(Run after the probe fixture was removed, so the suites' tree-wide walks at tests/inbox-to-memory-smoke.sh:515 and the shasum snapshot at :586/:1229 saw only the committed fixtures.)"
    },
    {
      "clause_id": "C-13",
      "severity": "major",
      "description": "The script's one comment block explains why the script exists rather than what it does, and carries none of the humanizer patterns in scope; its wrapping matches the neighbouring shell precedent, which is the comparison the clause itself makes decisive.",
      "evidence": "_maintenance/inbox-to-memory/eval-scope.sh:1-9. Audited against ~/.claude/skills/humanizer/SKILL.md patterns 1-13, 15-16, 18-33 (14 em-dash and 17 title-case excluded per the clause): no bolded inline headers (16), no padded triple (10) — the block is two sentences, no rule-of-three anywhere, no hedging preamble or filler (23, 24), no AI vocabulary (7), no negative parallelism (9), no passive/subjectless fragments (13), no signposting (28), no generic positive close (25). Only the opening clause states what the script does ('Stage a private copy of an eval fixture for a single scenario run'), matching the shape of the precedent's own opening line at inbox-to-memory/scripts/verify-migration.sh:2; everything after it is why — process mode empties _inbox/ and writes into the scope, so an in-place run destroys the fixture for every later scenario — and it points at tests/inbox-to-memory-smoke.sh:1226 as the spec instructed, which I read and confirmed holds the migration tests to that same rule. The second sentence gives the why for per-run staging rather than restating the code.\n\nJudgment call recorded, since the clause's 'Prose lines are never hard-wrapped' could be read to reach here: the comment is wrapped, at 68-76 columns. I rule that not a violation. The clause's own tiebreaker is 'matching its precedent beats matching a house voice it never used', and the neighbouring shell precedent wraps identically — verify-migration.sh:1-7 measures 19/75/77/74/71/1/52 columns against eval-scope.sh:1-9 at 19/76/75/71/75/73/68/74/7. The no-hard-wrap rule exists because a display-time renderer (GitHub, an IDE, git's pager) rewraps prose; shell source is shown verbatim everywhere, so the literal reading would fail every comment in this repo including the one the spec told the worker to point at. Flagging for the orchestrator in case it reads the clause the other way.\n\nHouse constraints also hold: no `grep -q`, no `cat`, no `stat` in the artifact (grep returned nothing)."
    }
  ],
  "timestamp": "2026-08-07T23:30:44Z",
  "duration_ms": null,
  "cost_usd": null
}
