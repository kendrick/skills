---
task: T-001
checker: checker-judgment
vendor: anthropic
model: claude-opus-5[1m]
verdict: PASS
checked_at: 2026-08-07T23:30:44Z
duration_ms: None
cost_usd: None
---

<!-- GENERATED FILE—do not hand-edit. Rendered by render-verdict.py
from the verdict JSON, the record of record. Edit the JSON and
re-render instead. -->

## Per-clause results

| clause | severity | description | evidence |
| ------ | -------- | ------------ | -------- |
| C-1 | blocker | The working tree touches exactly one path, the artifact itself, and it is inside the allowlist; nothing under inbox-to-memory/ or any other path changed. | $ .agent-guild/scripts/check-diff-scope.py _maintenance/inbox-to-memory/EVALS.md _maintenance/inbox-to-memory/eval-scope.sh tests/fixtures/inbox-to-memory/evals/ OK: 1 path(s) in scope EXIT=0 Confirmed independently that the one path is the artifact and not a collapsed directory entry (the script itself passes --untracked-files=all, per .agent-guild/scripts/check-diff-scope.py:86-95): $ git status --porcelain -uall ?? _maintenance/inbox-to-memory/eval-scope.sh $ git ls-files --others --exclude-standard _maintenance/inbox-to-memory/eval-scope.sh |
| C-10 | blocker | Script half only (the run-procedure half belongs to T-004, unrun). Staged a probe fixture and ran the script against it: it printed the staged path as its sole stdout line, the staged tree diffed clean against the source, and git status --porcelain was byte-identical before and after. The copy is verified positively by diff, not by the script's own report. | Probe fixture staged by the checker at tests/fixtures/inbox-to-memory/evals/checker-probe (copy of tests/fixtures/inbox-to-memory/mixed; 8 files incl. the dotfile _inbox/.gitkeep), then removed. $ git status --porcelain # BEFORE run ?? _maintenance/inbox-to-memory/ ?? tests/fixtures/inbox-to-memory/evals/ $ bash _maintenance/inbox-to-memory/eval-scope.sh checker-probe exit=0 stdout=[/private/tmp/i2m-eval-scope.jY2bZF] stderr=[] $ bash _maintenance/inbox-to-memory/eval-scope.sh checker-probe 2>/dev/null \| wc -l 1 Staged path is outside the repo: /private/tmp/i2m-eval-scope.jY2bZF (mktemp -d, eval-scope.sh:30). $ diff -r tests/fixtures/inbox-to-memory/evals/checker-probe /private/tmp/i2m-eval-scope.jY2bZF diff exit=0 # no output, tree faithful including the dotfile $ git status --porcelain # AFTER run ?? _maintenance/inbox-to-memory/ ?? tests/fixtures/inbox-to-memory/evals/ # identical to BEFORE Per-run staging (not per-fixture): two consecutive calls minted distinct directories and both survived the script's return, so no cleanup trap pulls the ground out from under the caller: a=/private/tmp/i2m-eval-scope.vsF0bL b=/private/tmp/i2m-eval-scope.V3jR0p DISTINCT / a exists / b exists Missing fixture (the judgment call the worker flagged) exits nonzero with a stderr message, which is what the task requires; the task states 'nonzero', not a specific code, and C-10's text sets no exit-code contract at all, so exit 1 satisfies the requirement as written: $ bash _maintenance/inbox-to-memory/eval-scope.sh no-such-fixture exit=1 stdout=[] stderr=[no such eval fixture: /Users/karnett/repos/skills/tests/fixtures/inbox-to-memory/evals/no-such-fixture] A missing argument takes the usage code: exit=2 / stderr=[usage: eval-scope.sh <fixture-name>] Non-blocking observation only: the house precedent inbox-to-memory/scripts/verify-migration.sh:58-60 exits 2 for a missing directory. No clause reaches that, so it is not a defect. Cwd-independent (resolves the repo from BASH_SOURCE, eval-scope.sh:22): run from /private/tmp it printed /private/tmp/i2m-eval-scope.fe6gB6. Checker contamination removed; tree restored to its pre-check state: $ rm -rf tests/fixtures/inbox-to-memory/evals && git status --porcelain \| diff <baseline> - IDENTICAL to baseline (?? _maintenance/inbox-to-memory/) |
| C-11 | blocker | All three smoke suites exit 0 with the artifact in the tree, both individually and as the chained command the check_method specifies. | $ bash tests/inbox-to-memory-smoke.sh i2m exit=0 inbox-to-memory smoke: ok $ bash tests/file-issue-smoke.sh file-issue exit=0 $ bash tests/handoff-smoke.sh handoff exit=0 Generated skill is current: /Users/karnett/repos/skills/handoff/SKILL.md $ bash tests/inbox-to-memory-smoke.sh && bash tests/file-issue-smoke.sh && bash tests/handoff-smoke.sh chain exit=0 (Run after the probe fixture was removed, so the suites' tree-wide walks at tests/inbox-to-memory-smoke.sh:515 and the shasum snapshot at :586/:1229 saw only the committed fixtures.) |
| C-13 | major | The script's one comment block explains why the script exists rather than what it does, and carries none of the humanizer patterns in scope; its wrapping matches the neighbouring shell precedent, which is the comparison the clause itself makes decisive. | _maintenance/inbox-to-memory/eval-scope.sh:1-9. Audited against ~/.claude/skills/humanizer/SKILL.md patterns 1-13, 15-16, 18-33 (14 em-dash and 17 title-case excluded per the clause): no bolded inline headers (16), no padded triple (10) — the block is two sentences, no rule-of-three anywhere, no hedging preamble or filler (23, 24), no AI vocabulary (7), no negative parallelism (9), no passive/subjectless fragments (13), no signposting (28), no generic positive close (25). Only the opening clause states what the script does ('Stage a private copy of an eval fixture for a single scenario run'), matching the shape of the precedent's own opening line at inbox-to-memory/scripts/verify-migration.sh:2; everything after it is why — process mode empties _inbox/ and writes into the scope, so an in-place run destroys the fixture for every later scenario — and it points at tests/inbox-to-memory-smoke.sh:1226 as the spec instructed, which I read and confirmed holds the migration tests to that same rule. The second sentence gives the why for per-run staging rather than restating the code. Judgment call recorded, since the clause's 'Prose lines are never hard-wrapped' could be read to reach here: the comment is wrapped, at 68-76 columns. I rule that not a violation. The clause's own tiebreaker is 'matching its precedent beats matching a house voice it never used', and the neighbouring shell precedent wraps identically — verify-migration.sh:1-7 measures 19/75/77/74/71/1/52 columns against eval-scope.sh:1-9 at 19/76/75/71/75/73/68/74/7. The no-hard-wrap rule exists because a display-time renderer (GitHub, an IDE, git's pager) rewraps prose; shell source is shown verbatim everywhere, so the literal reading would fail every comment in this repo including the one the spec told the worker to point at. Flagging for the orchestrator in case it reads the clause the other way. House constraints also hold: no `grep -q`, no `cat`, no `stat` in the artifact (grep returned nothing). |
