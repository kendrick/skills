---
task: CON-audit
checker: auditor
vendor: anthropic
model: claude-opus-5
verdict: FAIL
checked_at: 2026-08-07T20:06:08Z
duration_ms: None
cost_usd: None
---

<!-- GENERATED FILE—do not hand-edit. Rendered by render-verdict.py
from the verdict JSON, the record of record. Edit the JSON and
re-render instead. -->

## Per-clause results

| clause | severity | description | evidence |
| ------ | -------- | ------------ | -------- |
| C-1 | major | FAIL (scope, not check). The check command runs as written and is sound, but the allowlist fences off two files that this job's own deliverables make stale, so a worker cannot fix them without failing C-1. (a) README.md:70 is the repo's _maintenance/ inventory and reads "the decision ledger and evals behind `file-issue`"; landing _maintenance/inbox-to-memory/EVALS.md makes that sentence wrong. (b) tests/fixtures/inbox-to-memory/README.md opens "Three scopes the lint runs against" (it already lists four) and is the index for the directory gaining evals/; evals/ is not a lint scope, so it needs a distinguishing line rather than a fifth bullet. Fix: either add those two exact file paths to the allowlist (`README.md` and `tests/fixtures/inbox-to-memory/README.md` as exact-file arguments, not prefixes), or add an explicit non-goal saying both are knowingly left stale. Separately, `--ignore .agent-guild/state/` is a no-op that reads as coverage it does not provide: check-diff-scope.py matches --ignore by exact string against a set (line 111, `path in ignored`), never by prefix, and .agent-guild/state/ is already hardcoded-permitted at line 112. | Ran verbatim: `python3 .agent-guild/scripts/check-diff-scope.py _maintenance/inbox-to-memory/ tests/fixtures/inbox-to-memory/evals/ --ignore .agent-guild/state/` -> 'OK: 0 path(s) in scope', exit 0. Stale-doc consumers: README.md:70; tests/fixtures/inbox-to-memory/README.md:1-3. --ignore semantics: .agent-guild/scripts/check-diff-scope.py:107-117 (exact-match set at :111, hardcoded state/ carve-out at :112). |
| C-2 | blocker | PASS, and it is the model the other clauses should copy. It pins the search window explicitly ('followed - before the next scenario heading -', 'All three live inside the scenario's own section') and the check restates it operationally ('extract only the text between that heading and the next one... Anything found outside the slice under test does not count toward it'). A document that names every fixture in a global ## Fixtures section and nowhere else fails this, which is exactly the #29 failure mode being closed. One residual ambiguity worth a word: 'scenario heading' is not tied to a heading level, so a scenario carrying an #### Expected subheading could be read as ending early. C-12 requires numbered scenarios, so keying the slice on the numbering ('between one numbered scenario heading and the next') removes the reading. | .agent-guild/state/constitution.md:16-21. Contrast with the precedent's structure, _maintenance/file-issue/EVALS.md:16-30, where fixture, prompt, and table all sit inside the scenario's own slice. |
| C-3 | blocker | PASS. The opt-in test is quoted correctly and the subset direction is the safe one: collecting cited names from the whole doc and requiring them to be a subset of real directories cannot be satisfied by text in the wrong place. Verified the opt-in rule against source: SKILL.md:30-34 says a directory is opted in when it contains both `_inbox/` and one of `_memory/` (project or client) or `entries/` (journal), which is what the clause states. | inbox-to-memory/SKILL.md:30-34; also SKILL.md:3 (description line: 'Only operates on opted-in directories (containing `_inbox/` plus either `_memory/` or `entries/`)'). Existing fixtures confirm the shape, including the .gitkeep convention that keeps an empty `_inbox/` tracked: `git ls-files tests/fixtures/inbox-to-memory/ \| grep _inbox` -> four .gitkeep entries. |
| C-4 | blocker | PASS. Location is pinned ('Per scenario section') and the anti-boilerplate teeth are concrete and mechanical: 'Flag any baseline sentence that appears verbatim in two or more sections.' The clause also forecloses the obvious dodge by requiring the baseline to name 'something particular to that scenario's fixture or prompt', so a per-section copy of one generic sentence fails. This is the clause carrying acceptance criterion 2's expectation half; see C-12 for the recording half, which is not covered. | .agent-guild/state/constitution.md:30-35. Spec acceptance criterion at .agent-guild/state/spec.md:23. |
| C-5 | blocker | PASS, and every quoted artifact checks out against source rather than being plausible-sounding. Verified all five quotations: the flag grammar `[contradicts accepted: [[<filename>\|<short-name>]]] <statement> \| claims: <what the record says>` at SKILL.md:100; `status: accepted` as the only flag-producing status at SKILL.md:69; phase 2.5 as the phase that reads memory first at SKILL.md:53 and :104; the rewrite target `[[<target>\|memory - updated]]` at SKILL.md:151 and :162; `source_refs` append at SKILL.md:154; the `(added YYYY-MM-DD, <note-id>)` marker at SKILL.md:155. 'Phase 5 sign-off' is the skill's own term (SKILL.md:57, :79, :138). Location is pinned ('within that one scenario') and the check reaches the fixture, not just the prose. | inbox-to-memory/SKILL.md:53, :69, :100, :104, :136-138, :151-155, :162; inbox-to-memory/references/machine-contracts.md:74, :93-94. |
| C-6 | blocker | PASS. The trigger is quoted correctly - SKILL.md:122 reads 'Flag a slug open across three or more notes as `[tension: unacknowledged]`, unless the transcript shows someone naming it out loud' - and the clause demands both branches plus a fixture-side count, which is what makes the negative case real rather than asserted. Two clarifications that would remove checker-to-checker variance without changing the bar: say whether the negative case may share the positive case's fixture (the existing mixed/ fixture carries both slugs in one scope, so sharing is the natural build), and say what the negative case must be flagged as instead of unacknowledged, since the skill routes a named-aloud recurrence to a deferred tension rather than to nothing. | inbox-to-memory/SKILL.md:122; inbox-to-memory/references/machine-contracts.md:91. Precedent for a one-fixture two-slug build: tests/fixtures/inbox-to-memory/README.md ('The mixed scope also carries two open-question slugs that recur across three notes each. One gets named out loud in the third note and becomes a deferred tension; the other goes unnoticed'). |
| C-7 | blocker | FAIL. This is the #29 lesson unlearned, in the one clause where it costs the most. C-5 and C-6 both reach into the fixture ('read the fixture to confirm it holds an `accepted` record', 'count the prior notes in its fixture'); C-7's check reads only EVALS.md - it confirms three tokens 'appear as the expected outcome of a distinct input' and that one case 'presents conflicting signals'. Nothing verifies that any fixture can produce those outcomes. That gap is load-bearing here because scope is decided by path, not by content: SKILL.md:36 says a project sits under `pursuits/<name>/` or `projects/<name>/` and a client root is the directory containing those. A flat fixture directory satisfies C-3 in full (`_inbox/` plus `_memory/`) while the skill would never detect project scope inside it, so an EVALS.md asserting `[memory candidate: project]` against that fixture passes C-3, C-7, and C-9 and grades nothing. Fix: extend C-7's check to read each cited fixture and confirm its directory layout admits the tier the scenario expects - a project case rooted under `pursuits/<name>/` or `projects/<name>/`, a client case at the containing root that is itself opted in - and, for the tiebreak case, confirm the fixture's input actually carries two conflicting signals from the scope-decisions.md table rather than one. The tiebreak rule itself is quoted correctly and needs no change. | .agent-guild/state/constitution.md:51-56 (check at :54 names only EVALS.md content). Scope-by-path rule: inbox-to-memory/SKILL.md:36. Tiebreak verified present: inbox-to-memory/references/scope-decisions.md:21-25 ('When signals conflict, suggest the **narrowest** scope. Project is the floor.'). Contrast the fixture-side reach at constitution.md:40 (C-5) and :47 (C-6). |
| C-8 | major | FAIL on one clause of attribution; the substance is right. The rule the clause states is real and quoted almost verbatim from source - SKILL.md:386 reads 'Each summary is one line that asserts nothing the extract doesn't support: no date, name, decision, or outcome that isn't already up there. Each entity is a canonical name appearing in the extract verbatim.' But the clause introduces both halves as 'matching what the migrator already enforces', and the migrator enforces only the entity half. SKILL.md:388 says so explicitly ('The apply enforces the half it can'), and migrate-scope.sh:393-407 is a `grep -Fc` of each proposed entity against the note's extract with no summary-sourcing check anywhere. A checker reading C-8 will go looking for a script assertion behind the summary row, not find one, and either fail correct work or record a bogus evidence pointer. Fix: attribute the entity half to the migrator (migrate-scope.sh:396, diagnostic `tier2-entity-unsourced`) and the summary half to the skill instruction at SKILL.md:386 / references/migration.md:72. The anti-vacuity shape of the rest is sound: 'every row in the scenario', 'No pass condition in this scenario', and the unsourced row must 'name the specific fabricated fact the extract lacks' all pin the search to the scenario and force a concrete artifact. | inbox-to-memory/SKILL.md:386 (both rules stated), :388 ('The apply enforces the half it can'); inbox-to-memory/scripts/migrate-scope.sh:393-407 (entity-only `grep -Fc` loop, `tier2-entity-unsourced` at :407); inbox-to-memory/references/migration.md:72, :81. Constitution text at .agent-guild/state/constitution.md:60. |
| C-9 | blocker | PASS, including its failing example, which I tried to break rather than take. The example predicts `[memory candidate: journal]` is undefined; `grep -rn 'memory candidate: journal' inbox-to-memory/` returns nothing, while the two defined scope tokens (project, client) and the separate `[journal candidate: <generalized pattern>]` form all resolve, so the example fails a real artifact for the stated reason. The doc-to-skill direction makes whole-document extraction safe here: a universal quantifier over tokens cited in EVALS.md cannot be satisfied by a token sitting in the wrong section. The second half of the check ('any token whose use in the doc contradicts its definition at the source') is what stops a real token being graded with a wrong meaning, and it is the half most likely to be skipped - worth naming one worked instance in the clause so it is not read as boilerplate. | `grep -rn 'memory candidate: journal' inbox-to-memory/` -> no output. Defined forms: inbox-to-memory/SKILL.md:96-99; inbox-to-memory/references/machine-contracts.md:69-72; inbox-to-memory/references/scope-decisions.md:5, :74-75. |
| C-10 | blocker | FAIL. The clause hangs its whole grading half on 'every scenario's run procedure', and no clause anywhere requires a run procedure to exist. C-2's required per-scenario contents are fixture, prompt, and pass-condition table; C-12's required shape is a method statement plus ## Fixtures, ## Scenarios, and ## Grading. Neither forces a run procedure. So an EVALS.md with no run procedure at all satisfies both halves of C-10 vacuously: 'every scenario's run procedure goes through it' is true of the empty set, and 'no scenario's run procedure names a `tests/fixtures/` path' is true for the same reason. That is the #29 pattern in a different key - not the right words in the wrong place, but no place at all. Fix: add the run procedure to C-2's per-scenario required contents (so it is inside the scenario's own slice, where C-2 already pins the window), and rewrite C-10's second half as a positive assertion - each scenario's run procedure invokes `eval-scope.sh` and runs the skill against the path it prints - rather than only the negative one. The script-behavior half of the check (run it, confirm it writes only under a scratch directory, confirm `git status --porcelain` unchanged) is genuinely deterministic and good as written; note that it is a script run, not a judgment call, so it could route to checker-deterministic if split out. | .agent-guild/state/constitution.md:72-77 (C-10 text at :74, check at :75). Required per-scenario contents: constitution.md:18 (C-2). Required document shape: constitution.md:88 (C-12). Neither mentions a run procedure. |
| C-11 | blocker | FAIL, and this one is decisive on its own: the check command cannot pass in this repo, ever. `.agent-guild/hooks/test_hooks.py` does not exist here - there is no `.agent-guild/hooks/` directory at all. The file lives in the agent-guild plugin repo (~/.claude/plugins/marketplaces/kendrick/), not in kendrick/skills. Because the four commands are joined with `&&`, the chain's exit status is the python invocation's, so C-11 fails for every artifact regardless of the work. No retry, no escalation, and no correct deliverable can clear it; only editing the clause can. Fix: drop the `test_hooks.py` term. The remaining three suites are the right set and the set is complete for what this job touches - I derived the consumers of tests/fixtures/inbox-to-memory/ from the suite source rather than assuming. Adding a directory there has exactly two whole-tree consumers, both inside tests/inbox-to-memory-smoke.sh: line 515 forbids MEMORY.md/INDEX.md/index.md anywhere under a `*_memory*` path in the fixtures tree, and lines 583-586 with 1226-1229 shasum-snapshot every file under tests/fixtures/ and re-compare at the end, so an in-place run against an eval fixture goes red there (which is the same failure C-10 exists to prevent, caught twice - keep it). The per-scope loops at lines 109-117 and the lint invocations at 120/127/157 name old-only, mixed, and broken explicitly rather than globbing, so evals/ is not swept into the lint arithmetic. The other three suites and the guild's own scripts/test_*.py touch nothing this job writes. | Ran the C-11 chain term by term: `bash tests/inbox-to-memory-smoke.sh` -> 0 ('inbox-to-memory smoke: ok'); `bash tests/file-issue-smoke.sh` -> 0; `bash tests/handoff-smoke.sh` -> 0; `python3 .agent-guild/hooks/test_hooks.py` -> 2, "can't open file '/Users/karnett/repos/skills/.agent-guild/hooks/test_hooks.py': [Errno 2] No such file or directory". `ls .agent-guild/` -> CLAUDE.md, schemas, scripts, state (no hooks/). Real location: ~/.claude/plugins/marketplaces/kendrick/.agent-guild/hooks/test_hooks.py. Consumer derivation: tests/inbox-to-memory-smoke.sh:109-117, :120, :127, :157, :515, :583-586, :1226-1229. |
| C-12 | major | FAIL on two points. (1) Coverage gap against spec acceptance criterion 2. The criterion is 'Each runs with and without the skill, and the delta is what gets graded', and C-4 covers only the stated expectation - what the no-skill run should produce. Nothing requires the results table to have anywhere to record the no-skill result. A table with one 'passed / evidence' column per scenario satisfies C-12 as written and C-4 in full, and the delta has no home in the artifact this job ships. Fix: require the per-scenario results table to carry a with-skill column and a without-skill column (or an explicit delta column) so the first run has somewhere to put both halves. (2) The check says 'Compare section headings against `_maintenance/file-issue/EVALS.md`, confirm all four are present', but the method statement is not a heading in the precedent - it is unheaded prose at lines 3-5, above ## Fixtures. The clause text gets this right ('a method statement'); only the check's 'all four are present' phrasing invites a false fail on a document that follows the precedent exactly. Say three headings plus a method statement. | Spec criterion: .agent-guild/state/spec.md:23. Constitution: .agent-guild/state/constitution.md:88 (text), :89 (check). Precedent heading set, derived: `grep -n '^#' _maintenance/file-issue/EVALS.md` -> only '# file-issue Evals', '## Fixtures' (:7), '## Scenarios' (:14), six '### N.' scenario headings, '## Grading' (:105). The method statement sits at _maintenance/file-issue/EVALS.md:3-5 with no heading of its own. |
| C-13 | blocker | FAIL. Its three automatic-fail triggers collide with C-12 and with the repo's own prose, and one of them is ambiguous enough that two checkers will split. (1) Heading case directly contradicts C-12. C-13 says 'Headings stay title-cased' and makes 'a lowercased heading' an automatic fail; C-12 says keep the precedent's shape. Every one of the precedent's six scenario headings is sentence case - 'Bug with no reproduction offered', 'Ambiguous feature ask', 'Repo with issue forms', 'Agent-targeted bug', 'Spec-sized ask'. A worker who mirrors the precedent gets an automatic C-13 fail; one who title-cases diverges from C-12. The constitution has to rule, and neither clause does. (2) 'a lowercased heading' is ambiguous between 'entirely lowercase' and 'main words not capitalized' (i.e. sentence case). Paired with 'Headings stay title-cased' it reads as the latter, but a checker reading it as the former passes exactly the artifacts the other fails. That is below the two-checkers-agree bar this audit is measuring against. (3) The space-wrapped em dash trigger fires on the house style everywhere it can look: 11 instances in the precedent EVALS.md it is told to imitate, 47 in inbox-to-memory/SKILL.md, 4 in tests/fixtures/inbox-to-memory/README.md, and 6 in this constitution itself, including in C-1's own text. A rule the constitution breaks in the act of stating it will produce disputes the orchestrator has to rule on one by one, which is the failure mode the dispute section warns about ('If one checker keeps producing bad verdicts, the fault is usually the clause'). Fix: decide explicitly which authority wins for this document - the user's global style preference or the precedent's house style - and say so in the clause, resolving heading case for scenario headings by name. Keep the humanizer audit; drop or scope the automatic-fail list to triggers the surrounding corpus actually honors. | Precedent scenario headings: _maintenance/file-issue/EVALS.md:16, :31, :46, :60, :75, :90. Space-wrapped em dash counts, `grep -c ' \\u2014 '`: _maintenance/file-issue/EVALS.md = 11; inbox-to-memory/SKILL.md = 47; tests/fixtures/inbox-to-memory/README.md = 4; .agent-guild/state/constitution.md = 6 (including C-1's text at :11, 'The suite is exactly three things \\u2014'). Constitution C-13 at .agent-guild/state/constitution.md:93-98; C-12 at :86-91. |
| coverage | minor | PASS with one half-gap. All seven spec acceptance criteria have a clause behind them: criterion 1 -> C-2 plus C-3; criterion 2 -> C-4 for the stated expectation, but see C-12 for the missing recording half; criterion 3 -> C-5; criterion 4 -> C-6; criterion 5 -> C-7; criterion 6 -> C-10; criterion 7 -> C-1. The spec body's fourth behavior, 'whether generated summaries and entities are any good', is covered by C-8. No clause is orphaned: C-9, C-11, C-12, and C-13 each answer a job need rather than a criterion (vocabulary fidelity, regression safety, precedent shape, and the standing prose requirement), and all four are justified in their own text. The three settled decisions are each reachable and each constrained - purpose-built committed fixtures by C-1 and C-3, the staging script by C-1 and C-10, and specification-plus-empty-table by C-12 and the third non-goal. | .agent-guild/state/spec.md:22-28 mapped against .agent-guild/state/constitution.md:9-98. |
| non-goals | minor | PASS with one loose edge. The five non-goals are drawn tightly enough to stop gold-plating and none of them blocks a criterion: 'no graded runner' still leaves eval-scope.sh reachable because staging is not scoring; 'no execution' matches the settled empty-table decision and is enforced by C-12; 'no scaffold mode' narrows the surface without touching any criterion; 'no committing or pushing' matches standing user policy. The loose edge is the first one: 'If a scenario exposes a skill defect, the finding is filed as an issue, not fixed here' authorizes an unbounded external side effect (`gh issue create`) that no clause constrains and that check-diff-scope.py structurally cannot see, since it never touches the working tree. Prior runs in this repo staged issue drafts as files under state/ instead. Say which one is meant. | .agent-guild/state/constitution.md:104-111. Prior-run precedent for staging rather than posting: .agent-guild/state/archive/2026-08-07/issues/close-14.md. |

## Diagnosis

- **C-1** (major): FAIL (scope, not check). The check command runs as written and is sound, but the allowlist fences off two files that this job's own deliverables make stale, so a worker cannot fix them without failing C-1. (a) README.md:70 is the repo's _maintenance/ inventory and reads "the decision ledger and evals behind `file-issue`"; landing _maintenance/inbox-to-memory/EVALS.md makes that sentence wrong. (b) tests/fixtures/inbox-to-memory/README.md opens "Three scopes the lint runs against" (it already lists four) and is the index for the directory gaining evals/; evals/ is not a lint scope, so it needs a distinguishing line rather than a fifth bullet. Fix: either add those two exact file paths to the allowlist (`README.md` and `tests/fixtures/inbox-to-memory/README.md` as exact-file arguments, not prefixes), or add an explicit non-goal saying both are knowingly left stale. Separately, `--ignore .agent-guild/state/` is a no-op that reads as coverage it does not provide: check-diff-scope.py matches --ignore by exact string against a set (line 111, `path in ignored`), never by prefix, and .agent-guild/state/ is already hardcoded-permitted at line 112.
  evidence: Ran verbatim: `python3 .agent-guild/scripts/check-diff-scope.py _maintenance/inbox-to-memory/ tests/fixtures/inbox-to-memory/evals/ --ignore .agent-guild/state/` -> 'OK: 0 path(s) in scope', exit 0. Stale-doc consumers: README.md:70; tests/fixtures/inbox-to-memory/README.md:1-3. --ignore semantics: .agent-guild/scripts/check-diff-scope.py:107-117 (exact-match set at :111, hardcoded state/ carve-out at :112).
- **C-2** (blocker): PASS, and it is the model the other clauses should copy. It pins the search window explicitly ('followed - before the next scenario heading -', 'All three live inside the scenario's own section') and the check restates it operationally ('extract only the text between that heading and the next one... Anything found outside the slice under test does not count toward it'). A document that names every fixture in a global ## Fixtures section and nowhere else fails this, which is exactly the #29 failure mode being closed. One residual ambiguity worth a word: 'scenario heading' is not tied to a heading level, so a scenario carrying an #### Expected subheading could be read as ending early. C-12 requires numbered scenarios, so keying the slice on the numbering ('between one numbered scenario heading and the next') removes the reading.
  evidence: .agent-guild/state/constitution.md:16-21. Contrast with the precedent's structure, _maintenance/file-issue/EVALS.md:16-30, where fixture, prompt, and table all sit inside the scenario's own slice.
- **C-3** (blocker): PASS. The opt-in test is quoted correctly and the subset direction is the safe one: collecting cited names from the whole doc and requiring them to be a subset of real directories cannot be satisfied by text in the wrong place. Verified the opt-in rule against source: SKILL.md:30-34 says a directory is opted in when it contains both `_inbox/` and one of `_memory/` (project or client) or `entries/` (journal), which is what the clause states.
  evidence: inbox-to-memory/SKILL.md:30-34; also SKILL.md:3 (description line: 'Only operates on opted-in directories (containing `_inbox/` plus either `_memory/` or `entries/`)'). Existing fixtures confirm the shape, including the .gitkeep convention that keeps an empty `_inbox/` tracked: `git ls-files tests/fixtures/inbox-to-memory/ | grep _inbox` -> four .gitkeep entries.
- **C-4** (blocker): PASS. Location is pinned ('Per scenario section') and the anti-boilerplate teeth are concrete and mechanical: 'Flag any baseline sentence that appears verbatim in two or more sections.' The clause also forecloses the obvious dodge by requiring the baseline to name 'something particular to that scenario's fixture or prompt', so a per-section copy of one generic sentence fails. This is the clause carrying acceptance criterion 2's expectation half; see C-12 for the recording half, which is not covered.
  evidence: .agent-guild/state/constitution.md:30-35. Spec acceptance criterion at .agent-guild/state/spec.md:23.
- **C-5** (blocker): PASS, and every quoted artifact checks out against source rather than being plausible-sounding. Verified all five quotations: the flag grammar `[contradicts accepted: [[<filename>|<short-name>]]] <statement> | claims: <what the record says>` at SKILL.md:100; `status: accepted` as the only flag-producing status at SKILL.md:69; phase 2.5 as the phase that reads memory first at SKILL.md:53 and :104; the rewrite target `[[<target>|memory - updated]]` at SKILL.md:151 and :162; `source_refs` append at SKILL.md:154; the `(added YYYY-MM-DD, <note-id>)` marker at SKILL.md:155. 'Phase 5 sign-off' is the skill's own term (SKILL.md:57, :79, :138). Location is pinned ('within that one scenario') and the check reaches the fixture, not just the prose.
  evidence: inbox-to-memory/SKILL.md:53, :69, :100, :104, :136-138, :151-155, :162; inbox-to-memory/references/machine-contracts.md:74, :93-94.
- **C-6** (blocker): PASS. The trigger is quoted correctly - SKILL.md:122 reads 'Flag a slug open across three or more notes as `[tension: unacknowledged]`, unless the transcript shows someone naming it out loud' - and the clause demands both branches plus a fixture-side count, which is what makes the negative case real rather than asserted. Two clarifications that would remove checker-to-checker variance without changing the bar: say whether the negative case may share the positive case's fixture (the existing mixed/ fixture carries both slugs in one scope, so sharing is the natural build), and say what the negative case must be flagged as instead of unacknowledged, since the skill routes a named-aloud recurrence to a deferred tension rather than to nothing.
  evidence: inbox-to-memory/SKILL.md:122; inbox-to-memory/references/machine-contracts.md:91. Precedent for a one-fixture two-slug build: tests/fixtures/inbox-to-memory/README.md ('The mixed scope also carries two open-question slugs that recur across three notes each. One gets named out loud in the third note and becomes a deferred tension; the other goes unnoticed').
- **C-7** (blocker): FAIL. This is the #29 lesson unlearned, in the one clause where it costs the most. C-5 and C-6 both reach into the fixture ('read the fixture to confirm it holds an `accepted` record', 'count the prior notes in its fixture'); C-7's check reads only EVALS.md - it confirms three tokens 'appear as the expected outcome of a distinct input' and that one case 'presents conflicting signals'. Nothing verifies that any fixture can produce those outcomes. That gap is load-bearing here because scope is decided by path, not by content: SKILL.md:36 says a project sits under `pursuits/<name>/` or `projects/<name>/` and a client root is the directory containing those. A flat fixture directory satisfies C-3 in full (`_inbox/` plus `_memory/`) while the skill would never detect project scope inside it, so an EVALS.md asserting `[memory candidate: project]` against that fixture passes C-3, C-7, and C-9 and grades nothing. Fix: extend C-7's check to read each cited fixture and confirm its directory layout admits the tier the scenario expects - a project case rooted under `pursuits/<name>/` or `projects/<name>/`, a client case at the containing root that is itself opted in - and, for the tiebreak case, confirm the fixture's input actually carries two conflicting signals from the scope-decisions.md table rather than one. The tiebreak rule itself is quoted correctly and needs no change.
  evidence: .agent-guild/state/constitution.md:51-56 (check at :54 names only EVALS.md content). Scope-by-path rule: inbox-to-memory/SKILL.md:36. Tiebreak verified present: inbox-to-memory/references/scope-decisions.md:21-25 ('When signals conflict, suggest the **narrowest** scope. Project is the floor.'). Contrast the fixture-side reach at constitution.md:40 (C-5) and :47 (C-6).
- **C-8** (major): FAIL on one clause of attribution; the substance is right. The rule the clause states is real and quoted almost verbatim from source - SKILL.md:386 reads 'Each summary is one line that asserts nothing the extract doesn't support: no date, name, decision, or outcome that isn't already up there. Each entity is a canonical name appearing in the extract verbatim.' But the clause introduces both halves as 'matching what the migrator already enforces', and the migrator enforces only the entity half. SKILL.md:388 says so explicitly ('The apply enforces the half it can'), and migrate-scope.sh:393-407 is a `grep -Fc` of each proposed entity against the note's extract with no summary-sourcing check anywhere. A checker reading C-8 will go looking for a script assertion behind the summary row, not find one, and either fail correct work or record a bogus evidence pointer. Fix: attribute the entity half to the migrator (migrate-scope.sh:396, diagnostic `tier2-entity-unsourced`) and the summary half to the skill instruction at SKILL.md:386 / references/migration.md:72. The anti-vacuity shape of the rest is sound: 'every row in the scenario', 'No pass condition in this scenario', and the unsourced row must 'name the specific fabricated fact the extract lacks' all pin the search to the scenario and force a concrete artifact.
  evidence: inbox-to-memory/SKILL.md:386 (both rules stated), :388 ('The apply enforces the half it can'); inbox-to-memory/scripts/migrate-scope.sh:393-407 (entity-only `grep -Fc` loop, `tier2-entity-unsourced` at :407); inbox-to-memory/references/migration.md:72, :81. Constitution text at .agent-guild/state/constitution.md:60.
- **C-9** (blocker): PASS, including its failing example, which I tried to break rather than take. The example predicts `[memory candidate: journal]` is undefined; `grep -rn 'memory candidate: journal' inbox-to-memory/` returns nothing, while the two defined scope tokens (project, client) and the separate `[journal candidate: <generalized pattern>]` form all resolve, so the example fails a real artifact for the stated reason. The doc-to-skill direction makes whole-document extraction safe here: a universal quantifier over tokens cited in EVALS.md cannot be satisfied by a token sitting in the wrong section. The second half of the check ('any token whose use in the doc contradicts its definition at the source') is what stops a real token being graded with a wrong meaning, and it is the half most likely to be skipped - worth naming one worked instance in the clause so it is not read as boilerplate.
  evidence: `grep -rn 'memory candidate: journal' inbox-to-memory/` -> no output. Defined forms: inbox-to-memory/SKILL.md:96-99; inbox-to-memory/references/machine-contracts.md:69-72; inbox-to-memory/references/scope-decisions.md:5, :74-75.
- **C-10** (blocker): FAIL. The clause hangs its whole grading half on 'every scenario's run procedure', and no clause anywhere requires a run procedure to exist. C-2's required per-scenario contents are fixture, prompt, and pass-condition table; C-12's required shape is a method statement plus ## Fixtures, ## Scenarios, and ## Grading. Neither forces a run procedure. So an EVALS.md with no run procedure at all satisfies both halves of C-10 vacuously: 'every scenario's run procedure goes through it' is true of the empty set, and 'no scenario's run procedure names a `tests/fixtures/` path' is true for the same reason. That is the #29 pattern in a different key - not the right words in the wrong place, but no place at all. Fix: add the run procedure to C-2's per-scenario required contents (so it is inside the scenario's own slice, where C-2 already pins the window), and rewrite C-10's second half as a positive assertion - each scenario's run procedure invokes `eval-scope.sh` and runs the skill against the path it prints - rather than only the negative one. The script-behavior half of the check (run it, confirm it writes only under a scratch directory, confirm `git status --porcelain` unchanged) is genuinely deterministic and good as written; note that it is a script run, not a judgment call, so it could route to checker-deterministic if split out.
  evidence: .agent-guild/state/constitution.md:72-77 (C-10 text at :74, check at :75). Required per-scenario contents: constitution.md:18 (C-2). Required document shape: constitution.md:88 (C-12). Neither mentions a run procedure.
- **C-11** (blocker): FAIL, and this one is decisive on its own: the check command cannot pass in this repo, ever. `.agent-guild/hooks/test_hooks.py` does not exist here - there is no `.agent-guild/hooks/` directory at all. The file lives in the agent-guild plugin repo (~/.claude/plugins/marketplaces/kendrick/), not in kendrick/skills. Because the four commands are joined with `&&`, the chain's exit status is the python invocation's, so C-11 fails for every artifact regardless of the work. No retry, no escalation, and no correct deliverable can clear it; only editing the clause can. Fix: drop the `test_hooks.py` term. The remaining three suites are the right set and the set is complete for what this job touches - I derived the consumers of tests/fixtures/inbox-to-memory/ from the suite source rather than assuming. Adding a directory there has exactly two whole-tree consumers, both inside tests/inbox-to-memory-smoke.sh: line 515 forbids MEMORY.md/INDEX.md/index.md anywhere under a `*_memory*` path in the fixtures tree, and lines 583-586 with 1226-1229 shasum-snapshot every file under tests/fixtures/ and re-compare at the end, so an in-place run against an eval fixture goes red there (which is the same failure C-10 exists to prevent, caught twice - keep it). The per-scope loops at lines 109-117 and the lint invocations at 120/127/157 name old-only, mixed, and broken explicitly rather than globbing, so evals/ is not swept into the lint arithmetic. The other three suites and the guild's own scripts/test_*.py touch nothing this job writes.
  evidence: Ran the C-11 chain term by term: `bash tests/inbox-to-memory-smoke.sh` -> 0 ('inbox-to-memory smoke: ok'); `bash tests/file-issue-smoke.sh` -> 0; `bash tests/handoff-smoke.sh` -> 0; `python3 .agent-guild/hooks/test_hooks.py` -> 2, "can't open file '/Users/karnett/repos/skills/.agent-guild/hooks/test_hooks.py': [Errno 2] No such file or directory". `ls .agent-guild/` -> CLAUDE.md, schemas, scripts, state (no hooks/). Real location: ~/.claude/plugins/marketplaces/kendrick/.agent-guild/hooks/test_hooks.py. Consumer derivation: tests/inbox-to-memory-smoke.sh:109-117, :120, :127, :157, :515, :583-586, :1226-1229.
- **C-12** (major): FAIL on two points. (1) Coverage gap against spec acceptance criterion 2. The criterion is 'Each runs with and without the skill, and the delta is what gets graded', and C-4 covers only the stated expectation - what the no-skill run should produce. Nothing requires the results table to have anywhere to record the no-skill result. A table with one 'passed / evidence' column per scenario satisfies C-12 as written and C-4 in full, and the delta has no home in the artifact this job ships. Fix: require the per-scenario results table to carry a with-skill column and a without-skill column (or an explicit delta column) so the first run has somewhere to put both halves. (2) The check says 'Compare section headings against `_maintenance/file-issue/EVALS.md`, confirm all four are present', but the method statement is not a heading in the precedent - it is unheaded prose at lines 3-5, above ## Fixtures. The clause text gets this right ('a method statement'); only the check's 'all four are present' phrasing invites a false fail on a document that follows the precedent exactly. Say three headings plus a method statement.
  evidence: Spec criterion: .agent-guild/state/spec.md:23. Constitution: .agent-guild/state/constitution.md:88 (text), :89 (check). Precedent heading set, derived: `grep -n '^#' _maintenance/file-issue/EVALS.md` -> only '# file-issue Evals', '## Fixtures' (:7), '## Scenarios' (:14), six '### N.' scenario headings, '## Grading' (:105). The method statement sits at _maintenance/file-issue/EVALS.md:3-5 with no heading of its own.
- **C-13** (blocker): FAIL. Its three automatic-fail triggers collide with C-12 and with the repo's own prose, and one of them is ambiguous enough that two checkers will split. (1) Heading case directly contradicts C-12. C-13 says 'Headings stay title-cased' and makes 'a lowercased heading' an automatic fail; C-12 says keep the precedent's shape. Every one of the precedent's six scenario headings is sentence case - 'Bug with no reproduction offered', 'Ambiguous feature ask', 'Repo with issue forms', 'Agent-targeted bug', 'Spec-sized ask'. A worker who mirrors the precedent gets an automatic C-13 fail; one who title-cases diverges from C-12. The constitution has to rule, and neither clause does. (2) 'a lowercased heading' is ambiguous between 'entirely lowercase' and 'main words not capitalized' (i.e. sentence case). Paired with 'Headings stay title-cased' it reads as the latter, but a checker reading it as the former passes exactly the artifacts the other fails. That is below the two-checkers-agree bar this audit is measuring against. (3) The space-wrapped em dash trigger fires on the house style everywhere it can look: 11 instances in the precedent EVALS.md it is told to imitate, 47 in inbox-to-memory/SKILL.md, 4 in tests/fixtures/inbox-to-memory/README.md, and 6 in this constitution itself, including in C-1's own text. A rule the constitution breaks in the act of stating it will produce disputes the orchestrator has to rule on one by one, which is the failure mode the dispute section warns about ('If one checker keeps producing bad verdicts, the fault is usually the clause'). Fix: decide explicitly which authority wins for this document - the user's global style preference or the precedent's house style - and say so in the clause, resolving heading case for scenario headings by name. Keep the humanizer audit; drop or scope the automatic-fail list to triggers the surrounding corpus actually honors.
  evidence: Precedent scenario headings: _maintenance/file-issue/EVALS.md:16, :31, :46, :60, :75, :90. Space-wrapped em dash counts, `grep -c ' \u2014 '`: _maintenance/file-issue/EVALS.md = 11; inbox-to-memory/SKILL.md = 47; tests/fixtures/inbox-to-memory/README.md = 4; .agent-guild/state/constitution.md = 6 (including C-1's text at :11, 'The suite is exactly three things \u2014'). Constitution C-13 at .agent-guild/state/constitution.md:93-98; C-12 at :86-91.
- **coverage** (minor): PASS with one half-gap. All seven spec acceptance criteria have a clause behind them: criterion 1 -> C-2 plus C-3; criterion 2 -> C-4 for the stated expectation, but see C-12 for the missing recording half; criterion 3 -> C-5; criterion 4 -> C-6; criterion 5 -> C-7; criterion 6 -> C-10; criterion 7 -> C-1. The spec body's fourth behavior, 'whether generated summaries and entities are any good', is covered by C-8. No clause is orphaned: C-9, C-11, C-12, and C-13 each answer a job need rather than a criterion (vocabulary fidelity, regression safety, precedent shape, and the standing prose requirement), and all four are justified in their own text. The three settled decisions are each reachable and each constrained - purpose-built committed fixtures by C-1 and C-3, the staging script by C-1 and C-10, and specification-plus-empty-table by C-12 and the third non-goal.
  evidence: .agent-guild/state/spec.md:22-28 mapped against .agent-guild/state/constitution.md:9-98.
- **non-goals** (minor): PASS with one loose edge. The five non-goals are drawn tightly enough to stop gold-plating and none of them blocks a criterion: 'no graded runner' still leaves eval-scope.sh reachable because staging is not scoring; 'no execution' matches the settled empty-table decision and is enforced by C-12; 'no scaffold mode' narrows the surface without touching any criterion; 'no committing or pushing' matches standing user policy. The loose edge is the first one: 'If a scenario exposes a skill defect, the finding is filed as an issue, not fixed here' authorizes an unbounded external side effect (`gh issue create`) that no clause constrains and that check-diff-scope.py structurally cannot see, since it never touches the working tree. Prior runs in this repo staged issue drafts as files under state/ instead. Say which one is meant.
  evidence: .agent-guild/state/constitution.md:104-111. Prior-run precedent for staging rather than posting: .agent-guild/state/archive/2026-08-07/issues/close-14.md.
