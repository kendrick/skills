---
task: DEC-audit
checker: auditor
vendor: anthropic
model: claude-opus-5
verdict: PASS
checked_at: 2026-08-07T23:35:00Z
duration_ms: None
cost_usd: None
---

<!-- GENERATED FILE—do not hand-edit. Rendered by render-verdict.py
from the verdict JSON, the record of record. Edit the JSON and
re-render instead. -->

## Per-clause results

| clause | severity | description | evidence |
| ------ | -------- | ------------ | -------- |
| fixture-token-extraction (C-5, C-6, C-7, C-8) | minor | PASS. The r2 blocker is closed, and I re-derived the closure by running the four-step recipe against the linter's real pipeline rather than reading it. I implemented the recipe exactly as the four tasks now spell it -- skip CLAUDE.md/README.md, take only what extract_body yields, strip [[...]], read the bracketed shapes out of what remains, using the linter's own extraction regex from :230 -- and diffed it against `lint-scope.sh` over every correct fixture in the repo. old-only: recipe reports `[memory candidate:` and nothing else (legal, and the two lines at notes/2025-11-04-...:42-43 carry `project` and `client`); linter failures 0. mixed: recipe reports `[memory candidate:`, `[open question:`, `[open question resolved:`, `[tension:`, `[decision:`, `[contradicts accepted:` -- six shapes, every one a row of the ten-row table at machine-contracts.md:67-78; linter failures 0. journal-v1: recipe reports nothing; linter failures 0. broken: recipe reports `[followup candidate:` plus table rows only, and the linter's sole token-grammar failure on that fixture is `[followup candidate:`. Exact agreement on all four, positive and negative. Every one of r2's five false positives is gone, and I confirmed each by its mechanism rather than by absence: `[extends:` and `[confirms:` came from `related: [extends::<id>]` frontmatter, which step 2 now drops because extract_body's awk only sets its print flag at the second `---` (:214); `[rollback]` and `[runbook]` came from flow-style `topics:`/`tags:`, same reason; `[whatever brackets:` came from broken/notes/2026-03-04-unregistered-token-30z5F4kx6U.md:29, which step 2 now drops at the `## Raw Content` exit (:210). The four-step gloss is faithful to the source line by line: :418-419 is the CLAUDE.md/README.md `continue`, :208-216 is extract_body, :218-222 is the wiki-link sed, and `check_tokens` is called at :445 with `$body` from :429, which is extract_body's output -- so the pipeline the tasks describe is the pipeline the linter runs. | recipe-vs-linter diff over old-only, mixed, journal-v1, broken -> identical sets, and broken's single token-grammar failure `[followup candidate:` reproduced by both; inbox-to-memory/scripts/lint-scope.sh:208-216, :218-222, :230, :418-419, :429, :445; inbox-to-memory/references/machine-contracts.md:67-78; tests/fixtures/inbox-to-memory/broken/notes/2026-03-04-unregistered-token-30z5F4kx6U.md:29; tests/fixtures/inbox-to-memory/old-only/notes/2025-11-04-atlas-scoping-call-3iMu15QJ_x.md:42-43; tasks/T-002.md:29-43, T-003.md:19-31, T-005.md:31-48, T-006.md:31-47 |
| worker/checker scope agreement | minor | PASS on all four, and the asymmetry that remains runs in the safe direction. Each worker half now carries the matching scope sentence: T-002:73 and T-005:88 and T-003:72 all say 'The rule is scoped to note bodies above `## Raw Content` -- brackets in frontmatter and brackets inside raw content are legal and are not tokens'; T-006:84 says the same and adds the CLAUDE.md template illustrations, matching its own checker half at :37-40. So r2's 'the two halves disagree' finding is closed: a worker told to match 'every inline token you write' and a checker told to extract 'this fixture's tokens' are now reading the same boundary, and the `related: [extends::<id>]` a worker copies from the note template is out of scope for both. The asymmetry: the checker halves exclude strictly more than the worker halves state -- CLAUDE.md/README.md in T-002, T-003 and T-005, and HTML comments in all four. That direction is sound. A worker who believes fewer things are exempt writes a fixture the checker's wider exemption also passes. It cannot fail correct work, and it cannot let bad work through either, because the linter's own exemptions are exactly the checker's. | tasks/T-002.md:73 vs :29-43; T-003.md:72 vs :19-31; T-005.md:88 vs :31-48; T-006.md:84 vs :31-47; inbox-to-memory/scripts/lint-scope.sh:204-216 (HTML-comment rationale), :415-419 (CLAUDE.md/README.md rationale) |
| regression on the four edited files | minor | PASS. The fix introduced no defect. Three residuals, each named below, none of which fails correct work or passes bad work. (a) The recipe names no file set. The linter's is fixed by the glob at :414 -- `notes/*.md`, `_memory/*/*.md`, `entries/*.md` -- and `_inbox/` is not in it. I probed both shapes an eval fixture will actually ship: a frontmatter-less transcript carrying `[inaudible]` reports nothing, because extract_body never reaches its second `---` and yields an empty body; but a forwarded-email-shaped `_inbox/` file with two `---` separator lines and a lowercase bracketed word does report it, and the linter would never look at that file at all. The chain needed to trip it is thin -- an `_inbox/` .md, two `---` lines, then a lowercase token-shaped bracket -- and 'for every other note' plus the cited line range resolves it for a checker who opens :414. Residual. The one-line repair if it ever bites: name the three globs. (b) T-006's rationale misattributes its templates. Its client root's CLAUDE.md comes from `assets/claude-md/client.template.md`, which carries `[journal candidate:` -- a legal table row -- and neither `[memory candidate]` nor `[topic]`. Those live in `_memory.template.md` (SKILL.md:218 puts it at `_memory/CLAUDE.md`) and `patterns-journal.template.md`. The instruction is right whichever template ships: skip CLAUDE.md and README.md. Only the worked example is off, and it errs toward skipping. Worth noting too that a client-root CLAUDE.md is not excluded by :418-419 at all -- it sits outside all three globs, so the linter never considers it. Residual. (c) T-003's worker half at :72 still says 'this fixture is v1 throughout' while its corrected checker half at :22-23 says 'Do not read this as requiring the whole fixture be v1 -- only that no note the scenario grades is v2.' Not a contradiction in outcome: the worker's belief is the stricter of the two, the excerpt at :60 and :69 steers to a v1 scope modelled on old-only anyway, and a fixture built v1 throughout satisfies both halves. r2's overreach was in the half that could fail correct work; that is the half that got fixed. Residual. | inbox-to-memory/scripts/lint-scope.sh:414; probe: _inbox/call.md (no frontmatter, `[inaudible]`) -> recipe reports nothing; _inbox/thread.md (two `---` separators, `[crosstalk]`) -> recipe reports `[crosstalk]`, linter reads neither file; inbox-to-memory/assets/claude-md/client.template.md:60 (`[journal candidate:` only), _memory.template.md:66 (`[memory candidate]`), patterns-journal.template.md:106 (`[topic]`); inbox-to-memory/SKILL.md:209, :218; tasks/T-006.md:37-40, T-003.md:22-23 vs :60, :69, :72 |
| coverage | minor | PASS, re-derived from spec.md rather than carried from r2. AC1 (fixture, prompt, pass condition per scenario) -> C-2 -> T-004. AC2 (with and without the skill, delta graded) -> C-4's per-scenario baseline plus C-12's two-result table -> T-004, with the per-run second copy on T-001's script and T-004's procedure section. AC3 (contradiction round trip end to end) -> C-5 -> T-002 fixture, T-004 document. AC4 (unacknowledged tension, own scenario) -> C-6 -> T-005, T-004. AC5 (scope proposal across all three tiers) -> C-7 -> T-006, T-004, tiebreak as the fourth input. AC6 (nothing written to a real scope) -> C-10 -> T-001 script half, T-004 whole clause, backstopped by C-11's shasum snapshot on all six. AC7 (doc beside the other maintainer evals) -> C-1's path plus C-12's shape -> T-004. The spec body at :18 names a fourth behavior the criteria list does not -- 'whether generated summaries and entities are any good' -> C-8 -> T-003, T-004. Nothing in the spec is uncovered. In the other direction nothing is orphaned: C-1 and C-11 on all six; C-3 on the four fixture tasks plus T-004; C-2, C-4, C-9 and C-12 on T-004 alone, correctly, since all four govern the document only; C-5, C-6, C-7 and C-8 each on one fixture task plus T-004; C-10 on T-001 and T-004; C-13 on T-001 and T-004, which is exactly the two artifacts its own text names ('EVALS.md and the comments in eval-scope.sh'). Every task's check_method covers every clause in its own `clauses` field, which I checked field by field. | spec.md:16-28; constitution.md:9-98; tasks/T-001.md:5,:9-26, T-002.md:5,:9-45, T-003.md:5,:9-45, T-004.md:5,:9-31, T-005.md:5,:9-50, T-006.md:5,:9-49 |
| routing | minor | PASS, re-derived against the routing table in .agent-guild/CLAUDE.md. Every checker is checker-judgment, and the table forces it: a clause checked by a script routes to checker-deterministic, a clause checked by a rubric to checker-judgment, and every one of the six tasks carries at least one rubric clause. T-001 is the closest call -- C-1 and C-11 are bare script invocations -- but C-10's script half requires judging a staged tree a faithful copy by diffing it, and C-13 is a prose audit, so checker-judgment is right. No task is a worker-bulk candidate; all six require reading SKILL.md or its references and making a call that a mechanical worker cannot. Executors follow the work rather than a default. worker-standard/sonnet on T-001: a fifty-line shell script against a stated interface, judged on correctness. worker-craft/opus on T-002, T-005 and T-006: each turns on a reading of SKILL.md:122 or scope-decisions.md where a wrong call ships a fixture that grades correct behavior as a failure -- judgment density, not size. worker-standard/sonnet on T-003: its one trap (v2 notes emit no extract) is named outright in the excerpt at :60 and verification is a script run. worker-craft/opus on T-004: the taste artifact, and the job's central deliverable. Every `executor_model` matches its agent's tier. | .agent-guild/CLAUDE.md model routing table; tasks/T-001.md:6-8, T-002.md:6-8, T-003.md:6-8, T-004.md:6-8, T-005.md:6-8, T-006.md:6-8 |
| deps | minor | PASS, re-derived from the files. T-001 [] -> T-002 [T-001] -> T-005 [T-002] -> T-006 [T-005] -> T-003 [T-006] -> T-004 [T-001, T-002, T-003, T-005, T-006]. A DAG, no cycles, every referenced id exists, and the chain admits exactly one execution order. The total order earns its keep rather than over-constraining: C-11 is whole-repo, unqualified, and cited by all six, so under any parallelism one task's check would run against a tree another worker is mid-write in. C-1's whole-tree read is covered twice, by the ordering and by the sibling-tolerance sentence each task carries -- which I verified against the script, since check-diff-scope.py judges paths against a static allowlist and has no notion of which task wrote them, so a sibling's file under `tests/fixtures/inbox-to-memory/evals/` really is a pass. Two residuals carried from r1 and r2 and still correct: `deps` is a declaration with no dispatch-time gate in this repo, so the serialization is only as good as the dispatch discipline honoring it; and T-004's five-way `deps` is redundant against the chain, which is the right form for the one task that has to open every artifact. | tasks/T-001.md:30, T-002.md:49, T-003.md:49, T-004.md:35, T-005.md:54, T-006.md:53; .agent-guild/scripts/check-diff-scope.py:11-14 |
| fixture split | minor | PASS, re-derived. Four ways, not three: C-5 to T-002, C-6 to T-005, C-7 to T-006, C-8 to T-003, each owning one directory under `tests/fixtures/inbox-to-memory/evals/` and each told not to touch a sibling's. Every one carries C-1, C-3 and C-11 alongside its behavior clause, so nothing was dropped in the cut and nothing landed twice. The behavior most at risk of falling between fixtures travelled correctly: the two-project client root sits in T-006 with the cross-project recurrence signal that needs it, which re-derives against scope-decisions.md:31-37 (`grep -l -r "<subject>" "<client-root>"/{pursuits,projects}/*/notes/`, two or more hits in distinct project directories) and against SKILL.md:209 for where a project sits under a client root. T-005 carries the interference check with its reason attached at :81, and teaches it as a named trap rather than stating it once. Residual, unchanged: no task fixes its directory name, so `<the delivered directory>` in three check_methods resolves through the worker's `artifacts` list. The total order means the tree grows one directory at a time, which blunts it. | tasks/T-002.md:58, T-003.md:56, T-005.md:63, :81, T-006.md:62, :64, :77; inbox-to-memory/references/scope-decisions.md:31-37; inbox-to-memory/SKILL.md:209 |
| sizing | minor | PASS on all six. Each fixture task maps one behavior to one clause to one directory, so a FAIL names one thing and a rework diagnosis stays coherent -- which was the point of r0's split. T-001 is one shell script of roughly fifty lines against a stated interface. T-002 is one accepted record, one contradicting input, optional decoys. T-003 is a small v1 scope with one name and one fact planted below the Raw Content fence. T-005 needs two slugs across three or more prior notes each plus an acknowledging transcript, about the size of `mixed` (8 files). T-004 is one document against a 109-line precedent. T-006 stays the largest -- a client root, two opted-in project directories with `notes/` and `_memory/`, a CLAUDE.md, four validated inbox inputs -- but it is still one behavior and one clause, so it stays inside one dispatch. It remains the one to watch if a retry budget starts burning. | tasks/T-006.md:60-88; tests/fixtures/inbox-to-memory/mixed (8 files, 5 v2 / 3 v1); _maintenance/file-issue/EVALS.md (109 lines) |
| T-004 check_method | minor | PASS, re-derived and unchanged. The governing instruction is complete on its own and comes first: run each clause's `check` field from the constitution in full, exactly as written, nothing narrowed, no conjunct satisfied by an upstream verdict, the clause text is the contract. The paragraph that follows sits under a heading announcing itself as orientation, is generated by the universal that precedes it, and flags itself 'Concretely, and not exhaustively' -- so it reads as illustration, not as the checklist. Its four instances each derive correctly: expected-token-to-input pairing is C-7's second conjunct, positive/negative row-to-slug pairing is C-6's, per-row decidability against the emitted extract is C-8's, and the `tests/fixtures/` run target is C-10's fourth sentence. Clause accounting is complete -- C-1 and C-11 by script, the remaining eleven by their own check fields, and `clauses` lists all thirteen. One consequence rather than a defect, worth carrying into Phase 2: this checker runs eval-scope.sh, runs `--tier2-extract`, and judges eleven rubrics across one document and four fixture trees. It is the largest check in the job and the likeliest place to see a `blocked`. | tasks/T-004.md:5, :9-31; constitution.md:54, :47, :61, :75 |
| fresh sweep | minor | PASS on all four shapes. Halves that both defer: none. Every split clause carries a non-delegation sentence on the upstream side -- C-3's 'the citation half is checked again on T-004' in all four fixture tasks, C-5/C-6/C-7/C-8's 'that half is not delegated back here', C-10's 'the run-procedure half belongs to T-004' -- and the downstream side is T-004 running the whole clause unnarrowed. Every split is an overlap rather than a partition, which is the safe direction. Checks assuming work no task performs: none. T-004's C-10 phase needs eval-scope.sh from T-001, its C-8 phase needs an extract-emitting fixture from T-003, its C-12 phase needs `_maintenance/file-issue/EVALS.md`, which exists at 109 lines; the chain delivers all three ahead of it. T-006's '(a) ... run it over each opted-in subdirectory too' is correctly qualified, and the qualifier is load-bearing -- I confirmed lint-scope.sh exits 2 with 'not an opted-in scope' on a bare `notes/` directory, so an unqualified 'run it on every subdirectory' would have manufactured a failure. T-003's C-8 invocation form is exact against the shipped usage at migrate-scope.sh:39, and the migrator's extract really does stop at `## Raw Content` (:178), so 'confirm a fact planted only below that fence is genuinely absent from the extract' is checkable as written. Excerpts that would have a worker guess: no new ones. Paraphrase drift: none. I re-read every excerpt paraphrase against its clause and its source -- T-004:51's baseline transplant test keeps C-4's 'with or without its fixture name swapped' qualifier and all three named shapes; T-004:53 keeps C-10's 'Neither any scenario nor any step of the procedure'; T-004:66 keeps C-9's full enumeration including lint failure codes; T-004:49 pre-empts the precedent on C-2 locality rather than restating it; T-005:67 quotes SKILL.md:122 word for word; T-004:57's contradiction flag form matches SKILL.md:100 character for character; T-002:64 tracks SKILL.md:69 and T-002:68 tracks SKILL.md:67's five-body-read budget. | tasks/T-001.md:16, T-002.md:16-17, T-003.md:32, :34-36, T-004.md:21-31, :49, :51, :53, :57, :66, T-005.md:17-19, :67, T-006.md:15, :18-20, :28-30; probe: lint-scope.sh on a bare notes/ dir -> 'not an opted-in scope', exit 2; inbox-to-memory/scripts/migrate-scope.sh:39, :178; inbox-to-memory/SKILL.md:67, :69, :100, :122 |
| C-1, C-11 | minor | PASS on both. C-1's invocation appears with all three arguments in every task file, and check-diff-scope.py's contract backs the sibling-tolerance note each carries: it takes an exact path or a `/`-terminated directory prefix, judges the union of `git status --porcelain` and `git diff --name-only` against that allowlist, and always permits `.agent-guild/state/`. It has no notion of authorship, so a sibling's file under the evals prefix passes on the prefix alone. The non-goal most likely to trip C-1 reaches every task that could trip it: the four fixture tasks are each told not to update `tests/fixtures/inbox-to-memory/README.md`, T-004 not to update the `_maintenance/` inventory in the root README.md, each with the reason attached. C-11's tree-walking hazard is relayed where it lands -- the index-file prohibition appears in all four fixture excerpts, and the shasum byte-identity snapshot stays unviolable by this work, since `--tier2-extract` writes only to the directory named on the command line and eval-scope.sh is required to write only into `mktemp -d`. Residual, unchanged: T-004's C-1 invocation lists all three allowed paths unqualified, so a T-004 worker who also added a fixture directory would pass C-1 as checked there. Caught anyway by its excerpt's 'Touch only `_maintenance/inbox-to-memory/EVALS.md`' at :70 and by its running C-3 in full. | .agent-guild/scripts/check-diff-scope.py:10-30; tasks/T-002.md:74, :78, T-003.md:71, :77, T-005.md:89, :93, T-006.md:85, :90, T-004.md:70, T-001.md:59; constitution.md:110 |
| residuals for Phase 2 | minor | The full residual set the Phase 2 checkers inherit, each decidable by reading the text and none of them worth a fifth round. New this round, from the fix itself: the token recipe names no file set, so a checker has to read lint-scope.sh:414 to know `_inbox/` is outside it; T-006's CLAUDE.md rationale names the wrong templates while giving the right instruction; T-003's worker half still asserts 'v1 throughout' where its checker half no longer requires it. Carried and unchanged: 'runs only over files carrying `schema: 2`' omits the `body_schema: 1` gate at lint-scope.sh:444, moot because the same sentence puts the check on the worker by hand regardless; T-002 and T-006 state C-9's enumerated-value tightening explicitly while T-003 and T-005 leave it to the table's own row form, which does carry it; all four cite :218-230 while the memory-candidate scope check lives at :232-237, covered by the instruction to match against the table rather than the prefix list; T-003 never names `notes/` as where its gradable notes go, resolved by 'follow the file shapes of the existing old-only fixture'; T-004 never states C-13's automatic-fail-on-hard-wrap rule, resolved by the precedent, which I confirmed is not hard-wrapped (longest line 323 characters); T-005 never says how many `_inbox/` transcripts the fixture holds; T-006's 'where the tier requires it' never says when; no fixture task fixes its directory name. One more to add to that list, which prior rounds did not name: T-005:88's 'so the slug has to appear above the fence to count' is not true of the mechanism it describes. The skill's recurrence grep at SKILL.md:115 is `grep -rhoE '\\[open question( resolved)?: [^]]*\\]' <scope-root>/notes/` -- whole-file, not body-scoped -- so a slug below `## Raw Content` would count for the skill even though the linter's own v2 slug harvest at :430 is body-scoped. It steers the worker to the safe place and T-005's C-6 half counts whole-file at :20, matching the grep, so nothing fails either way. | inbox-to-memory/scripts/lint-scope.sh:414, :430, :444, :218-237; inbox-to-memory/SKILL.md:115; _maintenance/file-issue/EVALS.md (longest line 323 chars, not hard-wrapped); tasks/T-002.md:43, :73, T-003.md:22-23, :72, T-005.md:20, :88, T-006.md:37-40, :47, :84 |
