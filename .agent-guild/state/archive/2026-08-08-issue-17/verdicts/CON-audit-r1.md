---
task: CON-audit
checker: auditor
vendor: anthropic
model: claude-opus-5
verdict: FAIL
checked_at: 2026-08-07T21:46:30Z
duration_ms: None
cost_usd: None
---

<!-- GENERATED FILE—do not hand-edit. Rendered by render-verdict.py
from the verdict JSON, the record of record. Edit the JSON and
re-render instead. -->

## Per-clause results

| clause | severity | description | evidence |
| ------ | -------- | ------------ | -------- |
| C-1 | minor | PASS. I re-ran the check verbatim and it exits 0, so the r0 finding about the command itself is closed, and the new non-goal at :110 settles the two stale inventories the way the user decided. Two notes for the record. (a) `--ignore .agent-guild/state/` is still a no-op and I confirm r0 on this: check-diff-scope.py matches --ignore by exact string against a set (`path in ignored`, :110), never by prefix, and `.agent-guild/state/` is already unconditionally permitted at :112. It costs nothing but it reads as coverage it does not provide, and a later job copying this clause will inherit the misreading. (b) The check is one-directional. It exits 0 on a clean tree, so a green C-1 never proves any of the three named deliverables landed - it only proves nothing extra did. Existence rides entirely on C-3 (fixture directories must be there for the subset test to mean anything), C-10 (the checker actually executes eval-scope.sh), and C-2/C-12 (EVALS.md content). That is adequate coverage, but the clause text opens 'The suite is exactly three things' and a checker can reasonably read a green C-1 as having confirmed all three exist. One sentence saying the check verifies the ceiling and not the floor would prevent it. | Ran verbatim: `python3 .agent-guild/scripts/check-diff-scope.py _maintenance/inbox-to-memory/ tests/fixtures/inbox-to-memory/evals/ --ignore .agent-guild/state/` -> 'OK: 0 path(s) in scope', exit 0. --ignore semantics: .agent-guild/scripts/check-diff-scope.py:107-117 (exact-match set at :110, hardcoded state/ carve-out at :112). Non-goal at .agent-guild/state/constitution.md:110. |
| C-2 | minor | PASS, re-derived rather than inherited, and it remains the anti-vacuity model the weaker clauses should copy. The window is pinned in the text ('followed - before the next scenario heading', 'All three live inside the scenario's own section') and again operationally in the check ('extract only the text between that heading and the next one... Anything found outside the slice under test does not count toward it'). I tried the obvious dodge against it: an EVALS.md that names every fixture once in a global `## Fixtures` section and never inside a scenario fails, which is the right answer and is exactly what the precedent's own structure does correctly. No revision elsewhere opened a gap here. Residual, unchanged from r0 and still worth a word: 'scenario heading' is not tied to a heading level, so a scenario carrying a `#### Expected` subheading admits a reading where the slice ends early. C-12 requires numbered scenarios, so keying the slice on the numbering ('between one numbered scenario heading and the next') closes it. | .agent-guild/state/constitution.md:16-21. Precedent structure where fixture, prompt, and table all sit inside the scenario's own slice: _maintenance/file-issue/EVALS.md:16-29, :31-44. |
| C-3 | minor | PASS, re-derived. The opt-in test is quoted correctly against source and the subset direction is the safe one: cited names must be a subset of real directories, which cannot be satisfied by text sitting in the wrong section. Anti-vacuity sweep: a fixture that is well-formed but useless does pass C-3, but that is C-3's job - it is the existence-and-opt-in floor, and the usefulness bar belongs to C-5, C-6, C-7, and C-8. The gap that matters is that two of those (C-7, C-8) do not currently hold that bar; see their rows. C-3 itself is sound. | Opt-in rule verified at inbox-to-memory/SKILL.md:30-34 ('A directory is opted in when it contains both `_inbox/` and one of these markers: `_memory/` -> project or client scope. `entries/` -> journal scope.'). Existing fixtures confirm the shape: tests/fixtures/inbox-to-memory/{old-only,mixed,broken}/ carry `_inbox/` + `_memory/`, journal-v1/ carries `_inbox/` + `entries/`. |
| C-4 | minor | PASS, re-derived, and C-12's new results-table requirement complements it rather than colliding with it - C-4 owns the stated expectation per scenario, C-12 owns the place a run writes the observed result. Together they now cover both halves of spec criterion 2, which closes r0's half-gap. One residual worth fixing on the next pass: the check's mechanical anti-boilerplate test is 'Flag any baseline sentence that appears verbatim in two or more sections', and verbatim is trivially evaded by templating. 'Without the skill, the model will not flag the contradiction in fixture-X' repeated across five sections with only the fixture name swapped survives the verbatim test and also survives 'names something particular to that scenario's fixture or prompt', because the fixture name is a particular. The clause text already reaches further ('a baseline that says nothing about this scenario's input has skipped it'); the check should reach as far. A test that bites: flag any baseline that would still be true if you substituted another scenario's fixture and prompt. | .agent-guild/state/constitution.md:30-35 (text at :32, check at :33). Spec criterion at .agent-guild/state/spec.md:23. Complementary half at constitution.md:88 (C-12's without-skill column). |
| C-5 | minor | PASS, re-derived from source rather than inherited from r0. Every artifact the clause names checks out with the same spelling and the same meaning: the flag grammar `[contradicts accepted: [[<filename>\|<short-name>]]] <statement> \| claims: <what the record says>` (SKILL.md:100, and the fielded form at references/machine-contracts.md:93-94), `status: accepted` as the flag-producing status, phase 2.5 as the phase that reads memory before writing, the rewrite target `[[<target>\|memory - updated]]`, the `source_refs` append, and the `(added YYYY-MM-DD, <note-id>)` amendment marker. The check reaches into the fixture ('read the fixture to confirm it holds an `accepted` record that the planted input genuinely contradicts'), which is the reach C-7 and C-8 are missing. Anti-vacuity sweep: a well-formed-but-useless fixture is excluded here, because 'genuinely contradicts' is a content judgment the checker must make against the planted input. | inbox-to-memory/SKILL.md:100 (taxonomy), :136-138 (phase 5 sign-off gate), :151 (`[[<target>\|memory - updated]]` rewrite), :154 (`source_refs` append), :155 (`(added YYYY-MM-DD, <note-id>)`), :162 (amend outcome); inbox-to-memory/references/machine-contracts.md:93-94. |
| C-6 | major | FAIL, and this is a clause r0 passed - the sweep found it, not the revisions. The check reaches into the fixture for the positive branch only: 'count the prior notes in its fixture carrying the slug and confirm the count is three or more'. For the negative branch it asks only that a row be present in the table ('confirm both the positive and negative rows are present in its table'). So the negative case has exactly the vacuity C-6's own failing example names, one branch over. A fixture whose second slug appears in two notes, or whose third note contains no passage naming the question aloud, satisfies C-6 as written while grading the skill wrongly: the skill would correctly emit `[tension: unacknowledged]` (SKILL.md:122 fires at three or more unless someone names it out loud), and the eval would score correct behavior as a failure. That is the 'worse than no eval' condition C-9 states in its own text, arriving through an absence rather than through a token, so C-9 does not catch it either. Fix: make the fixture-side count cover both slugs, and add the condition the negative branch actually turns on - the fixture's input must contain the passage where someone names the slug aloud, quoted or line-referenced in the scenario. Separately, I disagree with r0's other suggestion here and would not adopt it: r0 wanted the clause to say what the negative case must be flagged as instead. SKILL.md:122 states only the suppression, not a replacement disposition; the repo's own fixture README calls it a deferred tension but SKILL.md does not require that, so mandating `[tension: deferred]` would over-specify past the skill text and invite exactly the kind of invented expectation C-9 exists to stop. | Constitution C-6 at .agent-guild/state/constitution.md:44-49 (text at :46, one-sided check at :47). Trigger: inbox-to-memory/SKILL.md:122 ('Flag a slug open across three or more notes as `[tension: unacknowledged]`, unless the transcript shows someone naming it out loud'). Disposition not mandated anywhere in SKILL.md; the deferred reading comes only from tests/fixtures/inbox-to-memory/README.md and machine-contracts.md:91's grammar row. |
| C-7 | blocker | FAIL. The repair relocated the hole rather than closing it, and it did so by adopting a mechanism claim that is wrong - r0's, which I disagree with on the substance. What C-7 grades is the phase 3 candidate token. references/scope-decisions.md:3 says so outright ('How to propose `project`, `client`, or `journal` scope when flagging a memory or journal candidate during phase 3'), and :9-19 is a table of content signals that picks the token. SKILL.md:35's path walk picks the phase 1 scope root, which is where the run is anchored and where records get written - a different thing. Nothing in phase 3 or scope-decisions.md constrains the token to the detected root; scope-decisions.md:57-61 says the opposite, that cross-scope references are fine. So the clause's stated reason ('because scope is detected from path and not from content (`SKILL.md` phase 1)') mis-attributes the mechanism, and the check inherits the error. Both directions bite. (1) It false-fails correct work. The natural journal-tier scenario is a client-rooted fixture whose input generalizes beyond the client (scope-decisions.md:17-19), expecting `[journal candidate: ...]`; C-7 demands that fixture's phase 1 walk resolve journal tier, i.e. an `entries/` marker, which turns the scenario into grooming an inbox inside the journal itself, where 'generalizes beyond this client' has no client to generalize beyond. (2) It still passes a well-formed-but-useless fixture, which is the sweep priority. Put a fixture at `client/pursuits/atlas/` so the walk resolves project, plant an inbox file whose only signal is a stakeholder org-chart fact (scope-decisions.md:13 -> client), and write a scenario expecting `[memory candidate: project]`. Layout check passes, token check passes, and the eval grades correct behavior as wrong. The tiebreak has the same shape: 'confirm one case presents conflicting signals and expects the narrower token' is evaluated against EVALS.md prose, so nothing confirms the fixture's input carries two signals from scope-decisions.md:9-19 pointing at different tiers. And the text's 'each on an input that could plausibly have gone elsewhere' has no counterpart in the check at all, which is the sentence carrying the whole judgment-testing point. Fix: replace the phase-1-walk requirement with a content-side one - read each scenario's fixture input and confirm it carries signals from the scope-decisions.md:9-19 table pointing at the expected tier, and for the tiebreak case at least two signals pointing at different tiers. Keep a layout requirement only where a signal needs one: the client-scope recurrence signal at :15 is verified by a grep of `<client-root>/{pursuits,projects}/*/notes/` (:32), so a fixture grading that signal does need a client root with two project note directories - which is a different property from 'the walk resolves client' and is not what the check currently asks for. | Constitution C-7 at .agent-guild/state/constitution.md:51-56 (mechanism claim at :53, check at :54). Token is content-picked: inbox-to-memory/references/scope-decisions.md:3, :5, :9-19, :23. Path walk picks the root, not the token: inbox-to-memory/SKILL.md:30, :35. Cross-scope proposals explicitly fine: scope-decisions.md:57-61. Recurrence grep needing two project note dirs: scope-decisions.md:29-35. Journal candidate arises from client work and routes to the cross-client journal at sign-off: scope-decisions.md:17, :75, :81. |
| C-8 | major | FAIL, on a different point than r0's - r0's finding is genuinely closed. The attribution is now correct and I verified both halves at source: `tier2-entity-unsourced` is the entity-half diagnostic the migrator emits (migrate-scope.sh:407), and SKILL.md:388's 'The apply enforces the half it can' confirms the summary half is unenforced judgment, which is what the revised clause now says. The new gap is fixture-side and it is the sweep's failure mode exactly. Every pass condition in this clause is stated against 'the note's emitted extract' - emitted by `migrate-scope.sh --tier2-extract` - and that command only emits for v1 notes. migrate-scope.sh:44 says 'for each v1 note'; SKILL.md's Tier 2 section says 'Once a note carries `schema: 2` the migrator skips it whole, so there is no adding summaries to a scope that already migrated.' Nothing in the constitution requires the Tier 2 scenario's fixture to be a pre-v2 scope. Build it out of v2-shaped notes and it is well-formed, passes C-3's opt-in test, passes C-1, and produces no extract at all, so every row in the scenario is unfalsifiable and the fourth judgment area the job exists to cover grades nothing. The check's 'confirm every row in the scenario is decidable by reading the extract' is the sentence that ought to catch this, but it does not say whose extract - a checker reading an illustrative extract quoted inside EVALS.md satisfies it without ever running the extractor. Fix: require the Tier 2 fixture to hold at least one note with no `schema` key, and have the check obtain the extract by running `migrate-scope.sh <fixture> --tier2-extract <dir>` rather than by reading one transcribed into the doc. The repo's `old-only/` fixture is the precedent for the shape and is what the smoke suite's own Tier 2 tests copy. | Constitution C-8 at .agent-guild/state/constitution.md:58-63. Attribution verified: inbox-to-memory/SKILL.md:386 (both rules), :388 ('The apply enforces the half it can', `tier2-entity-unsourced`); inbox-to-memory/scripts/migrate-scope.sh:407 (the diagnostic). v1-only extraction: migrate-scope.sh:11 ('hands an agent each v1'), :44 ('--tier2-extract for each v1 note, write its extracted sections'), v1/v2 gate at :157-164 (`has_schema_key`). Smoke-suite precedent, all four Tier 2 tests copy the v1 fixture: tests/inbox-to-memory-smoke.sh:778, :802, :843, :896 (`cp -R "$fixtures/old-only/."`). |
| C-9 | blocker | FAIL - a contradiction with C-10 and C-3 that r0 passed and that the C-10 revision made unavoidable. C-9 quantifies over 'Every candidate-flag token, phase number, frontmatter key, `status` value, `memory_type`, folder name, and script path the doc cites', and requires each to appear in `inbox-to-memory/SKILL.md` or its references. `eval-scope.sh` is a script path the doc must cite: C-10's revised check now fails the clause outright unless a run-procedure section exists and names it. That script does not appear anywhere under `inbox-to-memory/` - I grepped, zero hits - and it never can, because C-1 freezes that directory ('Nothing under `inbox-to-memory/` changes'). So C-1, C-9, and C-10 cannot all be satisfied by any artifact, and two of the three are blockers. The same reading catches the fixture directory names: C-3 requires the doc to name directories under `tests/fixtures/inbox-to-memory/evals/`, and those names are folder names the doc cites, absent from SKILL.md by construction. A charitable checker scopes C-9 to the skill's own vocabulary and lets both through; a literal one fails correct work; the two disagree, which is below the bar. Fix: bound the quantifier explicitly - C-9 governs vocabulary the skill emits or consumes, and exempts the paths and directory names this job creates, naming `eval-scope.sh` and `tests/fixtures/inbox-to-memory/evals/` so the exemption is not itself a judgment call. Everything else about the clause holds: I re-derived the failing example rather than taking it, and `[memory candidate: journal]` is indeed undefined (journal candidates take no scope token, SKILL.md:99), so the example fails a real artifact for the stated reason. | Constitution C-9 at .agent-guild/state/constitution.md:65-70 (quantifier at :67), C-10's naming requirement at :75, C-1's freeze at :11. `grep -rn 'eval-scope' inbox-to-memory/ _maintenance/` -> no output; `ls _maintenance/inbox-to-memory/` -> No such file or directory. Journal token has no scope form: inbox-to-memory/SKILL.md:96-99; references/machine-contracts.md:69-72. |
| C-10 | minor | PASS. The repair holds rather than relocating the hole. r0's diagnosis was that both halves quantified over 'every scenario's run procedure' while nothing required a run procedure to exist, so the empty set satisfied both vacuously. The revision makes existence a gate rather than a quantifier - 'First confirm a run-procedure section exists and names `eval-scope.sh`; if there is no such section this clause fails outright, with no further checking' - and the negative half now runs behind a positive that has to hold first. I checked the remaining shape for a new hole and did not find one: a scenario that names no run target at all is fine, because the clause requires a single named section that takes a reader 'from a fixture name to a running scenario', so a scenario naming only its fixture is complete by construction. I also note the constitution declined r0's suggested fix (put the run procedure inside each scenario's C-2 slice) in favour of one named section, and I think that was the better call - repeating the staging steps per scenario is the version that drifts. Two notes. The script-behavior half (run it, confirm the printed path, confirm `git status --porcelain` unchanged) is a deterministic script run inside a judgment check and could split out to checker-deterministic. And 'leaves `git status --porcelain` unchanged' should say before-versus-after rather than empty, since the job's own uncommitted deliverables will be in that output. | Constitution C-10 at .agent-guild/state/constitution.md:72-77 (existence gate at :75). Backstop confirming the same rule empirically: tests/inbox-to-memory-smoke.sh:586 and :1229 shasum every file under tests/fixtures/ and re-compare at the end. |
| C-11 | minor | PASS. r0's decisive finding is closed - the `.agent-guild/hooks/test_hooks.py` term is gone, and that directory still does not exist in this repo. I confirmed the check independently, both term by term and as the single `&&` chain the clause actually specifies, and all three suites exit 0 at baseline. I also re-derived the consumer claim rather than inheriting it: `$fixtures` is `tests/fixtures/inbox-to-memory` and is referenced 48 times in the suite, but exactly two of those walk the whole tree and so reach a new `evals/` directory - the index-file `find` and the shasum snapshot. Every other reference names `old-only`, `mixed`, `broken`, `journal-v1`, or a specific file, so a new directory is not swept into the lint arithmetic. Two line citations are off and should be corrected, since they are what a checker follows: `:514` is the `require_text` assertion about SKILL.md prose, and the tree-wide `find` is at `:515`; the shasum re-comparison is at `:1229`, not `:1227` (which is the middle line of the comment explaining why). The `:586` citation is exact. C-10's `:1226` is apt as written, because it points at the comment that says why. | Chain as written: `bash tests/inbox-to-memory-smoke.sh && bash tests/file-issue-smoke.sh && bash tests/handoff-smoke.sh` -> exit 0 (each individually 0 as well). `ls .agent-guild/` -> schemas, scripts, state, templates, CLAUDE.md; no hooks/. Tree-wide consumers: tests/inbox-to-memory-smoke.sh:515 (`find "$fixtures" -path '*_memory*' ...`), :586 (`fixtures_before="$(find "$fixtures" -type f -exec shasum {} + \| sort)"`), :1229 (re-comparison). Explicit-scope loop at :109-117; lint invocations at :120, :127, :157. |
| C-12 | minor | PASS. The added results-table requirement closes r0's coverage gap against spec criterion 2 and does not overlap C-4 harmfully - C-4 owns the per-scenario stated expectation, C-12 owns the column a run writes the observed baseline into, and the failing example ('\| Scenario \| Passed \|') falsifies exactly the artifact that would satisfy C-4 alone. I checked the new requirement against the precedent for a collision and there is none, though it is worth naming: the precedent's Grading section carries no table at all, only prose telling a runner what to record, so 'keeps the shape of its precedent' plus 'ships the empty table' is an addition to the precedent rather than a copy of it. The clause states that plainly enough that a checker will not read it as a contradiction. One residual r0 raised that the revision did not take, and I would still take it: the check opens 'Compare section headings against `_maintenance/file-issue/EVALS.md` and confirm all four are present with content', but only three of the four are headings. The method statement sits as unheaded prose above `## Fixtures` in the precedent. The clause text gets this right; the check's phrasing invites a false fail on a document that follows the precedent exactly. Say three headings plus a method statement. | Constitution C-12 at .agent-guild/state/constitution.md:86-91 (text at :88, check at :89). Precedent heading set, re-derived: _maintenance/file-issue/EVALS.md has `# file-issue Evals` (:1), `## Fixtures` (:7), `## Scenarios` (:14), six `### N.` scenario headings, `## Grading` (:105). Method statement unheaded at :3-5. Grading section is prose with no table: :107-109. |
| C-13 | minor | PASS. The three collisions r0 named are gone. Heading case and dash spacing now defer to `_maintenance/file-issue/EVALS.md`, which is the user's settled ruling and which I am not reopening; the check states the deference operationally ('Dash and heading style are judged only against `_maintenance/file-issue/EVALS.md`, and consistency with it is a pass'), so a checker has a concrete corpus to compare against rather than a house rule the corpus contradicts. The remaining automatic fail, hard-wrapped prose, is mechanically decidable and matches standing user policy. One thing I checked for a new collision and found clean: C-13 bans `- **Thing:** description`, and the precedent's Fixtures list uses `- **`fixture-bare`** - description`. Those are different forms, the colon versus the dash, and the user's own guidance calls the dash form fine, so a worker mirroring the precedent's bullet style does not trip C-13. The clause writes the banned form precisely enough that the distinction survives a careful checker, though naming the precedent's bullet as explicitly allowed would remove the last chance of conflation. | Constitution C-13 at .agent-guild/state/constitution.md:93-98. Precedent style corpus: _maintenance/file-issue/EVALS.md:11-12 (dash-form bullets), :16, :31, :46, :60, :75, :90 (sentence-case scenario headings), 11 space-wrapped em dashes throughout. |
| coverage | minor | PASS. All seven acceptance criteria have a clause whose violation would be caught, though two of those clauses fail on their own terms above and the coverage is therefore nominal until they are fixed. Mapping: criterion 1 -> C-2 with C-3; criterion 2 -> C-4 for the stated expectation and C-12 for the recorded result, which together now cover it (r0's half-gap is closed); criterion 3 -> C-5; criterion 4 -> C-6; criterion 5 -> C-7; criterion 6 -> C-10; criterion 7 -> C-1. The spec body's fourth behavior, 'whether generated summaries and entities are any good', maps to C-8. No clause is orphaned: C-9, C-11, C-12, and C-13 answer job needs stated in their own text rather than criteria - vocabulary fidelity, regression safety, precedent shape, and the standing prose requirement. The two-run method itself, which criterion 2 also asserts, is required by C-12's method statement rather than by C-10's run procedure; that is fine but slightly counterintuitive, since C-10 is the clause that owns how a scenario gets run. | .agent-guild/state/spec.md:22-28 mapped against .agent-guild/state/constitution.md:9-98. |
| non-goals | minor | PASS. Six non-goals now, and the new one at :110 records the stale-inventory decision explicitly, including the consequence for a worker who tries to be helpful ('a worker that updates them fails that clause'), which is the right way to write a non-goal that a well-meaning worker would otherwise violate. None of the six blocks a criterion: staging is not scoring, so 'no graded runner' leaves eval-scope.sh reachable; 'no execution' matches C-12's empty table; 'no scaffold mode' narrows the surface without touching a criterion. One loose edge survives from r0 and I agree it is still loose, at low stakes: the first non-goal says a skill defect 'is filed as an issue, not fixed here', which authorizes an external side effect (`gh issue create`) that no clause constrains and that check-diff-scope.py structurally cannot see, since it only reads the working tree. It is nearly dead because this job does not execute the suite, so no scenario can expose a defect during the job. Saying 'staged as a draft under state/' rather than 'filed' would close it for free. | .agent-guild/state/constitution.md:104-111. check-diff-scope.py reads only `git status --porcelain` and `git diff --name-only`: .agent-guild/scripts/check-diff-scope.py:86-104. |

## Diagnosis

- **C-1** (minor): PASS. I re-ran the check verbatim and it exits 0, so the r0 finding about the command itself is closed, and the new non-goal at :110 settles the two stale inventories the way the user decided. Two notes for the record. (a) `--ignore .agent-guild/state/` is still a no-op and I confirm r0 on this: check-diff-scope.py matches --ignore by exact string against a set (`path in ignored`, :110), never by prefix, and `.agent-guild/state/` is already unconditionally permitted at :112. It costs nothing but it reads as coverage it does not provide, and a later job copying this clause will inherit the misreading. (b) The check is one-directional. It exits 0 on a clean tree, so a green C-1 never proves any of the three named deliverables landed - it only proves nothing extra did. Existence rides entirely on C-3 (fixture directories must be there for the subset test to mean anything), C-10 (the checker actually executes eval-scope.sh), and C-2/C-12 (EVALS.md content). That is adequate coverage, but the clause text opens 'The suite is exactly three things' and a checker can reasonably read a green C-1 as having confirmed all three exist. One sentence saying the check verifies the ceiling and not the floor would prevent it.
  evidence: Ran verbatim: `python3 .agent-guild/scripts/check-diff-scope.py _maintenance/inbox-to-memory/ tests/fixtures/inbox-to-memory/evals/ --ignore .agent-guild/state/` -> 'OK: 0 path(s) in scope', exit 0. --ignore semantics: .agent-guild/scripts/check-diff-scope.py:107-117 (exact-match set at :110, hardcoded state/ carve-out at :112). Non-goal at .agent-guild/state/constitution.md:110.
- **C-2** (minor): PASS, re-derived rather than inherited, and it remains the anti-vacuity model the weaker clauses should copy. The window is pinned in the text ('followed - before the next scenario heading', 'All three live inside the scenario's own section') and again operationally in the check ('extract only the text between that heading and the next one... Anything found outside the slice under test does not count toward it'). I tried the obvious dodge against it: an EVALS.md that names every fixture once in a global `## Fixtures` section and never inside a scenario fails, which is the right answer and is exactly what the precedent's own structure does correctly. No revision elsewhere opened a gap here. Residual, unchanged from r0 and still worth a word: 'scenario heading' is not tied to a heading level, so a scenario carrying a `#### Expected` subheading admits a reading where the slice ends early. C-12 requires numbered scenarios, so keying the slice on the numbering ('between one numbered scenario heading and the next') closes it.
  evidence: .agent-guild/state/constitution.md:16-21. Precedent structure where fixture, prompt, and table all sit inside the scenario's own slice: _maintenance/file-issue/EVALS.md:16-29, :31-44.
- **C-3** (minor): PASS, re-derived. The opt-in test is quoted correctly against source and the subset direction is the safe one: cited names must be a subset of real directories, which cannot be satisfied by text sitting in the wrong section. Anti-vacuity sweep: a fixture that is well-formed but useless does pass C-3, but that is C-3's job - it is the existence-and-opt-in floor, and the usefulness bar belongs to C-5, C-6, C-7, and C-8. The gap that matters is that two of those (C-7, C-8) do not currently hold that bar; see their rows. C-3 itself is sound.
  evidence: Opt-in rule verified at inbox-to-memory/SKILL.md:30-34 ('A directory is opted in when it contains both `_inbox/` and one of these markers: `_memory/` -> project or client scope. `entries/` -> journal scope.'). Existing fixtures confirm the shape: tests/fixtures/inbox-to-memory/{old-only,mixed,broken}/ carry `_inbox/` + `_memory/`, journal-v1/ carries `_inbox/` + `entries/`.
- **C-4** (minor): PASS, re-derived, and C-12's new results-table requirement complements it rather than colliding with it - C-4 owns the stated expectation per scenario, C-12 owns the place a run writes the observed result. Together they now cover both halves of spec criterion 2, which closes r0's half-gap. One residual worth fixing on the next pass: the check's mechanical anti-boilerplate test is 'Flag any baseline sentence that appears verbatim in two or more sections', and verbatim is trivially evaded by templating. 'Without the skill, the model will not flag the contradiction in fixture-X' repeated across five sections with only the fixture name swapped survives the verbatim test and also survives 'names something particular to that scenario's fixture or prompt', because the fixture name is a particular. The clause text already reaches further ('a baseline that says nothing about this scenario's input has skipped it'); the check should reach as far. A test that bites: flag any baseline that would still be true if you substituted another scenario's fixture and prompt.
  evidence: .agent-guild/state/constitution.md:30-35 (text at :32, check at :33). Spec criterion at .agent-guild/state/spec.md:23. Complementary half at constitution.md:88 (C-12's without-skill column).
- **C-5** (minor): PASS, re-derived from source rather than inherited from r0. Every artifact the clause names checks out with the same spelling and the same meaning: the flag grammar `[contradicts accepted: [[<filename>|<short-name>]]] <statement> | claims: <what the record says>` (SKILL.md:100, and the fielded form at references/machine-contracts.md:93-94), `status: accepted` as the flag-producing status, phase 2.5 as the phase that reads memory before writing, the rewrite target `[[<target>|memory - updated]]`, the `source_refs` append, and the `(added YYYY-MM-DD, <note-id>)` amendment marker. The check reaches into the fixture ('read the fixture to confirm it holds an `accepted` record that the planted input genuinely contradicts'), which is the reach C-7 and C-8 are missing. Anti-vacuity sweep: a well-formed-but-useless fixture is excluded here, because 'genuinely contradicts' is a content judgment the checker must make against the planted input.
  evidence: inbox-to-memory/SKILL.md:100 (taxonomy), :136-138 (phase 5 sign-off gate), :151 (`[[<target>|memory - updated]]` rewrite), :154 (`source_refs` append), :155 (`(added YYYY-MM-DD, <note-id>)`), :162 (amend outcome); inbox-to-memory/references/machine-contracts.md:93-94.
- **C-6** (major): FAIL, and this is a clause r0 passed - the sweep found it, not the revisions. The check reaches into the fixture for the positive branch only: 'count the prior notes in its fixture carrying the slug and confirm the count is three or more'. For the negative branch it asks only that a row be present in the table ('confirm both the positive and negative rows are present in its table'). So the negative case has exactly the vacuity C-6's own failing example names, one branch over. A fixture whose second slug appears in two notes, or whose third note contains no passage naming the question aloud, satisfies C-6 as written while grading the skill wrongly: the skill would correctly emit `[tension: unacknowledged]` (SKILL.md:122 fires at three or more unless someone names it out loud), and the eval would score correct behavior as a failure. That is the 'worse than no eval' condition C-9 states in its own text, arriving through an absence rather than through a token, so C-9 does not catch it either. Fix: make the fixture-side count cover both slugs, and add the condition the negative branch actually turns on - the fixture's input must contain the passage where someone names the slug aloud, quoted or line-referenced in the scenario. Separately, I disagree with r0's other suggestion here and would not adopt it: r0 wanted the clause to say what the negative case must be flagged as instead. SKILL.md:122 states only the suppression, not a replacement disposition; the repo's own fixture README calls it a deferred tension but SKILL.md does not require that, so mandating `[tension: deferred]` would over-specify past the skill text and invite exactly the kind of invented expectation C-9 exists to stop.
  evidence: Constitution C-6 at .agent-guild/state/constitution.md:44-49 (text at :46, one-sided check at :47). Trigger: inbox-to-memory/SKILL.md:122 ('Flag a slug open across three or more notes as `[tension: unacknowledged]`, unless the transcript shows someone naming it out loud'). Disposition not mandated anywhere in SKILL.md; the deferred reading comes only from tests/fixtures/inbox-to-memory/README.md and machine-contracts.md:91's grammar row.
- **C-7** (blocker): FAIL. The repair relocated the hole rather than closing it, and it did so by adopting a mechanism claim that is wrong - r0's, which I disagree with on the substance. What C-7 grades is the phase 3 candidate token. references/scope-decisions.md:3 says so outright ('How to propose `project`, `client`, or `journal` scope when flagging a memory or journal candidate during phase 3'), and :9-19 is a table of content signals that picks the token. SKILL.md:35's path walk picks the phase 1 scope root, which is where the run is anchored and where records get written - a different thing. Nothing in phase 3 or scope-decisions.md constrains the token to the detected root; scope-decisions.md:57-61 says the opposite, that cross-scope references are fine. So the clause's stated reason ('because scope is detected from path and not from content (`SKILL.md` phase 1)') mis-attributes the mechanism, and the check inherits the error. Both directions bite. (1) It false-fails correct work. The natural journal-tier scenario is a client-rooted fixture whose input generalizes beyond the client (scope-decisions.md:17-19), expecting `[journal candidate: ...]`; C-7 demands that fixture's phase 1 walk resolve journal tier, i.e. an `entries/` marker, which turns the scenario into grooming an inbox inside the journal itself, where 'generalizes beyond this client' has no client to generalize beyond. (2) It still passes a well-formed-but-useless fixture, which is the sweep priority. Put a fixture at `client/pursuits/atlas/` so the walk resolves project, plant an inbox file whose only signal is a stakeholder org-chart fact (scope-decisions.md:13 -> client), and write a scenario expecting `[memory candidate: project]`. Layout check passes, token check passes, and the eval grades correct behavior as wrong. The tiebreak has the same shape: 'confirm one case presents conflicting signals and expects the narrower token' is evaluated against EVALS.md prose, so nothing confirms the fixture's input carries two signals from scope-decisions.md:9-19 pointing at different tiers. And the text's 'each on an input that could plausibly have gone elsewhere' has no counterpart in the check at all, which is the sentence carrying the whole judgment-testing point. Fix: replace the phase-1-walk requirement with a content-side one - read each scenario's fixture input and confirm it carries signals from the scope-decisions.md:9-19 table pointing at the expected tier, and for the tiebreak case at least two signals pointing at different tiers. Keep a layout requirement only where a signal needs one: the client-scope recurrence signal at :15 is verified by a grep of `<client-root>/{pursuits,projects}/*/notes/` (:32), so a fixture grading that signal does need a client root with two project note directories - which is a different property from 'the walk resolves client' and is not what the check currently asks for.
  evidence: Constitution C-7 at .agent-guild/state/constitution.md:51-56 (mechanism claim at :53, check at :54). Token is content-picked: inbox-to-memory/references/scope-decisions.md:3, :5, :9-19, :23. Path walk picks the root, not the token: inbox-to-memory/SKILL.md:30, :35. Cross-scope proposals explicitly fine: scope-decisions.md:57-61. Recurrence grep needing two project note dirs: scope-decisions.md:29-35. Journal candidate arises from client work and routes to the cross-client journal at sign-off: scope-decisions.md:17, :75, :81.
- **C-8** (major): FAIL, on a different point than r0's - r0's finding is genuinely closed. The attribution is now correct and I verified both halves at source: `tier2-entity-unsourced` is the entity-half diagnostic the migrator emits (migrate-scope.sh:407), and SKILL.md:388's 'The apply enforces the half it can' confirms the summary half is unenforced judgment, which is what the revised clause now says. The new gap is fixture-side and it is the sweep's failure mode exactly. Every pass condition in this clause is stated against 'the note's emitted extract' - emitted by `migrate-scope.sh --tier2-extract` - and that command only emits for v1 notes. migrate-scope.sh:44 says 'for each v1 note'; SKILL.md's Tier 2 section says 'Once a note carries `schema: 2` the migrator skips it whole, so there is no adding summaries to a scope that already migrated.' Nothing in the constitution requires the Tier 2 scenario's fixture to be a pre-v2 scope. Build it out of v2-shaped notes and it is well-formed, passes C-3's opt-in test, passes C-1, and produces no extract at all, so every row in the scenario is unfalsifiable and the fourth judgment area the job exists to cover grades nothing. The check's 'confirm every row in the scenario is decidable by reading the extract' is the sentence that ought to catch this, but it does not say whose extract - a checker reading an illustrative extract quoted inside EVALS.md satisfies it without ever running the extractor. Fix: require the Tier 2 fixture to hold at least one note with no `schema` key, and have the check obtain the extract by running `migrate-scope.sh <fixture> --tier2-extract <dir>` rather than by reading one transcribed into the doc. The repo's `old-only/` fixture is the precedent for the shape and is what the smoke suite's own Tier 2 tests copy.
  evidence: Constitution C-8 at .agent-guild/state/constitution.md:58-63. Attribution verified: inbox-to-memory/SKILL.md:386 (both rules), :388 ('The apply enforces the half it can', `tier2-entity-unsourced`); inbox-to-memory/scripts/migrate-scope.sh:407 (the diagnostic). v1-only extraction: migrate-scope.sh:11 ('hands an agent each v1'), :44 ('--tier2-extract  for each v1 note, write its extracted sections'), v1/v2 gate at :157-164 (`has_schema_key`). Smoke-suite precedent, all four Tier 2 tests copy the v1 fixture: tests/inbox-to-memory-smoke.sh:778, :802, :843, :896 (`cp -R "$fixtures/old-only/."`).
- **C-9** (blocker): FAIL - a contradiction with C-10 and C-3 that r0 passed and that the C-10 revision made unavoidable. C-9 quantifies over 'Every candidate-flag token, phase number, frontmatter key, `status` value, `memory_type`, folder name, and script path the doc cites', and requires each to appear in `inbox-to-memory/SKILL.md` or its references. `eval-scope.sh` is a script path the doc must cite: C-10's revised check now fails the clause outright unless a run-procedure section exists and names it. That script does not appear anywhere under `inbox-to-memory/` - I grepped, zero hits - and it never can, because C-1 freezes that directory ('Nothing under `inbox-to-memory/` changes'). So C-1, C-9, and C-10 cannot all be satisfied by any artifact, and two of the three are blockers. The same reading catches the fixture directory names: C-3 requires the doc to name directories under `tests/fixtures/inbox-to-memory/evals/`, and those names are folder names the doc cites, absent from SKILL.md by construction. A charitable checker scopes C-9 to the skill's own vocabulary and lets both through; a literal one fails correct work; the two disagree, which is below the bar. Fix: bound the quantifier explicitly - C-9 governs vocabulary the skill emits or consumes, and exempts the paths and directory names this job creates, naming `eval-scope.sh` and `tests/fixtures/inbox-to-memory/evals/` so the exemption is not itself a judgment call. Everything else about the clause holds: I re-derived the failing example rather than taking it, and `[memory candidate: journal]` is indeed undefined (journal candidates take no scope token, SKILL.md:99), so the example fails a real artifact for the stated reason.
  evidence: Constitution C-9 at .agent-guild/state/constitution.md:65-70 (quantifier at :67), C-10's naming requirement at :75, C-1's freeze at :11. `grep -rn 'eval-scope' inbox-to-memory/ _maintenance/` -> no output; `ls _maintenance/inbox-to-memory/` -> No such file or directory. Journal token has no scope form: inbox-to-memory/SKILL.md:96-99; references/machine-contracts.md:69-72.
- **C-10** (minor): PASS. The repair holds rather than relocating the hole. r0's diagnosis was that both halves quantified over 'every scenario's run procedure' while nothing required a run procedure to exist, so the empty set satisfied both vacuously. The revision makes existence a gate rather than a quantifier - 'First confirm a run-procedure section exists and names `eval-scope.sh`; if there is no such section this clause fails outright, with no further checking' - and the negative half now runs behind a positive that has to hold first. I checked the remaining shape for a new hole and did not find one: a scenario that names no run target at all is fine, because the clause requires a single named section that takes a reader 'from a fixture name to a running scenario', so a scenario naming only its fixture is complete by construction. I also note the constitution declined r0's suggested fix (put the run procedure inside each scenario's C-2 slice) in favour of one named section, and I think that was the better call - repeating the staging steps per scenario is the version that drifts. Two notes. The script-behavior half (run it, confirm the printed path, confirm `git status --porcelain` unchanged) is a deterministic script run inside a judgment check and could split out to checker-deterministic. And 'leaves `git status --porcelain` unchanged' should say before-versus-after rather than empty, since the job's own uncommitted deliverables will be in that output.
  evidence: Constitution C-10 at .agent-guild/state/constitution.md:72-77 (existence gate at :75). Backstop confirming the same rule empirically: tests/inbox-to-memory-smoke.sh:586 and :1229 shasum every file under tests/fixtures/ and re-compare at the end.
- **C-11** (minor): PASS. r0's decisive finding is closed - the `.agent-guild/hooks/test_hooks.py` term is gone, and that directory still does not exist in this repo. I confirmed the check independently, both term by term and as the single `&&` chain the clause actually specifies, and all three suites exit 0 at baseline. I also re-derived the consumer claim rather than inheriting it: `$fixtures` is `tests/fixtures/inbox-to-memory` and is referenced 48 times in the suite, but exactly two of those walk the whole tree and so reach a new `evals/` directory - the index-file `find` and the shasum snapshot. Every other reference names `old-only`, `mixed`, `broken`, `journal-v1`, or a specific file, so a new directory is not swept into the lint arithmetic. Two line citations are off and should be corrected, since they are what a checker follows: `:514` is the `require_text` assertion about SKILL.md prose, and the tree-wide `find` is at `:515`; the shasum re-comparison is at `:1229`, not `:1227` (which is the middle line of the comment explaining why). The `:586` citation is exact. C-10's `:1226` is apt as written, because it points at the comment that says why.
  evidence: Chain as written: `bash tests/inbox-to-memory-smoke.sh && bash tests/file-issue-smoke.sh && bash tests/handoff-smoke.sh` -> exit 0 (each individually 0 as well). `ls .agent-guild/` -> schemas, scripts, state, templates, CLAUDE.md; no hooks/. Tree-wide consumers: tests/inbox-to-memory-smoke.sh:515 (`find "$fixtures" -path '*_memory*' ...`), :586 (`fixtures_before="$(find "$fixtures" -type f -exec shasum {} + | sort)"`), :1229 (re-comparison). Explicit-scope loop at :109-117; lint invocations at :120, :127, :157.
- **C-12** (minor): PASS. The added results-table requirement closes r0's coverage gap against spec criterion 2 and does not overlap C-4 harmfully - C-4 owns the per-scenario stated expectation, C-12 owns the column a run writes the observed baseline into, and the failing example ('| Scenario | Passed |') falsifies exactly the artifact that would satisfy C-4 alone. I checked the new requirement against the precedent for a collision and there is none, though it is worth naming: the precedent's Grading section carries no table at all, only prose telling a runner what to record, so 'keeps the shape of its precedent' plus 'ships the empty table' is an addition to the precedent rather than a copy of it. The clause states that plainly enough that a checker will not read it as a contradiction. One residual r0 raised that the revision did not take, and I would still take it: the check opens 'Compare section headings against `_maintenance/file-issue/EVALS.md` and confirm all four are present with content', but only three of the four are headings. The method statement sits as unheaded prose above `## Fixtures` in the precedent. The clause text gets this right; the check's phrasing invites a false fail on a document that follows the precedent exactly. Say three headings plus a method statement.
  evidence: Constitution C-12 at .agent-guild/state/constitution.md:86-91 (text at :88, check at :89). Precedent heading set, re-derived: _maintenance/file-issue/EVALS.md has `# file-issue Evals` (:1), `## Fixtures` (:7), `## Scenarios` (:14), six `### N.` scenario headings, `## Grading` (:105). Method statement unheaded at :3-5. Grading section is prose with no table: :107-109.
- **C-13** (minor): PASS. The three collisions r0 named are gone. Heading case and dash spacing now defer to `_maintenance/file-issue/EVALS.md`, which is the user's settled ruling and which I am not reopening; the check states the deference operationally ('Dash and heading style are judged only against `_maintenance/file-issue/EVALS.md`, and consistency with it is a pass'), so a checker has a concrete corpus to compare against rather than a house rule the corpus contradicts. The remaining automatic fail, hard-wrapped prose, is mechanically decidable and matches standing user policy. One thing I checked for a new collision and found clean: C-13 bans `- **Thing:** description`, and the precedent's Fixtures list uses `- **`fixture-bare`** - description`. Those are different forms, the colon versus the dash, and the user's own guidance calls the dash form fine, so a worker mirroring the precedent's bullet style does not trip C-13. The clause writes the banned form precisely enough that the distinction survives a careful checker, though naming the precedent's bullet as explicitly allowed would remove the last chance of conflation.
  evidence: Constitution C-13 at .agent-guild/state/constitution.md:93-98. Precedent style corpus: _maintenance/file-issue/EVALS.md:11-12 (dash-form bullets), :16, :31, :46, :60, :75, :90 (sentence-case scenario headings), 11 space-wrapped em dashes throughout.
- **coverage** (minor): PASS. All seven acceptance criteria have a clause whose violation would be caught, though two of those clauses fail on their own terms above and the coverage is therefore nominal until they are fixed. Mapping: criterion 1 -> C-2 with C-3; criterion 2 -> C-4 for the stated expectation and C-12 for the recorded result, which together now cover it (r0's half-gap is closed); criterion 3 -> C-5; criterion 4 -> C-6; criterion 5 -> C-7; criterion 6 -> C-10; criterion 7 -> C-1. The spec body's fourth behavior, 'whether generated summaries and entities are any good', maps to C-8. No clause is orphaned: C-9, C-11, C-12, and C-13 answer job needs stated in their own text rather than criteria - vocabulary fidelity, regression safety, precedent shape, and the standing prose requirement. The two-run method itself, which criterion 2 also asserts, is required by C-12's method statement rather than by C-10's run procedure; that is fine but slightly counterintuitive, since C-10 is the clause that owns how a scenario gets run.
  evidence: .agent-guild/state/spec.md:22-28 mapped against .agent-guild/state/constitution.md:9-98.
- **non-goals** (minor): PASS. Six non-goals now, and the new one at :110 records the stale-inventory decision explicitly, including the consequence for a worker who tries to be helpful ('a worker that updates them fails that clause'), which is the right way to write a non-goal that a well-meaning worker would otherwise violate. None of the six blocks a criterion: staging is not scoring, so 'no graded runner' leaves eval-scope.sh reachable; 'no execution' matches C-12's empty table; 'no scaffold mode' narrows the surface without touching a criterion. One loose edge survives from r0 and I agree it is still loose, at low stakes: the first non-goal says a skill defect 'is filed as an issue, not fixed here', which authorizes an external side effect (`gh issue create`) that no clause constrains and that check-diff-scope.py structurally cannot see, since it only reads the working tree. It is nearly dead because this job does not execute the suite, so no scenario can expose a defect during the job. Saying 'staged as a draft under state/' rather than 'filed' would close it for free.
  evidence: .agent-guild/state/constitution.md:104-111. check-diff-scope.py reads only `git status --porcelain` and `git diff --name-only`: .agent-guild/scripts/check-diff-scope.py:86-104.
