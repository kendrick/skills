# T-001 notes

Built `_maintenance/inbox-to-memory/eval-scope.sh`. Takes a fixture name,
resolves `tests/fixtures/inbox-to-memory/evals/<name>/` relative to the
script's own location (`../..` from `_maintenance/inbox-to-memory/`, same
sibling-resolution idiom `verify-migration.sh` uses for `lint-scope.sh`),
copies it into a `mktemp -d "${TMPDIR:-/tmp}/i2m-eval-scope.XXXXXX"` scratch
directory with `cp -R "$fixture/." "$staged/"` (the exact copy idiom
`tests/inbox-to-memory-smoke.sh` uses for its own fixture copies), and prints
only the staged path.

Verified directly since `tests/fixtures/inbox-to-memory/evals/` doesn't
exist yet (T-002/T-003 build it):

- Missing fixture: exits 1, stderr message, no stdout.
- Temporarily staged `old-only` under a throwaway `evals/old-only/` dir,
  ran the script, confirmed `diff -r` between that source and the staged
  copy came back clean, confirmed `git status --porcelain` was byte-identical
  before and after the run, confirmed the staged path resolved under
  `/private/tmp` (outside the repo), and confirmed no trap fires on exit
  (directory survives). Removed the throwaway `evals/` dir and the scratch
  copy afterward so the working tree is back to holding only the one
  intended new file.
- `bash -n` and `shellcheck` both clean.

Judgment calls:

- Exit code 1 for "fixture not found," reserving 2 for usage errors (missing
  or empty argument). `verify-migration.sh` uses 2 for both its usage errors
  and its "not a directory" precondition failure; I split those into 1 vs 2
  here to keep the primary failure mode the spec calls out (missing
  fixture) distinct from plain misuse. The spec only requires "nonzero," so
  this shouldn't be load-bearing, but flagging the choice in case a checker
  expects a specific number.
- Ran the header comment through the humanizer skill (embedded mode) before
  finalizing, per C-13 and the user's standing instruction to humanize
  code comments.
- Did not add a trap, matching the spec's explicit requirement not to clean
  up the scratch directory on exit.
