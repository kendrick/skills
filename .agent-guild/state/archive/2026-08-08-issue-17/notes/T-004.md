# T-004 self-report

Wrote `_maintenance/inbox-to-memory/EVALS.md`. Nothing else in the tree changed.

## What I read before writing

Every fixture, whole, plus `_maintenance/file-issue/EVALS.md`, `eval-scope.sh`, `SKILL.md`, `references/machine-contracts.md`, and `references/scope-decisions.md`. I also executed against two fixtures rather than reasoning about them: staged `tier2-summary-entities` through `eval-scope.sh`, ran `--tier2-extract`, read the emitted `i59pVI65GO.extract.md`, and then ran `--tier2` with deliberately bad proposals to confirm the exact failure code and its message. That is where the `Kestrel` row came from, which I would not have found by reading.

## Taste calls

**Doc shape.** Title, method prose, `## Fixtures`, `## Running a scenario`, `## Scenarios`, `## Grading`. The run procedure sits between Fixtures and Scenarios because that is the reading order: here is what exists, here is how you stage one, here is what to do with it. Spaced em dashes and sentence-case scenario headings, matching the sibling rather than my usual house style.

**Four scenarios, not five.** The fixtures would support a fifth (the contradiction fixture has a dismiss angle), but every extra scenario is more surface for C-2 and C-4 to fail on. I folded the dismiss and supersede pressure into scenario 1 as negative rows instead.

**Negative rows carry most of the weight.** Scenario 1 gets two: the `superseded` Context record and the `proposed` Decision, both genuinely contradicted by the input, neither eligible for a flag. Scenario 2's negative is `noshow-charge-authority`, plus `kiosk-print-fallback` as a third row testing that the resolved token is not counted toward the three-note threshold.

**Baselines.** Each names a specific artifact the without-skill run will not produce, anchored on a filename, an id, or a frontmatter value from that fixture: `source_refs: [vC8nfl6HWd]` unchanged, `after-hours-gate-cover` absent because nobody says it aloud, `Tomas Krohl` proposed as an entity. I checked each sentence against the paste test rather than trusting that it felt specific.

## Where I had to make a judgment the spec left open

The scope-tiers tiebreak. The 2026-05-12 input carries a project claim (the cutover date, milestone 6) and an authority-boundary claim that reads as a client-tier org-chart signal. I grounded the expected `[memory candidate: project]` on the reference's own verification step: `grep -l -r "Dermot Sayer" {pursuits,projects}/*/notes/` returns claims-intake only, so the "referenced in 2+ projects" heuristic does not fire and the narrowest scope takes it. The mirror row on the governance sync uses the same grep on `Nadia Okonjo`, which does return both project directories. That makes both rows decidable by running one command rather than by arguing about tiers.

Second, phase 1 detects scope by walking up from cwd, so the four scope-tiers inputs cannot be processed in one run. Scenario 3 says so and names three run roots, with the claims-intake run grooming two inputs.

## Verification I ran

- `.agent-guild/scripts/check-diff-scope.py ...` — OK, 35 paths in scope.
- All three smoke suites — exit 0.
- Token extraction the way `lint-scope.sh:218-230` does it (strip `[[...]]`, then read brackets): seven distinct shapes, all registered rows, all enumerated values in set. The linter's own bad-scope grep returns nothing. I caught and fixed one bare `[memory candidate:` prefix in a baseline sentence during this pass.
- `eval-scope.sh scope-tiers` then `diff -r` against the committed fixture: identical. `git status --porcelain` unchanged across every run I did.
- One `tests/fixtures/` mention in the whole document, in the Fixtures block, with the sentence that says nothing is ever run there.

Ran the prose through the `humanizer` skill. It cost me a tricolon in the opening line, a borrowed sentence in Grading that was too close to a paraphrase of the sibling doc, a dash pile-up in scenario 3's baseline, and one embellishment ("no receipt in the building") that the transcript did not support.
