# T-007 notes

## What I did

Changed exactly one sentence at `_maintenance/inbox-to-memory/EVALS.md:110`, the
`Kestrel` rejected row in the Tier 2 scenario table.

Before:
> Not proposed. It sits in the note's `tags`, in both record titles, and in the
> fixture's own name, and it is nowhere in the extract

After:
> Not proposed. It sits in the note's `tags`, in one record's `applies_to` and
> the other's `tags`, and in a body line, and it is nowhere in the extract

## Verification of where Kestrel actually appears

Opened every file under
`tests/fixtures/inbox-to-memory/evals/tier2-summary-entities/`:

- Source note `notes/2025-09-08-kestrel-cutover-planning-i59pVI65GO.md`:
  `tags: [migration, kestrel]` in frontmatter. No `Kestrel` in its body. (This
  is the "note's `tags`" clause of the sentence, already correct, left
  untouched.)
- Record `_memory/context/index-rebuild-duration-_YEL9dotcZ.md`:
  `applies_to: [kestrel]` in frontmatter; title is "Index rebuild runs four to
  six hours under load" (no Kestrel); body line under `## Context Scope` reads
  "The Kestrel index rebuild step, wherever it runs ahead of a cutover."
- Record `_memory/decisions/cutover-rebuild-buffer-8taS8NdrXK.md`:
  `tags: [kestrel]` in frontmatter; title is "Cutover windows require a
  48-hour rebuild buffer" (no Kestrel); no Kestrel anywhere in its body.
- Fixture directory name is `tier2-summary-entities`, no Kestrel in it.

So both original claims the task flagged as false — "both record titles" and
"the fixture's own name" — are indeed false, and neither record's title has
Kestrel. The three real locations are: the note's tags, the two records'
frontmatter fields (`applies_to` on one, `tags` on the other), and one body
line in the first record. The sentence now names exactly those, keeping the
"why a model would be tempted" framing (it's genuinely all over the fixture's
metadata and prose, just not in titles or the fixture name).

## C-8 grading condition re-verified, unchanged

Staged the fixture with `eval-scope.sh tier2-summary-entities`, ran
`migrate-scope.sh <staged> --tier2-extract <scratch>`, and grepped the emitted
`i59pVI65GO.extract.md` for "kestrel" case-insensitively: no match. The pass
condition text itself ("Not proposed... nowhere in the extract") is byte-for-byte
unchanged from before my edit — I only replaced the middle clause explaining
where the name sits outside the extract.

## Other checks run

- `.agent-guild/scripts/check-diff-scope.py` against the three allowed paths:
  passed (35 paths in scope, exit 0). Only `EVALS.md` was touched; fixtures
  and `eval-scope.sh` are unmodified (confirmed via `git status --porcelain`,
  which shows the whole `_maintenance/inbox-to-memory/` and
  `tests/fixtures/inbox-to-memory/evals/` trees as untracked-and-unchanged
  from prior tasks, with no working-tree diff inside the fixture files).
- `tests/inbox-to-memory-smoke.sh`, `tests/file-issue-smoke.sh`,
  `tests/handoff-smoke.sh`: all exit 0.

## Judgment calls

- Kept "record" as the term for `_memory/` entries (matches existing usage
  elsewhere in EVALS.md, e.g. line 11's "Rule record", line 14's "two
  records").
- Phrased as "one record's `applies_to` and the other's `tags`" rather than
  naming the two record files explicitly, to match the sentence's existing
  register (terse, field-name-driven, no filenames quoted in this row).
- Did not touch the accurate line 14 fixture-listing sentence ("a v1 Kestrel
  cutover scope") — it wasn't named in the task and isn't false.
- Left no trace in the repo working tree that wasn't already there: the
  extract/staging scratch directories were created outside the fixtures tree
  and deleted after verification.
