# T-001 notes

## What I did

`inbox-to-memory/scripts/lint-scope.sh` pass one now builds two parallel
collections instead of one: `v2_files` (unchanged) and a new `v1_files`, each
with its own body cache (`v1_body_for_index`, files at `$work/body.v1.$N`).
The v1 branch inside pass one's classification loop caches the file's
extracted body via `extract_body` and appends to `v1_files`/`v1_body_for_index`,
then still `continue`s — it does **not** touch `$work/open-slugs`, which is
what keeps a v1 file's `[open question: ...]` tokens out of the scope-wide
resolution set (the thing C-3's second check block exists to catch).

After the existing pass-two loop over `v2_files` (which runs the full check
suite), I added a second, much smaller loop over `v1_files` that only calls
`check_links` per file, setting `current_file` first so `fail` reports the
right filename. Nothing else runs on v1 files: no `check_frontmatter`, no
`check_counts`, none of the six body-grammar checks, no open-question-
resolution loop.

Net diff is 19 inserted lines, 0 removed — pure addition, so the two comment
blocks at (original) `:10-12` and `:441-443` were never touched; they weren't
even loaded into the edit's `old_string` in a way that could clip them. I
also left the pass-one header comment ("Pass one classifies and caches each
v2 body...") alone since it's not one of C-8's five named locations and
editing it risked being read as scope creep under C-9 — it's not literally
false after this change, just silent about the new v1 caching.

I ran the new loop's explanatory comment through the `humanizer` skill per
my global instructions (code comments count as human-facing prose) before
finishing. One round of revision, mostly tightening a clunky first sentence.

## Verification

Ran every check command in the constitution's check_method for C-1 through
C-6, from the repo root, exactly as written (including the C-4 git-worktree
comparison, which I removed with `git worktree remove --force` afterward
both times I ran it):

- C-1: both scopes report `link-broken: \`this-target-does-not-exist-AAAAAAAAAA\``
  and `exit=1`.
- C-2: `failures: 0`, `exit=0` on the id-only-resolution scope.
- C-3 block 1 (six-way injection on `old-only`): `failures: 0`, `exit=0`.
- C-3 block 2 (open-slug leakage on `mixed`): prints the exact required line,
  `` open-question-resolution: `v1-only-slug` is resolved here but never
  opened anywhere in scope `` (failures: 2, as the clause itself expects —
  the second failure is the co-moved `resolved_questions` count, which the
  clause says to ignore).
- C-4: `diff=0`, no diff body, against a HEAD worktree.
- C-5: all three smoke suites (`inbox-to-memory-smoke.sh`, `file-issue-smoke.sh`,
  `handoff-smoke.sh`) exit 0.
- C-6: `check-diff-scope.py` exits 0, and `git status --porcelain` lists only
  `inbox-to-memory/scripts/lint-scope.sh`.

I reread `git diff -- inbox-to-memory/scripts/lint-scope.sh` against C-9
myself: every changed line is either new state (`v1_files`, `v1_index`,
`v1_body_for_index`), the caching call inside the v1 branch, or the new
links-only loop plus its comment — all mechanical consequences of "route v1
into `check_links`." Nothing widens an existing check or touches a v2-file
code path.

## Judgment calls

- Kept `v1_body_for_index` and `body_for_index` as separate arrays rather
  than reusing one keyed by a combined index space, since the existing
  `body_for_index` indexing is already load-bearing for the v2 pass-two
  loop and I didn't want to risk an off-by-one there. Slightly more state
  than a single shared array, but each array's indexing matches its own
  loop's counter exactly, mirroring the pattern the file already uses for
  `v2_files`/`body_for_index`.
- Did not touch the pass-one header comment (see above) or file a dispute
  over it — it's genuinely optional under the spec, and leaving it alone is
  the safer reading of "don't touch comments outside :10-12/:441-443 that
  aren't yours."
- C-7, C-8, and C-10 are not in this task's `clauses` list (they belong to
  T-002/T-003), so I didn't touch the smoke suite or the prose in
  `machine-contracts.md`, `migration.md`, or `SKILL.md`.
