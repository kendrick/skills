# T-004 self-report

## What I changed

Two comment blocks in `inbox-to-memory/scripts/lint-scope.sh`, nothing else. The diff for this task is 9 changed lines and all nine are comment text: 8 insertions and 1 deletion, which moves the file's total working diff from 31/5 to 39/6.

**`:2-5`, the opening description.** Was "Walk one opted-in scope and check every v2 note and record against the machine contracts in references/machine-contracts.md." Now:

```
# Walk one opted-in scope and check every v2 note and record against the machine
# contracts in references/machine-contracts.md. The walk takes in v1 files as
# well, checking their wiki links and nothing else: a link resolves against every
# file in the scope, and that resolution never reads the file's own frontmatter.
```

**`:414-420`, above pass one.** The original two lines are untouched; three sentences follow them:

```
# It caches v1 bodies too, for a narrower reason. Only the link pass at the
# bottom of the file reads them, and this is the one loop that visits every file,
# so extracting here costs one walk instead of two. They go to their own
# `body.v1.*` files and never join `open-slugs`, which is what keeps a v1
# question out of the two scope-wide checks.
```

## Taste calls

**No em dashes, deliberately.** `grep -c '—'` over the file returns 0. The task and C-10 both permit a chained em dash, and my first drafts of both comments used one, but a punctuation mark the file has never used once in 509 lines is a register break under C-10's own test. The humanizer's §14 and its voice-calibration rule agree here for once: the sample is the file, and the sample has none. Colons and full stops carry the same joins.

**Each comment got a reason the other passage doesn't already give.** The risk with the opening line is that the header block at `:10-15` sits five lines below it and already states the v1 rule with its reason ("broken whichever generation wrote it," plus the verify-migration.sh precedent). Restating that at `:2-5` would satisfy C-10 by repeating a paragraph the reader hits seconds later. So the opening reaches for a different true fact: `check_links` resolves by `find "$scope" -name "$target.md"` at `:379-381` and reads only the extracted body, so link resolution is indifferent to frontmatter by construction. That is a reason `:10-15` does not give, and it is the one that explains why this particular check survives the generation split.

For `:414`, the narrower reason is the engineering one. The link pass at `:483-488` is the only reader of `v1_body_for_index`, and pass one is the only loop that globs every file, so caching there is one walk instead of two. The last sentence carries the load-bearing half: the v1 bodies go to a separate `body.v1.*` cache and never feed `open-slugs`, which is the thing keeping a v1 question out of the resolution and recurrence checks. That is C-3's invariant stated where someone editing this loop would trip over it.

**"Checking their wiki links and nothing else," not "for their wiki links alone."** The earlier phrasing was ambiguous about the `v1 files:` count, which pass one does still produce for v1 files. The shipped wording scopes "nothing else" to checking, which is what's true.

## The sweep for a ninth

I read all 106 comment lines against the shipped code under the predicate the earlier sweeps missed, "what does the script do," rather than "what does a v1 file receive." No ninth. One borderline case worth recording rather than changing:

`:427-429` (the `CLAUDE.md`/`README.md` skip) says those files "would otherwise inflate the v1 count with files that can never be migrated." Since T-001 that `continue` has a second consequence the comment doesn't mention: it also keeps those two files out of `v1_files`, so they never reach the link pass. The stated claim is still true, though, and it doesn't assert it is the only consequence. Correcting an incomplete-but-true comment isn't in this task, and a third touched block would hand C-9 surface it doesn't need. Flagging it here so the observation isn't lost.

Everything else is per-check reasoning (`:366-369`, `:250-252`, `:312-313`, and the rest) that the v1 routing change doesn't reach, or already-repaired work from T-001 and T-003 at `:10-15`, `:451-457`, and `:478-482`.

## Verification run

- `ls -l` reports mode 755; edited in place, never recreated.
- `bash -n inbox-to-memory/scripts/lint-scope.sh` clean.
- All three suites exit 0: `tests/inbox-to-memory-smoke.sh`, `tests/file-issue-smoke.sh`, `tests/handoff-smoke.sh`.
- `check-diff-scope.py` over the five permitted paths: `OK: 5 path(s) in scope`, exit 0.
- `grep -c '—\|–'` over the file: 0.
- The `humanizer` skill was invoked formally via the Skill tool and both comments went through its draft, audit, and revise loop before either edit was applied. The audit is what killed the em dashes, flattened "V1 bodies are cached here too" back into the active "It caches v1 bodies too" that parallels the original sentence's subject, and checked both passages for §30 diff-anchored phrasing, since a comment that narrates the change rather than describing the code is exactly the failure mode this whole job is cleaning up.
