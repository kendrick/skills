---
id: T-004
title: Repair the two stale comments describing the script itself
spec: .agent-guild/state/spec.md#the-comment-is-the-contract
clauses: [C-5, C-6, C-8, C-9, C-10]
executor: worker-craft
executor_model: opus
checker: checker-judgment
check_method: >-
  C-5: bash tests/inbox-to-memory-smoke.sh, tests/file-issue-smoke.sh, and
  tests/handoff-smoke.sh, each exit 0. The suite runs bash -n on lint-scope.sh at :107
  and tests [[ -x ]] at :103, so a comment edit that breaks syntax or drops the mode
  bit fails there. Confirm the file is still mode 0755.
  C-6: python3 .agent-guild/scripts/check-diff-scope.py inbox-to-memory/scripts/lint-scope.sh
  inbox-to-memory/references/machine-contracts.md inbox-to-memory/references/migration.md
  inbox-to-memory/SKILL.md tests/inbox-to-memory-smoke.sh — exit 0. Only lint-scope.sh
  should have changed relative to the tree as this task received it; the other four
  already carry completed work from T-001 through T-003.
  C-8: checker-judgment — locations seven and eight only. Quote each shipped sentence
  and name the line of lint-scope.sh that decides it. A location fails if the named line
  makes the sentence false, or if no line can be tied to it. A location also fails if the
  claim was deleted rather than corrected: both must still describe what the script does.
  Then confirm locations one through six are byte-identical to what T-003 shipped — this
  task must not re-open them.
  C-9: checker-judgment — read `git diff -- inbox-to-memory/scripts/lint-scope.sh` for
  this task's changes only. Every changed line must be comment text. A single line of
  changed code fails the clause.
  C-10: checker-judgment — read both rewritten comments against the paragraphs around
  them and name any sentence that states a rule with no reason attached, that reads in a
  register the surrounding file does not use, that introduces a bolded inline header with
  a colon, or that wraps an em dash in spaces.
status: complete
retries: 0
max_retries: 2
deps: [T-001, T-002, T-003]
escalations: []
artifacts: [inbox-to-memory/scripts/lint-scope.sh]
---

## Spec excerpt

T-001 made `inbox-to-memory/scripts/lint-scope.sh` run `check_links` on v1 files (files carrying no `schema:` key) and cache their bodies in pass one. T-003 then repaired six passages that described the old behavior. Two more were missed by every sweep and are still false. They are the whole of this task.

**7. `lint-scope.sh:2-3`**, the file's opening description:

```bash
# Walk one opted-in scope and check every v2 note and record against the machine
# contracts in references/machine-contracts.md.
```

The lint now checks v1 files too. It checks only their wiki links, but "every v2 note and record" is no longer what it walks.

**8. `lint-scope.sh:414-415`**, above pass one:

```bash
# Pass one classifies and caches each v2 body, because the resolution and
# recurrence checks are scope-wide: they can't be answered from inside one file.
```

Pass one now caches v1 bodies as well, into `$work/body.v1.$v1_index`, so the third pass can link-check them. The stated *reason* is still exactly right for the v2 half and is worth keeping: the resolution and recurrence checks are scope-wide and cannot be answered from inside one file. What is missing is that v1 bodies are cached too, for a different and narrower reason.

**Why these were missed, which is also how to check your own work.** Every earlier sweep searched for passages describing *what a v1 file receives*. These two describe *what the script does*. Same subject, different predicate, and an exhaustive search under the first one returns a confident, complete-looking, wrong answer. Before you report done, re-read every comment in the file against the shipped code and satisfy yourself there is no ninth.

**Write from the code.** Read `git diff -- inbox-to-memory/scripts/lint-scope.sh` first. The three passes as shipped: pass one at `:414-436` classifies every file and caches both generations' bodies; pass two at `:442-473` iterates v2 files and runs the full check set; the third pass at `:478-488` iterates v1 files and calls `check_links` alone.

**Do not re-open locations one through six.** T-003's six passages are complete and verified, including the header comment at `:10-15` which sits directly above location seven. Your diff should touch two comment blocks and nothing else.

**Voice.** Match the file, which explains why a rule exists rather than restating it. The comment at `:414-415` already does this well; preserve its reasoning and extend it rather than replacing it. Do not introduce a bolded inline header with a colon. If you use an em dash, chain it directly to the text on both sides rather than wrapping it in spaces.

**Run the `humanizer` skill on both rewritten comments, formally, via the Skill tool**, and follow its audit-and-revise loop. Keep title-cased headings as they are, keep genuinely list-shaped content as lists, and keep a few em dashes rather than eliminating them. Your checker cannot invoke that skill and judges only what you ship.

**Out of scope.** Change no behavior; this task edits comments only. Do not touch `tests/inbox-to-memory-smoke.sh` or any of the three documentation files. Do not repair the `EXIT` trap gaps in the smoke suite (issue #30). Do not add a `link-broken` row to the "What the Lint Checks" table in `machine-contracts.md:140-154`; that gap is pre-existing, deliberately out of scope, and wants its own issue.

**Edit in place.** `lint-scope.sh` is mode 0755 and the suite tests `[[ -x ]]` at `:103`. Recreating the file drops the bit and fails with a message about executability that looks nothing like a prose problem.

## Rework diagnosis

## Courier comparison

**The verdicts disagree, for the second time and on the same clause.** Checker of record `pass`; courier `fail` on C-10. Crossing cost 79,664 in (56,576 cached) / 5,530 out over 119s, ledgered. The standard-stem verdict decides, so the task is `complete`.

**Cited clauses, and what was crossed.** T-004 cites five. C-5 and C-6 are deterministic. C-8 and C-9 both require tying shipped text to script lines and separating one task's diff from another's, which cannot travel. The crossing sampled **C-10 alone**, deliberately repeating T-003's sample to see whether that disagreement was a one-off.

**Findings only the courier raised:** three, all on C-10.

1. Passage 7's stated reason duplicates the unchanged block five lines below rather than supplying a different one.
2. "It caches v1 bodies too, for a narrower reason" states the rule while only *promising* its reason in that sentence; the reason arrives in the next one.
3. Neither passage matches the surrounding "direct policy-level voice"; the additions read as an implementation detour.

**Findings only the checker of record raised:** two, and neither is a C-10 finding. Sweeping all 47 comment blocks under the *what the script does* predicate, it found nothing else false, but flagged two comments now narrower than they sound: `:428-430`, whose file exclusion now also keeps README wiki links out of the link pass and not merely out of the v1 count, and `:497-503`, where "a question open across three or more notes" silently means v2 notes because `open-slugs` stayed v2-only. Both required reading the whole file against the code.

**Overlap:** both agree neither passage introduces a bolded inline header or a spaced em dash. The worker had removed its drafted em dashes on the grounds that the file contains none across 509 lines, so a mark the file never uses is itself a register break.

**Orchestrator's read.** Finding 2 is the same sentence-versus-passage scope ambiguity in C-10 that produced T-003's disagreement, now confirmed as systematic rather than incidental. The courier reads "each rewritten comment and doc **sentence** carries the reason alongside the rule" literally; the checker of record reads the clause's own check instruction, "read each rewritten passage against the paragraphs around it," as setting passage scope. Both are faithful to different halves of one clause. **That is a defect in C-10, not in either agent, and it is the clearest clause-level lesson this job produced: a clause whose text and check disagree about their unit of judgment will produce reproducible disagreement between checkers who are both reading carefully.**

Finding 1 is partly right on the merits and is not scope ambiguity. The worker argued explicitly that it avoided repeating the block below by choosing a different angle. Reading both, the angles do overlap: the block below already ends with "without ever reading `schema`," and passage 7 lands on "never reads the file's own frontmatter." Different words, adjacent claim. Neither verdict is wrong to weigh that differently, but the courier saw something real that the checker of record did not remark on.

**Cost note for #34.** This crossing cost four times any other in the job, 79,664 input tokens against roughly 19,000 for each of the first three, because the brief inlined neighbouring context so the far side could judge duplication. `brief_tokens` recorded 2,605, so the ratio between what the ledger measures and what the vendor bills reached 30x here against 13x on T-001. Spend tracked brief size, not task size, which reproduces #17's finding at a sharper multiple. It bought the run's second-most-useful #34 datum, so the spend was not wasted, but nothing in the ledger would have predicted it.
