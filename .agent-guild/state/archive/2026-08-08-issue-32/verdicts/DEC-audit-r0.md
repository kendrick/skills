---
task: DEC-audit
checker: auditor
vendor: anthropic
model: claude-opus-5[1m]
verdict: FAIL
checked_at: 2026-08-08T19:40:00Z
duration_ms: None
cost_usd: None
---

<!-- GENERATED FILE—do not hand-edit. Rendered by render-verdict.py
from the verdict JSON, the record of record. Edit the JSON and
re-render instead. -->

## Per-clause results

| clause | severity | description | evidence |
| ------ | -------- | ------------ | -------- |
| C-8 | blocker | A fifth location carries the exact sentence C-8 exists to repair, and no task owns it: `inbox-to-memory/references/migration.md:11` reads "V1 files are legal forever and the lint never flags them, so nothing forces this." T-001's change makes that false in precisely the way it makes machine-contracts.md:7 and SKILL.md:428 false. C-8 enumerates four locations in three files and C-6 confines the diff to four paths that exclude `references/migration.md`, so the decomposition cannot repair it and a worker that tried would fail C-6. The job as decomposed ships a document that still states the pre-fix contract, which is the defect the job exists to close. | inbox-to-memory/references/migration.md:11 — `V1 files are legal forever and the lint never flags them, so nothing forces this.` Found by `grep -rn -e 'never flags' -e 'flags them' inbox-to-memory tests --include=*.md --include=*.sh`, which returns exactly five hits outside tests/fixtures: machine-contracts.md:7, migration.md:11, SKILL.md:428, lint-scope.sh:10, plus lint-scope.sh:478 (the `v1 files:` echo, not a claim). The file is live user-facing documentation, linked as the migration spec from SKILL.md:344. Repairing it requires both a fifth location in C-8 and a fifth path in C-6's allowlist and in the three tasks' C-6 check_method lines. |
| T-002+T-003 | blocker | Concurrent execution of T-002 and T-003 can make either task's cited checks fail for a reason belonging to the other. The two tasks' *edits* are disjoint; their *checks* are not. T-002's C-5 runs `bash tests/inbox-to-memory-smoke.sh`, which executes 14 `require_text "$contracts"` assertions against `machine-contracts.md` — the file T-003 is concurrently rewriting. T-003's C-10 addendum runs the same suite — the file T-002 is concurrently rewriting. A defective or half-written edit by either worker fails the other's check, and both workers are additionally instructed to run the suite themselves mid-edit (T-002 "Sanity check your own work", T-003 "run the suite after you edit"), so the collision reaches the workers, not just the checkers. Serialize T-003 after T-002 (or at minimum serialize the two checker dispatches and forbid mid-edit suite runs while a sibling is live). | T-002 check_method C-5 and T-003 check_method C-10 both invoke `bash tests/inbox-to-memory-smoke.sh`. `grep -n 'require_text "$contracts"' tests/inbox-to-memory-smoke.sh` returns 14 call sites at :309, :310, :320, :335, :382, :383, :388, :416, :421, :565, :568, :569, :1236, :1237, with `contracts=inbox-to-memory/references/machine-contracts.md` at :305. T-002.md:57 and T-003.md:76 each direct their worker to run the suite. C-6 is not the shared surface — check-diff-scope.py takes an allowlist and does not require a listed path to have changed, so all four paths stay legal for both tasks concurrently. |
| T-002 | major | T-002's check_method closes with "Additionally confirm each new case would fail against the pre-fix lint" and names no method for producing a pre-fix lint. T-001's fix is uncommitted working-tree state, so the two instincts a checker reaches for first — `git stash` and reverting `inbox-to-memory/scripts/lint-scope.sh` — mutate the shared tree, and under the intended concurrency they would destroy T-003's in-flight edits to that same file. The check needs an explicit non-destructive recipe, the way C-4 spells out its `git worktree add -q --detach` comparison rather than leaving the baseline to the checker's imagination. | T-002.md:25-26. The only safe shape is a detached worktree at HEAD with the new suite copied in and run from that root: `git worktree add -q --detach "$B" HEAD; cp tests/inbox-to-memory-smoke.sh "$B/tests/"; (cd "$B" && bash tests/inbox-to-memory-smoke.sh)`. That works because the suite resolves both `fixtures=tests/fixtures/inbox-to-memory` (:12) and the lint path relatively, so it picks up the worktree's pre-fix lint — but nothing in the task says so, and the fixture-hash guard at :1229 plus C-4's worktree usage mean a checker improvising here has several ways to corrupt state. |
| T-001 | major | T-001's negative boundary is stated for lint-scope.sh's two comment blocks and for issue #30's trap chain, but nowhere does it forbid T-001 from editing the three files its siblings own. Nothing in T-001's clause set catches such an edit: C-6 permits all four paths in the diff by design, and C-9's judgment rubric accounts only for changed lines in `git diff -- inbox-to-memory/scripts/lint-scope.sh`. A T-001 worker that adds a regression case to the smoke suite (its own excerpt tells it the suite must stay green) or that corrects `machine-contracts.md:7` and `SKILL.md:428` after making them false would pass every check T-001 cites and collide head-on with T-002 or T-003. T-002 and T-003 each state their negative boundary explicitly; T-001 states only half of its. | T-001.md:63 forbids the two comment blocks and the trap-chain repair, and says nothing about adding smoke cases or editing the two docs. Compare T-002.md:55 ("Do not modify inbox-to-memory/scripts/lint-scope.sh — T-001 owns it") and T-003.md:74 ("Do not touch tests/inbox-to-memory-smoke.sh (T-002 owns it)"). The missing clause is symmetric: T-001 owns lint-scope.sh's code and nothing else. |
| T-003 | minor | The excerpt's closing caution asserts a count that does not survive `grep -c`. "`tests/inbox-to-memory-smoke.sh` pins 15 literal strings from `machine-contracts.md` via `require_text`" — there are 14 `require_text "$contracts"` call sites, one of which (:335) loops over 9 token strings, so the number of distinct strings pinned is 22. Neither figure is 15. The range `:309` to `:1237` is correct, and the load-bearing claim is correct: I checked every pinned string and none comes from the sentence at machine-contracts.md:7. But this job's entire subject is prose that disagrees with code, and #17's standing lesson is that an asserted instrument is the recurring defect. Either drop the number or make it survive the grep. | T-003.md:76. `grep -c 'require_text "$contracts"' tests/inbox-to-memory-smoke.sh` → 14; call sites at :309, :310, :320, :335, :382, :383, :388, :416, :421, :565, :568, :569, :1236, :1237. The loop at :325-336 supplies 9 token strings to the single call at :335. Strings pinned, verified individually: the two key orders, "first 20 lines", the 9 tokens, "explicit exception to omit-if-empty", "**Absent is not zero.**", "A missing field is reported and never filled in.", "The discarded alternatives are a decision's payload.", "The snippet is authoritative and the line number is a convenience.", "frontmatter-key-domain", "contradiction-fields", "\| claims: <what the record says>", "body_schema", "A body-shape check therefore keys off `body_schema`, never off `schema: 2`." None is from :7. |
| T-001 | minor | Two line citations in T-001's excerpt are off by a line or two against the shipped file. "Pass one at `:414-424`" heads a quoted block whose last two lines sit outside that range: `:414` is the `for file in` head and `:424` is the closing `fi`, while the quoted `v2=$((v2 + 1))` and `v2_files+=("$file")` are at `:425-426`. The correct range for the quote is `:421-426`; for the whole of pass one it is `:414-432`. (This one is inherited verbatim from the spec's "Where It Happens" at spec.md:60, so the spec carries the same error.) Separately, "the `RECURRING` report at `:471-476`" overshoots by one: the report is `:471-475` and `:476` is blank. | T-001.md:43 and T-001.md:59, against `inbox-to-memory/scripts/lint-scope.sh`: :414 `for file in "$scope"/notes/*.md …`, :421 `if ! has_schema_key "$file"; then`, :424 `fi`, :425 `v2=$((v2 + 1))`, :426 `v2_files+=("$file")`, :432 `done`; :471 `RECURRENCE_THRESHOLD=3` through :475 `done < <(sort "$work/open-slugs" …)`, :476 blank, :477 `echo "scope: $scope"`. |
| T-002 | minor | The helper list T-002 hands its worker omits the one helper three of its four cases actually need. Cases 2, 3, and 4 all assert that a scope passes, and the file's idiom for that is `require_line "$out" "failures: 0" <label>` (defined at :49), not any of the six helpers named. `refute_failure` refutes a failure against a named file, which covers part of it, but the "still passes" assertion the excerpt describes has no listed tool. A worker told to "follow the file's existing idiom rather than inventing one" and handed an incomplete list will invent one. | T-002.md:49 lists run_lint, require_output, require_failure, refute_failure, require_text, refute_text. All six exist (:98, :75, :63, :85, :28, :39). `require_line` at :49 and `require_dir` at :21 are not listed. The file's own passing-scope assertions use require_line: see :125 `require_line "$old_only_out" "failures: 0" old-only` and :132 for mixed. |
| C-7 | minor | T-002's excerpt tells the worker "Two scopes are enough for all four cases," while C-7's clause text reads "Each builds its scope with `mktemp -d`" — which a literal-minded checker can read as four scratch scopes and fail a compliant two-scope artifact. CON-audit-r2 already ruled the two-scope shape correct, but that ruling lives in a verdict file the task's checker has no reason to open. One sentence in T-002's C-7 check_method stating that two scopes satisfy the clause would close it; the alternative is amending C-7's text. | T-002.md:53 against constitution.md:133. CON-audit-r2.md records the same reading under C-7: "C-7's four cases need only two scratch scopes (case one must fail, cases two through four can share one passing scope) and both can sit under a single added trap." Note the second-order consequence recorded there and still live: with one added trap, C-7's "walk every added trap in order" and "the last one carries every scratch path" collapse onto a single comparison against the pre-existing trap at :1203. |
| coverage | info | PASS on clause coverage and on the template's per-clause check rule. All ten clauses are cited by at least one task, every clause named in a task's `clauses` list appears by name in that task's `check_method`, and all six of the spec's acceptance criteria land in a task. Nothing in the spec is orphaned. | Citations: T-001 [C-1..C-6, C-9], T-002 [C-5, C-6, C-7], T-003 [C-6, C-8, C-10] — union is C-1 through C-10. check_method literal match verified per task: T-001 names C-1, C-2, C-3, C-4, C-5, C-6, C-9 (7 of 7); T-002 names C-5, C-6, C-7 (3 of 3); T-003 names C-6, C-8, C-10 (3 of 3). Acceptance criteria at spec.md:87-92: AC1→C-1→T-001, AC2→C-2→T-001, AC3→C-3→T-001, AC4→C-4→T-001, AC5→C-5→T-001 and T-002, AC6→C-8→T-003. C-7 exceeds the spec (durable regression coverage the issue did not ask for), which is the constitution being stricter, not a task without a home. |
| routing | info | PASS on routing, including the specific question about T-001. Sonnet is right for T-001 and I would not start it on opus. The design space is small and the excerpt has already closed it: v1 files must reach `check_links` and nothing else, the body cache and `current_file` mechanics are named outright, and every plausible wrong turn has a deterministic check waiting for it. The one genuinely subtle failure mode — harvesting v1 slugs into the scope-wide open-slug set, which passes C-1, C-2, C-4, C-5, and C-6 and fails only C-3's second block — is called out by name in the excerpt itself and is caught mechanically rather than by taste. Escalation is cheap here precisely because the diagnosis will be a command's output, not a judgment. T-002 on sonnet and T-003 on opus are both correct by the table. | .agent-guild/CLAUDE.md model routing table: sonnet for "clear-spec implementation judged on correctness", opus/worker-craft for "user-facing/taste work". T-001.md:56-61 states four hard boundaries with the mechanism for each; five of its seven clauses are script checks. T-003 is prose in three files a person reads, with a humanizer obligation at T-003.md:72 — worker-craft/opus is the table's answer. Checker assignments: all three tasks mix script clauses with at least one judgment clause (C-9, C-7, C-8/C-10 respectively), and a task carries one `checker`, so checker-judgment is correct for all three; none is a pure-script task that should have gone to checker-deterministic. |
| deps | info | PASS on the dependency graph as a graph. T-001 has no deps; T-002 and T-003 each depend on T-001 and on nothing else. Every referenced task exists, there are no cycles, and the ordering is semantically right — T-003 is told to write its prose from T-001's shipped diff rather than from a description, which requires T-001 to be complete first. The defect is not the graph, it is the concurrency the graph permits; see the T-002+T-003 finding. | T-001.md:34 `deps: []`, T-002.md:30 `deps: [T-001]`, T-003.md:27 `deps: [T-001]`. Three task files exist under .agent-guild/state/tasks/ and no dep names a task outside that set. T-003.md:36: "Read T-001's actual diff first. Do not write these from this description — write them from the code." |
| boundaries | info | PASS on the specific boundary the orchestrator asked about. The `:10-12` and `:441-443` comment blocks are claimed by exactly one task, and the claim is stated from both sides consistently. T-002's exclusion of the pre-existing trap gaps matches C-7's own carve-out line for line. No two tasks claim the same lines of any file. | T-001.md:63 "Do not touch the comments at `:10-12` or `:441-443` — those are T-003's"; T-003.md:38 and :48 claim exactly those two blocks and T-003.md:74 forbids "any code in `lint-scope.sh` beyond the two comment blocks." T-002.md:55 names `$overrun_scope` at :195 and `$tpl_scope` at :345 as out of scope, matching constitution.md:134. Verified in the file: overrun_scope created at :195 and dropped by the trap at :239; tpl_scope created at :345 and dropped by the trap at :625; the last pre-existing trap is at :1203. |
| isolation | info | PASS on worker isolation, read cold as each worker would see it. T-001's excerpt is self-contained: it defines the v1 discriminant, quotes the offending block, names the body cache and `current_file` mechanics, and states the counts invariant. T-003's excerpt quotes all four target passages verbatim, so its worker never needs the source files to know what it is replacing. T-002's is actionable modulo the missing `require_line` noted above. The residual gaps are small enough to name rather than fail: T-003 is told to "read T-001's actual diff" without being given the command (`git diff -- inbox-to-memory/scripts/lint-scope.sh`, which C-9 states for T-001's checker but not for T-003's worker), and all three excerpts route the worker to the constitution for full check commands, which is legitimate since the constitution is in every worker's read set. | T-001.md:41 (discriminant plus `has_schema_key` at :82-89), :43-54 (quoted block, pass two, check_links), :61 (extract_body, body cache, current_file). T-003.md:38-64 quotes all four passages. T-002.md:46-53 gives the mktemp idiom, the fixture-hash prohibition, the `## Raw Content` fence rule, and the trap convention. |
| facts | info | Every other line number, function name, and behavioral claim in the three excerpts checks out against the shipped tree. Re-derived rather than read: the fixture facts the excerpts repeat from the Baseline hold, including the one T-001 leans on to justify the change being silent inside this repo. | Verified in inbox-to-memory/scripts/lint-scope.sh: has_schema_key :82-89; extract_body :208-216; check_links :366-380 with the ten-character id fallback at :376-377; pass two :435-462; body-grammar gate :444-451 (`if ! has_v1_body` through `fi`, wrapping exactly the six named checks); open-question-resolution loop :458-461; `$work/open-slugs` initialized :408 and fed :430-431; body cache `$work/body.$index` :428-429; `fail` echoes `$current_file` at :74-75, set at :437. Header comment :10-12 and pass-two comment :441-443 both present as quoted. machine-contracts.md:7 and SKILL.md:428 quoted verbatim and correct; machine-contracts.md:65 scopes the token grammar to v2 as stated; SKILL.md:178 and :363 are both still accurate after the fix, as T-003 claims. tests/inbox-to-memory-smoke.sh: fixture-hash guard at :1229; helpers as cited; trap chain as cited. Fixtures: ten opted-in scopes under tests/fixtures/inbox-to-memory (`find … -type d -name _inbox` → 10); `old-only/_memory/decisions/one-vendor-per-region-G2k65qG3Nc.md` exists as C-2 requires; exactly three wiki links live in v1 files across all fixtures, all in evals/contradiction-amend, and all three resolve by filename (receipt-barcode-lookup-Qj2vTy_2pZ → _memory/context/, refund-approval-code-ypAes5QNas → _memory/rules/, self-checkout-refunds-DPtlHN4Ut4 → _memory/decisions/). |

## Diagnosis

- **C-8** (blocker): A fifth location carries the exact sentence C-8 exists to repair, and no task owns it: `inbox-to-memory/references/migration.md:11` reads "V1 files are legal forever and the lint never flags them, so nothing forces this." T-001's change makes that false in precisely the way it makes machine-contracts.md:7 and SKILL.md:428 false. C-8 enumerates four locations in three files and C-6 confines the diff to four paths that exclude `references/migration.md`, so the decomposition cannot repair it and a worker that tried would fail C-6. The job as decomposed ships a document that still states the pre-fix contract, which is the defect the job exists to close.
  evidence: inbox-to-memory/references/migration.md:11 — `V1 files are legal forever and the lint never flags them, so nothing forces this.` Found by `grep -rn -e 'never flags' -e 'flags them' inbox-to-memory tests --include=*.md --include=*.sh`, which returns exactly five hits outside tests/fixtures: machine-contracts.md:7, migration.md:11, SKILL.md:428, lint-scope.sh:10, plus lint-scope.sh:478 (the `v1 files:` echo, not a claim). The file is live user-facing documentation, linked as the migration spec from SKILL.md:344. Repairing it requires both a fifth location in C-8 and a fifth path in C-6's allowlist and in the three tasks' C-6 check_method lines.
- **T-002+T-003** (blocker): Concurrent execution of T-002 and T-003 can make either task's cited checks fail for a reason belonging to the other. The two tasks' *edits* are disjoint; their *checks* are not. T-002's C-5 runs `bash tests/inbox-to-memory-smoke.sh`, which executes 14 `require_text "$contracts"` assertions against `machine-contracts.md` — the file T-003 is concurrently rewriting. T-003's C-10 addendum runs the same suite — the file T-002 is concurrently rewriting. A defective or half-written edit by either worker fails the other's check, and both workers are additionally instructed to run the suite themselves mid-edit (T-002 "Sanity check your own work", T-003 "run the suite after you edit"), so the collision reaches the workers, not just the checkers. Serialize T-003 after T-002 (or at minimum serialize the two checker dispatches and forbid mid-edit suite runs while a sibling is live).
  evidence: T-002 check_method C-5 and T-003 check_method C-10 both invoke `bash tests/inbox-to-memory-smoke.sh`. `grep -n 'require_text "$contracts"' tests/inbox-to-memory-smoke.sh` returns 14 call sites at :309, :310, :320, :335, :382, :383, :388, :416, :421, :565, :568, :569, :1236, :1237, with `contracts=inbox-to-memory/references/machine-contracts.md` at :305. T-002.md:57 and T-003.md:76 each direct their worker to run the suite. C-6 is not the shared surface — check-diff-scope.py takes an allowlist and does not require a listed path to have changed, so all four paths stay legal for both tasks concurrently.
- **T-002** (major): T-002's check_method closes with "Additionally confirm each new case would fail against the pre-fix lint" and names no method for producing a pre-fix lint. T-001's fix is uncommitted working-tree state, so the two instincts a checker reaches for first — `git stash` and reverting `inbox-to-memory/scripts/lint-scope.sh` — mutate the shared tree, and under the intended concurrency they would destroy T-003's in-flight edits to that same file. The check needs an explicit non-destructive recipe, the way C-4 spells out its `git worktree add -q --detach` comparison rather than leaving the baseline to the checker's imagination.
  evidence: T-002.md:25-26. The only safe shape is a detached worktree at HEAD with the new suite copied in and run from that root: `git worktree add -q --detach "$B" HEAD; cp tests/inbox-to-memory-smoke.sh "$B/tests/"; (cd "$B" && bash tests/inbox-to-memory-smoke.sh)`. That works because the suite resolves both `fixtures=tests/fixtures/inbox-to-memory` (:12) and the lint path relatively, so it picks up the worktree's pre-fix lint — but nothing in the task says so, and the fixture-hash guard at :1229 plus C-4's worktree usage mean a checker improvising here has several ways to corrupt state.
- **T-001** (major): T-001's negative boundary is stated for lint-scope.sh's two comment blocks and for issue #30's trap chain, but nowhere does it forbid T-001 from editing the three files its siblings own. Nothing in T-001's clause set catches such an edit: C-6 permits all four paths in the diff by design, and C-9's judgment rubric accounts only for changed lines in `git diff -- inbox-to-memory/scripts/lint-scope.sh`. A T-001 worker that adds a regression case to the smoke suite (its own excerpt tells it the suite must stay green) or that corrects `machine-contracts.md:7` and `SKILL.md:428` after making them false would pass every check T-001 cites and collide head-on with T-002 or T-003. T-002 and T-003 each state their negative boundary explicitly; T-001 states only half of its.
  evidence: T-001.md:63 forbids the two comment blocks and the trap-chain repair, and says nothing about adding smoke cases or editing the two docs. Compare T-002.md:55 ("Do not modify inbox-to-memory/scripts/lint-scope.sh — T-001 owns it") and T-003.md:74 ("Do not touch tests/inbox-to-memory-smoke.sh (T-002 owns it)"). The missing clause is symmetric: T-001 owns lint-scope.sh's code and nothing else.
- **T-003** (minor): The excerpt's closing caution asserts a count that does not survive `grep -c`. "`tests/inbox-to-memory-smoke.sh` pins 15 literal strings from `machine-contracts.md` via `require_text`" — there are 14 `require_text "$contracts"` call sites, one of which (:335) loops over 9 token strings, so the number of distinct strings pinned is 22. Neither figure is 15. The range `:309` to `:1237` is correct, and the load-bearing claim is correct: I checked every pinned string and none comes from the sentence at machine-contracts.md:7. But this job's entire subject is prose that disagrees with code, and #17's standing lesson is that an asserted instrument is the recurring defect. Either drop the number or make it survive the grep.
  evidence: T-003.md:76. `grep -c 'require_text "$contracts"' tests/inbox-to-memory-smoke.sh` → 14; call sites at :309, :310, :320, :335, :382, :383, :388, :416, :421, :565, :568, :569, :1236, :1237. The loop at :325-336 supplies 9 token strings to the single call at :335. Strings pinned, verified individually: the two key orders, "first 20 lines", the 9 tokens, "explicit exception to omit-if-empty", "**Absent is not zero.**", "A missing field is reported and never filled in.", "The discarded alternatives are a decision's payload.", "The snippet is authoritative and the line number is a convenience.", "frontmatter-key-domain", "contradiction-fields", "| claims: <what the record says>", "body_schema", "A body-shape check therefore keys off `body_schema`, never off `schema: 2`." None is from :7.
- **T-001** (minor): Two line citations in T-001's excerpt are off by a line or two against the shipped file. "Pass one at `:414-424`" heads a quoted block whose last two lines sit outside that range: `:414` is the `for file in` head and `:424` is the closing `fi`, while the quoted `v2=$((v2 + 1))` and `v2_files+=("$file")` are at `:425-426`. The correct range for the quote is `:421-426`; for the whole of pass one it is `:414-432`. (This one is inherited verbatim from the spec's "Where It Happens" at spec.md:60, so the spec carries the same error.) Separately, "the `RECURRING` report at `:471-476`" overshoots by one: the report is `:471-475` and `:476` is blank.
  evidence: T-001.md:43 and T-001.md:59, against `inbox-to-memory/scripts/lint-scope.sh`: :414 `for file in "$scope"/notes/*.md …`, :421 `if ! has_schema_key "$file"; then`, :424 `fi`, :425 `v2=$((v2 + 1))`, :426 `v2_files+=("$file")`, :432 `done`; :471 `RECURRENCE_THRESHOLD=3` through :475 `done < <(sort "$work/open-slugs" …)`, :476 blank, :477 `echo "scope: $scope"`.
- **T-002** (minor): The helper list T-002 hands its worker omits the one helper three of its four cases actually need. Cases 2, 3, and 4 all assert that a scope passes, and the file's idiom for that is `require_line "$out" "failures: 0" <label>` (defined at :49), not any of the six helpers named. `refute_failure` refutes a failure against a named file, which covers part of it, but the "still passes" assertion the excerpt describes has no listed tool. A worker told to "follow the file's existing idiom rather than inventing one" and handed an incomplete list will invent one.
  evidence: T-002.md:49 lists run_lint, require_output, require_failure, refute_failure, require_text, refute_text. All six exist (:98, :75, :63, :85, :28, :39). `require_line` at :49 and `require_dir` at :21 are not listed. The file's own passing-scope assertions use require_line: see :125 `require_line "$old_only_out" "failures: 0" old-only` and :132 for mixed.
- **C-7** (minor): T-002's excerpt tells the worker "Two scopes are enough for all four cases," while C-7's clause text reads "Each builds its scope with `mktemp -d`" — which a literal-minded checker can read as four scratch scopes and fail a compliant two-scope artifact. CON-audit-r2 already ruled the two-scope shape correct, but that ruling lives in a verdict file the task's checker has no reason to open. One sentence in T-002's C-7 check_method stating that two scopes satisfy the clause would close it; the alternative is amending C-7's text.
  evidence: T-002.md:53 against constitution.md:133. CON-audit-r2.md records the same reading under C-7: "C-7's four cases need only two scratch scopes (case one must fail, cases two through four can share one passing scope) and both can sit under a single added trap." Note the second-order consequence recorded there and still live: with one added trap, C-7's "walk every added trap in order" and "the last one carries every scratch path" collapse onto a single comparison against the pre-existing trap at :1203.
- **coverage** (info): PASS on clause coverage and on the template's per-clause check rule. All ten clauses are cited by at least one task, every clause named in a task's `clauses` list appears by name in that task's `check_method`, and all six of the spec's acceptance criteria land in a task. Nothing in the spec is orphaned.
  evidence: Citations: T-001 [C-1..C-6, C-9], T-002 [C-5, C-6, C-7], T-003 [C-6, C-8, C-10] — union is C-1 through C-10. check_method literal match verified per task: T-001 names C-1, C-2, C-3, C-4, C-5, C-6, C-9 (7 of 7); T-002 names C-5, C-6, C-7 (3 of 3); T-003 names C-6, C-8, C-10 (3 of 3). Acceptance criteria at spec.md:87-92: AC1→C-1→T-001, AC2→C-2→T-001, AC3→C-3→T-001, AC4→C-4→T-001, AC5→C-5→T-001 and T-002, AC6→C-8→T-003. C-7 exceeds the spec (durable regression coverage the issue did not ask for), which is the constitution being stricter, not a task without a home.
- **routing** (info): PASS on routing, including the specific question about T-001. Sonnet is right for T-001 and I would not start it on opus. The design space is small and the excerpt has already closed it: v1 files must reach `check_links` and nothing else, the body cache and `current_file` mechanics are named outright, and every plausible wrong turn has a deterministic check waiting for it. The one genuinely subtle failure mode — harvesting v1 slugs into the scope-wide open-slug set, which passes C-1, C-2, C-4, C-5, and C-6 and fails only C-3's second block — is called out by name in the excerpt itself and is caught mechanically rather than by taste. Escalation is cheap here precisely because the diagnosis will be a command's output, not a judgment. T-002 on sonnet and T-003 on opus are both correct by the table.
  evidence: .agent-guild/CLAUDE.md model routing table: sonnet for "clear-spec implementation judged on correctness", opus/worker-craft for "user-facing/taste work". T-001.md:56-61 states four hard boundaries with the mechanism for each; five of its seven clauses are script checks. T-003 is prose in three files a person reads, with a humanizer obligation at T-003.md:72 — worker-craft/opus is the table's answer. Checker assignments: all three tasks mix script clauses with at least one judgment clause (C-9, C-7, C-8/C-10 respectively), and a task carries one `checker`, so checker-judgment is correct for all three; none is a pure-script task that should have gone to checker-deterministic.
- **deps** (info): PASS on the dependency graph as a graph. T-001 has no deps; T-002 and T-003 each depend on T-001 and on nothing else. Every referenced task exists, there are no cycles, and the ordering is semantically right — T-003 is told to write its prose from T-001's shipped diff rather than from a description, which requires T-001 to be complete first. The defect is not the graph, it is the concurrency the graph permits; see the T-002+T-003 finding.
  evidence: T-001.md:34 `deps: []`, T-002.md:30 `deps: [T-001]`, T-003.md:27 `deps: [T-001]`. Three task files exist under .agent-guild/state/tasks/ and no dep names a task outside that set. T-003.md:36: "Read T-001's actual diff first. Do not write these from this description — write them from the code."
- **boundaries** (info): PASS on the specific boundary the orchestrator asked about. The `:10-12` and `:441-443` comment blocks are claimed by exactly one task, and the claim is stated from both sides consistently. T-002's exclusion of the pre-existing trap gaps matches C-7's own carve-out line for line. No two tasks claim the same lines of any file.
  evidence: T-001.md:63 "Do not touch the comments at `:10-12` or `:441-443` — those are T-003's"; T-003.md:38 and :48 claim exactly those two blocks and T-003.md:74 forbids "any code in `lint-scope.sh` beyond the two comment blocks." T-002.md:55 names `$overrun_scope` at :195 and `$tpl_scope` at :345 as out of scope, matching constitution.md:134. Verified in the file: overrun_scope created at :195 and dropped by the trap at :239; tpl_scope created at :345 and dropped by the trap at :625; the last pre-existing trap is at :1203.
- **isolation** (info): PASS on worker isolation, read cold as each worker would see it. T-001's excerpt is self-contained: it defines the v1 discriminant, quotes the offending block, names the body cache and `current_file` mechanics, and states the counts invariant. T-003's excerpt quotes all four target passages verbatim, so its worker never needs the source files to know what it is replacing. T-002's is actionable modulo the missing `require_line` noted above. The residual gaps are small enough to name rather than fail: T-003 is told to "read T-001's actual diff" without being given the command (`git diff -- inbox-to-memory/scripts/lint-scope.sh`, which C-9 states for T-001's checker but not for T-003's worker), and all three excerpts route the worker to the constitution for full check commands, which is legitimate since the constitution is in every worker's read set.
  evidence: T-001.md:41 (discriminant plus `has_schema_key` at :82-89), :43-54 (quoted block, pass two, check_links), :61 (extract_body, body cache, current_file). T-003.md:38-64 quotes all four passages. T-002.md:46-53 gives the mktemp idiom, the fixture-hash prohibition, the `## Raw Content` fence rule, and the trap convention.
- **facts** (info): Every other line number, function name, and behavioral claim in the three excerpts checks out against the shipped tree. Re-derived rather than read: the fixture facts the excerpts repeat from the Baseline hold, including the one T-001 leans on to justify the change being silent inside this repo.
  evidence: Verified in inbox-to-memory/scripts/lint-scope.sh: has_schema_key :82-89; extract_body :208-216; check_links :366-380 with the ten-character id fallback at :376-377; pass two :435-462; body-grammar gate :444-451 (`if ! has_v1_body` through `fi`, wrapping exactly the six named checks); open-question-resolution loop :458-461; `$work/open-slugs` initialized :408 and fed :430-431; body cache `$work/body.$index` :428-429; `fail` echoes `$current_file` at :74-75, set at :437. Header comment :10-12 and pass-two comment :441-443 both present as quoted. machine-contracts.md:7 and SKILL.md:428 quoted verbatim and correct; machine-contracts.md:65 scopes the token grammar to v2 as stated; SKILL.md:178 and :363 are both still accurate after the fix, as T-003 claims. tests/inbox-to-memory-smoke.sh: fixture-hash guard at :1229; helpers as cited; trap chain as cited. Fixtures: ten opted-in scopes under tests/fixtures/inbox-to-memory (`find … -type d -name _inbox` → 10); `old-only/_memory/decisions/one-vendor-per-region-G2k65qG3Nc.md` exists as C-2 requires; exactly three wiki links live in v1 files across all fixtures, all in evals/contradiction-amend, and all three resolve by filename (receipt-barcode-lookup-Qj2vTy_2pZ → _memory/context/, refund-approval-code-ypAes5QNas → _memory/rules/, self-checkout-refunds-DPtlHN4Ut4 → _memory/decisions/).
