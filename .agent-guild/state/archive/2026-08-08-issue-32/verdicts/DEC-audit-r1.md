---
task: DEC-audit
checker: auditor
vendor: anthropic
model: claude-opus-5[1m]
verdict: FAIL
checked_at: 2026-08-08T20:15:00Z
duration_ms: None
cost_usd: None
---

<!-- GENERATED FILE—do not hand-edit. Rendered by render-verdict.py
from the verdict JSON, the record of record. Edit the JSON and
re-render instead. -->

## Per-clause results

| clause | severity | description | evidence |
| ------ | -------- | ------------ | -------- |
| C-8 | blocker | The enumeration is still incomplete at five. There is a sixth location, it lives in a file already in scope, and it is the strongest statement of the v1 contract in the repository: `inbox-to-memory/references/machine-contracts.md:13-21` — the generation table plus the sentence under it, "Links and derived counts are checked in all three of the rows the lint touches." Three things are wrong with leaving it out. (1) That sentence is the doc twin of the `:441-443` comment: pre-fix it is FALSE for row one (`no schema key`), because links are exactly what the lint does not check there, so it states the intended contract the code violates — the same shape the issue titles "The Comment Is the Contract." T-001 makes its links half true for the first time. (2) Its "derived counts" half is false for row one and always was, which is verbatim the "and counts" defect C-8 already names at `:441-443` and requires be fixed rather than preserved. Repairing one and not the other leaves the same error standing in the document the comment points at. (3) `:7` and `:21` contradict each other in that document today, before any change — one says the lint never flags a v1 file, the other says links are checked in all three rows — and T-003 is being asked to repair `:7` without being told `:21` exists. A worker who rewrites `:7` to say only links are checked, then leaves `:21` claiming links *and derived counts* are checked in all three rows, ships a document internally inconsistent about counts and passes every check as written. The table itself is part of the location: it has columns for Frontmatter contract and Body grammar and none for Links, so after the fix the document's most authoritative artifact about generations still answers "what does the lint do to a v1 file" with "nothing." | inbox-to-memory/references/machine-contracts.md:13-17 — `\| no `schema` key \| not checked \| not checked \|` as row one of a three-row table with no Links column; :21 — `Links and derived counts are checked in all three of the rows the lint touches. A link resolving nowhere is broken in any generation, and the migrator computes the counts it writes rather than guessing them.` Compare lint-scope.sh:441-443, which C-8 already enumerates: `Links and counts stay on either way: a link that resolves nowhere is broken in any generation, and the migrator computes the counts it writes.` The two passages are the same sentence in two registers. Repair touches C-8's enumeration and check (five → six), T-003's title, its excerpt (a sixth section), and its C-8 check line. No change to C-6 — machine-contracts.md is already allowlisted. |
| sweep | info | Answering item 2 explicitly, so the record says we looked rather than reading later as though we did not. My r0 grep was insufficient and the orchestrator's suspicion was correct: it matched two phrasings of the claim, and the sixth location above makes the same claim in the inverse direction ("checked in all three of the rows" rather than "never flags them"), so phrase-matching could not have found it. The broader sweep did. Method this round: `git ls-files` (739 tracked files), filtered to text formats and excluding `tests/fixtures/` and `.agent-guild/state/archive/`, matched case-insensitively on `v1\|schema key`; then, separately and more usefully, a full read of every sentence containing "lint" across `inbox-to-memory/` and `_maintenance/inbox-to-memory/`. The second sweep is what found `machine-contracts.md:21`, because it keys off the lint's own vocabulary rather than off how the claim happens to be phrased. Seventeen candidate locations were read in context and cleared. Beyond the six, I am now reasonably confident there is no seventh — but the confidence rests on the second method, not the first. | Cleared, each read in full context: references/retrieval-funnel.md:7, :39, :44, :50, :60, :69, :102 (all claims about which greps match which generation, none about the lint); references/scope-decisions.md:55 ("v1" there means the skill's own version, an unrelated sense); README.md:56; assets/claude-md/notes.template.md:97 ("They stay legal, nothing rewrites them" — a claim about the migrator, true after the fix; the closest miss, and it is user-facing scaffold); assets/claude-md/_memory.template.md:28 (same shape, records); scripts/migrate-scope.sh:17-18 ("`body_schema: 1` … is what stops the lint from holding a note written in 2025 to a grammar that did not exist yet" — body grammar only, still true); references/migration.md:35, :103, :140, :144; SKILL.md:172, :178, :180, :363, :388, :402, :477; machine-contracts.md:19, :65, :109, :119, :138; _maintenance/inbox-to-memory/EVALS.md:14, :103. The `databricks-api/**` hits are API version strings (`/2.0/`, `v1` endpoints), unrelated to this skill. |
| serialization | info | Answering item 1: T-001 → T-002 → T-003 is sufficient, not merely sequential, and the specific hazard you named does not exist. `require_text` is presence-based over the whole file rather than line-anchored, so a doc edit can only break it by deleting a pinned string — editing a sentence in place cannot, however close the neighbors sit. Re-derived: all 22 machine-contracts strings resolve, the nearest to the sentence T-003 rewrites is `body_schema` at :11, four lines below :7, and none of the 22 is drawn from :7. SKILL.md carries 27 assertions; the nearest pinned lines to :428 are :399 and :457, so :428 is unpinned with roughly thirty lines of margin either side. And the clean negative you asked for: `tests/file-issue-smoke.sh` and `tests/handoff-smoke.sh` contain zero references to `inbox-to-memory`, so T-003 running only the one suite under C-10 costs nothing — verified, not assumed. | 22 strings resolved against inbox-to-memory/references/machine-contracts.md; earliest occurrences at :11 (`body_schema`), :19 (`A body-shape check therefore keys off …`), :29 (`first 20 lines`), :69-71 (the token rows). 27 `require_text`/`refute_text` calls against inbox-to-memory/SKILL.md resolved to :20, :53, :62, :67, :108, :124, :166, :175, :186, :310, :314, :315, :317, :323, :344, :347, :375, :380, :399, :457, :458, :459 plus four variable-interpolated. `grep -n 'inbox-to-memory' tests/file-issue-smoke.sh tests/handoff-smoke.sh` → no match. |
| T-003 | major | Two assertion surfaces T-003's worker is not told about, both inside files it edits. First, the suite does assert against `references/migration.md`: `require_file` at :602 and `refute_text … "is not implemented here"` at :610. That is a refutation, so the hazard runs opposite to the one T-003's caution describes — the worker breaks it by *introducing* that phrase while rewriting the "Why Migrate at All" section, not by deleting a pinned string. A rewrite explaining that link checking is not a reason to migrate is one careless clause away from "link checking is not implemented here." Second, the suite reads `lint-scope.sh`'s own source text: `require_text "$lint" "$migrator_note_order"` and `"$migrator_record_order"` at :618-619 pin the two key-order strings that live at lint-scope.sh:20-21, eight lines below the `:10-12` comment block T-003 rewrites, and `bash -n "$lint"` at :107 syntax-checks the script. Both are safe as long as the worker edits in place, which is the point — the caution at T-003.md:85 currently names only machine-contracts.md and tells the worker the margin is thin there, which is the one file where it is not. | tests/inbox-to-memory-smoke.sh:602 `require_file inbox-to-memory/references/migration.md`; :610 `refute_text inbox-to-memory/references/migration.md "is not implemented here"`, with the reason at :604-606 ("the migration.md line they replaced used to say Tier 2 wasn't implemented at all"); :11 `lint=inbox-to-memory/scripts/lint-scope.sh`; :107 `bash -n "$lint"`; :618-619 against lint-scope.sh:20-21 (`NOTE_KEY_ORDER=`, `RECORD_KEY_ORDER=`). T-003.md:85 names machine-contracts.md only. |
| T-003 | minor | An unowned decision sitting inside a file T-003 edits. `machine-contracts.md:140-154`, the "What the Lint Checks" table, lists 9 checks; the lint emits 17 distinct failure codes, and `link-broken` is one of the 8 the table omits. The gap is pre-existing and this job does not widen it. But T-003's worker is told that every location must "still say which checks a v1 file receives" while editing that very document, and adding a `link-broken` row is a natural move, legal under C-6, and plausibly rewarded by C-10. Decide it in the task rather than discovering it in a diff — either direction is defensible, and an unstated one costs a round. | inbox-to-memory/references/machine-contracts.md:144-154 lists frontmatter-fences, frontmatter-parses, frontmatter-budget, frontmatter-single-line, frontmatter-known-keys, frontmatter-key-order, frontmatter-key-domain, token-grammar, contradiction-fields. The lint's actual codes: those nine plus anchor-form (lint-scope.sh:359), derived-counts, link-broken, open-question-resolution, open-question-slug, tension-deferred-pairing, tension-fields, decision-fields. |
| preamble | minor | The constitution's own preamble carries the stale count the rest of the document just shed. constitution.md:3 still reads "Four sentences across three files say the opposite happens," while C-6's heading says five files, C-8's text enumerates five locations, and all three tasks were amended to match. This is the same class of defect as the two you caught yourself while repairing — a count inherited rather than derived — sitting in the first paragraph a reader meets. With the finding above it becomes six sentences across four files. | constitution.md:3 against constitution.md:124 ("C-6: the diff touches five files and no others") and constitution.md:140 ("Five locations state which checks a v1 file receives"). |
| T-001 | minor | One artifact now has two names across two documents. T-001's check_method calls C-3's injection block "the seven-token injection"; C-7's amended text calls the same block "the same six-way injection C-3 uses." Both are defensible readings — the block injects seven lines, six of which are tokens and the seventh a bare `(raw: line 12)` anchor — but a checker reconciling the two has to work out that they refer to the same thing, and the discrepancy reads like the stale counts this round already retired twice. | T-001.md:17 "C-3: both of its check blocks. The seven-token injection on an old-only copy…" against constitution.md:133 "…the same six-way injection C-3 uses, so that all six body-grammar checks have something to catch" and the block at constitution.md:64, which contains seven injected lines. |
| repairs | info | Every r0 repair landed as described, verified against the files rather than the summary. C-6 carries `inbox-to-memory/references/migration.md` in its heading count, its text, and its command, and all three tasks' C-6 check_method lines carry the new path. T-003's deps are `[T-001, T-002]`, giving a strict chain. T-002's check_method carries the worktree recipe verbatim, the explicit prohibition on `git stash` and on reverting lint-scope.sh with the reason attached, and an evidence bar that a bare failure will not satisfy. T-001 has the symmetric ownership paragraph, including the admission that C-6 will not enforce it, plus the `git status --porcelain` confirmation in its check. T-003's counts read 14 call sites / 22 distinct strings / :309 to :1237 in both the excerpt and the C-10 line. T-002's helper list names all eight with definition lines and points at the `require_line "$out" "failures: 0"` idiom. T-002's C-7 check states the two-scope ruling. T-001's citations are corrected. The two defects you found yourself are both properly repaired, and the six-check non-goal now says outright that the issue's count is wrong rather than silently correcting it, which is the right call for a document someone will diff against the issue later. | constitution.md:124-127, :133-134, :140-141, :167; T-001.md:24-29, :45, :56, :61, :65; T-002.md:14-17, :20-22, :28-38, :61; T-003.md:11-12, :22-24, :28, :69-73, :85. Citation spot-checks against the shipped lint: pass one :414-432, quoted block :421-426, pass two :435-462, RECURRING :471-475, gate :444-451 — all correct as now written. |

## Diagnosis

- **C-8** (blocker): The enumeration is still incomplete at five. There is a sixth location, it lives in a file already in scope, and it is the strongest statement of the v1 contract in the repository: `inbox-to-memory/references/machine-contracts.md:13-21` — the generation table plus the sentence under it, "Links and derived counts are checked in all three of the rows the lint touches." Three things are wrong with leaving it out. (1) That sentence is the doc twin of the `:441-443` comment: pre-fix it is FALSE for row one (`no schema key`), because links are exactly what the lint does not check there, so it states the intended contract the code violates — the same shape the issue titles "The Comment Is the Contract." T-001 makes its links half true for the first time. (2) Its "derived counts" half is false for row one and always was, which is verbatim the "and counts" defect C-8 already names at `:441-443` and requires be fixed rather than preserved. Repairing one and not the other leaves the same error standing in the document the comment points at. (3) `:7` and `:21` contradict each other in that document today, before any change — one says the lint never flags a v1 file, the other says links are checked in all three rows — and T-003 is being asked to repair `:7` without being told `:21` exists. A worker who rewrites `:7` to say only links are checked, then leaves `:21` claiming links *and derived counts* are checked in all three rows, ships a document internally inconsistent about counts and passes every check as written. The table itself is part of the location: it has columns for Frontmatter contract and Body grammar and none for Links, so after the fix the document's most authoritative artifact about generations still answers "what does the lint do to a v1 file" with "nothing."
  evidence: inbox-to-memory/references/machine-contracts.md:13-17 — `| no `schema` key | not checked | not checked |` as row one of a three-row table with no Links column; :21 — `Links and derived counts are checked in all three of the rows the lint touches. A link resolving nowhere is broken in any generation, and the migrator computes the counts it writes rather than guessing them.` Compare lint-scope.sh:441-443, which C-8 already enumerates: `Links and counts stay on either way: a link that resolves nowhere is broken in any generation, and the migrator computes the counts it writes.` The two passages are the same sentence in two registers. Repair touches C-8's enumeration and check (five → six), T-003's title, its excerpt (a sixth section), and its C-8 check line. No change to C-6 — machine-contracts.md is already allowlisted.
- **sweep** (info): Answering item 2 explicitly, so the record says we looked rather than reading later as though we did not. My r0 grep was insufficient and the orchestrator's suspicion was correct: it matched two phrasings of the claim, and the sixth location above makes the same claim in the inverse direction ("checked in all three of the rows" rather than "never flags them"), so phrase-matching could not have found it. The broader sweep did. Method this round: `git ls-files` (739 tracked files), filtered to text formats and excluding `tests/fixtures/` and `.agent-guild/state/archive/`, matched case-insensitively on `v1|schema key`; then, separately and more usefully, a full read of every sentence containing "lint" across `inbox-to-memory/` and `_maintenance/inbox-to-memory/`. The second sweep is what found `machine-contracts.md:21`, because it keys off the lint's own vocabulary rather than off how the claim happens to be phrased. Seventeen candidate locations were read in context and cleared. Beyond the six, I am now reasonably confident there is no seventh — but the confidence rests on the second method, not the first.
  evidence: Cleared, each read in full context: references/retrieval-funnel.md:7, :39, :44, :50, :60, :69, :102 (all claims about which greps match which generation, none about the lint); references/scope-decisions.md:55 ("v1" there means the skill's own version, an unrelated sense); README.md:56; assets/claude-md/notes.template.md:97 ("They stay legal, nothing rewrites them" — a claim about the migrator, true after the fix; the closest miss, and it is user-facing scaffold); assets/claude-md/_memory.template.md:28 (same shape, records); scripts/migrate-scope.sh:17-18 ("`body_schema: 1` … is what stops the lint from holding a note written in 2025 to a grammar that did not exist yet" — body grammar only, still true); references/migration.md:35, :103, :140, :144; SKILL.md:172, :178, :180, :363, :388, :402, :477; machine-contracts.md:19, :65, :109, :119, :138; _maintenance/inbox-to-memory/EVALS.md:14, :103. The `databricks-api/**` hits are API version strings (`/2.0/`, `v1` endpoints), unrelated to this skill.
- **serialization** (info): Answering item 1: T-001 → T-002 → T-003 is sufficient, not merely sequential, and the specific hazard you named does not exist. `require_text` is presence-based over the whole file rather than line-anchored, so a doc edit can only break it by deleting a pinned string — editing a sentence in place cannot, however close the neighbors sit. Re-derived: all 22 machine-contracts strings resolve, the nearest to the sentence T-003 rewrites is `body_schema` at :11, four lines below :7, and none of the 22 is drawn from :7. SKILL.md carries 27 assertions; the nearest pinned lines to :428 are :399 and :457, so :428 is unpinned with roughly thirty lines of margin either side. And the clean negative you asked for: `tests/file-issue-smoke.sh` and `tests/handoff-smoke.sh` contain zero references to `inbox-to-memory`, so T-003 running only the one suite under C-10 costs nothing — verified, not assumed.
  evidence: 22 strings resolved against inbox-to-memory/references/machine-contracts.md; earliest occurrences at :11 (`body_schema`), :19 (`A body-shape check therefore keys off …`), :29 (`first 20 lines`), :69-71 (the token rows). 27 `require_text`/`refute_text` calls against inbox-to-memory/SKILL.md resolved to :20, :53, :62, :67, :108, :124, :166, :175, :186, :310, :314, :315, :317, :323, :344, :347, :375, :380, :399, :457, :458, :459 plus four variable-interpolated. `grep -n 'inbox-to-memory' tests/file-issue-smoke.sh tests/handoff-smoke.sh` → no match.
- **T-003** (major): Two assertion surfaces T-003's worker is not told about, both inside files it edits. First, the suite does assert against `references/migration.md`: `require_file` at :602 and `refute_text … "is not implemented here"` at :610. That is a refutation, so the hazard runs opposite to the one T-003's caution describes — the worker breaks it by *introducing* that phrase while rewriting the "Why Migrate at All" section, not by deleting a pinned string. A rewrite explaining that link checking is not a reason to migrate is one careless clause away from "link checking is not implemented here." Second, the suite reads `lint-scope.sh`'s own source text: `require_text "$lint" "$migrator_note_order"` and `"$migrator_record_order"` at :618-619 pin the two key-order strings that live at lint-scope.sh:20-21, eight lines below the `:10-12` comment block T-003 rewrites, and `bash -n "$lint"` at :107 syntax-checks the script. Both are safe as long as the worker edits in place, which is the point — the caution at T-003.md:85 currently names only machine-contracts.md and tells the worker the margin is thin there, which is the one file where it is not.
  evidence: tests/inbox-to-memory-smoke.sh:602 `require_file inbox-to-memory/references/migration.md`; :610 `refute_text inbox-to-memory/references/migration.md "is not implemented here"`, with the reason at :604-606 ("the migration.md line they replaced used to say Tier 2 wasn't implemented at all"); :11 `lint=inbox-to-memory/scripts/lint-scope.sh`; :107 `bash -n "$lint"`; :618-619 against lint-scope.sh:20-21 (`NOTE_KEY_ORDER=`, `RECORD_KEY_ORDER=`). T-003.md:85 names machine-contracts.md only.
- **T-003** (minor): An unowned decision sitting inside a file T-003 edits. `machine-contracts.md:140-154`, the "What the Lint Checks" table, lists 9 checks; the lint emits 17 distinct failure codes, and `link-broken` is one of the 8 the table omits. The gap is pre-existing and this job does not widen it. But T-003's worker is told that every location must "still say which checks a v1 file receives" while editing that very document, and adding a `link-broken` row is a natural move, legal under C-6, and plausibly rewarded by C-10. Decide it in the task rather than discovering it in a diff — either direction is defensible, and an unstated one costs a round.
  evidence: inbox-to-memory/references/machine-contracts.md:144-154 lists frontmatter-fences, frontmatter-parses, frontmatter-budget, frontmatter-single-line, frontmatter-known-keys, frontmatter-key-order, frontmatter-key-domain, token-grammar, contradiction-fields. The lint's actual codes: those nine plus anchor-form (lint-scope.sh:359), derived-counts, link-broken, open-question-resolution, open-question-slug, tension-deferred-pairing, tension-fields, decision-fields.
- **preamble** (minor): The constitution's own preamble carries the stale count the rest of the document just shed. constitution.md:3 still reads "Four sentences across three files say the opposite happens," while C-6's heading says five files, C-8's text enumerates five locations, and all three tasks were amended to match. This is the same class of defect as the two you caught yourself while repairing — a count inherited rather than derived — sitting in the first paragraph a reader meets. With the finding above it becomes six sentences across four files.
  evidence: constitution.md:3 against constitution.md:124 ("C-6: the diff touches five files and no others") and constitution.md:140 ("Five locations state which checks a v1 file receives").
- **T-001** (minor): One artifact now has two names across two documents. T-001's check_method calls C-3's injection block "the seven-token injection"; C-7's amended text calls the same block "the same six-way injection C-3 uses." Both are defensible readings — the block injects seven lines, six of which are tokens and the seventh a bare `(raw: line 12)` anchor — but a checker reconciling the two has to work out that they refer to the same thing, and the discrepancy reads like the stale counts this round already retired twice.
  evidence: T-001.md:17 "C-3: both of its check blocks. The seven-token injection on an old-only copy…" against constitution.md:133 "…the same six-way injection C-3 uses, so that all six body-grammar checks have something to catch" and the block at constitution.md:64, which contains seven injected lines.
- **repairs** (info): Every r0 repair landed as described, verified against the files rather than the summary. C-6 carries `inbox-to-memory/references/migration.md` in its heading count, its text, and its command, and all three tasks' C-6 check_method lines carry the new path. T-003's deps are `[T-001, T-002]`, giving a strict chain. T-002's check_method carries the worktree recipe verbatim, the explicit prohibition on `git stash` and on reverting lint-scope.sh with the reason attached, and an evidence bar that a bare failure will not satisfy. T-001 has the symmetric ownership paragraph, including the admission that C-6 will not enforce it, plus the `git status --porcelain` confirmation in its check. T-003's counts read 14 call sites / 22 distinct strings / :309 to :1237 in both the excerpt and the C-10 line. T-002's helper list names all eight with definition lines and points at the `require_line "$out" "failures: 0"` idiom. T-002's C-7 check states the two-scope ruling. T-001's citations are corrected. The two defects you found yourself are both properly repaired, and the six-check non-goal now says outright that the issue's count is wrong rather than silently correcting it, which is the right call for a document someone will diff against the issue later.
  evidence: constitution.md:124-127, :133-134, :140-141, :167; T-001.md:24-29, :45, :56, :61, :65; T-002.md:14-17, :20-22, :28-38, :61; T-003.md:11-12, :22-24, :28, :69-73, :85. Citation spot-checks against the shipped lint: pass one :414-432, quoted block :421-426, pass two :435-462, RECURRING :471-475, gate :444-451 — all correct as now written.
