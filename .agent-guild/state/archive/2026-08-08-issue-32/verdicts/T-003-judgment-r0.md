---
task: T-003
checker: checker-judgment
vendor: anthropic
model: claude-opus-5[1m]
verdict: PASS
checked_at: 2026-08-08T00:00:00Z
duration_ms: None
cost_usd: None
---

<!-- GENERATED FILE—do not hand-edit. Rendered by render-verdict.py
from the verdict JSON, the record of record. Edit the JSON and
re-render instead. -->

## Per-clause results

| clause | severity | description | evidence |
| ------ | -------- | ------------ | -------- |
| C-6 | blocker | The working tree's changes are confined to the five allowlisted paths; the scope script exits 0 and git status names nothing else. | $ python3 .agent-guild/scripts/check-diff-scope.py inbox-to-memory/scripts/lint-scope.sh inbox-to-memory/references/machine-contracts.md inbox-to-memory/references/migration.md inbox-to-memory/SKILL.md tests/inbox-to-memory-smoke.sh OK: 5 path(s) in scope scope exit=0 $ git status --porcelain M inbox-to-memory/SKILL.md M inbox-to-memory/references/machine-contracts.md M inbox-to-memory/references/migration.md M inbox-to-memory/scripts/lint-scope.sh M tests/inbox-to-memory-smoke.sh (verify-migration.sh, named as read-only material in the task, is unmodified) |
| C-8 | blocker | Location 1, lint-scope.sh:10-15 (the file header). Shipped: "The only check here that touches a v1 file is the link check. Files without a `schema` key predate the contract, they stay legal indefinitely... Links stay on because a target that resolves to no file in scope is broken whichever generation wrote it, and verify-migration.sh already runs that same resolution over the whole scope without ever reading `schema`." Decided by lint-scope.sh:484-488, the v1 pass, whose body is `check_links` and nothing else; the named line makes the sentence true, and the claim is corrected rather than deleted (it still states which check a v1 file receives). | inbox-to-memory/scripts/lint-scope.sh:10-15 (claim) decided by :484-488: index=0 for file in ${v1_files[@]+"${v1_files[@]}"}; do index=$((index + 1)) current_file="$file" check_links "${v1_body_for_index[$index]}" done and by :427-434, where a file with no schema key is pushed onto v1_files and `continue`s out of pass two, so none of check_frontmatter (:449), the six body-grammar checks (:458-465), or check_counts (:468) can reach it. |
| C-8 | blocker | Location 1's factual claim about precedent is true. "verify-migration.sh already runs that same resolution over the whole scope without ever reading `schema`" holds: verify-migration.sh:129-165 sweeps every .md in the since-ref git tree, resolves by filename then by trailing ten-character id — the same order check_links uses at :379-381 — and the string `schema` appears zero times in the file. | $ /usr/bin/grep -c 'schema' inbox-to-memory/scripts/verify-migration.sh 0 inbox-to-memory/scripts/verify-migration.sh:153-163: by_name="$(find "$scope" -name "$target.md" -print -quit)" if [[ -n "$by_name" ]]; then continue; fi id="${target: -10}" by_id="$(find "$scope" -name "*$id*.md" -print -quit)" against inbox-to-memory/scripts/lint-scope.sh:379-381: find "$scope" -name "$target.md" -print -quit \| grep -q . && continue local id="${target: -10}" find "$scope" -name "*$id*.md" -print -quit \| grep -q . && continue |
| C-8 | blocker | Location 2, lint-scope.sh:451-457 in pass two (the comment the issue was filed against; it has shifted from :441-443). Shipped: "Links stay on either way, and they run on files with no `schema` key too, in the pass below: a link that resolves nowhere is broken in any generation. Counts do not carry across the generation line the way links do. They run here because the migrator computed these four keys when it rewrote the frontmatter, and a file with no `schema` key carries none of them for a count to be compared against." The links half is decided by :487 and is true; the "and counts" half was fixed rather than preserved — the comment now says counts do not cross the generation line, decided by :468 (check_counts called only inside the v2 loop) and :399-401 (an absent key is a derived-counts failure, so there is nothing on a v1 note to compare). | inbox-to-memory/scripts/lint-scope.sh:451-457 (claim). Deciding lines: :468 grep -q '^memory_type:' "$file" \|\| check_counts "$file" "$body" # inside `for file in ${v2_files[@]...}` at :445 :389 for key in open_questions resolved_questions deferred_tensions unpromoted_candidates; do :400-401 if [[ "$stated" == "absent" ]]; then fail derived-counts "\\`$key\\` is missing; v2 notes carry all four even at zero" The pre-edit text "Links and counts stay on either way" no longer appears anywhere in the file. |
| C-8 | blocker | Location 3, references/machine-contracts.md:7. Shipped: "...the only thing the lint checks on them is whether their wiki links resolve, since a link pointing at no file in scope is broken whichever generation wrote it. The frontmatter contract, the token grammar, the anchor rule, and the four derived counts all stop at the generation line." Decided by lint-scope.sh:484-488 for the links half and by :449, :458-465, :468 for the four exclusions, all of which sit inside the v2-only loop. True, and the claim is stated rather than dropped. | inbox-to-memory/references/machine-contracts.md:7 (claim). Empirically re-derived: a v1 note injected with an unregistered token, an unopened question slug, a malformed tension, a bare `(raw:` anchor, and a broken link produced exactly one failure — FAIL .../notes/2025-11-04-atlas-scoping-call-3iMu15QJ_x.md: link-broken: `this-target-does-not-exist-AAAAAAAAAA` resolves neither by name nor by id `AAAAAAAAAA` v1 files: 4 / v2 files: 0 / failures: 1 / exit=1 No token-grammar, anchor-form, frontmatter, or derived-counts line appeared, despite the v1 note carrying none of the four count keys. |
| C-8 | blocker | Location 4, SKILL.md:428. Shipped: "Files with no `schema` key are v1. They stay legal forever, and a scope holding both generations is a supported state rather than an unfinished migration. The lint checks one thing on them, whether their wiki links resolve, because a link pointing at no file in scope is broken whichever generation wrote it." Decided by lint-scope.sh:484-488; "the lint never flags them" is gone and the narrower true claim replaces it. | inbox-to-memory/SKILL.md:428 (claim) decided by inbox-to-memory/scripts/lint-scope.sh:484-488 (the v1 pass calls check_links and nothing else) and :427-434 (v1 files `continue` before pass two). The out-of-scope neighbours SKILL.md:178 and :363 are unmodified per `git diff` (SKILL.md shows +1/-1, at :428 only). |
| C-8 | blocker | Location 5, references/migration.md:11. Shipped: "V1 files are legal forever, and the lint's one check on them—whether their wiki links resolve—is one they pass as readily as a v2 file does, so nothing forces this." Decided by lint-scope.sh:487 and :466, which call the identical check_links function on v1 and v2 bodies, so a resolving v1 link passes exactly as a v2 one does. The "nothing forces this" premise the rest of the section builds on is preserved intact. | inbox-to-memory/references/migration.md:11 (claim). Deciding lines: lint-scope.sh:466 `check_links "$body"` (v2 loop) and :487 `check_links "${v1_body_for_index[$index]}"` (v1 loop) — the same function, so generation has no bearing on the outcome. The inverted smoke assertion at tests/inbox-to-memory-smoke.sh:610 is not tripped: `/usr/bin/grep -n 'is not implemented here' inbox-to-memory/references/migration.md` exits 1 (phrase absent). |
| C-8 | blocker | Location 6, first limb — the sentence at references/machine-contracts.md:21. Shipped: "Links are the one column that runs the whole way down, because a link resolving nowhere is broken in any generation... The derived counts are part of the frontmatter contract rather than a fourth column of their own. The migrator computes those four keys when it rewrites a file rather than guessing them, so they can be checked on any note whose frontmatter contract is checked, and a file carrying no `schema` key carries nothing for them to be compared against." The old "Links and derived counts are checked in all three of the rows" is gone: the counts half was fixed, not preserved. Decided by :466 (links, unconditional in the v2 loop, so it covers rows two and three including body_schema: 1), :487 (links, row one), and :468 with :389 and :400-401 (counts, v2 notes only, four keys, absent is a failure). | inbox-to-memory/references/machine-contracts.md:21 (claim). Deciding lines in inbox-to-memory/scripts/lint-scope.sh: :466 check_links "$body" # outside the `if ! has_v1_body` guard, so row 3 gets it too :487 check_links "${v1_body_for_index[$index]}" # row 1 :467-468 # Records carry no body tokens and no counts; the four keys are a note contract. grep -q '^memory_type:' "$file" \|\| check_counts "$file" "$body" The sentence's hedge "any note whose frontmatter contract is checked" is precise against :468, which exempts records. The migrator claim checks out at inbox-to-memory/scripts/migrate-scope.sh:358-361, which computes open_questions, deferred_tensions, and unpromoted_candidates from body tokens. |
| C-8 | blocker | Location 6, second limb — the reader test on the generation table at references/machine-contracts.md:13-17, applied with nothing else in view. A fourth column, Links, was added, and row one now reads `no schema key \| not checked \| not checked \| checked`. Reading the table alone, the answer to "what does the lint check on a file carrying no schema key" is: its wiki links, and neither the frontmatter contract nor the body grammar. That answer is available from the table by itself, so the location passes; the alignment of the pre-existing rows was normalized in the same edit but no row's content moved. | inbox-to-memory/references/machine-contracts.md:13-17 as shipped: \| Carries \| Frontmatter contract \| Body grammar \| Links \| \| -------------------------------- \| -------------------- \| ------------ \| ------- \| \| no `schema` key \| not checked \| not checked \| checked \| \| `schema: 2` \| checked \| checked \| checked \| \| `schema: 2` and `body_schema: 1` \| checked \| not checked \| checked \| Each Links cell is true against lint-scope.sh:487 (row 1) and :466 (rows 2 and 3, the call sitting outside the has_v1_body guard at :458-465). The forbidden drive-by row for `link-broken` in the separate "What the Lint Checks" table was not added: that table is unchanged in `git diff`, which shows machine-contracts.md touching only :7, :13-17, and :21. |
| C-8 | blocker | The internal contradiction C-8 calls out is resolved: :7 and :21 now agree about counts. :7 says the four derived counts stop at the generation line; :21 says the derived counts belong to the frontmatter contract and a file with no `schema` key carries nothing for them to be compared against. Both are true of the code, and neither claims counts are checked on a v1 file. | inbox-to-memory/references/machine-contracts.md:7 — "The frontmatter contract, the token grammar, the anchor rule, and the four derived counts all stop at the generation line." inbox-to-memory/references/machine-contracts.md:21 — "...a file carrying no `schema` key carries nothing for them to be compared against." Both decided by lint-scope.sh:468 (check_counts reached only from the v2 loop at :445) and :389 (the four keys: open_questions, resolved_questions, deferred_tensions, unpromoted_candidates). |
| C-10 | major | Every rewritten passage carries a reason alongside its rule, in the register of the file around it, and no passage states a bare rule. No rewritten passage introduces a bolded inline header with a colon, and no em dash is wrapped in spaces — the one em dash pair added, in migration.md:11, is chained directly to the text on both sides. | Reasons, one per passage: lint-scope.sh:13-15 "Links stay on because a target that resolves to no file in scope is broken whichever generation wrote it, and verify-migration.sh already runs that same resolution..."; lint-scope.sh:454-457 "Counts do not carry across the generation line the way links do. They run here because the migrator computed these four keys..."; machine-contracts.md:7 "since a link pointing at no file in scope is broken whichever generation wrote it"; machine-contracts.md:21 "because a link resolving nowhere is broken in any generation. That is not a rule invented for the lint:..."; SKILL.md:428 "because a link pointing at no file in scope is broken whichever generation wrote it"; migration.md:11 "is one they pass as readily as a v2 file does, so nothing forces this". Typography scan over the added lines: $ git diff -U0 -- inbox-to-memory/ \| /usr/bin/grep '^+' \| /usr/bin/grep ' — \\\|\\*\\*[^*]*:\\*\\*' (no output; exit 1) The only added em dashes are migration.md:11 "the lint's one check on them—whether their wiki links resolve—is one they pass". |
| C-10 | major | The smoke suite stays green after the prose edits and lint-scope.sh keeps mode 0755, so the executable assertion at tests/inbox-to-memory-smoke.sh:103 and the pinned-string assertions across all four edited files still hold, and the comment rewrite did not break shell syntax. | $ bash tests/inbox-to-memory-smoke.sh inbox-to-memory smoke: ok smoke exit=0 $ stat -f '%A %N' inbox-to-memory/scripts/lint-scope.sh 755 inbox-to-memory/scripts/lint-scope.sh $ bash -n inbox-to-memory/scripts/lint-scope.sh; echo $? 0 The pinned constants NOTE_KEY_ORDER and RECORD_KEY_ORDER at lint-scope.sh:23-24 are unmodified in `git diff` (the header hunk is +6/-3 and stops at :15). |
