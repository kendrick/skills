# T-004 self-report

## What I did

Added the `# last_confirmed write-through (#27)` section to
`tests/inbox-to-memory-smoke.sh`, inserted between the v1-link-checking
block and the fixture guard's comment/comparison (now lines ~1298-1425,
guard snapshot at :587, comparison at :1425 — the file grew, so absolute
line numbers moved; I re-derived them from the file itself rather than
trusting the task's pre-T-002 numbers, as instructed).

Also reworded the guard's own comment and failure message (previously
"a migration test" / "a migration test wrote to a checked-in fixture") to
say "migration or write-through test" / "migration or stamping test",
since the guard now covers both.

Did not touch `inbox-to-memory/scripts/stamp-confirmed.sh` — read it and
ran it directly against the real fixtures to confirm behavior and pin
exact output strings before writing assertions against it.

## The five cases, each on its own `mktemp -d` copy of `mixed/`

1. **Headline** (`wt_headline`): one invocation naming both the v1 record
   (`freeze-window-owned-by-ops-ocPwdpeY0a.md`) and the v2 record
   (`atlas-region-topology-mPmy8XBe5H.md`). Asserts the v1 file's shasum is
   unchanged (byte comparison, not output text) and the v2 file's
   `last_confirmed` line reads the note's date.
2. **Backward and equal** (`wt_backward`): two sequential invocations on one
   copy, with a 2025-12-02 note (older) and a 2026-02-10 note (exactly
   equal to the record's current value). Asserts shasum held across both.
3. **Cross-group dedupe** (`wt_dedupe`): one invocation, two `--note` groups
   naming the same record. Asserts exactly one output line names the record
   and it reads `stamped: 1  skipped: 0`.
4. **Status gate** (`wt_status`): record mutated to `status: proposed` via
   the same `python3`/`open().read()`-then-`write()` pattern the
   constitution's own checks use (read-before-write, to avoid the
   zero-byte-truncation bug the constitution calls out). Asserts shasum
   held and the skip line names the status.
5. **Key insertion** (`wt_keyins`): `last_confirmed` stripped from the v2
   record, then asserts the inserted line lands exactly between `date:` and
   `source_refs:` by line number, and `lint-scope.sh` over the scope still
   reports `failures: 0`.

## Verification run

- `bash -n tests/inbox-to-memory-smoke.sh` — syntax clean.
- `bash tests/inbox-to-memory-smoke.sh` — exits 0 (`inbox-to-memory smoke: ok`).
- C-13's placement block, run by hand: `snap=587 banner=1298 bind=1307
  first-use=1320 last-use=1408 cmp=1425` → prints `inside the guard`.
- C-13's saboteur block, run by hand: installed the saboteur that delegates
  to the real script and additionally stamps v1 records, ran the suite
  under it — `sabotaged exit=1` (nonzero, as required). Restored the real
  script and confirmed `restored` via shasum match. `git status --porcelain
  tests/fixtures/` came back empty (no fixture damage left behind).

## Judgment calls

- Variable naming: used a `wt_` prefix (write-through) for all locals in
  the section, since nothing in the file already claimed that prefix
  (checked via grep before writing).
- Bound the stamper invocations through `bash "$stamper" ...`, matching the
  suite's existing `bash "$migrator" ...` / `bash "$lint" ...` house style,
  rather than relying on the script's own executable bit.
- For the reworded guard comment/message, changed both the prose comment
  block (:1296-1298 pre-edit) and the `echo` failure string inside the
  guard's `[[ ... ]] || { ... }`, since the clause text explicitly calls out
  the failure message as something "the worker reworks... to mention
  stamping."
- Left the `require_text inbox-to-memory/SKILL.md "scripts/stamp-confirmed.sh"`
  line at :301 untouched, per the task's explicit out-of-scope note.
