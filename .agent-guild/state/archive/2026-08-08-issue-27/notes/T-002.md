# T-002 self-report

Four sections of `inbox-to-memory/SKILL.md` and one assertion in `tests/inbox-to-memory-smoke.sh`. Suites: all three exit 0. C-12's inventory diff is empty. `check-diff-scope.py` reports 21 paths in scope.

## What changed

**Phase 2.5 (C-8).** Three touches, no new numbered step. A paragraph after step 5 says the body read answers two questions instead of one, contradiction or restatement, and names a restatement a confirmation. The budget paragraph keeps its pinned opening sentence verbatim and gains a closing one saying confirmations come out of that same budget, with no second pass and no sixth read. The `accepted`-only paragraph became "produce flags, and only they can be confirmed," and its reasoning grew a clause for the agreeing direction: a `proposed` record asserts nothing when you agree with it either. A closing paragraph mirrors the contradiction paragraph above it, contradiction goes to phase 5, confirmation gets no flag and never passes through it.

**The invocation (C-17).** A new `### Stamp Confirmations` between phase 4 and phase 5, showing the call in `<skill-path>` form. Bolded lead-ins carry the two derived rules: one call for the whole queue, and no call at all on a run that found nothing. The placement paragraph says in plain words that sitting there is a convention, that the close of phase 3 would serve as well because phase 4 only disposes sources and opens no gate, and that the batch rule is the part carrying real weight. No invented justification.

**Phase 6 (C-9).** Two bullets appended to the by-hand list, in the declarative form the existing bullets use. Stamped records with their new dates, read off the `stamped:` lines; declined records with the reason, read off the `skipped:` lines. The second bullet states its own scope, what the stamper turned down rather than everything phase 2.5 opened, and says why the v1 skips are the ones that matter.

**Operating Rules (C-10).** One bullet, placed after "Never auto-promote across scopes" rather than beside the VTT bullet.

## Taste calls

**The two exceptions.** The collision was two bullets four lines apart each calling itself the one sanctioned exception. I fixed it three ways at once and left the VTT bullet untouched. The new bullet names its rule inside the same sentence as its exception ("The gate on `_memory/` ... has one exception, and this is it"), so a reader never holds an unresolved "the one exception" in their head. It avoids the phrase "sanctioned exception" entirely, so only one bullet in the list uses those words. And putting it with the other memory-write rules rather than adjacent to VTT means the two never appear in the same eyeful. Every word of the pinned VTT string survives.

**No phase number.** The obvious move was `### Phase 4.5`, and I rejected it. The numbers are cited by name across scaffolds, phase 2.5's own paragraph makes a point of that, and one line of prose explains the absence: it is a call, not a phase of work.

**Where I did not go.** `SKILL.md:26`, `:138`, `:168`, and the auto-promote bullet are C-11 hits belonging to another task, so I left them even where my change makes them slightly less complete. Line 26's "the user reviews and approves between phase 4 and phase 5" is still true as written.

**Em dashes.** The file uses spaced em dashes everywhere, including in its headings, so I matched the document rather than my usual unspaced habit. Consistency inside one artifact beat the personal rule.

## Assertions

Added exactly one, `require_text inbox-to-memory/SKILL.md "scripts/stamp-confirmed.sh"`, directly under the `lint-scope.sh` one. The comment above it said "the lint," singular, and now covers both scripts. No existing assertion needed moving: all five pinned strings in my sections survive verbatim, which I got by writing around them rather than by rewording and chasing.

## Traps cleared

No new token, and no bracketed lowercase aside or markdown link with an all-lowercase label anywhere in the new prose, which is the shape C-12's extraction grep would read as a token. I ran that inventory diff myself rather than trusting the eye.
