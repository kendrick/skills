# T-003 self-report

## What I changed

Six edits across five files. Five come from C-11; one is the T-002 handdown.

- `inbox-to-memory/SKILL.md:26` — the Process Mode overview. Added the write-through to the sentence that says the user reviews and approves between phase 4 and phase 5, because Stamp Confirmations now sits in exactly that gap.
- `inbox-to-memory/SKILL.md:152` — the T-002 handdown, not C-11. Replaced the closing clause `so the same inbox produces a different record and a different report depending on which file got read first` with `so the same inbox writes that record once or twice and reports it differently depending on which file got read first`. The record half was false: the monotonic guard converges the stored date whichever order the inputs arrive in, so what actually varies is the write count and the report. Kept the sentence's shape and the rest of T-002's paragraph untouched.
- `inbox-to-memory/README.md:74` — the pre-ruled fail. Carries the exception now, and names the concrete symptom (`git status` showing modified memory files after a run you approved nothing in) plus the bound (no record created, nothing a record claims edited).
- `inbox-to-memory/references/migration.md:37` — narrowed `Any later date would assert a review that never happened` to `a review the migrator has no evidence of`, since a later date is now exactly what the stamper writes and it does assert a review that happened. Added a following paragraph naming process mode as what supplies that evidence, with the monotonic refusal derived from the same argument.
- `inbox-to-memory/references/machine-contracts.md` — the named document obligation. New bolded rule after the record/note distinction at `:45`, stating the full lifecycle in one place: migration initializes to the record's own `date`, the stamper advances it to the confirming input's date with no sign-off prompt and refuses anything not later, and a file with no `schema` key gains the key by neither route. The last part is written as a consequence of the generation table's first row, not as a new rule.
- `inbox-to-memory/assets/claude-md/_memory.template.md` — the other named document obligation. New paragraph directly under the Required Frontmatter block, which is where an agent reading this file hits `last_confirmed` at `:21` and asks who sets it. Placed there deliberately so it does not disturb the pre-ruled-passing sentence at `:65`.

## The census, re-derived

The clause's sweep returns 60-odd hits. I ran it, then ran a second sweep on write vocabulary the clause's predicate cannot see (`never writes`, `nothing is written`, `read-only`, `before writing`, `touch`, `modif`, `crystalliz`, `gated`) to answer the "question the category" instruction. The second sweep is what surfaced `scope-decisions.md:68`, `client.template.md:57`, and the two `assets/readme/` templates, none of which use approval or `last_confirmed` vocabulary. Old SKILL.md line numbers resolved against `git show 924f81f`.

The twelve, at current line numbers, with rulings:

| Location | Ruling |
| --- | --- |
| `SKILL.md:26` | **fixed** — mode overview; Q1 yes, and it now misdescribes the phase 4/5 gap specifically |
| `SKILL.md:162` (was `:138`) | pass — phase 5's own instruction, and Stamp Confirmations sits immediately above it |
| `SKILL.md:192` (was `:168`) | pass — `Never auto-promote` is precisely true; the write-through promotes nothing. Q2 no: SKILL.md accounts for the write in four other places |
| `SKILL.md:387` (was `:361`) | pass — one row of the migrator's change list, scoped to migrate mode |
| `SKILL.md:481` (was `:455`) | pass — about cross-scope moves, not about the gate |
| `README.md:74` | **fixed** — pre-ruled |
| `_memory.template.md:21` | **fixed at document level** — `:21` is a YAML line and cannot carry prose, so the account went into the paragraph below the block |
| `_memory.template.md:65` | pass unchanged — pre-ruled |
| `client.template.md:71` | pass — see below |
| `project.template.md:55` | pass — see below |
| `migration.md:37` | **fixed** |
| `retrieval-funnel.md:40` | pass — a grep-recipe comment about v1 absence, making no claim about writes. The write-through makes this query more meaningful rather than less: before it, `last_confirmed` was always the record's creation date, so a staleness query was really a "created before" query |

### The two scaffold CLAUDE.md templates

`client.template.md:71` and `project.template.md:55` both read "crystallized only after user sign-off," inside a bullet list titled Conventions Inherited From the Skill. I ruled both pass, and this is the call I'd most expect a checker to push on, so here is the reasoning.

Q1: they are creation-scoped in exactly the shape the constitution pre-rules as passing at `_memory.template.md:65`. The operative verb is "crystallized," and a reader takes it as a statement about how candidates become records rather than an inventory of every write.

Q2: both files explicitly delegate `_memory/` conventions to `_memory/CLAUDE.md` ("Directory-specific conventions live in ... [`_memory/CLAUDE.md`]"), and that file now carries the account. So neither is the account-bearing document for `_memory/`. That is a pointer in the shipped text, not an inference I made.

The clause names exactly two documents as owing more than the sentence test, and these are not among them. Adding a `last_confirmed` bullet to both would have been the over-mentioning the spec warns against.

## Candidates outside the twelve that I found and did not fix

Reported here so the checker can re-derive rather than re-discover.

- `references/scope-decisions.md:68` — "Content here is durable — once a record lands, it stays (with `status` transitions) rather than getting rewritten." The strongest category miss in the second sweep: it makes a durability claim about `_memory/` in neither census vocabulary. Passed because the parenthetical already concedes that record files change after landing, and because the passage's subject is choosing between working-state, `_memory/`, and the patterns journal, which the write-through has no bearing on.
- `README.md:5` and `README.md:32` — both creation-scoped ("before any record is created", "before crystallizing anything"), same shape as the pre-ruled pass. The README's caveat belongs in Gotchas, where `:74` now carries it; three mentions in a 79-line README would be over-mentioning.
- `assets/claude-md/client.template.md:57` — "raw notes in `notes/` flow into `_memory/` records and `patterns-journal/` candidates, all as user-reviewed proposals." Quantified over records and candidates flowing from notes; the stamp is neither. Same delegation reasoning as `:71`.
- `assets/readme/client.template.md:34` and `assets/readme/project.template.md:37` — identical "crystallized only after explicit sign-off" sentence, creation-scoped.
- `SKILL.md:10` — the Process mode one-liner in the three-modes summary. Passed to avoid a sixth mention in a file that already has five.
- `assets/claude-md/journal.template.md:23` — `last_confirmed` in journal entry frontmatter. Journal scope has `entries/` and no `_memory/`, so the write-through does not reach it.
- `assets/records/*.template.md:10` (six templates) — `last_confirmed: '{{YYYY-MM-DD}}'`. Template placeholders, no prose claim.
- `assets/claude-md/patterns-journal.template.md:122` and `:134` — "Promotion is deliberate, never automatic" / "Auto-promote. Promotion is always user-initiated." Both about the cross-client journal, which the stamper never touches.
- `references/retrieval-funnel.md:102` — v1 records emit an empty cell for `last_confirmed`. A query note, no write claim.
- `_maintenance/inbox-to-memory/EVALS.md:5` — "Nothing in this suite writes to the repo." True and about the repo.

## EVALS.md

The live hit moved to `:35` after T-005 shipped scenario 5. I read the file as it stands. `:35` passes: it is an instruction about driving the phase 5 gate, not a claim about what gets written, and T-005's `:126` ("the stamp does not pass through that gate anyway, which is the behavior under test") means it is no longer the document's only account. Editing it with a full account 90 lines below would have been over-mentioning. I touched nothing in EVALS.md.

## Out of bounds, surfaced not fixed

- `inbox-to-memory/scripts/migrate-scope.sh:348-349` — a code comment carrying the same reasoning as `migration.md:37`, verbatim: "A record nobody has re-confirmed was last confirmed the day it was written. Any later date would assert a review that never happened." I narrowed that second sentence in the prose doc and left the comment alone. The two now disagree in wording. Frozen non-goal; whoever unfreezes `migrate-scope.sh` should carry the same narrowing across.
- `inbox-to-memory/scripts/lint-scope.sh:26` — one sweep hit, `RECORD_KEY_ORDER` containing `last_confirmed`. No prose claim. Editing it fails a blocker clause in a later task.
- `inbox-to-memory/scripts/stamp-confirmed.sh` — nine sweep hits, all in the script this feature is about.
- `SKILL.md:449` — the record key order, pinned verbatim by the smoke suite at `:317`. Untouched.
- `_maintenance/inbox-to-memory/EVALS.md` enumerations — T-005's clause.
- `tests/fixtures/inbox-to-memory/README.md:3` — "Three scopes" above four bullets. Explicit non-goal.
- `_maintenance/inbox-to-memory/eval-scope.sh` — another worker is in it. Not opened.

## Taste calls

The hardest one was drawing a line that lets `README.md:74` fail while `_memory.template.md:65` passes, since both are creation-adjacent and both stay literally true. The line I used: a passage framed as a claim about the skill's autonomy ("nothing is ever promoted automatically," "the user reviews and approves between phase 4 and phase 5") fails, and a passage scoped to the creation step ("records are created only after approval," "crystallized only after sign-off") passes. That is what the two pre-rulings imply, and it predicts both of them. `SKILL.md:26` is the one passage in the twelve that the autonomy half catches which was not already pre-ruled.

I also avoided the T-007 token trap in every edit: no bracketed all-lowercase link labels or asides. The two new markdown links carry `/` and `.` in their labels, and the third starts with a backtick. The C-12 inventory diff is clean.

## Verification

- `bash tests/inbox-to-memory-smoke.sh` exits 0.
- C-12's four-file token inventory diff against `f027515c9f5487acd4b8d0be973af0e5bc9017d0` is empty, `inventory exit=0`.
- `.agent-guild/scripts/check-links.sh` over the five edited files reports two DEAD URLs, `https://github.com/kendrick/skills` in README.md and `https://github.com/kendrick-at-slalom/memory-bank` in SKILL.md. Both predate this job and neither is on a line I touched.
- Relative link targets I added resolve: `references/machine-contracts.md` to `migration.md` and to `../scripts/stamp-confirmed.sh`, `references/migration.md` to `../scripts/stamp-confirmed.sh`. The `~/.claude/skills/...` link in `_memory.template.md` matches that file's existing style at `:73` and is a template link by design.
- Prose was run through the `humanizer` skill. Its audit changed three of the six edits: README's redundant "a run ... a run" became the concrete `git status` symptom, migration.md's cleft opener was flattened and its strained "invent one forwards" was replaced with the backlog-draining case, and `_memory.template.md`'s closing three-clause pile was split so it stopped reading as a rule-of-three cadence. Em dashes were kept spaced to match the surrounding files rather than the global unspaced preference, since a single unspaced dash among forty spaced ones in a shipped doc reads worse than either convention held consistently.
