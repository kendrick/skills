# T-007 self-report

## Base-commit-move verification (prerequisite)

Confirmed via `diff <(git show 924f81f:<f>) <(git show f027515:<f>)` for all four of
C-12's enumerated files (`inbox-to-memory/SKILL.md`,
`inbox-to-memory/references/machine-contracts.md`,
`inbox-to-memory/assets/note.template.md`, `inbox-to-memory/scripts/lint-scope.sh`):
all four are byte-identical between `924f81f` and `f027515`. The archive retrofit
did not touch any of them. Safe to check against `f027515` as HEAD.

## C-12 — inventory diff

Ran the `inv()`/`diff` snippet verbatim (using `/bin/cat`, `/usr/bin/grep`) against
`BASE=f027515c9f5487acd4b8d0be973af0e5bc9017d0`. Output: no diff lines, `inventory
exit=0`. No new token shape registered anywhere in the four enumerating files.

## C-16 — suites, HEAD, diff scope

- `tests/inbox-to-memory-smoke.sh`: ok (exit 0)
- `tests/file-issue-smoke.sh`: exit 0
- `tests/handoff-smoke.sh`: ok (exit 0, ran as part of the combined `&&` chain)
- `git rev-parse HEAD` = `f027515c9f5487acd4b8d0be973af0e5bc9017d0` → printed "HEAD is
  the base"
- `.agent-guild/scripts/check-diff-scope.py` over the C-16 allowlist: `OK: 17 path(s)
  in scope`, exit 0

Re-checked `git status --porcelain` before and after running the suites: identical
both times (8 modified tracked files, 2 untracked additions — `stamp-confirmed.sh`
and the `confirmation-writethrough` eval fixture dir — both within the allowlist).
Running the smoke suite did not leak writes into any checked-in fixture, and no
stray temp directories were left behind (the suite's own `mktemp -d` copies clean up
after themselves).

`inbox-to-memory/scripts/lint-scope.sh` does not appear in `git status` as modified,
consistent with the constitution's note that it's deliberately outside the C-16
allowlist and untouched by this job.

## Repairs

None needed. Both clauses passed clean on the first run; nothing was broken, so
nothing was repaired and no artifacts were produced or changed by this task.

## Tree state left behind

Confirmed uncommitted, per instructions — did not commit anything. HEAD is still
`f027515c9f5487acd4b8d0be973af0e5bc9017d0`. `artifacts: []` in the task frontmatter
is correct: this task is pure verification and made no changes to the tree.
