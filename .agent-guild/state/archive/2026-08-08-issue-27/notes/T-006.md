# T-006 self-report

## What I did

Edited the header comment block of `_maintenance/inbox-to-memory/eval-scope.sh`
(lines above `set -euo pipefail` only). Replaced the stale line-number citation
`tests/inbox-to-memory-smoke.sh:1226` with a citation anchored on the guard's
own comment text instead of a line number:

> say why beside the comment "The checked-in fixtures are never migrated or
> stamped in place." in tests/inbox-to-memory-smoke.sh.

I located the actual guard by searching the smoke suite for the fixture-hash
comparison it protects (`fixtures_before=` / `"$fixtures_before"` bookends).
The guard's comment currently sits at `tests/inbox-to-memory-smoke.sh:1421-1424`
and reads "The checked-in fixtures are never migrated or stamped in place. /
Every migration or write-through test works on a copy, and a test that
forgets to copy would otherwise rewrite the fixture it is asserting against
and pass forever after." I quoted the first sentence verbatim as the anchor
text, since it's the sentence that states the rule itself (the rest is the
"why," which the citing sentence in eval-scope.sh already summarizes in its
own words).

I did not touch the number-based form at all — no correction to a new line
number anywhere, per the spec's explicit instruction not to just fix the
digit.

## Verification

- `/usr/bin/grep -rnoE 'inbox-to-memory-smoke\.sh:[0-9]+' _maintenance/ inbox-to-memory/ tests/`
  returns zero hits (exit 1) — the sweep C-15 and the task's own check_method
  both specify. This confirms no numeric citation into that file remains
  anywhere in scope.
- `git diff -- _maintenance/inbox-to-memory/eval-scope.sh` shows exactly 3
  lines changed (2 deletions, 3 insertions — one line wrapped into two), all
  within the header comment block, none touching `set -euo pipefail` or any
  line below it.

## Judgment calls / caveats

- The spec's own excerpt (written before T-004 landed) quoted the pre-T-004
  guard comment as "The checked-in fixtures are never migrated in place."
  The guard's comment as it exists in the shipped tree today reads "The
  checked-in fixtures are never migrated **or stamped** in place" — evidently
  reworded by an earlier task (T-013's write-through work per the
  constitution's C-13) to also cover the new stamping tests. I quoted the
  *current* shipped wording rather than the spec excerpt's slightly older
  wording, since quoting stale text would itself create a mismatch between
  the citation and the thing it points at — exactly the defect this task
  exists to remove. If that guard comment's wording changes again, the
  citation will need a follow-up edit regardless of form, but at least it no
  longer depends on a line number.
- Left the rest of the sentence and surrounding prose untouched; only the
  citation clause changed, so the sentence still reads as the same aside.
