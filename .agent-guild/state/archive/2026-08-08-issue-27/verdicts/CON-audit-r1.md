---
task: CON-audit
checker: auditor
vendor: anthropic
model: claude-opus-5[1m]
verdict: FAIL
checked_at: 2026-08-08T21:40:00Z
duration_ms: None
cost_usd: None
---

<!-- GENERATED FILE—do not hand-edit. Rendered by render-verdict.py
from the verdict JSON, the record of record. Edit the JSON and
re-render instead. -->

## Per-clause results

| clause | severity | description | evidence |
| ------ | -------- | ------------ | -------- |
| COVERAGE | blocker | AC6 ('A record confirmed twice in one run is stamped once') is no longer closed at the granularity the spec states, and the r1 fix is what opened it: C-17 pins the stamp to run 'per input as the closing step of phase 3', so one process-mode run over an _inbox/ holding N files makes N separate invocations, while C-5 guarantees dedupe only 'in a single invocation' — two inputs confirming the same record in one run produce two writes and two report lines, and no clause fails that. | spec.md:32 'A record confirmed twice in one run is stamped once'; constitution.md:428 (C-17) 'it runs **per input** as the closing step of phase 3'; constitution.md:143 (C-5) 'When the same record is named more than once in a single invocation'; constitution.md:325 (C-13) calls the assertion 'the once-per-run dedupe' while the section it describes exercises one invocation. Worked case against the mixed fixture: R at last_confirmed 2026-02-05, input A groomed to the 2026-02-10 note and input B to the 2026-02-17 note, both confirming R. Order A-then-B stamps twice (both writes are monotonic-legal, so C-3 skips neither); order B-then-A stamps once. The write count and the phase 6 list are order-dependent, and C-9 only requires the report to 'name every record whose last_confirmed was stamped' (constitution.md:259), which is satisfied by naming R twice. |
| C-6 | major | The budget-refusal half — the half r0 failed and r1 rebuilt — is non-discriminating as written: the probe scope's notes/ directory is created empty and the invocation passes '--note notes/nonexistent.md', so the run cannot reach the budget path at all, yet the check's pass string 'held' prints anyway; a stamper with no budget refusal passes it. | constitution.md:188 'mkdir -p "$S/_memory/context" "$S/notes" "$S/_inbox"' (notes/ left empty), constitution.md:216 'bash inbox-to-memory/scripts/stamp-confirmed.sh "$S" --note notes/nonexistent.md "$R" 2>&1 \|\| true', constitution.md:217 '[ "$before" = "$(shasum ...)" ] && echo "held"'. The prose at constitution.md:221 ('Supply a real note dated later than 2026-01-01 in `$S/notes/` for the run') contradicts the block rather than being folded into it. Nothing in the script contract at constitution.md:33-47 or in C-1's all-or-nothing rule (constitution.md:53, scoped to record paths) says what a missing --note does, so the 'held' outcome is doubly unattributable. Verified the surrounding premise is now correct: the 17-key-plus-one-comment probe lints `failures: 0` closing on line 20, and inserting last_confirmed closes on 21 and reports `FAIL ... frontmatter-budget: closing --- on line 21, past the 20-line budget`, `failures: 1`. |
| C-13 | major | The restore assertion added in r1 cannot fail: `git diff --stat -- inbox-to-memory/scripts/stamp-confirmed.sh` is always empty for an untracked file, and the stamper is new in this job, so it is untracked in the working tree the checker runs against — the one step meant to prove the saboteur was reverted proves nothing, and its failure mode is shipping the saboteur as the deliverable. | constitution.md:360 'git diff --stat -- inbox-to-memory/scripts/stamp-confirmed.sh' and constitution.md:363 'The final `git diff --stat` must be empty, proving the restore landed; C-16 inspects this tree.' Reproduced at 924f81f: created inbox-to-memory/scripts/stamp-confirmed.sh, `git status --porcelain --untracked-files=all` -> '?? inbox-to-memory/scripts/stamp-confirmed.sh', `git diff --stat -- inbox-to-memory/scripts/stamp-confirmed.sh` -> empty output, exit 0. The same step is silent about the other tree damage a sabotaged run can do: if the new smoke section writes to a checked-in fixture, the guard fires (which the check reads as the expected nonzero exit) and tests/fixtures/inbox-to-memory is left modified for C-16 to trip over later. |
| C-13 | minor | The placement check is bounded on one side only. The clause's stated purpose is that the guard actually covers the new section, which requires it to sit between the snapshot and the comparison, but the check asserts only that the last `stamp-confirmed` reference precedes the comparison — a section placed above the snapshot satisfies the check and is baked into the baseline rather than guarded by it. | constitution.md:325 'The section lands above the guard's comparison, so the guard actually covers it'; constitution.md:330-332 the check compares the last `stamp-confirmed` hit against the 'wrote to a checked-in fixture' hit only. Guard pair confirmed at tests/inbox-to-memory-smoke.sh:586 `fixtures_before="$(find "$fixtures" -type f -exec shasum {} + \| sort)"` and :1299 the comparison, :1300 the message. The check also locates the guard by the message line (:1300) rather than the comparison (:1299), and loses its anchor silently if the worker rewords the message while obeying the clause's own instruction to make it mention stamping. |
| C-13 | minor | The saboteur is too blunt to falsify what the clause says it falsifies: it discards the whole interface, not just the v1 guard, so a nonzero suite exit proves the suite is sensitive to something rather than that it tests the v1 hold. | constitution.md:341-357: the stub ignores --dry-run entirely (it would consume the flag as a record path), emits `stamped: $f -> $d` instead of the contract's `stamped: <path> <old-or-none> -> <new>` (constitution.md:40), never emits a `skipped:` line, and under `set -euo pipefail` exits nonzero on any invocation whose second argument is not --note. Any of those alone reddens a conforming suite, so `sabotaged exit=` nonzero (constitution.md:363) does not discriminate a suite that asserts the v1 hold from one that only asserts the output format. |
| C-14 | minor | The clause requires two of EVALS.md's three enumerations to be updated and misses the third: the Grading table lists scenarios 1 through 4 by name, and a fifth scenario with no row leaves exactly the stale-enumeration defect that made ':9 Four' a required fix. | constitution.md:371 requires only that 'The document's fixture count at `:9` moves from "Four" to five, and the fixture list gains its entry.' _maintenance/inbox-to-memory/EVALS.md:119-128 is the Grading section, whose table rows are '\| 1. Contradiction round trip \|', '\| 2. Unacknowledged tension \|', '\| 3. Scope proposal and the tiebreak \|', '\| 4. Tier 2 summary and entities \|'; EVALS.md:130 'The delta is widest on 1 and 4' is a second claim about the scenario set that a fifth scenario touches. |
| C-17 | minor | The clause's reasoning under-determines the pin it demands be reasoned. 'Once the groomed note exists' rules out phase 2.5 and 'before phase 5' rules out the gate, but both constraints are equally satisfied by the end of phase 4, so a worker asked to write down why the stamp sits at the close of phase 3 has no true discriminator to give and will produce a post-hoc one. | constitution.md:428 'it runs **per input** as the closing step of phase 3, once the groomed note exists and its `date` is fixed ... and **before phase 5**, so the gate is never in front of it'; constitution.md:436 'Does SKILL.md say *why* it sits where it does, so the next person to reorganize the phases knows what the placement is load-bearing for?' Phase 4 (SKILL.md:128-134) sits entirely inside the admissible window: it disposes the source and creates no gate. The clause text is still decidable literally, so this is not unfalsifiable — the reasoning requirement is what cannot be met honestly. |
| C-17 | minor | The clause text names a string SKILL.md by convention never writes, so the text and its own check disagree about what satisfies it. | constitution.md:428 '`SKILL.md` names `inbox-to-memory/scripts/stamp-confirmed.sh`'. SKILL.md's convention for every script it invokes is the `<skill-path>` placeholder: SKILL.md:175 'bash <skill-path>/scripts/lint-scope.sh <scope-root>', :347 'bash <skill-path>/scripts/migrate-scope.sh <scope-root>', :399 'bash <skill-path>/scripts/verify-migration.sh <scope-root> --since ...'. The deterministic check at constitution.md:432 greps the shorter 'scripts/stamp-confirmed.sh', which does match — so a checker reading the text rather than running the check can fail a correct artifact. |
| BASELINE | minor | The r1 correction overstates the frontmatter-comment premise it rests C-6's recipe on: not every shipped record template carries four comment lines. | constitution.md:25 'every shipped record template in `assets/records/` carries four of them' and constitution.md:185 'every one of them carries four such comments'. Counted comment lines inside the frontmatter block of each: context 4 (closes 18), decision 4 (18), exception 4 (19), policy-rule 4 (18), rule 4 (18), journal-entry 2 (15). The point the premise serves — a comment line inside the block is ordinary, not exotic — survives; the universal quantifier does not. |
| SCRIPT-CONTRACT | minor | `--dry-run` is pinned as part of the interface C-1 says the script must honor, and no clause exercises it; a stamper shipping without it passes every check in the document. | constitution.md:47 '`--dry-run` exists for inspection and for the tests, and prints the same lines while writing nothing'; C-1 (constitution.md:53) 'honors the interface above'. No check in C-1 through C-7, C-13, or C-17 passes --dry-run. Related: constitution.md:37 says every outcome prints one line per record and the run ends with a count, but no clause asserts the closing `stamped: N skipped: N` line except C-5's 'stamped: 1' (constitution.md:157). |
| C-12 | minor | C-12 and C-16 both establish scope by comparing the working tree against HEAD, and nothing in the document says the job's work must still be uncommitted when the checks run; a worker who commits before the check makes both a blocker and a major clause vacuous at once. | constitution.md:311 'if [ -n "$1" ]; then git show "$1:$f"; else /bin/cat "$f"; fi' with 'diff <(inv HEAD) <(inv)' at :314; .agent-guild/scripts/check-diff-scope.py unions `git status --porcelain --untracked-files=all` with `git diff --name-only`, both of which go empty against a committed tree. constitution.md:12 says 'Everything below is measured against the shipped tree' without pinning what 'shipped' means for a git comparison. Verified the C-12 inventory diff runs clean at 924f81f: 'inventory exit=0', inventory = [a-z0-9 -], [a-z0-9-], [a-z0-9], [a-z], [contradicts accepted:, [decision:, [extends:, [journal candidate:, [memory candidate:, [open question resolved:, [open question:, [target], [tension:, [working-state candidate]. |
| C-11 | minor | The pre-ruling that assets/claude-md/_memory.template.md:65 passes unchanged is defensible per passage, but it is the only hook C-11 has into that file, so the conventions document that ships into the `_memory/` directory itself can come out of this job with no mention of the ungated write while README.md is required to gain one. | constitution.md:285 pre-rules ':65 passes unchanged'. The file is the agent-facing CLAUDE.md for `_memory/`: it lists `last_confirmed: YYYY-MM-DD` as required frontmatter at _memory.template.md:21 with no account of how the value is set, 'How Records Get Created' at :60-74, 'When Records Graduate' at :76-78 ('The user edits status: proposed -> status: accepted after review'), and 'What Never Goes Here' at :80-86. Question 2's 'only account' test answers no because :74 and :82 also speak to unattended writes, which is what makes the pre-ruling hold — and what leaves the document-level gap open. |
| C-17 | pass | The r0 blocker is genuinely closed rather than restated: the clause requires the string in SKILL.md, requires the smoke suite to pin the wiring the way the migrator's is pinned, and locates the write in the flow, so the r0 failure mode — a correct stamper nothing ever calls — now fails a blocker clause. | constitution.md:424-438. The mirror it names is real: tests/inbox-to-memory-smoke.sh:596-602 is the migrate wiring block, with `require_text inbox-to-memory/SKILL.md "scripts/migrate-scope.sh"` at :599, so the required assertion string has a working precedent. C-8 now defers placement explicitly at constitution.md:250 ('Where the write itself happens is C-17's business, not this clause's'). Both deterministic greps return zero hits today, which is the expected pre-implementation state. |
| C-12 | pass | The r0 defect is closed: the inventory now extracts from all four enumerating files including SKILL.md, so a `[confirms accepted:` row added to SKILL.md's own token block — the exact hole r0 found — now shows up as a set difference. | constitution.md:307-315 includes inbox-to-memory/SKILL.md in the loop. Ran the check verbatim at 924f81f: no diff, 'inventory exit=0'. The extraction pattern '\\[[a-z][a-z0-9 -]*(:\|\\])' does match a synthetic '[confirms accepted:' and already picks up the real '[contradicts accepted:' at SKILL.md:100, so the pattern reaches the block r0 said it missed. Residual, not a defect: a capitalized or underscored token shape falls outside the character class. |
| C-11 | pass | Both r0 defects are closed. The literal-truth test is gone and replaced by two ordered questions that do settle the case the clause was written for, the two boundary passages are pre-ruled in opposite directions with reasons, and the scope collision is genuinely resolved rather than papered over. | constitution.md:278-296. Re-derived the sweep rather than inheriting the list: all ten cited locations resolve (SKILL.md:26 'The user reviews and approves between phase 4 and phase 5; never auto-promote', :138, :168, :361, :455; README.md:74; assets/claude-md/_memory.template.md:65, client.template.md:71, project.template.md:55; references/migration.md:37), and re-derivation surfaces further real candidates the floor omits — references/retrieval-funnel.md:40 and :102, both accounts of what last_confirmed means for staleness, and both inside C-16's allowlist. The one hit outside the allowlist, inbox-to-memory/scripts/migrate-scope.sh:348-349 (the same 'would assert a review that never happened' reasoning as migration.md:37), is covered by the last non-goal's surface-it-don't-fix-it escape hatch at constitution.md:451. |
| C-16 | pass | Widening the allowlist to all of inbox-to-memory/assets/ opens no hole another clause depended on being closed, and narrowing C-12 from a whole-file freeze on note.template.md to a token-shape freeze does not either — the smoke suite independently pins everything under assets/ that matters. | Ran the C-16 check verbatim at 924f81f: three suites exit 0, 'OK: 0 path(s) in scope', 'scope exit=0'. The newly-admitted paths are pinned elsewhere: tests/inbox-to-memory-smoke.sh:344-356 stands the note template and all six record templates up as a scope and asserts 'v2 files: 7' and 'failures: 0', :359-361 requires 'schema: 2' in each, :364-369 requires the flat relationship forms and refutes the nested ones, :402-405 requires the three token shapes in note.template.md, :407-412 pins section order. lint-scope.sh stays out of the allowlist, so the second lock on the no-lint-changes non-goal holds. The four unpinned admits (assets/personal.template.md, assets/working-state.template.md, assets/patterns-journal/journal.template.md, assets/readme/*) are prose-only and are exactly what C-11's census needs to reach. |
| C-9 | pass | The r0 ambiguity is resolved: the skipped list is now scoped to what the stamper declined, records merely read at phase 2.5 are explicitly excluded, and the rubric asks the checker to confirm that scoping — so C-9 and C-14's fourth fixture record no longer pull in opposite directions. | constitution.md:259 'every record the **stamper** declined to write ... Records merely read at phase 2.5 and not judged confirmed are outside this list'; constitution.md:260 'Is the skipped list scoped to what the stamper declined, so a reader cannot mistake it for an inventory of everything phase 2.5 opened?' Reconciles with C-14's 'read at phase 2.5 and neither confirmed nor contradicted' record at constitution.md:371, which is now graded as an absence-of-stamp rather than as a required report line. |
| C-5 | pass | Sound at the scope it claims: the tempdir leak r0 flagged is fixed, and the realpath trap is real, so the clause discriminates a string-equality dedupe from a realpath one. Its run-level shortfall is filed under COVERAGE rather than here, because the clause never claimed run scope. | constitution.md:154 'rm -rf "$S"' now present. Confirmed the trap at 924f81f: `mktemp -d` returns /var/folders/2b/m3q305ss1tb55qfhrv6v0n3m0000gn/T/tmp.5JeMO5GUTz, whose real path is under /private/var/folders/..., so the relative and absolute arguments differ by more than a prefix. The grep -c pattern '^stamped: .*atlas-region-topology' correctly excludes the closing 'stamped: N skipped: N' summary. |
| C-6 | pass | The premise r0 falsified is now correct, and the insertion half of the check is sound against the real fixture. | Rebuilt the probe from constitution.md:190-213 verbatim: `lint-scope.sh` reports 'failures: 0' with the closing --- on line 20; inserting last_confirmed before source_refs closes on 21 and reports 'FAIL .../budget-probe-AAAAAAAAAA.md: frontmatter-budget: closing --- on line 21, past the 20-line budget', 'failures: 1'. The insertion half's expected position is right: tests/fixtures/inbox-to-memory/mixed/_memory/context/atlas-region-topology-mPmy8XBe5H.md carries date: at :7, last_confirmed: at :8, source_refs: at :9 and no effective_from/effective_to, so 'directly after date:, directly before source_refs:' is exactly what a contract-position insert produces. |
| C-1 | pass | Unchanged from r0 and still sound: the interface is pinned, the fixture paths resolve, the all-or-nothing validation half is discriminating, and the only reason the harness goes red today is the absent script. | constitution.md:51-70. tests/fixtures/inbox-to-memory/mixed/_memory/context/atlas-region-topology-mPmy8XBe5H.md:2 'schema: 2', :6 'status: accepted', :8 'last_confirmed: 2026-02-10'; the note it names, mixed/notes/2026-02-17-atlas-freeze-exceptions-SDy5SGVwfu.md, exists. Exit 127 on the harness today is expected. |
| C-2 | pass | Unchanged and still falsifiable against the real fixture: the v1 record genuinely carries no schema key and is status: accepted, so the shasum comparison goes red on exactly the named failing example. | tests/fixtures/inbox-to-memory/mixed/_memory/decisions/freeze-window-owned-by-ops-ocPwdpeY0a.md:1-16 — keys id, memory_type, title, status, date, source_refs, applies_to, owners, tags, related; no schema, no last_confirmed; :5 'status: accepted'. |
| C-3 | pass | Unchanged and still discriminating: the fixture supplies a strictly-older note and an exactly-equal one, so 'earlier' and 'equal' are exercised separately rather than collapsed. | mixed/notes/2025-12-02-atlas-steerco-ZGulgExW0q.md and mixed/notes/2026-02-10-atlas-runbook-review-j5jLCGc5il.md both present; record last_confirmed: 2026-02-10. |
| C-4 | pass | Unchanged, and the justification for building its own input still holds after re-derivation: no record fixture anywhere is non-accepted while carrying last_confirmed. | Swept every *.md under tests/fixtures with a _memory path component for 'last_confirmed present and status != accepted': zero hits. The python mutation at constitution.md:123-126 uses count=1 and targets a file it just copied, so it cannot escape the tempdir. |
| C-7 | pass | Unchanged and correctly baselined: the mixed fixture lints clean today so the before/after comparison has a real fixed point, and the yq round-trip failing example is concrete against the record's actual quoting. | constitution.md:225-246; record :5 "title: 'Billing region and deployment region are not the same boundary'", :13 'related: [clarifies::G2k65qG3Nc]'. `cp -R "$S" "$S.orig"` into a non-existent destination copies the tree, so `diff -r` compares what the clause says it compares. |
| C-8 | pass | Still sound, and the r1 edit strictly improves it: four independently checkable propositions plus a concrete structural failure mode, with placement now handed to C-17 instead of half-asserted here. | constitution.md:248-253; the budget sentence it cites is verbatim at SKILL.md:67 '**Five body reads per input, and the budget binds.**' and the accepted-only rule it says it matches is at SKILL.md:69. |
| C-10 | pass | Unchanged and still the sharpest rubric in the document: the two-exceptions contradiction test is anchored to a line that really exists and really is the last bullet of the Operating Rules list. | SKILL.md:459 '**VTT raw content is collapsed, not verbatim.** ... This is the one sanctioned exception to preserving raw content exactly as captured.'; the Operating Rules heading is at SKILL.md:450. |
| C-15 | pass | Unchanged, still runs, and still returns exactly the one hit the clause predicts. | Ran the sweep at 924f81f: one hit, '_maintenance//inbox-to-memory/eval-scope.sh:6:inbox-to-memory-smoke.sh:1226'. The never-migrate-a-fixture rule the comment cites is at tests/inbox-to-memory-smoke.sh:1296-1302, so the citation is wrong today and this job moves the target again. |
| BASELINE | pass | All four r0 baseline corrections land, and the rest of the baseline re-verifies against the shipped tree. | The two schema:2/accepted/no-last_confirmed eval fixtures are named correctly and confirmed present (evals/unacknowledged-tension/_memory/context/bexley-dock-capacity-MH-2ub4mwK.md and .../decisions/bexley-pilots-first-LvJedcverX.md, and they are the only two in the tree). `type stat` -> /usr/bin/stat, no longer claimed shadowed; `type cat` -> alias for ccat, now claimed. The hash guard is described as a pair over tests/fixtures/inbox-to-memory: snapshot at :586, comparison at :1299 inside the :1296-1302 range the constitution gives. Smoke suite is 1309 lines; all three suites exit 0. |
| COVERAGE | pass | Six of the seven acceptance criteria map to clauses that would catch their violation, and C-17 closes the r0 gap. AC6 is the exception, filed as the blocker above. | AC1 (spec.md:27) -> C-1, C-6, C-8, C-13, C-17; AC2 (:28) -> C-2, C-13; AC3 (:29) -> C-9; AC4 (:30) -> C-8 ('the resulting stamp does not pass through phase 5', constitution.md:250), C-17 ('before phase 5, so the gate is never in front of it', :428), the contract's 'It writes by default. No --apply' (:47), and C-14's 'absence of any per-record prompt' row (:371); AC5 (:31) -> C-10; AC7 (:33) -> C-13 with C-2 supplying the assertion's content. AC6 (:32) -> C-5 and C-13, both invocation-scoped. |
| NON-GOALS | pass | None of the seven non-goals has become load-bearing in a way that blocks an acceptance criterion, and the one that could — the freeze on lint-scope.sh, migrate-scope.sh, verify-migration.sh, and collapse-vtt.sh — carries its own escape hatch, which is what keeps C-11's re-derived sweep from colliding with it. | constitution.md:444-451. The collision candidate is real: the C-11 sweep hits inbox-to-memory/scripts/migrate-scope.sh:348-349, whose comment carries the same last_confirmed reasoning as migration.md:37, and that path is outside C-16's allowlist. constitution.md:451 'If the job appears to need one, that is a finding to surface, not a task to do' resolves it. The fixtures-README non-goal stays safe: tests/fixtures/inbox-to-memory/README.md does not enumerate eval fixtures, so a fifth one does not make it staler. |
| PROTECTED | pass | Correct and unchanged: this job ships no verbatim author words, so there is no manifest to parse and no protected-passage check to route. | constitution.md:440-442; the job's surface is a shell script, SKILL.md/README.md prose, a smoke-test section, and an eval fixture. |

## Diagnosis

- **COVERAGE** (blocker): AC6 ('A record confirmed twice in one run is stamped once') is no longer closed at the granularity the spec states, and the r1 fix is what opened it: C-17 pins the stamp to run 'per input as the closing step of phase 3', so one process-mode run over an _inbox/ holding N files makes N separate invocations, while C-5 guarantees dedupe only 'in a single invocation' — two inputs confirming the same record in one run produce two writes and two report lines, and no clause fails that.
  evidence: spec.md:32 'A record confirmed twice in one run is stamped once'; constitution.md:428 (C-17) 'it runs **per input** as the closing step of phase 3'; constitution.md:143 (C-5) 'When the same record is named more than once in a single invocation'; constitution.md:325 (C-13) calls the assertion 'the once-per-run dedupe' while the section it describes exercises one invocation. Worked case against the mixed fixture: R at last_confirmed 2026-02-05, input A groomed to the 2026-02-10 note and input B to the 2026-02-17 note, both confirming R. Order A-then-B stamps twice (both writes are monotonic-legal, so C-3 skips neither); order B-then-A stamps once. The write count and the phase 6 list are order-dependent, and C-9 only requires the report to 'name every record whose last_confirmed was stamped' (constitution.md:259), which is satisfied by naming R twice.
- **C-6** (major): The budget-refusal half — the half r0 failed and r1 rebuilt — is non-discriminating as written: the probe scope's notes/ directory is created empty and the invocation passes '--note notes/nonexistent.md', so the run cannot reach the budget path at all, yet the check's pass string 'held' prints anyway; a stamper with no budget refusal passes it.
  evidence: constitution.md:188 'mkdir -p "$S/_memory/context" "$S/notes" "$S/_inbox"' (notes/ left empty), constitution.md:216 'bash inbox-to-memory/scripts/stamp-confirmed.sh "$S" --note notes/nonexistent.md "$R" 2>&1 || true', constitution.md:217 '[ "$before" = "$(shasum ...)" ] && echo "held"'. The prose at constitution.md:221 ('Supply a real note dated later than 2026-01-01 in `$S/notes/` for the run') contradicts the block rather than being folded into it. Nothing in the script contract at constitution.md:33-47 or in C-1's all-or-nothing rule (constitution.md:53, scoped to record paths) says what a missing --note does, so the 'held' outcome is doubly unattributable. Verified the surrounding premise is now correct: the 17-key-plus-one-comment probe lints `failures: 0` closing on line 20, and inserting last_confirmed closes on 21 and reports `FAIL ... frontmatter-budget: closing --- on line 21, past the 20-line budget`, `failures: 1`.
- **C-13** (major): The restore assertion added in r1 cannot fail: `git diff --stat -- inbox-to-memory/scripts/stamp-confirmed.sh` is always empty for an untracked file, and the stamper is new in this job, so it is untracked in the working tree the checker runs against — the one step meant to prove the saboteur was reverted proves nothing, and its failure mode is shipping the saboteur as the deliverable.
  evidence: constitution.md:360 'git diff --stat -- inbox-to-memory/scripts/stamp-confirmed.sh' and constitution.md:363 'The final `git diff --stat` must be empty, proving the restore landed; C-16 inspects this tree.' Reproduced at 924f81f: created inbox-to-memory/scripts/stamp-confirmed.sh, `git status --porcelain --untracked-files=all` -> '?? inbox-to-memory/scripts/stamp-confirmed.sh', `git diff --stat -- inbox-to-memory/scripts/stamp-confirmed.sh` -> empty output, exit 0. The same step is silent about the other tree damage a sabotaged run can do: if the new smoke section writes to a checked-in fixture, the guard fires (which the check reads as the expected nonzero exit) and tests/fixtures/inbox-to-memory is left modified for C-16 to trip over later.
- **C-13** (minor): The placement check is bounded on one side only. The clause's stated purpose is that the guard actually covers the new section, which requires it to sit between the snapshot and the comparison, but the check asserts only that the last `stamp-confirmed` reference precedes the comparison — a section placed above the snapshot satisfies the check and is baked into the baseline rather than guarded by it.
  evidence: constitution.md:325 'The section lands above the guard's comparison, so the guard actually covers it'; constitution.md:330-332 the check compares the last `stamp-confirmed` hit against the 'wrote to a checked-in fixture' hit only. Guard pair confirmed at tests/inbox-to-memory-smoke.sh:586 `fixtures_before="$(find "$fixtures" -type f -exec shasum {} + | sort)"` and :1299 the comparison, :1300 the message. The check also locates the guard by the message line (:1300) rather than the comparison (:1299), and loses its anchor silently if the worker rewords the message while obeying the clause's own instruction to make it mention stamping.
- **C-13** (minor): The saboteur is too blunt to falsify what the clause says it falsifies: it discards the whole interface, not just the v1 guard, so a nonzero suite exit proves the suite is sensitive to something rather than that it tests the v1 hold.
  evidence: constitution.md:341-357: the stub ignores --dry-run entirely (it would consume the flag as a record path), emits `stamped: $f -> $d` instead of the contract's `stamped: <path> <old-or-none> -> <new>` (constitution.md:40), never emits a `skipped:` line, and under `set -euo pipefail` exits nonzero on any invocation whose second argument is not --note. Any of those alone reddens a conforming suite, so `sabotaged exit=` nonzero (constitution.md:363) does not discriminate a suite that asserts the v1 hold from one that only asserts the output format.
- **C-14** (minor): The clause requires two of EVALS.md's three enumerations to be updated and misses the third: the Grading table lists scenarios 1 through 4 by name, and a fifth scenario with no row leaves exactly the stale-enumeration defect that made ':9 Four' a required fix.
  evidence: constitution.md:371 requires only that 'The document's fixture count at `:9` moves from "Four" to five, and the fixture list gains its entry.' _maintenance/inbox-to-memory/EVALS.md:119-128 is the Grading section, whose table rows are '| 1. Contradiction round trip |', '| 2. Unacknowledged tension |', '| 3. Scope proposal and the tiebreak |', '| 4. Tier 2 summary and entities |'; EVALS.md:130 'The delta is widest on 1 and 4' is a second claim about the scenario set that a fifth scenario touches.
- **C-17** (minor): The clause's reasoning under-determines the pin it demands be reasoned. 'Once the groomed note exists' rules out phase 2.5 and 'before phase 5' rules out the gate, but both constraints are equally satisfied by the end of phase 4, so a worker asked to write down why the stamp sits at the close of phase 3 has no true discriminator to give and will produce a post-hoc one.
  evidence: constitution.md:428 'it runs **per input** as the closing step of phase 3, once the groomed note exists and its `date` is fixed ... and **before phase 5**, so the gate is never in front of it'; constitution.md:436 'Does SKILL.md say *why* it sits where it does, so the next person to reorganize the phases knows what the placement is load-bearing for?' Phase 4 (SKILL.md:128-134) sits entirely inside the admissible window: it disposes the source and creates no gate. The clause text is still decidable literally, so this is not unfalsifiable — the reasoning requirement is what cannot be met honestly.
- **C-17** (minor): The clause text names a string SKILL.md by convention never writes, so the text and its own check disagree about what satisfies it.
  evidence: constitution.md:428 '`SKILL.md` names `inbox-to-memory/scripts/stamp-confirmed.sh`'. SKILL.md's convention for every script it invokes is the `<skill-path>` placeholder: SKILL.md:175 'bash <skill-path>/scripts/lint-scope.sh <scope-root>', :347 'bash <skill-path>/scripts/migrate-scope.sh <scope-root>', :399 'bash <skill-path>/scripts/verify-migration.sh <scope-root> --since ...'. The deterministic check at constitution.md:432 greps the shorter 'scripts/stamp-confirmed.sh', which does match — so a checker reading the text rather than running the check can fail a correct artifact.
- **BASELINE** (minor): The r1 correction overstates the frontmatter-comment premise it rests C-6's recipe on: not every shipped record template carries four comment lines.
  evidence: constitution.md:25 'every shipped record template in `assets/records/` carries four of them' and constitution.md:185 'every one of them carries four such comments'. Counted comment lines inside the frontmatter block of each: context 4 (closes 18), decision 4 (18), exception 4 (19), policy-rule 4 (18), rule 4 (18), journal-entry 2 (15). The point the premise serves — a comment line inside the block is ordinary, not exotic — survives; the universal quantifier does not.
- **SCRIPT-CONTRACT** (minor): `--dry-run` is pinned as part of the interface C-1 says the script must honor, and no clause exercises it; a stamper shipping without it passes every check in the document.
  evidence: constitution.md:47 '`--dry-run` exists for inspection and for the tests, and prints the same lines while writing nothing'; C-1 (constitution.md:53) 'honors the interface above'. No check in C-1 through C-7, C-13, or C-17 passes --dry-run. Related: constitution.md:37 says every outcome prints one line per record and the run ends with a count, but no clause asserts the closing `stamped: N  skipped: N` line except C-5's 'stamped: 1' (constitution.md:157).
- **C-12** (minor): C-12 and C-16 both establish scope by comparing the working tree against HEAD, and nothing in the document says the job's work must still be uncommitted when the checks run; a worker who commits before the check makes both a blocker and a major clause vacuous at once.
  evidence: constitution.md:311 'if [ -n "$1" ]; then git show "$1:$f"; else /bin/cat "$f"; fi' with 'diff <(inv HEAD) <(inv)' at :314; .agent-guild/scripts/check-diff-scope.py unions `git status --porcelain --untracked-files=all` with `git diff --name-only`, both of which go empty against a committed tree. constitution.md:12 says 'Everything below is measured against the shipped tree' without pinning what 'shipped' means for a git comparison. Verified the C-12 inventory diff runs clean at 924f81f: 'inventory exit=0', inventory = [a-z0-9 -], [a-z0-9-], [a-z0-9], [a-z], [contradicts accepted:, [decision:, [extends:, [journal candidate:, [memory candidate:, [open question resolved:, [open question:, [target], [tension:, [working-state candidate].
- **C-11** (minor): The pre-ruling that assets/claude-md/_memory.template.md:65 passes unchanged is defensible per passage, but it is the only hook C-11 has into that file, so the conventions document that ships into the `_memory/` directory itself can come out of this job with no mention of the ungated write while README.md is required to gain one.
  evidence: constitution.md:285 pre-rules ':65 passes unchanged'. The file is the agent-facing CLAUDE.md for `_memory/`: it lists `last_confirmed: YYYY-MM-DD` as required frontmatter at _memory.template.md:21 with no account of how the value is set, 'How Records Get Created' at :60-74, 'When Records Graduate' at :76-78 ('The user edits status: proposed -> status: accepted after review'), and 'What Never Goes Here' at :80-86. Question 2's 'only account' test answers no because :74 and :82 also speak to unattended writes, which is what makes the pre-ruling hold — and what leaves the document-level gap open.
- **C-17** (pass): The r0 blocker is genuinely closed rather than restated: the clause requires the string in SKILL.md, requires the smoke suite to pin the wiring the way the migrator's is pinned, and locates the write in the flow, so the r0 failure mode — a correct stamper nothing ever calls — now fails a blocker clause.
  evidence: constitution.md:424-438. The mirror it names is real: tests/inbox-to-memory-smoke.sh:596-602 is the migrate wiring block, with `require_text inbox-to-memory/SKILL.md "scripts/migrate-scope.sh"` at :599, so the required assertion string has a working precedent. C-8 now defers placement explicitly at constitution.md:250 ('Where the write itself happens is C-17's business, not this clause's'). Both deterministic greps return zero hits today, which is the expected pre-implementation state.
- **C-12** (pass): The r0 defect is closed: the inventory now extracts from all four enumerating files including SKILL.md, so a `[confirms accepted:` row added to SKILL.md's own token block — the exact hole r0 found — now shows up as a set difference.
  evidence: constitution.md:307-315 includes inbox-to-memory/SKILL.md in the loop. Ran the check verbatim at 924f81f: no diff, 'inventory exit=0'. The extraction pattern '\[[a-z][a-z0-9 -]*(:|\])' does match a synthetic '[confirms accepted:' and already picks up the real '[contradicts accepted:' at SKILL.md:100, so the pattern reaches the block r0 said it missed. Residual, not a defect: a capitalized or underscored token shape falls outside the character class.
- **C-11** (pass): Both r0 defects are closed. The literal-truth test is gone and replaced by two ordered questions that do settle the case the clause was written for, the two boundary passages are pre-ruled in opposite directions with reasons, and the scope collision is genuinely resolved rather than papered over.
  evidence: constitution.md:278-296. Re-derived the sweep rather than inheriting the list: all ten cited locations resolve (SKILL.md:26 'The user reviews and approves between phase 4 and phase 5; never auto-promote', :138, :168, :361, :455; README.md:74; assets/claude-md/_memory.template.md:65, client.template.md:71, project.template.md:55; references/migration.md:37), and re-derivation surfaces further real candidates the floor omits — references/retrieval-funnel.md:40 and :102, both accounts of what last_confirmed means for staleness, and both inside C-16's allowlist. The one hit outside the allowlist, inbox-to-memory/scripts/migrate-scope.sh:348-349 (the same 'would assert a review that never happened' reasoning as migration.md:37), is covered by the last non-goal's surface-it-don't-fix-it escape hatch at constitution.md:451.
- **C-16** (pass): Widening the allowlist to all of inbox-to-memory/assets/ opens no hole another clause depended on being closed, and narrowing C-12 from a whole-file freeze on note.template.md to a token-shape freeze does not either — the smoke suite independently pins everything under assets/ that matters.
  evidence: Ran the C-16 check verbatim at 924f81f: three suites exit 0, 'OK: 0 path(s) in scope', 'scope exit=0'. The newly-admitted paths are pinned elsewhere: tests/inbox-to-memory-smoke.sh:344-356 stands the note template and all six record templates up as a scope and asserts 'v2 files: 7' and 'failures: 0', :359-361 requires 'schema: 2' in each, :364-369 requires the flat relationship forms and refutes the nested ones, :402-405 requires the three token shapes in note.template.md, :407-412 pins section order. lint-scope.sh stays out of the allowlist, so the second lock on the no-lint-changes non-goal holds. The four unpinned admits (assets/personal.template.md, assets/working-state.template.md, assets/patterns-journal/journal.template.md, assets/readme/*) are prose-only and are exactly what C-11's census needs to reach.
- **C-9** (pass): The r0 ambiguity is resolved: the skipped list is now scoped to what the stamper declined, records merely read at phase 2.5 are explicitly excluded, and the rubric asks the checker to confirm that scoping — so C-9 and C-14's fourth fixture record no longer pull in opposite directions.
  evidence: constitution.md:259 'every record the **stamper** declined to write ... Records merely read at phase 2.5 and not judged confirmed are outside this list'; constitution.md:260 'Is the skipped list scoped to what the stamper declined, so a reader cannot mistake it for an inventory of everything phase 2.5 opened?' Reconciles with C-14's 'read at phase 2.5 and neither confirmed nor contradicted' record at constitution.md:371, which is now graded as an absence-of-stamp rather than as a required report line.
- **C-5** (pass): Sound at the scope it claims: the tempdir leak r0 flagged is fixed, and the realpath trap is real, so the clause discriminates a string-equality dedupe from a realpath one. Its run-level shortfall is filed under COVERAGE rather than here, because the clause never claimed run scope.
  evidence: constitution.md:154 'rm -rf "$S"' now present. Confirmed the trap at 924f81f: `mktemp -d` returns /var/folders/2b/m3q305ss1tb55qfhrv6v0n3m0000gn/T/tmp.5JeMO5GUTz, whose real path is under /private/var/folders/..., so the relative and absolute arguments differ by more than a prefix. The grep -c pattern '^stamped: .*atlas-region-topology' correctly excludes the closing 'stamped: N  skipped: N' summary.
- **C-6** (pass): The premise r0 falsified is now correct, and the insertion half of the check is sound against the real fixture.
  evidence: Rebuilt the probe from constitution.md:190-213 verbatim: `lint-scope.sh` reports 'failures: 0' with the closing --- on line 20; inserting last_confirmed before source_refs closes on 21 and reports 'FAIL .../budget-probe-AAAAAAAAAA.md: frontmatter-budget: closing --- on line 21, past the 20-line budget', 'failures: 1'. The insertion half's expected position is right: tests/fixtures/inbox-to-memory/mixed/_memory/context/atlas-region-topology-mPmy8XBe5H.md carries date: at :7, last_confirmed: at :8, source_refs: at :9 and no effective_from/effective_to, so 'directly after date:, directly before source_refs:' is exactly what a contract-position insert produces.
- **C-1** (pass): Unchanged from r0 and still sound: the interface is pinned, the fixture paths resolve, the all-or-nothing validation half is discriminating, and the only reason the harness goes red today is the absent script.
  evidence: constitution.md:51-70. tests/fixtures/inbox-to-memory/mixed/_memory/context/atlas-region-topology-mPmy8XBe5H.md:2 'schema: 2', :6 'status: accepted', :8 'last_confirmed: 2026-02-10'; the note it names, mixed/notes/2026-02-17-atlas-freeze-exceptions-SDy5SGVwfu.md, exists. Exit 127 on the harness today is expected.
- **C-2** (pass): Unchanged and still falsifiable against the real fixture: the v1 record genuinely carries no schema key and is status: accepted, so the shasum comparison goes red on exactly the named failing example.
  evidence: tests/fixtures/inbox-to-memory/mixed/_memory/decisions/freeze-window-owned-by-ops-ocPwdpeY0a.md:1-16 — keys id, memory_type, title, status, date, source_refs, applies_to, owners, tags, related; no schema, no last_confirmed; :5 'status: accepted'.
- **C-3** (pass): Unchanged and still discriminating: the fixture supplies a strictly-older note and an exactly-equal one, so 'earlier' and 'equal' are exercised separately rather than collapsed.
  evidence: mixed/notes/2025-12-02-atlas-steerco-ZGulgExW0q.md and mixed/notes/2026-02-10-atlas-runbook-review-j5jLCGc5il.md both present; record last_confirmed: 2026-02-10.
- **C-4** (pass): Unchanged, and the justification for building its own input still holds after re-derivation: no record fixture anywhere is non-accepted while carrying last_confirmed.
  evidence: Swept every *.md under tests/fixtures with a _memory path component for 'last_confirmed present and status != accepted': zero hits. The python mutation at constitution.md:123-126 uses count=1 and targets a file it just copied, so it cannot escape the tempdir.
- **C-7** (pass): Unchanged and correctly baselined: the mixed fixture lints clean today so the before/after comparison has a real fixed point, and the yq round-trip failing example is concrete against the record's actual quoting.
  evidence: constitution.md:225-246; record :5 "title: 'Billing region and deployment region are not the same boundary'", :13 'related: [clarifies::G2k65qG3Nc]'. `cp -R "$S" "$S.orig"` into a non-existent destination copies the tree, so `diff -r` compares what the clause says it compares.
- **C-8** (pass): Still sound, and the r1 edit strictly improves it: four independently checkable propositions plus a concrete structural failure mode, with placement now handed to C-17 instead of half-asserted here.
  evidence: constitution.md:248-253; the budget sentence it cites is verbatim at SKILL.md:67 '**Five body reads per input, and the budget binds.**' and the accepted-only rule it says it matches is at SKILL.md:69.
- **C-10** (pass): Unchanged and still the sharpest rubric in the document: the two-exceptions contradiction test is anchored to a line that really exists and really is the last bullet of the Operating Rules list.
  evidence: SKILL.md:459 '**VTT raw content is collapsed, not verbatim.** ... This is the one sanctioned exception to preserving raw content exactly as captured.'; the Operating Rules heading is at SKILL.md:450.
- **C-15** (pass): Unchanged, still runs, and still returns exactly the one hit the clause predicts.
  evidence: Ran the sweep at 924f81f: one hit, '_maintenance//inbox-to-memory/eval-scope.sh:6:inbox-to-memory-smoke.sh:1226'. The never-migrate-a-fixture rule the comment cites is at tests/inbox-to-memory-smoke.sh:1296-1302, so the citation is wrong today and this job moves the target again.
- **BASELINE** (pass): All four r0 baseline corrections land, and the rest of the baseline re-verifies against the shipped tree.
  evidence: The two schema:2/accepted/no-last_confirmed eval fixtures are named correctly and confirmed present (evals/unacknowledged-tension/_memory/context/bexley-dock-capacity-MH-2ub4mwK.md and .../decisions/bexley-pilots-first-LvJedcverX.md, and they are the only two in the tree). `type stat` -> /usr/bin/stat, no longer claimed shadowed; `type cat` -> alias for ccat, now claimed. The hash guard is described as a pair over tests/fixtures/inbox-to-memory: snapshot at :586, comparison at :1299 inside the :1296-1302 range the constitution gives. Smoke suite is 1309 lines; all three suites exit 0.
- **COVERAGE** (pass): Six of the seven acceptance criteria map to clauses that would catch their violation, and C-17 closes the r0 gap. AC6 is the exception, filed as the blocker above.
  evidence: AC1 (spec.md:27) -> C-1, C-6, C-8, C-13, C-17; AC2 (:28) -> C-2, C-13; AC3 (:29) -> C-9; AC4 (:30) -> C-8 ('the resulting stamp does not pass through phase 5', constitution.md:250), C-17 ('before phase 5, so the gate is never in front of it', :428), the contract's 'It writes by default. No --apply' (:47), and C-14's 'absence of any per-record prompt' row (:371); AC5 (:31) -> C-10; AC7 (:33) -> C-13 with C-2 supplying the assertion's content. AC6 (:32) -> C-5 and C-13, both invocation-scoped.
- **NON-GOALS** (pass): None of the seven non-goals has become load-bearing in a way that blocks an acceptance criterion, and the one that could — the freeze on lint-scope.sh, migrate-scope.sh, verify-migration.sh, and collapse-vtt.sh — carries its own escape hatch, which is what keeps C-11's re-derived sweep from colliding with it.
  evidence: constitution.md:444-451. The collision candidate is real: the C-11 sweep hits inbox-to-memory/scripts/migrate-scope.sh:348-349, whose comment carries the same last_confirmed reasoning as migration.md:37, and that path is outside C-16's allowlist. constitution.md:451 'If the job appears to need one, that is a finding to surface, not a task to do' resolves it. The fixtures-README non-goal stays safe: tests/fixtures/inbox-to-memory/README.md does not enumerate eval fixtures, so a fifth one does not make it staler.
- **PROTECTED** (pass): Correct and unchanged: this job ships no verbatim author words, so there is no manifest to parse and no protected-passage check to route.
  evidence: constitution.md:440-442; the job's surface is a shell script, SKILL.md/README.md prose, a smoke-test section, and an eval fixture.
