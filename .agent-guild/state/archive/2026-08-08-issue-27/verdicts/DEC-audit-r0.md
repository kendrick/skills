---
task: DEC-audit
checker: auditor
vendor: anthropic
model: claude-opus-5[1m]
verdict: FAIL
checked_at: 2026-08-08T20:15:00Z
duration_ms: None
cost_usd: None
---

<!-- GENERATED FILE—do not hand-edit. Rendered by render-verdict.py
from the verdict JSON, the record of record. Edit the JSON and
re-render instead. -->

## Per-clause results

| clause | severity | description | evidence |
| ------ | -------- | ------------ | -------- |
| T-002 | major | T-002 changes a shared contract and its check_method omits the consumer-suite check. It rewrites four sections of inbox-to-memory/SKILL.md and inserts a line into tests/inbox-to-memory-smoke.sh, but nothing in its check_method ever runs a suite. The smoke suite carries 27 assertions against SKILL.md, and at least two of them sit inside the exact sentences C-10 and C-8 tell this worker to work on. A break lands as a false FAIL on T-004, whose worker is told not to touch other tasks' deliverables. | T-002.md:9-22 lists four checks — three judgment rubrics and C-17's two greps — and no suite invocation. `grep -c 'inbox-to-memory/SKILL.md' tests/inbox-to-memory-smoke.sh` returns 27. Two are directly in the blast radius. :524 `require_text inbox-to-memory/SKILL.md "one sanctioned exception to preserving raw content"` pins the VTT bullet at SKILL.md:459 verbatim — and T-002.md:47 instructs the worker to resolve the two-exceptions collision so "each names the rule it excepts," which invites rewording that exact string. :554 `require_text inbox-to-memory/SKILL.md "**Five body reads per input, and the budget binds.**"` pins SKILL.md:67 verbatim — the sentence T-002.md:43 tells the worker to leave binding while adding a second detection to the same pass; a worker who rewrites it to say so ("Five body reads per input, covering both detections, and the budget binds") reddens the suite and still passes every check T-002 names. C-17's grep #2 is an exact-string match, so the inserted require_text line is itself well pinned; the 27 pre-existing assertions are not. The decompose skill's rule is unconditional: "A task that changes a shared contract—a schema, a template shape, a hook-visible format—cites the constitution's consumer-suite clause in its check_method, so the checker runs every suite that consumes the contract." The fix is one line in T-002's check_method: run `bash tests/inbox-to-memory-smoke.sh` and require exit 0, citing C-16. |
| T-003 | major | T-003 and T-005 both own edits inside _maintenance/inbox-to-memory/EVALS.md, they are siblings in the DAG with no dep between them, and neither task file mentions the other's claim on that file. Worse than the write collision: C-11's ruling on EVALS.md is order-dependent, so T-003's checker can reach opposite verdicts on the same passage depending on whether T-005 has landed. | T-003.md deps: [T-002]; T-005.md deps: [T-002]. Both are dispatchable the moment T-002 completes. T-003's census (T-003.md:61-62, verbatim from constitution.md:412-413) sweeps `_maintenance/inbox-to-memory/`, and I ran it: EVALS.md returns 8 hits, second only to SKILL.md's 10. One is a live C-11 candidate — EVALS.md:34, "Phase 5 is gated, so a scenario that grades a crystallized record needs you to approve the candidates it names at the sign-off prompt, and only those." Question 1 (a reader surprised by git status showing modified _memory/ files) is at least arguable, and question 2 — "is this passage its document's only account of what the skill does to _memory/ on its own" — answers YES before T-005 ships and NO after, because T-005's fifth scenario is an account of exactly that write. T-003's own "Out of scope" (T-003.md:69-75) names migrate-scope.sh:348-349 and the fixtures README but is silent on EVALS.md; T-005's (T-005.md:73-75) names eval-scope.sh, the smoke suite and existing fixtures but is silent on T-003. Two fixes, either sufficient: add T-005 to T-003's deps and say in T-003's excerpt that the EVALS.md hits are judged against a document that already carries the new scenario; or carve `_maintenance/inbox-to-memory/EVALS.md` out of T-003's census scope explicitly and hand the obligation to T-005. The orchestrator caught this exact hazard for SKILL.md (T-003 deps on T-002) and missed the second file where it recurs. |
| T-006 | major | T-006's check_method ends with an assertion no artifact can satisfy: "Also confirm the diff touches only _maintenance/inbox-to-memory/eval-scope.sh." T-006 runs after T-004, so by check time the working tree already carries five tasks' worth of changes. A checker-deterministic reading the instruction literally FAILs a correct artifact. | T-006.md:15-16. T-006 deps: [T-004], and T-004 deps on T-001 and T-002, so at check time `git status --porcelain` shows at minimum inbox-to-memory/SKILL.md, the untracked inbox-to-memory/scripts/stamp-confirmed.sh, tests/inbox-to-memory-smoke.sh, and the 18 archive files under .agent-guild/state/archive/, plus whatever T-003 and T-005 have landed. Git has no per-task attribution, so there is no diff for the checker to isolate — the assertion is not merely hard, it is unsatisfiable as written. This is the mirror of the failure mode the constitution names at :18 ("a check whose pass string prints for a reason other than the property it names"): a check whose fail string prints for a reason other than the property it names. It is also not derived from C-15, which asserts nothing about diff scope; diff scope is C-16's, owned by T-007. Either drop the sentence, or restate it as the property actually intended and checkable — e.g. "confirm eval-scope.sh's diff is confined to the header comment block at :1-9 and no other hunk in that file." |
| T-003 | minor | The shared-contract question the orchestrator raised, ruled: assets/claude-md/_memory.template.md IS a shared contract in the literal sense the decompose skill means — a template shape a suite pins — so T-003 owes the consumer-suite citation. The risk is genuinely low and T-007 backstops it, which is why this is minor rather than a second copy of the T-002 finding. | tests/inbox-to-memory-smoke.sh:375 asserts `require_text "inbox-to-memory/assets/claude-md/$scaffold.template.md" "schema: 2"` in a loop that includes _memory. That is one consuming suite, so the file qualifies. What holds the severity down: C-11's obligation on that file (constitution.md:397) is additive narrative — "accounts for the write-through somewhere in the document" — and does not reach the required-frontmatter block at :12-22 that the assertion pins, so a conforming edit cannot break it. What T-007's sweep does NOT give you is the thing the citation is for: attribution and timing. T-003's checker runs no suite at all (T-003.md:9-22 is a pure rubric), so a break surfaces in T-007's lap, and T-007.md:66 tells that worker "if the repair would mean editing another task's deliverable to change its behavior, that is a finding, not a task: report it and stop" — at which point T-003 is already `complete` and re-opening it is off the documented lifecycle. Same one-line fix as T-002: add `bash tests/inbox-to-memory-smoke.sh`, exit 0, citing C-16. |
| T-006 | minor | Routing: T-006 goes to worker-bulk/haiku, and the work is not zero-judgment. The worker has to invent a citation form that survives future insertions and preserve the comment's voice, which is prose authorship, not a mechanical substitution. | The routing table in .agent-guild/CLAUDE.md gives haiku "Mechanical, zero-judgment work" and sonnet "Clear-spec implementation judged on correctness." T-006.md:38 explicitly forbids the mechanical answer — "do not simply correct the number" — and asks for a rewrite "to a form that does not depend on one," then T-006.md:40 adds "Keep the sentence reading as the same helpful aside it is now." What keeps this at minor: the task enumerates the acceptable forms ("point at the rule by what it says, or by the guard's variable, or by naming the section"), the surface is one sentence in one comment, C-15 is the document's only minor-severity clause, and the retry ladder escalates haiku to sonnet after two failures. Recorded because the table says to route by the work, not the default; not a reason on its own to revise. |
| T-004 | minor | T-004's spec excerpt carries pre-T-002 line numbers for the fixture guard. T-002 inserts a require_text line at :300, so every anchor below it shifts by one before T-004's worker ever opens the file. | T-004.md:46 says the guard "snapshots at :586 and compares at :1299" and T-004.md:54 says "reword the guard's own comment at :1296-1298." Those are correct against the tree today (verified: `fixtures_before=` at tests/inbox-to-memory-smoke.sh:586, the comparison at :1299, the guard comment opening at :1296), but T-004 deps on T-002, whose one deliverable line goes in beside the `scripts/lint-scope.sh` assertion at :300 — above all four anchors. The worker will find :587, :1300 and :1297-1299. Harmless in practice: C-13's placement block computes both bounds dynamically (constitution.md:460-461), and the excerpt tells the worker to locate the guard by content. Worth one word — "approximately," or an instruction to locate by the `fixtures_before=` assignment rather than by number — since this task's whole subject is that a line number is not an anchor. |
| T-007 | minor | C-12's token extraction is far more sensitive than "no new token," and neither T-002 nor T-003 is warned in the terms that would let them avoid tripping it. Any bracketed lowercase phrase added to SKILL.md or machine-contracts.md — an ordinary markdown link, a bracketed aside — enters the inventory and fails C-12 at T-007. | I ran C-12's `inv()` extraction at base. It returns 14 entries, and one of them is `[target]` — prose noise, not a registered token, which proves the pattern `\\[[a-z][a-z0-9 -]*(:\|\\])` catches ordinary bracketed lowercase text. Both files T-002 and T-003 edit are in the loop: inbox-to-memory/SKILL.md (T-002's four sections) and inbox-to-memory/references/machine-contracts.md (T-003's named document-level obligation at constitution.md:396). A sentence like "see [the write-through] below" in either file adds an entry that does not cancel and prints a diff, failing C-12. T-002.md:63 warns only about "a token added to SKILL.md's own candidate-flag block at :95-101" and T-003.md:75 only about "the grammar table, REGISTERED_TOKENS, or note.template.md's token shapes" — both aimed at deliberate tokens, neither at the accidental bracket. T-007's deps are already right; this is a warning gap, not an ordering one. One sentence in each excerpt closes it: no bracketed lowercase text of any kind in these two files, links included, because C-12 extracts on shape rather than on meaning. |
| COVERAGE | pass | All seven acceptance criteria map to at least one task, and no spec requirement is unowned. I walked each criterion to the task that delivers it rather than to the clause that describes it. | spec.md:27-33. AC1 (a confirmation stamps last_confirmed on a v2 record) -> T-001 builds it (C-1, C-6 insertion half, C-7, C-18), T-002 wires it (C-8, C-17), T-004 asserts it (C-13). AC2 (v1 skipped, never gains the field) -> T-001 (C-2, C-18's v1 row), T-004 (C-13's saboteur, which is the only check that falsifies the assertion rather than the behavior). AC3 (every stamped record named in the verify report) -> T-002 (C-9). AC4 (no per-record approval prompt) -> T-002 (C-8, C-10, C-17) and T-005 (C-14's required pass-table row on the absence of a prompt); correctly all judgment, since the absence of a prompt is not scriptable. AC5 (operating rules name the sole sanctioned exception, with reasoning) -> T-002 (C-10). AC6 (confirmed twice in one run, stamped once) -> T-001 (C-5's four cases, C-18's two-group record), T-002 (C-17 requirement 1), T-004 (C-13's required cross-group assertion). AC7 (the smoke test asserts v1 untouched over the mixed fixture) -> T-004 (C-13), the whole task. The two non-criterion requirements in the spec body are also owned: "Records missing the field gain it only if they are v2" -> T-001 (C-6 plus C-2), and "Confirmation is detected by the same pass that detects contradictions" -> T-002 (C-8). |
| CLAUSE-OWNERSHIP | pass | All eighteen clauses are owned, each by exactly one task. No clause is unenforced and none is split across two checkers. I went looking for a functional split — a clause whose satisfaction needs artifacts from two tasks while only one is checked — and did not find one. | T-001 C-1..C-7 and C-18 (8); T-002 C-8, C-9, C-10, C-17 (4); T-003 C-11; T-004 C-13; T-005 C-14; T-006 C-15; T-007 C-12, C-16. Sums to 18, no duplicates, no gaps, matching the eighteen clause headings in constitution.md. C-17 is kept whole in T-002 as intended, and it is the one that would have split: its deterministic half greps SKILL.md (T-002's prose) AND `require_text ... "scripts/stamp-confirmed.sh"` in the smoke suite (constitution.md:605-606), so putting the test line in T-004 would have left T-002's checker unable to pass its own blocker clause. The one that looks like a near-miss and is not: C-13 belongs to T-004 but its part 1 runs the whole suite, which now contains T-002's line — that is a dependency, satisfied by T-004 deps [T-001, T-002], not a split, because T-002's line is not something T-004's checker has to judge. C-14's mechanical half invokes _maintenance/inbox-to-memory/eval-scope.sh, which T-006 edits; I read that script and it takes any fixture name off argv with no registry (eval-scope.sh:16-28), so T-005 is self-sufficient and the two tasks do not share a contract there. |
| ROUTING | pass | Six of seven assignments follow the table exactly, and T-004 — the one the orchestrator was least sure of — is right for a stronger reason than the one given. The seventh, T-006's worker tier, is filed separately as a minor. | T-001 worker-standard/sonnet with checker-deterministic: all eight cited clauses carry runnable bash and none names a rubric, so deterministic is what the table dictates. T-002, T-003, T-005 worker-craft/opus with checker-judgment: SKILL.md prose, the prose census, and an eval fixture needing a plausible client and a negative case with teeth are all taste work, and C-8, C-9, C-10, C-11, C-14 each literally open with `checker-judgment:`. T-007 worker-standard/sonnet with checker-deterministic: C-12 and C-16 are pure scripts. On T-004 specifically: checker-judgment is not merely defensible, it is required — constitution.md:483 writes "Then `checker-judgment`: read the new section" inside C-13's own check, so the clause names its checker family and the routing table's "a clause checked by a rubric routes to checker-judgment" settles it without needing the argument about which half is load-bearing. The orchestrator's reasoning reaches the right answer by the longer road; the shorter one is that C-13 already made the call. The corollary the orchestrator states is also correct and worth keeping on the record: a judgment checker can run bash, so routing a mixed clause to checker-judgment loses nothing, while routing it to checker-deterministic would silently drop the rubric — which is why the mixed clause C-17 also belongs where it is, in T-002 under checker-judgment. |
| CHECK-METHOD | pass | Every clause in every task's `clauses` list appears in that task's `check_method` with a named check. No cited clause is left unverifiable. The defect in T-006 is an extra assertion, not a missing one, and is filed separately. | T-001.md:10-19 names all eight (C-1 interface/dry-run/four malformed invocations, C-2 byte-identical v1, C-3 earlier and equal with exit assertions, C-4 the status skip, C-5 four dedupe cases plus the arity-2 order comparison, C-6 both halves each behind its fixture guard, C-7 diff -r plus before/after lint, C-18 four branches with both anchored counts and the insert-vs-update assertion) — and it is the only check_method in the set that gets the `blocked` semantics right, at :18-19: "A harness whose fixture-integrity guard prints its VOID string is a blocked check, not a pass or a fail." That is exactly the distinction the schema's `blocked` enum exists for, and it is the one thing that keeps six rounds of constitution history from being wasted at check time. T-002.md:9-22 names C-8, C-9, C-10, C-17 with a rubric each. T-003.md:9-22 names C-11 with the sweep, both ordered questions, both pre-rulings and both document-level obligations. T-004.md:9-19 names C-13's four parts in order. T-005.md:9-20 names C-14's rubric plus all four enumeration greps and the :12 decoy. T-007.md:9-19 names C-12 and C-16. |
| DEPS | pass | The graph is acyclic, every referenced task exists, and the five load-bearing edges are each necessary rather than serialization for its own sake. One edge is over-constrained and harmless; the missing edge is filed under T-003. | T-001 [] -> T-002 [T-001] -> {T-003 [T-002], T-004 [T-001,T-002], T-005 [T-002]}, T-006 [T-004], T-007 [T-001..T-006]. Topological, no cycles, all six referenced ids resolve to files. Necessary: T-002 on T-001, because SKILL.md must document the shipped interface and C-17's grep asserts the string exists in it. T-003 on T-002, confirmed for the reason given — the census hits SKILL.md at :26, :138, :168, :361, :455 (I re-ran the sweep: 10 hits in that file), and T-002 rewrites four sections of it. T-004 on T-002, confirmed — both write tests/inbox-to-memory-smoke.sh, T-002 at :300 and T-004 between :586 and :1299. T-004 on T-001, necessary: C-13 part 4's saboteur copies and restores the real stamper. T-007 on everything, by definition. Over-constrained but harmless: T-006 on T-004. T-006's entire deliverable is a citation that does not depend on a line number, so T-004's insertion cannot affect it, and a T-006-before-T-004 ordering would produce zero sweep hits that T-004 could not reintroduce. It is still the right call, because C-15 says "every line-number cross-reference that this job's edits move," and running the check after every move has happened is the honest reading — and because if the worker falls back to a number despite the instruction, at least the number is against the final file. Keep it. Nothing else in the graph is under-constrained except the T-003/T-005 edge filed above. |
| T-001 | pass | T-001 is one dispatch for a sonnet worker, and it should not be split. It is the largest task in the set, but there is no seam that does not cost more than it saves, and two independent auditors have already built the artifact from this same contract in a single pass. | The direct evidence is in CON-audit-r7.md's SHIPPABILITY row: "I transcribed the script contract at constitution.md:53-95 into a working stamper in one pass — the second auditor in a row to do so from the contract alone" — and it then passed C-1 through C-7 and C-18 verbatim. r6 did the same. That is two existence proofs that the deliverable is one-sitting-sized from exactly the inputs T-001 hands its worker. On the seam question: the candidate cuts are argument parsing / stage 1 resolution / stage 2 branching / frontmatter editing, and every one of them produces a first task whose deliverable is an unrunnable fragment that no clause in the constitution can check — C-1 through C-7 and C-18 all invoke the whole script end to end, so a half-script task would have to invent its own acceptance criteria outside the constitution, which is worse than a big task. The orchestrator's own caveat is the decisive one and is correct: C-18 exists precisely because C-2 through C-6 "hold simultaneously, in one run" (constitution.md:619), and its own failing example is a stamper that passes every branch apart and leaks state across them — a defect that only exists at the seam a split would create. The mitigations are already in place: bash 3.2's no-associative-arrays trap is called out at T-001.md:54, the realpath dedupe at :56, the yq round-trip trap at :50, and the retry ladder gives sonnet two attempts before opus gets two more. |
| SPEC-EXCERPTS | pass | I read all seven excerpts cold and could do six of the seven from that section plus the constitution alone. T-001's pointer to "The script contract" is the right call, not a gap. Every anchor I could resolve, I resolved, and they hold. | On T-001's pointer specifically: constitution.md:30 instructs every reader to "read the script contract below, then only your clauses," and T-001.md:32 tells the worker to do exactly that, in full, first. Restating the six-row branch table inside the task file would create a second copy of the one rule the document says must not drift — constitution.md:20 records that the r2 draft "ended up with two clauses contradicting each other" on precisely this rule, and constitution.md:77 says "both C-3 and C-5 describe the same rule from different sides." A paraphrase in a task file is a third side. What T-001 does restate is the right subset: the interface block, the batch rule, the two-stage order with the worked straddle example, and the traps. Anchors verified against the tree: migrate-scope.sh:25 does carry RECORD_KEY_ORDER with `effective_to last_confirmed source_refs` adjacent (T-001.md:60); the mixed/ fixture is as described (T-001.md:61); the 17-key/line-19 arithmetic matches constitution.md:49 (T-001.md:62). T-002's anchors all resolve: SKILL.md:67 is the five-read budget sentence, :69 the accepted-only rule, :175/:347/:399 the three `<skill-path>` invocation examples, :459 the VTT bullet, :95-101 the candidate-flag block, smoke :300 the lint-scope require_text, :600 the migrator precedent. T-005's all resolve: EVALS.md:9 "Four, all under", :12 "Four prior notes", the Grading table at :123-128 with four numbered rows, :130 the delta sentence. T-006's all resolve: eval-scope.sh:6 carries the citation, smoke :1226 is a `# ---` divider inside the "v1 link checking (#32)" banner, and the rule it means is at :1296. The one excerpt with a defect is T-004's, filed above as a minor. Boundary honesty: neither T-002 nor T-003 states outright that T-003 also edits SKILL.md outside T-002's four sections, but T-003.md:65 lists five SKILL.md line numbers in its census and adds "Line numbers will have shifted in SKILL.md since T-002," which is unambiguous enough to act on. |
| T-005 | pass | T-005 is correctly sized, correctly routed, and self-sufficient apart from the EVALS.md collision filed under T-003. Its hardest requirement — the negative case with teeth — is stated in the excerpt rather than left to the checker to discover. | T-005.md:52, "Make the meeting touch its territory without ever restating its claim — someone mentions the system, nobody says the thing the record says," is the operational form of C-14's "is the unconfirmed record close enough to the transcript's subject that a model stamping everything it read would actually stamp it" (constitution.md:532). The four-record table at T-005.md:45-50 matches constitution.md:529 row for row. The new fixture path lands inside C-16's allowlist (`tests/fixtures/inbox-to-memory/evals/`), and the smoke suite's whole-fixture guard takes its snapshot and its comparison inside one run over `fixtures=tests/fixtures/inbox-to-memory` (:12, :586, :1299), so a fixture added between runs is invisible to it — no interaction. Nothing in either suite iterates `evals/`, so the new directory has no consumer beyond eval-scope.sh. |
| T-007 | pass | T-007's scope, deps and tree-state notes are accurate, and its refusal to repair another task's deliverable is the right instinct even though the T-002 and T-003 gaps above are what will send work its way. | I verified the tree note: `git status --porcelain` returns exactly 18 entries, all under .agent-guild/state/archive/*/tasks/, and inbox-to-memory/, tests/ and _maintenance/ are clean. T-007.md:72's claim that the `.agent-guild/` allowlist entry admits them is correct against constitution.md:584. Every file the other six tasks touch falls inside C-16's allowlist: stamp-confirmed.sh by filename, SKILL.md and README.md by filename, references/ and assets/ by prefix, the smoke suite by filename, the evals fixture directory by prefix, EVALS.md and eval-scope.sh by filename. T-007.md:68's reading of why lint-scope.sh is deliberately absent matches constitution.md:587 exactly. The one thing T-007 inherits and cannot fix is C-12's bracket sensitivity, filed above as a minor against the two tasks that can. |

## Diagnosis

- **T-002** (major): T-002 changes a shared contract and its check_method omits the consumer-suite check. It rewrites four sections of inbox-to-memory/SKILL.md and inserts a line into tests/inbox-to-memory-smoke.sh, but nothing in its check_method ever runs a suite. The smoke suite carries 27 assertions against SKILL.md, and at least two of them sit inside the exact sentences C-10 and C-8 tell this worker to work on. A break lands as a false FAIL on T-004, whose worker is told not to touch other tasks' deliverables.
  evidence: T-002.md:9-22 lists four checks — three judgment rubrics and C-17's two greps — and no suite invocation. `grep -c 'inbox-to-memory/SKILL.md' tests/inbox-to-memory-smoke.sh` returns 27. Two are directly in the blast radius. :524 `require_text inbox-to-memory/SKILL.md "one sanctioned exception to preserving raw content"` pins the VTT bullet at SKILL.md:459 verbatim — and T-002.md:47 instructs the worker to resolve the two-exceptions collision so "each names the rule it excepts," which invites rewording that exact string. :554 `require_text inbox-to-memory/SKILL.md "**Five body reads per input, and the budget binds.**"` pins SKILL.md:67 verbatim — the sentence T-002.md:43 tells the worker to leave binding while adding a second detection to the same pass; a worker who rewrites it to say so ("Five body reads per input, covering both detections, and the budget binds") reddens the suite and still passes every check T-002 names. C-17's grep #2 is an exact-string match, so the inserted require_text line is itself well pinned; the 27 pre-existing assertions are not. The decompose skill's rule is unconditional: "A task that changes a shared contract—a schema, a template shape, a hook-visible format—cites the constitution's consumer-suite clause in its check_method, so the checker runs every suite that consumes the contract." The fix is one line in T-002's check_method: run `bash tests/inbox-to-memory-smoke.sh` and require exit 0, citing C-16.
- **T-003** (major): T-003 and T-005 both own edits inside _maintenance/inbox-to-memory/EVALS.md, they are siblings in the DAG with no dep between them, and neither task file mentions the other's claim on that file. Worse than the write collision: C-11's ruling on EVALS.md is order-dependent, so T-003's checker can reach opposite verdicts on the same passage depending on whether T-005 has landed.
  evidence: T-003.md deps: [T-002]; T-005.md deps: [T-002]. Both are dispatchable the moment T-002 completes. T-003's census (T-003.md:61-62, verbatim from constitution.md:412-413) sweeps `_maintenance/inbox-to-memory/`, and I ran it: EVALS.md returns 8 hits, second only to SKILL.md's 10. One is a live C-11 candidate — EVALS.md:34, "Phase 5 is gated, so a scenario that grades a crystallized record needs you to approve the candidates it names at the sign-off prompt, and only those." Question 1 (a reader surprised by git status showing modified _memory/ files) is at least arguable, and question 2 — "is this passage its document's only account of what the skill does to _memory/ on its own" — answers YES before T-005 ships and NO after, because T-005's fifth scenario is an account of exactly that write. T-003's own "Out of scope" (T-003.md:69-75) names migrate-scope.sh:348-349 and the fixtures README but is silent on EVALS.md; T-005's (T-005.md:73-75) names eval-scope.sh, the smoke suite and existing fixtures but is silent on T-003. Two fixes, either sufficient: add T-005 to T-003's deps and say in T-003's excerpt that the EVALS.md hits are judged against a document that already carries the new scenario; or carve `_maintenance/inbox-to-memory/EVALS.md` out of T-003's census scope explicitly and hand the obligation to T-005. The orchestrator caught this exact hazard for SKILL.md (T-003 deps on T-002) and missed the second file where it recurs.
- **T-006** (major): T-006's check_method ends with an assertion no artifact can satisfy: "Also confirm the diff touches only _maintenance/inbox-to-memory/eval-scope.sh." T-006 runs after T-004, so by check time the working tree already carries five tasks' worth of changes. A checker-deterministic reading the instruction literally FAILs a correct artifact.
  evidence: T-006.md:15-16. T-006 deps: [T-004], and T-004 deps on T-001 and T-002, so at check time `git status --porcelain` shows at minimum inbox-to-memory/SKILL.md, the untracked inbox-to-memory/scripts/stamp-confirmed.sh, tests/inbox-to-memory-smoke.sh, and the 18 archive files under .agent-guild/state/archive/, plus whatever T-003 and T-005 have landed. Git has no per-task attribution, so there is no diff for the checker to isolate — the assertion is not merely hard, it is unsatisfiable as written. This is the mirror of the failure mode the constitution names at :18 ("a check whose pass string prints for a reason other than the property it names"): a check whose fail string prints for a reason other than the property it names. It is also not derived from C-15, which asserts nothing about diff scope; diff scope is C-16's, owned by T-007. Either drop the sentence, or restate it as the property actually intended and checkable — e.g. "confirm eval-scope.sh's diff is confined to the header comment block at :1-9 and no other hunk in that file."
- **T-003** (minor): The shared-contract question the orchestrator raised, ruled: assets/claude-md/_memory.template.md IS a shared contract in the literal sense the decompose skill means — a template shape a suite pins — so T-003 owes the consumer-suite citation. The risk is genuinely low and T-007 backstops it, which is why this is minor rather than a second copy of the T-002 finding.
  evidence: tests/inbox-to-memory-smoke.sh:375 asserts `require_text "inbox-to-memory/assets/claude-md/$scaffold.template.md" "schema: 2"` in a loop that includes _memory. That is one consuming suite, so the file qualifies. What holds the severity down: C-11's obligation on that file (constitution.md:397) is additive narrative — "accounts for the write-through somewhere in the document" — and does not reach the required-frontmatter block at :12-22 that the assertion pins, so a conforming edit cannot break it. What T-007's sweep does NOT give you is the thing the citation is for: attribution and timing. T-003's checker runs no suite at all (T-003.md:9-22 is a pure rubric), so a break surfaces in T-007's lap, and T-007.md:66 tells that worker "if the repair would mean editing another task's deliverable to change its behavior, that is a finding, not a task: report it and stop" — at which point T-003 is already `complete` and re-opening it is off the documented lifecycle. Same one-line fix as T-002: add `bash tests/inbox-to-memory-smoke.sh`, exit 0, citing C-16.
- **T-006** (minor): Routing: T-006 goes to worker-bulk/haiku, and the work is not zero-judgment. The worker has to invent a citation form that survives future insertions and preserve the comment's voice, which is prose authorship, not a mechanical substitution.
  evidence: The routing table in .agent-guild/CLAUDE.md gives haiku "Mechanical, zero-judgment work" and sonnet "Clear-spec implementation judged on correctness." T-006.md:38 explicitly forbids the mechanical answer — "do not simply correct the number" — and asks for a rewrite "to a form that does not depend on one," then T-006.md:40 adds "Keep the sentence reading as the same helpful aside it is now." What keeps this at minor: the task enumerates the acceptable forms ("point at the rule by what it says, or by the guard's variable, or by naming the section"), the surface is one sentence in one comment, C-15 is the document's only minor-severity clause, and the retry ladder escalates haiku to sonnet after two failures. Recorded because the table says to route by the work, not the default; not a reason on its own to revise.
- **T-004** (minor): T-004's spec excerpt carries pre-T-002 line numbers for the fixture guard. T-002 inserts a require_text line at :300, so every anchor below it shifts by one before T-004's worker ever opens the file.
  evidence: T-004.md:46 says the guard "snapshots at :586 and compares at :1299" and T-004.md:54 says "reword the guard's own comment at :1296-1298." Those are correct against the tree today (verified: `fixtures_before=` at tests/inbox-to-memory-smoke.sh:586, the comparison at :1299, the guard comment opening at :1296), but T-004 deps on T-002, whose one deliverable line goes in beside the `scripts/lint-scope.sh` assertion at :300 — above all four anchors. The worker will find :587, :1300 and :1297-1299. Harmless in practice: C-13's placement block computes both bounds dynamically (constitution.md:460-461), and the excerpt tells the worker to locate the guard by content. Worth one word — "approximately," or an instruction to locate by the `fixtures_before=` assignment rather than by number — since this task's whole subject is that a line number is not an anchor.
- **T-007** (minor): C-12's token extraction is far more sensitive than "no new token," and neither T-002 nor T-003 is warned in the terms that would let them avoid tripping it. Any bracketed lowercase phrase added to SKILL.md or machine-contracts.md — an ordinary markdown link, a bracketed aside — enters the inventory and fails C-12 at T-007.
  evidence: I ran C-12's `inv()` extraction at base. It returns 14 entries, and one of them is `[target]` — prose noise, not a registered token, which proves the pattern `\[[a-z][a-z0-9 -]*(:|\])` catches ordinary bracketed lowercase text. Both files T-002 and T-003 edit are in the loop: inbox-to-memory/SKILL.md (T-002's four sections) and inbox-to-memory/references/machine-contracts.md (T-003's named document-level obligation at constitution.md:396). A sentence like "see [the write-through] below" in either file adds an entry that does not cancel and prints a diff, failing C-12. T-002.md:63 warns only about "a token added to SKILL.md's own candidate-flag block at :95-101" and T-003.md:75 only about "the grammar table, REGISTERED_TOKENS, or note.template.md's token shapes" — both aimed at deliberate tokens, neither at the accidental bracket. T-007's deps are already right; this is a warning gap, not an ordering one. One sentence in each excerpt closes it: no bracketed lowercase text of any kind in these two files, links included, because C-12 extracts on shape rather than on meaning.
- **COVERAGE** (pass): All seven acceptance criteria map to at least one task, and no spec requirement is unowned. I walked each criterion to the task that delivers it rather than to the clause that describes it.
  evidence: spec.md:27-33. AC1 (a confirmation stamps last_confirmed on a v2 record) -> T-001 builds it (C-1, C-6 insertion half, C-7, C-18), T-002 wires it (C-8, C-17), T-004 asserts it (C-13). AC2 (v1 skipped, never gains the field) -> T-001 (C-2, C-18's v1 row), T-004 (C-13's saboteur, which is the only check that falsifies the assertion rather than the behavior). AC3 (every stamped record named in the verify report) -> T-002 (C-9). AC4 (no per-record approval prompt) -> T-002 (C-8, C-10, C-17) and T-005 (C-14's required pass-table row on the absence of a prompt); correctly all judgment, since the absence of a prompt is not scriptable. AC5 (operating rules name the sole sanctioned exception, with reasoning) -> T-002 (C-10). AC6 (confirmed twice in one run, stamped once) -> T-001 (C-5's four cases, C-18's two-group record), T-002 (C-17 requirement 1), T-004 (C-13's required cross-group assertion). AC7 (the smoke test asserts v1 untouched over the mixed fixture) -> T-004 (C-13), the whole task. The two non-criterion requirements in the spec body are also owned: "Records missing the field gain it only if they are v2" -> T-001 (C-6 plus C-2), and "Confirmation is detected by the same pass that detects contradictions" -> T-002 (C-8).
- **CLAUSE-OWNERSHIP** (pass): All eighteen clauses are owned, each by exactly one task. No clause is unenforced and none is split across two checkers. I went looking for a functional split — a clause whose satisfaction needs artifacts from two tasks while only one is checked — and did not find one.
  evidence: T-001 C-1..C-7 and C-18 (8); T-002 C-8, C-9, C-10, C-17 (4); T-003 C-11; T-004 C-13; T-005 C-14; T-006 C-15; T-007 C-12, C-16. Sums to 18, no duplicates, no gaps, matching the eighteen clause headings in constitution.md. C-17 is kept whole in T-002 as intended, and it is the one that would have split: its deterministic half greps SKILL.md (T-002's prose) AND `require_text ... "scripts/stamp-confirmed.sh"` in the smoke suite (constitution.md:605-606), so putting the test line in T-004 would have left T-002's checker unable to pass its own blocker clause. The one that looks like a near-miss and is not: C-13 belongs to T-004 but its part 1 runs the whole suite, which now contains T-002's line — that is a dependency, satisfied by T-004 deps [T-001, T-002], not a split, because T-002's line is not something T-004's checker has to judge. C-14's mechanical half invokes _maintenance/inbox-to-memory/eval-scope.sh, which T-006 edits; I read that script and it takes any fixture name off argv with no registry (eval-scope.sh:16-28), so T-005 is self-sufficient and the two tasks do not share a contract there.
- **ROUTING** (pass): Six of seven assignments follow the table exactly, and T-004 — the one the orchestrator was least sure of — is right for a stronger reason than the one given. The seventh, T-006's worker tier, is filed separately as a minor.
  evidence: T-001 worker-standard/sonnet with checker-deterministic: all eight cited clauses carry runnable bash and none names a rubric, so deterministic is what the table dictates. T-002, T-003, T-005 worker-craft/opus with checker-judgment: SKILL.md prose, the prose census, and an eval fixture needing a plausible client and a negative case with teeth are all taste work, and C-8, C-9, C-10, C-11, C-14 each literally open with `checker-judgment:`. T-007 worker-standard/sonnet with checker-deterministic: C-12 and C-16 are pure scripts. On T-004 specifically: checker-judgment is not merely defensible, it is required — constitution.md:483 writes "Then `checker-judgment`: read the new section" inside C-13's own check, so the clause names its checker family and the routing table's "a clause checked by a rubric routes to checker-judgment" settles it without needing the argument about which half is load-bearing. The orchestrator's reasoning reaches the right answer by the longer road; the shorter one is that C-13 already made the call. The corollary the orchestrator states is also correct and worth keeping on the record: a judgment checker can run bash, so routing a mixed clause to checker-judgment loses nothing, while routing it to checker-deterministic would silently drop the rubric — which is why the mixed clause C-17 also belongs where it is, in T-002 under checker-judgment.
- **CHECK-METHOD** (pass): Every clause in every task's `clauses` list appears in that task's `check_method` with a named check. No cited clause is left unverifiable. The defect in T-006 is an extra assertion, not a missing one, and is filed separately.
  evidence: T-001.md:10-19 names all eight (C-1 interface/dry-run/four malformed invocations, C-2 byte-identical v1, C-3 earlier and equal with exit assertions, C-4 the status skip, C-5 four dedupe cases plus the arity-2 order comparison, C-6 both halves each behind its fixture guard, C-7 diff -r plus before/after lint, C-18 four branches with both anchored counts and the insert-vs-update assertion) — and it is the only check_method in the set that gets the `blocked` semantics right, at :18-19: "A harness whose fixture-integrity guard prints its VOID string is a blocked check, not a pass or a fail." That is exactly the distinction the schema's `blocked` enum exists for, and it is the one thing that keeps six rounds of constitution history from being wasted at check time. T-002.md:9-22 names C-8, C-9, C-10, C-17 with a rubric each. T-003.md:9-22 names C-11 with the sweep, both ordered questions, both pre-rulings and both document-level obligations. T-004.md:9-19 names C-13's four parts in order. T-005.md:9-20 names C-14's rubric plus all four enumeration greps and the :12 decoy. T-007.md:9-19 names C-12 and C-16.
- **DEPS** (pass): The graph is acyclic, every referenced task exists, and the five load-bearing edges are each necessary rather than serialization for its own sake. One edge is over-constrained and harmless; the missing edge is filed under T-003.
  evidence: T-001 [] -> T-002 [T-001] -> {T-003 [T-002], T-004 [T-001,T-002], T-005 [T-002]}, T-006 [T-004], T-007 [T-001..T-006]. Topological, no cycles, all six referenced ids resolve to files. Necessary: T-002 on T-001, because SKILL.md must document the shipped interface and C-17's grep asserts the string exists in it. T-003 on T-002, confirmed for the reason given — the census hits SKILL.md at :26, :138, :168, :361, :455 (I re-ran the sweep: 10 hits in that file), and T-002 rewrites four sections of it. T-004 on T-002, confirmed — both write tests/inbox-to-memory-smoke.sh, T-002 at :300 and T-004 between :586 and :1299. T-004 on T-001, necessary: C-13 part 4's saboteur copies and restores the real stamper. T-007 on everything, by definition. Over-constrained but harmless: T-006 on T-004. T-006's entire deliverable is a citation that does not depend on a line number, so T-004's insertion cannot affect it, and a T-006-before-T-004 ordering would produce zero sweep hits that T-004 could not reintroduce. It is still the right call, because C-15 says "every line-number cross-reference that this job's edits move," and running the check after every move has happened is the honest reading — and because if the worker falls back to a number despite the instruction, at least the number is against the final file. Keep it. Nothing else in the graph is under-constrained except the T-003/T-005 edge filed above.
- **T-001** (pass): T-001 is one dispatch for a sonnet worker, and it should not be split. It is the largest task in the set, but there is no seam that does not cost more than it saves, and two independent auditors have already built the artifact from this same contract in a single pass.
  evidence: The direct evidence is in CON-audit-r7.md's SHIPPABILITY row: "I transcribed the script contract at constitution.md:53-95 into a working stamper in one pass — the second auditor in a row to do so from the contract alone" — and it then passed C-1 through C-7 and C-18 verbatim. r6 did the same. That is two existence proofs that the deliverable is one-sitting-sized from exactly the inputs T-001 hands its worker. On the seam question: the candidate cuts are argument parsing / stage 1 resolution / stage 2 branching / frontmatter editing, and every one of them produces a first task whose deliverable is an unrunnable fragment that no clause in the constitution can check — C-1 through C-7 and C-18 all invoke the whole script end to end, so a half-script task would have to invent its own acceptance criteria outside the constitution, which is worse than a big task. The orchestrator's own caveat is the decisive one and is correct: C-18 exists precisely because C-2 through C-6 "hold simultaneously, in one run" (constitution.md:619), and its own failing example is a stamper that passes every branch apart and leaks state across them — a defect that only exists at the seam a split would create. The mitigations are already in place: bash 3.2's no-associative-arrays trap is called out at T-001.md:54, the realpath dedupe at :56, the yq round-trip trap at :50, and the retry ladder gives sonnet two attempts before opus gets two more.
- **SPEC-EXCERPTS** (pass): I read all seven excerpts cold and could do six of the seven from that section plus the constitution alone. T-001's pointer to "The script contract" is the right call, not a gap. Every anchor I could resolve, I resolved, and they hold.
  evidence: On T-001's pointer specifically: constitution.md:30 instructs every reader to "read the script contract below, then only your clauses," and T-001.md:32 tells the worker to do exactly that, in full, first. Restating the six-row branch table inside the task file would create a second copy of the one rule the document says must not drift — constitution.md:20 records that the r2 draft "ended up with two clauses contradicting each other" on precisely this rule, and constitution.md:77 says "both C-3 and C-5 describe the same rule from different sides." A paraphrase in a task file is a third side. What T-001 does restate is the right subset: the interface block, the batch rule, the two-stage order with the worked straddle example, and the traps. Anchors verified against the tree: migrate-scope.sh:25 does carry RECORD_KEY_ORDER with `effective_to last_confirmed source_refs` adjacent (T-001.md:60); the mixed/ fixture is as described (T-001.md:61); the 17-key/line-19 arithmetic matches constitution.md:49 (T-001.md:62). T-002's anchors all resolve: SKILL.md:67 is the five-read budget sentence, :69 the accepted-only rule, :175/:347/:399 the three `<skill-path>` invocation examples, :459 the VTT bullet, :95-101 the candidate-flag block, smoke :300 the lint-scope require_text, :600 the migrator precedent. T-005's all resolve: EVALS.md:9 "Four, all under", :12 "Four prior notes", the Grading table at :123-128 with four numbered rows, :130 the delta sentence. T-006's all resolve: eval-scope.sh:6 carries the citation, smoke :1226 is a `# ---` divider inside the "v1 link checking (#32)" banner, and the rule it means is at :1296. The one excerpt with a defect is T-004's, filed above as a minor. Boundary honesty: neither T-002 nor T-003 states outright that T-003 also edits SKILL.md outside T-002's four sections, but T-003.md:65 lists five SKILL.md line numbers in its census and adds "Line numbers will have shifted in SKILL.md since T-002," which is unambiguous enough to act on.
- **T-005** (pass): T-005 is correctly sized, correctly routed, and self-sufficient apart from the EVALS.md collision filed under T-003. Its hardest requirement — the negative case with teeth — is stated in the excerpt rather than left to the checker to discover.
  evidence: T-005.md:52, "Make the meeting touch its territory without ever restating its claim — someone mentions the system, nobody says the thing the record says," is the operational form of C-14's "is the unconfirmed record close enough to the transcript's subject that a model stamping everything it read would actually stamp it" (constitution.md:532). The four-record table at T-005.md:45-50 matches constitution.md:529 row for row. The new fixture path lands inside C-16's allowlist (`tests/fixtures/inbox-to-memory/evals/`), and the smoke suite's whole-fixture guard takes its snapshot and its comparison inside one run over `fixtures=tests/fixtures/inbox-to-memory` (:12, :586, :1299), so a fixture added between runs is invisible to it — no interaction. Nothing in either suite iterates `evals/`, so the new directory has no consumer beyond eval-scope.sh.
- **T-007** (pass): T-007's scope, deps and tree-state notes are accurate, and its refusal to repair another task's deliverable is the right instinct even though the T-002 and T-003 gaps above are what will send work its way.
  evidence: I verified the tree note: `git status --porcelain` returns exactly 18 entries, all under .agent-guild/state/archive/*/tasks/, and inbox-to-memory/, tests/ and _maintenance/ are clean. T-007.md:72's claim that the `.agent-guild/` allowlist entry admits them is correct against constitution.md:584. Every file the other six tasks touch falls inside C-16's allowlist: stamp-confirmed.sh by filename, SKILL.md and README.md by filename, references/ and assets/ by prefix, the smoke suite by filename, the evals fixture directory by prefix, EVALS.md and eval-scope.sh by filename. T-007.md:68's reading of why lint-scope.sh is deliberately absent matches constitution.md:587 exactly. The one thing T-007 inherits and cannot fix is C-12's bracket sensitivity, filed above as a minor against the two tasks that can.
