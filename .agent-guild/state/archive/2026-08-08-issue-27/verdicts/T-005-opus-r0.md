---
task: T-005
checker: checker-judgment
vendor: anthropic
model: claude-opus-5[1m]
verdict: PASS
checked_at: 2026-08-08T21:40:00Z
duration_ms: None
cost_usd: None
---

<!-- GENERATED FILE—do not hand-edit. Rendered by render-verdict.py
from the verdict JSON, the record of record. Edit the JSON and
re-render instead. -->

## Per-clause results

| clause | severity | description | evidence |
| ------ | -------- | ------------ | -------- |
| C-14 | pass | The fixture sits at the pinned path, stages through eval-scope.sh, lints at failures: 0, and holds exactly the four records the clause names with the statuses and generations it names: an accepted v2 carrying last_confirmed: 2026-05-19, an accepted v1 with no schema key, an accepted v2 with no last_confirmed, and a proposed v2. | Ran `staged="$(bash _maintenance/inbox-to-memory/eval-scope.sh confirmation-writethrough)"; bash inbox-to-memory/scripts/lint-scope.sh "$staged" \| grep -E '^(failures\|v1 files\|v2 files)'` -> `v1 files: 1` / `v2 files: 6` / `failures: 0`. Then `grep -H '^status:\\\|^schema:\\\|^last_confirmed:' "$staged"/_memory/*/*.md` -> harrow-route-sequence-mr1b_3yB4S.md: `schema: 2`, `status: accepted` (no last_confirmed); harrow-write-window-kQ2MiN7kOA.md: `schema: 2`, `status: accepted`, `last_confirmed: 2026-05-19`; handsets-not-tablets-Ho0R3o1JGL.md: `status: accepted` only, no schema line; paper-worksheet-until-close-Y_fVVkE5D8.md: `schema: 2`, `status: proposed`. A recursive grep for `last_confirmed` over the whole staged scope returns exactly one hit, matching EVALS.md:15's claim. |
| C-14 | pass | The negative case has real teeth. harrow-route-sequence-mr1b_3yB4S claims Harrow orders stops by meter route code and that drive time is not an input; the meeting spends its middle on who builds the day's run, names the same overnight job the just-confirmed record turns on, and has Fiona pitching a drive-time optimiser -- yet nobody states the record's claim. A model stamping everything it opened at phase 2.5 would stamp this one. | Record claim, fixture/_memory/context/harrow-route-sequence-mr1b_3yB4S.md:23: "The overnight run orders each crew's stops by the meter route code held on the asset... Drive time, traffic, and the distance between two consecutive stops are not inputs and never have been." Transcript's nearest approach, _inbox/2026-06-16-ferndale-pilot-review.md:23-27: "who is building the day's run after the pilot?" / "It sequences on drive time, it re-optimises when a job overruns" (Fiona pitching Quillon's module, not describing Harrow) / "What I do not want is two systems both deciding what a crew does at nine in the morning." The bait is compounded by :15, "It is shut in that window while the overnight run rebuilds the day" -- the same overnight job as the record that DOES get stamped. No line asserts the sort key or denies drive time as an input, so the record is genuinely unconfirmed; the row is neither a straw negative nor a mis-set expectation. |
| C-14 | pass | The two positive cases and the proposed case are equally unambiguous: the transcript restates the write-window record and the v1 handsets decision in as many words, and says the proposed record's title back almost exactly, so no row turns on whether a mention counts. | Write-window record :24 "Harrow shuts to writes at 01:00 and reopens at 04:00 while the overnight run rebuilds the day. A push arriving inside that window is not rejected -- it queues, and it lands at 04:00. The window has been in place since Harrow went in" vs transcript :15 "Harrow does not take writes between one and four in the morning. It is shut in that window while the overnight run rebuilds the day, it has been shut in that window since it went in, and anything Tanager pushes into it queues and lands at four." v1 record title "Crews run Tanager on the handsets they already carry" vs transcript :11 "We are not buying tablets. That was settled in March and it stays settled. Tanager runs on the phones the crews already have." Proposed record title "The paper worksheet stays in the van until the pilot closes" vs transcript :33 "The book stays in the van until we close the pilot." |
| C-14 | pass | The pass table's predictions match what the shipped stamper actually does. Staged a second copy, synthesized the 2026-06-16 groomed note the run would write, and invoked stamp-confirmed.sh with the three records phase 2.5 would judge confirmed; the output and the resulting tree reproduce rows 1, 2, 4 and 5 line for line, including "every other line in the file is unchanged." | `bash inbox-to-memory/scripts/stamp-confirmed.sh "$S" --note notes/2026-06-16-...-ZZtestZZ01.md <write-window> <handsets> <paper-worksheet>` -> `stamped: _memory/context/harrow-write-window-kQ2MiN7kOA.md 2026-05-19 -> 2026-06-16` / `skipped: _memory/decisions/handsets-not-tablets-Ho0R3o1JGL.md (no schema key, v1)` / `skipped: _memory/decisions/paper-worksheet-until-close-Y_fVVkE5D8.md (status: proposed, not accepted)` / `stamped: 1 skipped: 2`, exit=0. Diffing every record against the pristine fixture: IDENTICAL for route-sequence, handsets and paper-worksheet; write-window differs on line 8 only (`8c8 < last_confirmed: 2026-05-19 --- > last_confirmed: 2026-06-16`). SKILL.md:212 confirms the phase 6 skipped list is scoped to "what the stamper turned down, not everything phase 2.5 opened," so the phase 6 row's "harrow-route-sequence is in neither list" is a statement about shipped behavior, not a guess. |
| C-14 | pass | Every row is binary and settles without an argument, matching the bar the four scenarios above it set. Four rows are byte-identity or a single grepped line off the staged copy; the phase 6 row reads off the stamper's own sorted output; the no-prompt row is an observed fact about a run the grader conducted. The explanatory clauses appended to two rows are rationale, not additional conditions. | EVALS.md:130-137, the six rows. Row 1 settles by `grep '^last_confirmed:'` plus a diff; rows 2, 3 and 4 by `diff` against the fixture plus the absence of a `last_confirmed:` line and, for row 3, the absence of a `[contradicts accepted: ...]` flag in the groomed note the run writes into the staged copy. EVALS.md:145 is the standing bar: "Every row is binary on purpose. A row that needs an argument to settle was specified badly." Consistency also holds: EVALS.md:141 says "Four of these six rows go green for a run that writes nothing into `_memory/`" and the four are exactly rows 2, 3, 4 and 6. |
| C-14 | pass | The baseline paragraph is as concrete as scenario 1's: it names what the model produces instead of the correct output, and ends on the same byte-identity assertion scenario 1 uses. | EVALS.md:139: "Expect Deshawn's one-to-four window written up as something this meeting established, under a findings or constraints heading of the model's own choosing, and the tablet line filed as a decision this meeting took rather than one it repeated. Diff all four records afterwards and every one is byte-identical, with `harrow-write-window-kQ2MiN7kOA` still reading `last_confirmed: 2026-05-19` and still the only file in the scope carrying the key." Compare scenario 1's baseline at EVALS.md:57, which has the same two-part shape (concrete misfiling, then a byte-identical diff assertion naming the frontmatter that did not move). |
| C-14 | pass | All four enumerations moved together, plus the document's opening sentence, and :12's "Four prior notes" is untouched. | `grep -c '^Four, all under'` -> 0; `grep -c '^Five, all under'` -> 1; `grep -c 'confirmation-writethrough'` -> 2 (EVALS.md:15 fixture bullet, :122 scenario header); `grep -c '^\| [0-9]\\.'` -> 5. EVALS.md:12 still reads "- **`unacknowledged-tension`** -- Meridian Freight's Dockside pilot at the Bexley Road terminal. Four prior notes...". The closing sentence at :155 was revisited rather than left stale: "Scenario 5 has scenario 1's baseline and a much narrower delta, since four of its six rows pass for a run that never opens `_memory/` at all." The intro at :3 also gained "and telling a confirmation apart from a mention." Scope is clean: `git diff --name-status 924f81f` shows eval-scope.sh unmodified and no existing fixture touched. |
| C-14 | observation | Non-blocking, recorded rather than charged: EVALS.md:128 says "Every row below settles off the staged copy except the phase 6 one," but the no-prompt row at :137 also settles off the run rather than off the copy. The sentence's operative instruction -- keep the run's report -- covers both, and the clause itself mandates a row about the absence of a prompt, which no staged copy can hold, so nothing here makes a row arguable or unsettleable. | EVALS.md:128 "Every row below settles off the staged copy except the phase 6 one, so keep the run's report" against EVALS.md:137 "\| No prompt \| Nothing in the run asks whether to move a date, and no `last_confirmed` appears among the candidates put up at the phase 5 gate \|". C-14's text requires the table to cover "the absence of any per-record prompt," so the row is required and its evidence necessarily comes from the run. |
