---
task: CON-audit
checker: auditor
vendor: anthropic
model: claude-opus-5[1m]
verdict: FAIL
checked_at: 2026-08-09T02:40:00Z
duration_ms: None
cost_usd: None
---

<!-- GENERATED FILE—do not hand-edit. Rendered by render-verdict.py
from the verdict JSON, the record of record. Edit the JSON and
re-render instead. -->

## Per-clause results

| clause | severity | description | evidence |
| ------ | -------- | ------------ | -------- |
| C-5 | major | The blocker's headline requirement — 'the run's output is byte-identical whichever order the groups were passed in' — is violated by a conforming stamper as soon as a run names two records, and C-5's own check runs at arity 1, the one arity where the requirement costs nothing. r2 filed this as a minor; r3 hardened 'identical' to 'byte-identical' and left the check where it was, so the gap between what the text demands and what any check reaches is now wider than it was. | constitution.md:183 'In every case the run's output is byte-identical whichever order the groups were passed in.' The check at :186-217 pins R=_memory/context/atlas-region-topology-mPmy8XBe5H.md and never names a second record, so first-encounter output order is unobservable. Demonstrated against the conforming reference stamper I built for this audit (the same one that passes C-1 through C-7 verbatim): two groups, two records, swapped. Order AB -> 'stamped: ...atlas-region-topology 2026-02-10 -> 2026-02-17' then 'skipped: ...freeze-window... (v1 record, no schema key)' then 'stamped: 1 skipped: 1'. Order BA -> the skipped line first, then the stamped line, same closing count. Byte-different. The contract at :49-55 pins the two line formats and the closing count but specifies no output ordering, and ':49 Every outcome prints one line per distinct record' actively suggests first-encounter order. So the natural implementation fails a blocker's text while passing that blocker's check, and a judgment checker constructing the obvious multi-record case fails a correct artifact. |
| C-13 | major | The placement check's anchors are a bare grep for 'stamp-confirmed' over the whole smoke suite, which also matches the require_text line C-17 separately mandates and the guard failure message C-13 itself tells the worker to reword. Two blocker clauses are coupled through one grep with no cross-reference between them, and the coupling produces a FAIL on a correct artifact. | constitution.md:418-421 'first=$(grep -n \\'stamp-confirmed\\' tests/inbox-to-memory-smoke.sh \| head -1 ...)' / 'last=$(... \| tail -1 ...)' and ':421 [ "$snap" -lt "$first" ] && [ "$last" -lt "$cmp" ]'. C-17 at :549 requires 'require_text inbox-to-memory/SKILL.md "scripts/stamp-confirmed.sh"' to exist somewhere in that file. The nearest precedent for a script-wiring assertion is tests/inbox-to-memory-smoke.sh:300, 'require_text inbox-to-memory/SKILL.md "scripts/lint-scope.sh"', which sits at :300 — outside the guard, whose snapshot is at :586. Ran the placement block verbatim against a synthetic correct artifact (wiring grep at :301, stamping section at :702): 'snap=587 first=301 last=702 cmp=1301' -> 'OUTSIDE THE GUARD'. The identical artifact with the wiring grep at :601 instead (C-17's stated precedent, :600) gives 'snap=586 first=601 last=703 cmp=1301' -> 'inside the guard'. Second exposure on the other side: C-13's own text at :410 says 'The guard's comment says "a migration test"; it now also covers stamping tests and must say so.' That string occurs twice, at :1296 (comment, before the anchor) and at :1300 ('a migration test wrote to a checked-in fixture', after cmp=1299). Reworded the message to name the script, as the clause asks: 'snap=586 first=601 last=1302 cmp=1301' -> 'OUTSIDE THE GUARD'. Both failures are false FAILs on correct work. |
| C-6 | major | The fourth pass-string-for-the-wrong-reason. The r3 guard 'fixture intact and still v2' asserts two properties the insertion half does not depend on (nonempty, schema: 2) and omits the one the mutation exists to establish (the record no longer carries last_confirmed). Because the unmutated fixture already carries last_confirmed at exactly the contract position the clause asks the inserted key to land in, a mutation that silently fails to apply leaves every single downstream assertion satisfied and the whole half vacuous. It does not fire today — the re.sub as written works — but the guard is one grep short of being exact, and this is the fourth round in which a check's setup has not been verified to do what the check claims it did. | constitution.md:239-240 '[ -s "$R" ] && /usr/bin/grep -q \\'^schema: 2\\' "$R" && echo "fixture intact and still v2"'. The fixture at tests/fixtures/inbox-to-memory/mixed/_memory/context/atlas-region-topology-mPmy8XBe5H.md has date: at :7, last_confirmed: 2026-02-10 at :8, source_refs: at :9. Ran the insertion half with the mutation replaced by a no-op, everything else verbatim, against the conforming reference stamper: 'fixture intact and still v2', 'stamped: ... 2026-02-10 -> 2026-02-17', 'exit=0', sed 1,20p showing 'last_confirmed: 2026-02-17' directly after 'date: 2026-01-13' and directly before 'source_refs:', and 'failures: 0' — i.e. constitution.md:249's stated pass condition, verbatim, from a run in which no insertion happened and a replace-only stamper would score identically. The fix is one predicate: '! /usr/bin/grep -q \\'^last_confirmed:\\' "$R"' alongside the two already there. |
| STRUCTURE | major | At 567 lines and 8,653 words the document is longer than it was when r2 filed the same observation, and the structural cause of the two blockers r2 found is still in place: the two-stage resolution rule is normative prose sitting outside every clause, with no check of its own, that five clauses lean on. No, a worker or checker will not read it whole — a checker-deterministic on haiku holds a task file and a code block, not 567 lines. The passage most likely to be skimmed past is the resolution rule at :59-64 itself, and the evidence that the skim has already happened is inside the document: C-6's insertion path is governed by stage 2 and never names it, and C-9's skip enumeration does not match stage 2's. This is a finding, not a style note — the rule that exists to keep clauses from drifting apart is the one thing in the document nothing verifies. | wc -l -w -c .agent-guild/state/constitution.md -> 567 8653 58646, against r2's 530 / 7368 / 50923: r3 closed two blockers and added 37 lines and 1,285 words. constitution.md:59-64 is the two-stage rule; it is prose under '## The script contract', has no 'check:' field, no severity, and no failing example, yet C-1 (:76), C-3 (:129), C-5 (:176-183), C-6 (:227) and C-9 (:340) all decide against it. C-3 and C-5 name it explicitly; C-6 and C-9 do not, and both diverge from it (see the two minors below). The fix that would actually hold: promote the resolution rule to its own clause with one harness exercising every stage-2 branch — later, equal, earlier, missing key, v1, non-accepted, over budget — so the unifying rule is itself falsifiable instead of being background five clauses quote from memory. |
| SCRIPT-CONTRACT | minor | Stage 2's decision sentence has no branch for a record that carries no last_confirmed at all, which is the entire subject of C-6 and one of the two write paths the script has. Read literally it says such a record is skipped, since nothing can be strictly later than a value that is absent. The document as a whole resolves it — C-6 says the record 'gains it' and C-1's output format carries '<old-or-none>' — but the passage whose stated purpose is to be the one rule the clauses cannot drift from is missing the branch one of them owns. | constitution.md:62 'Stamp it if the candidate date is strictly later than the record's current `last_confirmed`, and skip it otherwise — along with the v1, non-`accepted`, and over-budget skips.' Compare :227 (C-6) 'A `schema: 2`, `status: accepted` record with no `last_confirmed` gains it' and :52 'stamped: <record-path> <old-or-none> -> <new>'. C-3 at :129 is careful here — it says 'its existing `last_confirmed`', which correctly scopes the monotonic rule to records that have one — so the omission is only in the summary rule. One sentence closes it: a resolved candidate for a record with no `last_confirmed` is always a stamp, subject to the budget. |
| C-9 | minor | C-9's enumeration of the stamper's skip reasons says 'already current', which names only the equal case; stage 2 and C-3 skip on 'earlier than or equal'. A phase 6 report built to C-9's list has no category for the backward case, which is the one C-3 calls a blocker and the one the monotonic guard exists for. | constitution.md:340 'which it then skipped as v1, non-`accepted`, already current, or over budget' against :62 'strictly later ... and skip it otherwise' and :129 'earlier than or equal to its existing `last_confirmed`'. Observed in the reference stamper's own output, which is conforming: 'skipped: ...atlas-region-topology (2025-12-02 is not later than 2026-02-10)' — a record that is not 'already current' in any reading of that phrase. This is the same stale-enumeration defect C-14 exists to police in EVALS.md. |
| C-5 | minor | 'fixture intact' is the same shape as the C-6 guard above, one notch less dangerous. '[ -s "$S/$R" ]' catches the r2 truncation exactly and catches nothing else, so it does not establish the precondition case 2 is defined by — that both group dates are later than the record's current value. | constitution.md:198 '[ -s "$S/$R" ] && echo "fixture intact"'. Ran the case-2 block with the mutation to 2026-01-20 replaced by a no-op, leaving the record at its fixture value of 2026-02-10, against the conforming reference stamper: 'fixture intact', 'stamped: ... 2026-02-10 -> 2026-02-17', 'order-independent', grep -c = 1, 'last_confirmed: 2026-02-17'. Every stated pass condition of case 2 holds, but group A (2026-02-10) is now equal rather than later, so the case actually exercised is case 3's straddle and not case 2 at all. '/usr/bin/grep -q \\'^last_confirmed: 2026-01-20\\'' is the exact guard. |
| C-2 | minor | Skip-reason precedence is unpinned for a record that trips two gates at once. A v1 record whose status is not `accepted` must, by C-2's text, be reported with a reason naming its generation and, by C-4's text, with a reason naming its status; stage 2 lists both skips without ordering them and the contract allows exactly one output line per record. Unlike the r2 C-3/C-5 defect this is satisfiable — a reason naming both settles it — so it is a gap rather than a contradiction, and neither check constructs the case. | constitution.md:105 (C-2) 'reported as skipped with a reason naming its generation' vs :150 (C-4) 'reported as skipped with a reason naming its status' vs :62 'skip it otherwise — along with the v1, non-`accepted`, and over-budget skips. One output line either way.' C-2's check builds the mixed fixture's v1 record, which is `status: accepted`; C-4's check builds a v2 record and flips it to `proposed`. The reference stamper prints only the generation reason for a record that is both, which satisfies C-2 and violates C-4's text. |
| C-14 | minor | One of the four enumeration greps has no stated expected value, so it asserts nothing. The clause pins the fixture path name and says 'every reference to it uses that exact name', but the check that counts those references carries no floor, unlike the three greps around it which all carry an inline expected count. | constitution.md:485 '/usr/bin/grep -c \\'confirmation-writethrough\\' _maintenance/inbox-to-memory/EVALS.md' — no trailing comment, and the prose at :489 names the Four/Five move and the five Grading rows but never this count. Ran it at 924f81f: 0. A scenario satisfying the clause needs the name in its heading, in the fixture list under :9, and in the Grading row, so the floor is 3; 1 would pass the check as written. The three siblings at :483, :484 and :486 all carry '# 0', '# 1', '# 5'. |
| C-3 | pass | The r2 blocker is genuinely closed, and closed at the level that prevents recurrence rather than clause by clause. C-3 is now stated against the resolved candidate, the losing group is explicitly given no skip line, and the r2 minor about the missing exit assertion is closed too. | constitution.md:129 'When a record's resolved candidate date — the latest note date among the groups naming it, per stage 1 of the resolution rule — is earlier than or equal to its existing `last_confirmed` ... An individual group whose date loses at stage 1 produces no skip line of its own; it produced no candidate.' This is the same rule C-5 case 3 states from the other side, so the r2 contradiction has no input left to live on. Ran the check verbatim against the conforming reference stamper: 'skipped: ...(2025-12-02 is not later than 2026-02-10)' / 'stamped: 0 skipped: 1' / 'exit=0', then '(2026-02-10 is not later than 2026-02-10)' / 'stamped: 0 skipped: 1' / 'exit=0', then 'held'. The exit assertions the r2 minor asked for are present at :138. |
| C-5 | pass | The r2 truncation blocker is closed and the straddle it exposed is now a first-class case with a check that reaches it. I ran the whole block verbatim against the conforming reference stamper and it discriminates. | constitution.md:192-197 reads into 't' on its own line before opening the file for writing, the form C-4 always had. Ran the full check verbatim: 'fixture intact', 'stamped: ...atlas-region-topology 2026-01-20 -> 2026-02-17', 'stamped: 1 skipped: 0', 'order-independent', grep -c = 1, 'last_confirmed: 2026-02-17'; the straddle at :208-211 printed 'stamped: ...2026-02-10 -> 2026-02-17' / 'stamped: 1 skipped: 0' with grep -c = 1 and no skipped line for the December group; cases 1 and 4 at :213-215 printed 1. The realpath trap still bites — mktemp -d returned /var/folders/... whose real path is /private/var/folders/... — so case 4 still separates a string dedupe from a realpath one. The two findings above are about the clause's order-independence sentence and its fixture guard, not about this. |
| RESOLUTION-RULE | pass | I attacked the two-stage rule directly and could not make stages 1 and 2 disagree. It is well-founded by construction: stage 1 never consults record state, so its output is a pure function of the argument list, and stage 2 is a pure function of (candidate, record state). Both attacks the dispatch brief proposed fail on inspection, and I confirmed why. | constitution.md:61-64. 'A record named by two groups where one is v1-skipped and the other is not' cannot arise: generation is a property of the record, not of the group naming it, so both groups resolve to the same record and stage 2 makes one v1 decision. 'The resolved candidate is over budget but a losing group's date would not have been' cannot arise either: the budget test is a line count (lint-scope.sh:40 FRONTMATTER_LINE_BUDGET=20, :177 'closing --- on line $end'), every ISO date is ten characters, and the inserted line count is one regardless of value — the budget outcome is date-independent. The only stage-2 branch the rule's own sentence does not name is the missing-key case, filed as a minor above. Verified the bash 3.2.57 constraint the baseline names is real and surmountable: the reference stamper does the keyed max-date dedupe with parallel indexed arrays and a linear scan, no declare -A, and runs clean. |
| C-1 | pass | Runs clean against a conforming batch-scoped stamper, and both r2 gaps are closed: 'dry-run reported the stamp' now excludes the silent stamper, and the zero-group hole is pinned on both sides — no group at all, and a group naming no records. | Ran the check verbatim against the reference stamper: 'dry-run wrote nothing', 'dry-run reported the stamp', 'exit=0', 'dry and wet output identical', 'stamped: _memory/context/atlas-region-topology-mPmy8XBe5H.md 2026-02-10 -> 2026-02-17', 'stamped: 1 skipped: 0', 'last_confirmed: 2026-02-17'; then 'no such record: ...' bad-record exit=2, 'no such note: ...' bad-note exit=2, 'record path before any --note: ...' no-group exit=2, '--note group names no records: ...' empty-group exit=2. Note-path resolution is now pinned to the same rule as record paths at :49, which closes the r2 observation that every check passes notes relative to the scope root while the checker's cwd is the repo root. |
| C-2 | pass | Unchanged and still the sharpest script clause. Ran verbatim against the reference stamper. | 'skipped: _memory/decisions/freeze-window-owned-by-ops-ocPwdpeY0a.md (v1 record, no schema key)', 'stamped: _memory/context/atlas-region-topology-mPmy8XBe5H.md 2026-02-10 -> 2026-02-17', 'stamped: 1 skipped: 1', 'exit=0', 'v1 untouched'. The fixture still has the shape the clause needs: no schema key, status: accepted, no last_confirmed. |
| C-4 | pass | Runs clean and its mutation is still the correct read-then-write form the other two clauses now copy. | Ran verbatim against the reference stamper: 'skipped: ...atlas-region-topology-mPmy8XBe5H.md (status: proposed, not accepted)', 'stamped: 0 skipped: 1', 'exit=0', 'held'. |
| C-6 | pass | The r2 truncation blocker is closed and both halves discriminate against a conforming stamper. The budget recipe's arithmetic is exactly right and I re-derived its premise from the shipped contract rather than taking the baseline's word. | Insertion half, run verbatim: 'fixture intact and still v2', 'stamped: ... none -> 2026-02-17', 'stamped: 1 skipped: 0', 'exit=0', the key landing at :8 directly after 'date: 2026-01-13' and directly before 'source_refs:', and 'failures: 0'. Budget half, run verbatim: 'failures: 0' on the probe scope, then 'skipped: _memory/context/budget-probe-AAAAAAAAAA.md (frontmatter budget: inserting last_confirmed closes --- on line 21, past the 20-line budget)', 'stamped: 0 skipped: 1', 'exit=0', 'held'. The baseline's 18-key claim at :33 checks out against references/machine-contracts.md:29, which says the record order 'lists 19 names ... but `tags` and `themes` are mutually exclusive, so a record carries at most 18 and closes on exactly line 20 ... One commented-out key puts a full record over', and the comment claim checks out too: context, decision, exception, policy-rule and rule templates carry four frontmatter comments each, journal-entry carries two. |
| C-7 | pass | Runs clean, and passing the v1 and v2 records in one invocation makes the diff prove the run touched one file out of the two it was handed. | Ran verbatim against the reference stamper: diff -r reported one file and one line pair, '8c8 < last_confirmed: 2026-02-10 --- > last_confirmed: 2026-02-17', and 'before=failures: 0 after=failures: 0'. |
| C-8 | pass | Unchanged, and its anchors still resolve. Four independently checkable propositions plus a concrete structural failure mode. | constitution.md:333-334. The budget sentence is verbatim at SKILL.md:67 and the accepted-only rule at SKILL.md:69; phase 2.5's per-input loop opens at SKILL.md:59. The clause still hands placement to C-17 rather than duplicating it. |
| C-10 | pass | Unchanged and still the best rubric in the document. The two-exceptions collision test is anchored on a line that really exists and really closes the list. | SKILL.md:459 '**VTT raw content is collapsed, not verbatim.** ... This is the one sanctioned exception to preserving raw content exactly as captured.' Operating Rules heading at SKILL.md:450, list ends at :459. |
| C-11 | pass | Re-derived the sweep rather than reading the list. All twelve floor locations resolve, the two ordered questions are the right discriminator, and the document-level obligation on _memory.template.md still holds its shape against the :65 pre-ruling. | Ran the sweep at constitution.md:374-376 verbatim: 60 distinct hits. The twelve named all resolve — SKILL.md:26, :138, :168, :361, :455; README.md:74; assets/claude-md/_memory.template.md:21 and :65; client.template.md:71; project.template.md:55; references/migration.md:37; references/retrieval-funnel.md:40. inbox-to-memory/scripts/migrate-scope.sh:348-349 is exactly the reasoning comment the clause says it is ('A record nobody has re-confirmed was last confirmed the day it was written. / Any later date would assert a review that never happened.') and sits outside C-16's allowlist, which the last non-goal governs. The r2 observation about journal.template.md:23 is still live and still an observation, not a defect. |
| C-12 | pass | Unchanged, still runs clean, and still immune to the commit-first vacuity that C-16 only warns about. | Ran the check verbatim at 924f81f: no diff, 'inventory exit=0'. All four enumerating files are in the loop. The cosmetic overstatement r2 noted at C-16's failure string ('THIS CHECK AND C-12 ARE BOTH VACUOUS') survives into r3 at :516 and is still wrong about C-12, which pins the literal base SHA rather than HEAD. |
| C-13 | pass | The saboteur is sound and I confirmed it behaves exactly as the clause claims, and the placement check still fails safe on zero hits. The major above is about the anchors' coupling to C-17, not about either of these. | Installed the saboteur from constitution.md:433-455 over the conforming reference stamper and ran it on a copy of mixed/. Output byte-identical to the real run ('skipped: ...freeze-window... (v1 record, no schema key)', 'stamped: ...atlas-region-topology 2026-02-10 -> 2026-02-17', 'stamped: 1 skipped: 1', exit 0) and the v1 record then carried 'last_confirmed: 2026-02-17' at :7 — real behavior in every respect except the v1 hold, so a red suite means the hold is tested. Placement block run at 924f81f: 'snap=586 first= last= cmp=1299' -> 'OUTSIDE THE GUARD', failing safe on empty anchors. Guard bounds re-derived directly: fixtures_before assigned at tests/inbox-to-memory-smoke.sh:586, compared at :1299, file ends at :1309. |
| C-14 | pass | The r2 unsatisfiable-check blocker is closed and closed precisely. The anchor separates the two 'Four's exactly as the clause claims. | Ran the enumeration greps at 924f81f: '^Four, all under' -> 1 (EVALS.md:9, 'Four, all under `tests/fixtures/inbox-to-memory/evals/`'), '^Five, all under' -> 0, '^\| [0-9]\\.' -> 4 (the Grading rows at :125-128). EVALS.md:12's 'Four prior notes, two open-question slugs live in all four' is untouched by the anchor because 'Four' is mid-line there. The table spans :123-128 and the closing claim is at :130 as the clause says. |
| C-15 | pass | Unchanged, still runs, still returns exactly the one hit the clause predicts, and the migrate precedent it polices is now cited correctly. | Ran the sweep verbatim: one hit, '_maintenance//inbox-to-memory/eval-scope.sh:6:inbox-to-memory-smoke.sh:1226', against a rule that actually lives at tests/inbox-to-memory-smoke.sh:1296-1298. The r2 BASELINE minor is closed: 'require_text inbox-to-memory/SKILL.md "scripts/migrate-scope.sh"' is at :600 and constitution.md:29 now says :600. |
| C-16 | pass | Runs clean, and the allowlist correctly admits the untracked stamper while keeping lint-scope.sh out. | Ran the check verbatim with the reference stamper in place: 'suites exit=0', 'HEAD is the base', 'OK: 1 path(s) in scope', 'scope exit=0'. git rev-parse HEAD = 924f81f7d4c9794968a2954f8152dc6ed53cc9df. The untracked stamper is picked up by check-diff-scope.py, so the allowlist entry is load-bearing rather than decorative. Removed the reference stamper afterwards; git status --porcelain is empty. |
| C-17 | pass | The r2 major is closed the honest way. r3 stopped claiming batch scope forces the boundary, split the clause into two derived requirements and one declared convention, and — this is the part that makes it checkable — requires SKILL.md not to dress the convention up as a derivation. That is a settleable question, not a matter of taste: a checker reads phase 4, sees it disposes sources and opens no gate, and can tell whether the prose claims otherwise. The zero-confirmation hole r2 filed is closed alongside it. | constitution.md:544 'This is a **convention, not a derivation**, and SKILL.md must not dress it up as one. The close of phase 3 and the close of phase 4 both satisfy every constraint the script imposes — phase 4 only disposes sources and opens no gate.' Confirmed against SKILL.md's actual structure, which is what r2's finding turned on: :24 'Six phases, plus a 2.5', :59 phase 2.5 'For each inbox file, in order:', :83 phase 3 'Emit one note per input', :128-134 phase 4 'Dispose Source'. Requirement 2 at :543 ('The call is made only when phase 2.5 found at least one confirmation') is genuinely forced by C-1's zero-group error and closes the r2 major. The deterministic half at :548-549 fails safe today: grep for 'scripts/stamp-confirmed.sh' in SKILL.md returns nothing. |
| COVERAGE | pass | All seven acceptance criteria map to clauses that would catch their violation, and I re-walked each rather than inheriting r2's table. AC6 in particular is now sound end to end, which it was not in r1 and was contradictory in r2. | spec.md:27-33 walked against the shipped document. AC1 (stamps a v2 record) -> C-1 (ran it: 2026-02-10 -> 2026-02-17, exit 0), C-6 (insertion path, ran it), C-7 (nothing else moves, ran it), C-8 (detection), C-13 (smoke), C-17 (SKILL.md calls it). AC2 (v1 skipped, never gains the field) -> C-2 (ran it: 'v1 untouched') and C-13's saboteur, which I installed and confirmed reddens for a v1-write reason rather than an output-shape one. AC3 (every stamped record named in the report) -> C-9's judgment rubric, with the stamper's per-record output lines pinned by the contract at :52-54 as the raw material; C-9 goes further than AC3 by also requiring the declined list. AC4 (no per-record approval prompts) -> C-8 ('the resulting stamp does not pass through phase 5'), C-17 (the call sits before phase 5), the contract's 'It writes by default. No `--apply`' at :66, C-10, and C-14's required pass-table row on the absence of any per-record prompt. All judgment, which is right: the absence of a prompt in prose is not scriptable. AC5 (operating rules name the sole exception with reasoning) -> C-10, whose VTT collision anchor at SKILL.md:459 I verified. AC6 (confirmed twice in one run, stamped once) -> the batch contract at :47, C-5 cases 1-4, C-17 requirement 1, C-13's required cross-group smoke assertion. Ran C-5 whole: one stamped line, the later date, order-independent at arity 1, straddle collapsing to one line. AC7 (smoke asserts the mixed fixture leaves v1 untouched) -> C-13's headline plus saboteur plus placement, with C-2 supplying the content. The two majors on C-5 and C-13 are defects in how AC6's and AC7's clauses are worded and anchored, not gaps in coverage. |
| NON-GOALS | pass | Unchanged and still sound. None of the six silently excludes an acceptance criterion, and the migrate-scope.sh escape hatch still resolves the collision with C-11's sweep. | constitution.md:560-567 walked against spec.md:27-33. The C-11 sweep does reach inbox-to-memory/scripts/migrate-scope.sh (hits at :25, :350, :352, reasoning comment at :348-349), it is outside C-16's allowlist, and :567 rules it a finding to surface rather than a task to do. tests/fixtures/inbox-to-memory/README.md:3's 'Three scopes' above four bullets is correctly held as unrelated. |
| PROTECTED | pass | Correct and unchanged: no verbatim author words ship in this job, so there is no manifest to parse. | constitution.md:556-558. The job's surface is a shell script, SKILL.md and README.md prose, a smoke-test section, and an eval fixture. |

## Diagnosis

- **C-5** (major): The blocker's headline requirement — 'the run's output is byte-identical whichever order the groups were passed in' — is violated by a conforming stamper as soon as a run names two records, and C-5's own check runs at arity 1, the one arity where the requirement costs nothing. r2 filed this as a minor; r3 hardened 'identical' to 'byte-identical' and left the check where it was, so the gap between what the text demands and what any check reaches is now wider than it was.
  evidence: constitution.md:183 'In every case the run's output is byte-identical whichever order the groups were passed in.' The check at :186-217 pins R=_memory/context/atlas-region-topology-mPmy8XBe5H.md and never names a second record, so first-encounter output order is unobservable. Demonstrated against the conforming reference stamper I built for this audit (the same one that passes C-1 through C-7 verbatim): two groups, two records, swapped. Order AB -> 'stamped: ...atlas-region-topology 2026-02-10 -> 2026-02-17' then 'skipped: ...freeze-window... (v1 record, no schema key)' then 'stamped: 1  skipped: 1'. Order BA -> the skipped line first, then the stamped line, same closing count. Byte-different. The contract at :49-55 pins the two line formats and the closing count but specifies no output ordering, and ':49 Every outcome prints one line per distinct record' actively suggests first-encounter order. So the natural implementation fails a blocker's text while passing that blocker's check, and a judgment checker constructing the obvious multi-record case fails a correct artifact.
- **C-13** (major): The placement check's anchors are a bare grep for 'stamp-confirmed' over the whole smoke suite, which also matches the require_text line C-17 separately mandates and the guard failure message C-13 itself tells the worker to reword. Two blocker clauses are coupled through one grep with no cross-reference between them, and the coupling produces a FAIL on a correct artifact.
  evidence: constitution.md:418-421 'first=$(grep -n \'stamp-confirmed\' tests/inbox-to-memory-smoke.sh | head -1 ...)' / 'last=$(... | tail -1 ...)' and ':421 [ "$snap" -lt "$first" ] && [ "$last" -lt "$cmp" ]'. C-17 at :549 requires 'require_text inbox-to-memory/SKILL.md "scripts/stamp-confirmed.sh"' to exist somewhere in that file. The nearest precedent for a script-wiring assertion is tests/inbox-to-memory-smoke.sh:300, 'require_text inbox-to-memory/SKILL.md "scripts/lint-scope.sh"', which sits at :300 — outside the guard, whose snapshot is at :586. Ran the placement block verbatim against a synthetic correct artifact (wiring grep at :301, stamping section at :702): 'snap=587 first=301 last=702 cmp=1301' -> 'OUTSIDE THE GUARD'. The identical artifact with the wiring grep at :601 instead (C-17's stated precedent, :600) gives 'snap=586 first=601 last=703 cmp=1301' -> 'inside the guard'. Second exposure on the other side: C-13's own text at :410 says 'The guard's comment says "a migration test"; it now also covers stamping tests and must say so.' That string occurs twice, at :1296 (comment, before the anchor) and at :1300 ('a migration test wrote to a checked-in fixture', after cmp=1299). Reworded the message to name the script, as the clause asks: 'snap=586 first=601 last=1302 cmp=1301' -> 'OUTSIDE THE GUARD'. Both failures are false FAILs on correct work.
- **C-6** (major): The fourth pass-string-for-the-wrong-reason. The r3 guard 'fixture intact and still v2' asserts two properties the insertion half does not depend on (nonempty, schema: 2) and omits the one the mutation exists to establish (the record no longer carries last_confirmed). Because the unmutated fixture already carries last_confirmed at exactly the contract position the clause asks the inserted key to land in, a mutation that silently fails to apply leaves every single downstream assertion satisfied and the whole half vacuous. It does not fire today — the re.sub as written works — but the guard is one grep short of being exact, and this is the fourth round in which a check's setup has not been verified to do what the check claims it did.
  evidence: constitution.md:239-240 '[ -s "$R" ] && /usr/bin/grep -q \'^schema: 2\' "$R" && echo "fixture intact and still v2"'. The fixture at tests/fixtures/inbox-to-memory/mixed/_memory/context/atlas-region-topology-mPmy8XBe5H.md has date: at :7, last_confirmed: 2026-02-10 at :8, source_refs: at :9. Ran the insertion half with the mutation replaced by a no-op, everything else verbatim, against the conforming reference stamper: 'fixture intact and still v2', 'stamped: ... 2026-02-10 -> 2026-02-17', 'exit=0', sed 1,20p showing 'last_confirmed: 2026-02-17' directly after 'date: 2026-01-13' and directly before 'source_refs:', and 'failures: 0' — i.e. constitution.md:249's stated pass condition, verbatim, from a run in which no insertion happened and a replace-only stamper would score identically. The fix is one predicate: '! /usr/bin/grep -q \'^last_confirmed:\' "$R"' alongside the two already there.
- **STRUCTURE** (major): At 567 lines and 8,653 words the document is longer than it was when r2 filed the same observation, and the structural cause of the two blockers r2 found is still in place: the two-stage resolution rule is normative prose sitting outside every clause, with no check of its own, that five clauses lean on. No, a worker or checker will not read it whole — a checker-deterministic on haiku holds a task file and a code block, not 567 lines. The passage most likely to be skimmed past is the resolution rule at :59-64 itself, and the evidence that the skim has already happened is inside the document: C-6's insertion path is governed by stage 2 and never names it, and C-9's skip enumeration does not match stage 2's. This is a finding, not a style note — the rule that exists to keep clauses from drifting apart is the one thing in the document nothing verifies.
  evidence: wc -l -w -c .agent-guild/state/constitution.md -> 567 8653 58646, against r2's 530 / 7368 / 50923: r3 closed two blockers and added 37 lines and 1,285 words. constitution.md:59-64 is the two-stage rule; it is prose under '## The script contract', has no 'check:' field, no severity, and no failing example, yet C-1 (:76), C-3 (:129), C-5 (:176-183), C-6 (:227) and C-9 (:340) all decide against it. C-3 and C-5 name it explicitly; C-6 and C-9 do not, and both diverge from it (see the two minors below). The fix that would actually hold: promote the resolution rule to its own clause with one harness exercising every stage-2 branch — later, equal, earlier, missing key, v1, non-accepted, over budget — so the unifying rule is itself falsifiable instead of being background five clauses quote from memory.
- **SCRIPT-CONTRACT** (minor): Stage 2's decision sentence has no branch for a record that carries no last_confirmed at all, which is the entire subject of C-6 and one of the two write paths the script has. Read literally it says such a record is skipped, since nothing can be strictly later than a value that is absent. The document as a whole resolves it — C-6 says the record 'gains it' and C-1's output format carries '<old-or-none>' — but the passage whose stated purpose is to be the one rule the clauses cannot drift from is missing the branch one of them owns.
  evidence: constitution.md:62 'Stamp it if the candidate date is strictly later than the record's current `last_confirmed`, and skip it otherwise — along with the v1, non-`accepted`, and over-budget skips.' Compare :227 (C-6) 'A `schema: 2`, `status: accepted` record with no `last_confirmed` gains it' and :52 'stamped: <record-path> <old-or-none> -> <new>'. C-3 at :129 is careful here — it says 'its existing `last_confirmed`', which correctly scopes the monotonic rule to records that have one — so the omission is only in the summary rule. One sentence closes it: a resolved candidate for a record with no `last_confirmed` is always a stamp, subject to the budget.
- **C-9** (minor): C-9's enumeration of the stamper's skip reasons says 'already current', which names only the equal case; stage 2 and C-3 skip on 'earlier than or equal'. A phase 6 report built to C-9's list has no category for the backward case, which is the one C-3 calls a blocker and the one the monotonic guard exists for.
  evidence: constitution.md:340 'which it then skipped as v1, non-`accepted`, already current, or over budget' against :62 'strictly later ... and skip it otherwise' and :129 'earlier than or equal to its existing `last_confirmed`'. Observed in the reference stamper's own output, which is conforming: 'skipped: ...atlas-region-topology (2025-12-02 is not later than 2026-02-10)' — a record that is not 'already current' in any reading of that phrase. This is the same stale-enumeration defect C-14 exists to police in EVALS.md.
- **C-5** (minor): 'fixture intact' is the same shape as the C-6 guard above, one notch less dangerous. '[ -s "$S/$R" ]' catches the r2 truncation exactly and catches nothing else, so it does not establish the precondition case 2 is defined by — that both group dates are later than the record's current value.
  evidence: constitution.md:198 '[ -s "$S/$R" ] && echo "fixture intact"'. Ran the case-2 block with the mutation to 2026-01-20 replaced by a no-op, leaving the record at its fixture value of 2026-02-10, against the conforming reference stamper: 'fixture intact', 'stamped: ... 2026-02-10 -> 2026-02-17', 'order-independent', grep -c = 1, 'last_confirmed: 2026-02-17'. Every stated pass condition of case 2 holds, but group A (2026-02-10) is now equal rather than later, so the case actually exercised is case 3's straddle and not case 2 at all. '/usr/bin/grep -q \'^last_confirmed: 2026-01-20\'' is the exact guard.
- **C-2** (minor): Skip-reason precedence is unpinned for a record that trips two gates at once. A v1 record whose status is not `accepted` must, by C-2's text, be reported with a reason naming its generation and, by C-4's text, with a reason naming its status; stage 2 lists both skips without ordering them and the contract allows exactly one output line per record. Unlike the r2 C-3/C-5 defect this is satisfiable — a reason naming both settles it — so it is a gap rather than a contradiction, and neither check constructs the case.
  evidence: constitution.md:105 (C-2) 'reported as skipped with a reason naming its generation' vs :150 (C-4) 'reported as skipped with a reason naming its status' vs :62 'skip it otherwise — along with the v1, non-`accepted`, and over-budget skips. One output line either way.' C-2's check builds the mixed fixture's v1 record, which is `status: accepted`; C-4's check builds a v2 record and flips it to `proposed`. The reference stamper prints only the generation reason for a record that is both, which satisfies C-2 and violates C-4's text.
- **C-14** (minor): One of the four enumeration greps has no stated expected value, so it asserts nothing. The clause pins the fixture path name and says 'every reference to it uses that exact name', but the check that counts those references carries no floor, unlike the three greps around it which all carry an inline expected count.
  evidence: constitution.md:485 '/usr/bin/grep -c \'confirmation-writethrough\' _maintenance/inbox-to-memory/EVALS.md' — no trailing comment, and the prose at :489 names the Four/Five move and the five Grading rows but never this count. Ran it at 924f81f: 0. A scenario satisfying the clause needs the name in its heading, in the fixture list under :9, and in the Grading row, so the floor is 3; 1 would pass the check as written. The three siblings at :483, :484 and :486 all carry '# 0', '# 1', '# 5'.
- **C-3** (pass): The r2 blocker is genuinely closed, and closed at the level that prevents recurrence rather than clause by clause. C-3 is now stated against the resolved candidate, the losing group is explicitly given no skip line, and the r2 minor about the missing exit assertion is closed too.
  evidence: constitution.md:129 'When a record's resolved candidate date — the latest note date among the groups naming it, per stage 1 of the resolution rule — is earlier than or equal to its existing `last_confirmed` ... An individual group whose date loses at stage 1 produces no skip line of its own; it produced no candidate.' This is the same rule C-5 case 3 states from the other side, so the r2 contradiction has no input left to live on. Ran the check verbatim against the conforming reference stamper: 'skipped: ...(2025-12-02 is not later than 2026-02-10)' / 'stamped: 0  skipped: 1' / 'exit=0', then '(2026-02-10 is not later than 2026-02-10)' / 'stamped: 0  skipped: 1' / 'exit=0', then 'held'. The exit assertions the r2 minor asked for are present at :138.
- **C-5** (pass): The r2 truncation blocker is closed and the straddle it exposed is now a first-class case with a check that reaches it. I ran the whole block verbatim against the conforming reference stamper and it discriminates.
  evidence: constitution.md:192-197 reads into 't' on its own line before opening the file for writing, the form C-4 always had. Ran the full check verbatim: 'fixture intact', 'stamped: ...atlas-region-topology 2026-01-20 -> 2026-02-17', 'stamped: 1  skipped: 0', 'order-independent', grep -c = 1, 'last_confirmed: 2026-02-17'; the straddle at :208-211 printed 'stamped: ...2026-02-10 -> 2026-02-17' / 'stamped: 1  skipped: 0' with grep -c = 1 and no skipped line for the December group; cases 1 and 4 at :213-215 printed 1. The realpath trap still bites — mktemp -d returned /var/folders/... whose real path is /private/var/folders/... — so case 4 still separates a string dedupe from a realpath one. The two findings above are about the clause's order-independence sentence and its fixture guard, not about this.
- **RESOLUTION-RULE** (pass): I attacked the two-stage rule directly and could not make stages 1 and 2 disagree. It is well-founded by construction: stage 1 never consults record state, so its output is a pure function of the argument list, and stage 2 is a pure function of (candidate, record state). Both attacks the dispatch brief proposed fail on inspection, and I confirmed why.
  evidence: constitution.md:61-64. 'A record named by two groups where one is v1-skipped and the other is not' cannot arise: generation is a property of the record, not of the group naming it, so both groups resolve to the same record and stage 2 makes one v1 decision. 'The resolved candidate is over budget but a losing group's date would not have been' cannot arise either: the budget test is a line count (lint-scope.sh:40 FRONTMATTER_LINE_BUDGET=20, :177 'closing --- on line $end'), every ISO date is ten characters, and the inserted line count is one regardless of value — the budget outcome is date-independent. The only stage-2 branch the rule's own sentence does not name is the missing-key case, filed as a minor above. Verified the bash 3.2.57 constraint the baseline names is real and surmountable: the reference stamper does the keyed max-date dedupe with parallel indexed arrays and a linear scan, no declare -A, and runs clean.
- **C-1** (pass): Runs clean against a conforming batch-scoped stamper, and both r2 gaps are closed: 'dry-run reported the stamp' now excludes the silent stamper, and the zero-group hole is pinned on both sides — no group at all, and a group naming no records.
  evidence: Ran the check verbatim against the reference stamper: 'dry-run wrote nothing', 'dry-run reported the stamp', 'exit=0', 'dry and wet output identical', 'stamped: _memory/context/atlas-region-topology-mPmy8XBe5H.md 2026-02-10 -> 2026-02-17', 'stamped: 1  skipped: 0', 'last_confirmed: 2026-02-17'; then 'no such record: ...' bad-record exit=2, 'no such note: ...' bad-note exit=2, 'record path before any --note: ...' no-group exit=2, '--note group names no records: ...' empty-group exit=2. Note-path resolution is now pinned to the same rule as record paths at :49, which closes the r2 observation that every check passes notes relative to the scope root while the checker's cwd is the repo root.
- **C-2** (pass): Unchanged and still the sharpest script clause. Ran verbatim against the reference stamper.
  evidence: 'skipped: _memory/decisions/freeze-window-owned-by-ops-ocPwdpeY0a.md (v1 record, no schema key)', 'stamped: _memory/context/atlas-region-topology-mPmy8XBe5H.md 2026-02-10 -> 2026-02-17', 'stamped: 1  skipped: 1', 'exit=0', 'v1 untouched'. The fixture still has the shape the clause needs: no schema key, status: accepted, no last_confirmed.
- **C-4** (pass): Runs clean and its mutation is still the correct read-then-write form the other two clauses now copy.
  evidence: Ran verbatim against the reference stamper: 'skipped: ...atlas-region-topology-mPmy8XBe5H.md (status: proposed, not accepted)', 'stamped: 0  skipped: 1', 'exit=0', 'held'.
- **C-6** (pass): The r2 truncation blocker is closed and both halves discriminate against a conforming stamper. The budget recipe's arithmetic is exactly right and I re-derived its premise from the shipped contract rather than taking the baseline's word.
  evidence: Insertion half, run verbatim: 'fixture intact and still v2', 'stamped: ... none -> 2026-02-17', 'stamped: 1  skipped: 0', 'exit=0', the key landing at :8 directly after 'date: 2026-01-13' and directly before 'source_refs:', and 'failures: 0'. Budget half, run verbatim: 'failures: 0' on the probe scope, then 'skipped: _memory/context/budget-probe-AAAAAAAAAA.md (frontmatter budget: inserting last_confirmed closes --- on line 21, past the 20-line budget)', 'stamped: 0  skipped: 1', 'exit=0', 'held'. The baseline's 18-key claim at :33 checks out against references/machine-contracts.md:29, which says the record order 'lists 19 names ... but `tags` and `themes` are mutually exclusive, so a record carries at most 18 and closes on exactly line 20 ... One commented-out key puts a full record over', and the comment claim checks out too: context, decision, exception, policy-rule and rule templates carry four frontmatter comments each, journal-entry carries two.
- **C-7** (pass): Runs clean, and passing the v1 and v2 records in one invocation makes the diff prove the run touched one file out of the two it was handed.
  evidence: Ran verbatim against the reference stamper: diff -r reported one file and one line pair, '8c8 < last_confirmed: 2026-02-10 --- > last_confirmed: 2026-02-17', and 'before=failures: 0 after=failures: 0'.
- **C-8** (pass): Unchanged, and its anchors still resolve. Four independently checkable propositions plus a concrete structural failure mode.
  evidence: constitution.md:333-334. The budget sentence is verbatim at SKILL.md:67 and the accepted-only rule at SKILL.md:69; phase 2.5's per-input loop opens at SKILL.md:59. The clause still hands placement to C-17 rather than duplicating it.
- **C-10** (pass): Unchanged and still the best rubric in the document. The two-exceptions collision test is anchored on a line that really exists and really closes the list.
  evidence: SKILL.md:459 '**VTT raw content is collapsed, not verbatim.** ... This is the one sanctioned exception to preserving raw content exactly as captured.' Operating Rules heading at SKILL.md:450, list ends at :459.
- **C-11** (pass): Re-derived the sweep rather than reading the list. All twelve floor locations resolve, the two ordered questions are the right discriminator, and the document-level obligation on _memory.template.md still holds its shape against the :65 pre-ruling.
  evidence: Ran the sweep at constitution.md:374-376 verbatim: 60 distinct hits. The twelve named all resolve — SKILL.md:26, :138, :168, :361, :455; README.md:74; assets/claude-md/_memory.template.md:21 and :65; client.template.md:71; project.template.md:55; references/migration.md:37; references/retrieval-funnel.md:40. inbox-to-memory/scripts/migrate-scope.sh:348-349 is exactly the reasoning comment the clause says it is ('A record nobody has re-confirmed was last confirmed the day it was written. / Any later date would assert a review that never happened.') and sits outside C-16's allowlist, which the last non-goal governs. The r2 observation about journal.template.md:23 is still live and still an observation, not a defect.
- **C-12** (pass): Unchanged, still runs clean, and still immune to the commit-first vacuity that C-16 only warns about.
  evidence: Ran the check verbatim at 924f81f: no diff, 'inventory exit=0'. All four enumerating files are in the loop. The cosmetic overstatement r2 noted at C-16's failure string ('THIS CHECK AND C-12 ARE BOTH VACUOUS') survives into r3 at :516 and is still wrong about C-12, which pins the literal base SHA rather than HEAD.
- **C-13** (pass): The saboteur is sound and I confirmed it behaves exactly as the clause claims, and the placement check still fails safe on zero hits. The major above is about the anchors' coupling to C-17, not about either of these.
  evidence: Installed the saboteur from constitution.md:433-455 over the conforming reference stamper and ran it on a copy of mixed/. Output byte-identical to the real run ('skipped: ...freeze-window... (v1 record, no schema key)', 'stamped: ...atlas-region-topology 2026-02-10 -> 2026-02-17', 'stamped: 1  skipped: 1', exit 0) and the v1 record then carried 'last_confirmed: 2026-02-17' at :7 — real behavior in every respect except the v1 hold, so a red suite means the hold is tested. Placement block run at 924f81f: 'snap=586 first= last= cmp=1299' -> 'OUTSIDE THE GUARD', failing safe on empty anchors. Guard bounds re-derived directly: fixtures_before assigned at tests/inbox-to-memory-smoke.sh:586, compared at :1299, file ends at :1309.
- **C-14** (pass): The r2 unsatisfiable-check blocker is closed and closed precisely. The anchor separates the two 'Four's exactly as the clause claims.
  evidence: Ran the enumeration greps at 924f81f: '^Four, all under' -> 1 (EVALS.md:9, 'Four, all under `tests/fixtures/inbox-to-memory/evals/`'), '^Five, all under' -> 0, '^| [0-9]\.' -> 4 (the Grading rows at :125-128). EVALS.md:12's 'Four prior notes, two open-question slugs live in all four' is untouched by the anchor because 'Four' is mid-line there. The table spans :123-128 and the closing claim is at :130 as the clause says.
- **C-15** (pass): Unchanged, still runs, still returns exactly the one hit the clause predicts, and the migrate precedent it polices is now cited correctly.
  evidence: Ran the sweep verbatim: one hit, '_maintenance//inbox-to-memory/eval-scope.sh:6:inbox-to-memory-smoke.sh:1226', against a rule that actually lives at tests/inbox-to-memory-smoke.sh:1296-1298. The r2 BASELINE minor is closed: 'require_text inbox-to-memory/SKILL.md "scripts/migrate-scope.sh"' is at :600 and constitution.md:29 now says :600.
- **C-16** (pass): Runs clean, and the allowlist correctly admits the untracked stamper while keeping lint-scope.sh out.
  evidence: Ran the check verbatim with the reference stamper in place: 'suites exit=0', 'HEAD is the base', 'OK: 1 path(s) in scope', 'scope exit=0'. git rev-parse HEAD = 924f81f7d4c9794968a2954f8152dc6ed53cc9df. The untracked stamper is picked up by check-diff-scope.py, so the allowlist entry is load-bearing rather than decorative. Removed the reference stamper afterwards; git status --porcelain is empty.
- **C-17** (pass): The r2 major is closed the honest way. r3 stopped claiming batch scope forces the boundary, split the clause into two derived requirements and one declared convention, and — this is the part that makes it checkable — requires SKILL.md not to dress the convention up as a derivation. That is a settleable question, not a matter of taste: a checker reads phase 4, sees it disposes sources and opens no gate, and can tell whether the prose claims otherwise. The zero-confirmation hole r2 filed is closed alongside it.
  evidence: constitution.md:544 'This is a **convention, not a derivation**, and SKILL.md must not dress it up as one. The close of phase 3 and the close of phase 4 both satisfy every constraint the script imposes — phase 4 only disposes sources and opens no gate.' Confirmed against SKILL.md's actual structure, which is what r2's finding turned on: :24 'Six phases, plus a 2.5', :59 phase 2.5 'For each inbox file, in order:', :83 phase 3 'Emit one note per input', :128-134 phase 4 'Dispose Source'. Requirement 2 at :543 ('The call is made only when phase 2.5 found at least one confirmation') is genuinely forced by C-1's zero-group error and closes the r2 major. The deterministic half at :548-549 fails safe today: grep for 'scripts/stamp-confirmed.sh' in SKILL.md returns nothing.
- **COVERAGE** (pass): All seven acceptance criteria map to clauses that would catch their violation, and I re-walked each rather than inheriting r2's table. AC6 in particular is now sound end to end, which it was not in r1 and was contradictory in r2.
  evidence: spec.md:27-33 walked against the shipped document. AC1 (stamps a v2 record) -> C-1 (ran it: 2026-02-10 -> 2026-02-17, exit 0), C-6 (insertion path, ran it), C-7 (nothing else moves, ran it), C-8 (detection), C-13 (smoke), C-17 (SKILL.md calls it). AC2 (v1 skipped, never gains the field) -> C-2 (ran it: 'v1 untouched') and C-13's saboteur, which I installed and confirmed reddens for a v1-write reason rather than an output-shape one. AC3 (every stamped record named in the report) -> C-9's judgment rubric, with the stamper's per-record output lines pinned by the contract at :52-54 as the raw material; C-9 goes further than AC3 by also requiring the declined list. AC4 (no per-record approval prompts) -> C-8 ('the resulting stamp does not pass through phase 5'), C-17 (the call sits before phase 5), the contract's 'It writes by default. No `--apply`' at :66, C-10, and C-14's required pass-table row on the absence of any per-record prompt. All judgment, which is right: the absence of a prompt in prose is not scriptable. AC5 (operating rules name the sole exception with reasoning) -> C-10, whose VTT collision anchor at SKILL.md:459 I verified. AC6 (confirmed twice in one run, stamped once) -> the batch contract at :47, C-5 cases 1-4, C-17 requirement 1, C-13's required cross-group smoke assertion. Ran C-5 whole: one stamped line, the later date, order-independent at arity 1, straddle collapsing to one line. AC7 (smoke asserts the mixed fixture leaves v1 untouched) -> C-13's headline plus saboteur plus placement, with C-2 supplying the content. The two majors on C-5 and C-13 are defects in how AC6's and AC7's clauses are worded and anchored, not gaps in coverage.
- **NON-GOALS** (pass): Unchanged and still sound. None of the six silently excludes an acceptance criterion, and the migrate-scope.sh escape hatch still resolves the collision with C-11's sweep.
  evidence: constitution.md:560-567 walked against spec.md:27-33. The C-11 sweep does reach inbox-to-memory/scripts/migrate-scope.sh (hits at :25, :350, :352, reasoning comment at :348-349), it is outside C-16's allowlist, and :567 rules it a finding to surface rather than a task to do. tests/fixtures/inbox-to-memory/README.md:3's 'Three scopes' above four bullets is correctly held as unrelated.
- **PROTECTED** (pass): Correct and unchanged: no verbatim author words ship in this job, so there is no manifest to parse.
  evidence: constitution.md:556-558. The job's surface is a shell script, SKILL.md and README.md prose, a smoke-test section, and an eval fixture.
