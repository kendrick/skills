---
task: CON-audit
checker: auditor
vendor: anthropic
model: claude-opus-5[1m]
verdict: FAIL
checked_at: 2026-08-08T20:15:00Z
duration_ms: None
cost_usd: None
---

<!-- GENERATED FILE—do not hand-edit. Rendered by render-verdict.py
from the verdict JSON, the record of record. Edit the JSON and
re-render instead. -->

## Per-clause results

| clause | severity | description | evidence |
| ------ | -------- | ------------ | -------- |
| C-1 | pass | Falsifiable and the harness is sound: the interface is pinned, the check names real fixture paths, and the only reason it goes red today is the absent script, which is expected; the all-or-nothing validation half is discriminating because a partial-write implementation leaves the scope half stamped. | constitution.md:52-64; ran verbatim: `bash inbox-to-memory/scripts/stamp-confirmed.sh ...` -> 'No such file or directory', exit 127; record under test reads last_confirmed: 2026-02-10 (mixed/_memory/context/atlas-region-topology-mPmy8XBe5H.md:8) |
| C-2 | pass | Falsifiable against a real fixture: freeze-window-owned-by-ops-ocPwdpeY0a.md genuinely carries no `schema` key, is `status: accepted`, and has no `last_confirmed`, so the shasum comparison would go red on exactly the failing example the clause names. | tests/fixtures/inbox-to-memory/mixed/_memory/decisions/freeze-window-owned-by-ops-ocPwdpeY0a.md:1-16 (keys: id, memory_type, title, status, date, source_refs, applies_to, owners, tags, related; no schema, no last_confirmed) |
| C-3 | pass | The fixture supplies both halves the clause needs: note 2025-12-02-atlas-steerco is strictly older than the record's last_confirmed: 2026-02-10 and note 2026-02-10-atlas-runbook-review is exactly equal, so the check discriminates 'earlier' and 'equal' separately rather than collapsing them. | mixed/notes/2025-12-02-atlas-steerco-ZGulgExW0q.md `date: 2025-12-02`; mixed/notes/2026-02-10-atlas-runbook-review-j5jLCGc5il.md `date: 2026-02-10`; record :8 `last_confirmed: 2026-02-10` |
| C-4 | pass | Falsifiable, and the check correctly builds its own input rather than trusting the fixtures, since no shipped record fixture is non-accepted with the key present; the python mutation targets `status: accepted` at line 6 of the record and count=1 confines it to that record. | constitution.md:116-131; swept all of tests/fixtures/ for records with status != accepted carrying last_confirmed: none |
| C-5 | pass | Discriminating, and stronger than it looks: mktemp -d returns /var/folders/... while the real path is /private/var/folders/..., so a string-equality dedupe fails the absolute-vs-relative case while a realpath dedupe passes it; the grep pattern correctly excludes the closing summary line, though the check leaks its tempdir (no `rm -rf "$S"`). | constitution.md:140-150; observed mktemp -d -> /var/folders/2b/.../T/tmp.RJIJBsJPsS resolving to /private/var/... |
| C-6 | major | The second half is not provable by the check the clause gives, because the failing case it describes cannot be built the way it says: a lint-clean record missing `last_confirmed` carries at most 17 contract keys and closes on line 19, so inserting the key closes on 20 and passes the budget; overflow requires a comment line in the block, which the clause never mentions. | constitution.md:174 ('padding a record to exactly 20 lines with optional keys before removing last_confirmed'); built the maximal 17-key record, closing --- on line 19, inserted last_confirmed -> closing on line 20, lint-scope.sh reported `failures: 0`; only after adding one `# ...` comment line did insertion close on 21 and report `failures: 1` frontmatter-budget |
| C-7 | pass | Falsifiable and correctly baselined: the mixed fixture lints at `failures: 0` today, so the before/after comparison has a real fixed point, and the yq round-trip failing example is concrete against the record's actual single-quoted title and inline `related` list. | ran `bash inbox-to-memory/scripts/lint-scope.sh tests/fixtures/inbox-to-memory/mixed` -> `v2 files: 5`, `total files: 8`, `failures: 0`; record :5 `title: 'Billing region...'`, :13 `related: [clarifies::G2k65qG3Nc]` |
| C-8 | pass | The rubric is specific enough to converge: it names four independently checkable propositions (detection rides the contradiction pass, eligibility is `accepted`, the five-read budget covers both unraised, the stamp skips phase 5) and a concrete structural failure mode (a separate numbered step with its own reads). | SKILL.md:67 is the budget sentence the clause cites, verbatim '**Five body reads per input, and the budget binds.**'; SKILL.md:69 is the existing accepted-only rule the clause says it matches |
| C-9 | minor | The rubric is ambiguous on the scope of 'every record the run considered and skipped', which two checkers would resolve differently: the failing example implies the stamper's own skip list, while C-14's fixture requires grading a record 'read at phase 2.5 and neither confirmed nor contradicted', which implies the wider phase-2.5 read set. | constitution.md:210 ('every record the run considered and skipped along with the reason') vs constitution.md:213 ('stamps four records and skips six') vs constitution.md:266 ('an accepted v2 record that is read at phase 2.5 and neither confirmed nor contradicted') |
| C-10 | pass | The strongest rubric in the document: the contradiction test is anchored to a verbatim line that really exists and really is the last bullet in the list, so a checker can settle 'do two rules both claim to be the one sanctioned exception' by reading two adjacent bullets. | SKILL.md:459 '**VTT raw content is collapsed, not verbatim.** ... This is the one sanctioned exception to preserving raw content exactly as captured.'; Operating Rules heading at SKILL.md:450, list runs :452-459 |
| C-11 | major | The clause's discriminating test contradicts its own failing example, so it cannot settle the case it was written for: the rubric says a passage scoped to record creation 'is still true and needs nothing', but README.md:74 is scoped to promotion and record creation in both sentences and remains literally true after this change, yet the failing example declares shipping it unchanged a defect. | constitution.md:225 ('A passage scoped to record creation ("only create record files after sign-off") is still true and needs nothing') vs constitution.md:229; README.md:74 'Nothing is ever promoted to memory automatically. Every record requires your explicit per-candidate approval, and unpromoted candidates keep their `[memory candidate: ...]` flag as a record of what was considered.' |
| C-11 | major | The census scope collides with C-16 and C-12: the sweep is told to cover all of `inbox-to-memory/assets/` and is explicitly a floor rather than a boundary, but C-16's allowlist admits only `inbox-to-memory/assets/claude-md/`, and C-12 requires `assets/note.template.md` unchanged, so a hit the worker is required to fix outside claude-md/ cannot be fixed without failing another blocker clause. | constitution.md:225 (sweep scope) vs constitution.md:300-310 (allowlist: only inbox-to-memory/assets/claude-md/) and constitution.md:233 (note.template.md unchanged); ran the prescribed sweep, hits outside claude-md/ include assets/records/*.template.md:8-10, assets/readme/client.template.md:34, assets/readme/project.template.md:37 |
| C-11 | pass | The nine cited candidate locations all resolve to what the clause says they do, and the sweep command runs and returns them, so the floor is real; the sweep also surfaces at least one obvious omission (SKILL.md:361, the migrator's own account of how last_confirmed comes to hold its value), which is consistent with the clause's own 'floor rather than boundary' framing. | verified SKILL.md:26, :138, :168, :455; README.md:74; assets/claude-md/_memory.template.md:65, client.template.md:71, project.template.md:55; references/migration.md:37 — all nine present and on-topic. Additional un-cited hit: SKILL.md:361 'Sets `last_confirmed` on a record to the record's own date.' |
| C-12 | major | The check cannot detect the thing the clause forbids: it diffs only lint-scope.sh, note.template.md, and machine-contracts.md, but the skill's own token inventory is at SKILL.md:96-100 and SKILL.md is inside C-16's allowlist, so a `[confirms accepted: ...]` line added there passes both greps and the clause's text ('This job registers no new inline token') goes unenforced. | constitution.md:237-239 (check diffs three files, none of them SKILL.md); SKILL.md:96-100 is the token block, SKILL.md:100 '[contradicts accepted: [[<filename>\|<short-name>]]] <statement> \| claims: ...'; both greps ran clean on the clean tree (first empty, second exit 1) |
| C-13 | major | The clause asserts the whole-fixture hash guard 'now also covers stamping tests' but nothing makes that true: the snapshot is taken at smoke.sh:586 and compared at :1299, so only writes between those lines are covered, and a write-through section appended after :1302 (the natural place, the file ends at :1309) satisfies the clause literally while the guard covers none of it. | tests/inbox-to-memory-smoke.sh:586 `fixtures_before="$(find "$fixtures" -type f -exec shasum {} + \| sort)"`; :1299 the comparison; :1302 the closing brace; :1303-1309 two require_text calls and the success echo; wc -l = 1309 |
| C-13 | minor | The falsification step is a comment placeholder rather than a runnable procedure ('# remove the v1 guard, run the suite, expect nonzero, then restore'), so the one step that would catch a green-but-vacuous suite is the one step a checker can skip without leaving a trace; it also mutates a tracked working-tree file mid-check, which dirties the tree C-16 inspects if the restore is missed. | constitution.md:256-260 |
| C-14 | minor | The mechanical half is not runnable as written because the clause never names the new fixture, leaving `<fixture-name>` unbound and giving the smoke suite, EVALS.md, and the checker no pinned string to agree on; the rubric half is otherwise specific enough to converge, and the four-record composition is a real teeth test. | constitution.md:270 `staged="$(bash _maintenance/inbox-to-memory/eval-scope.sh <fixture-name>)"`; ran the same shape against `contradiction-amend` and it works: `failures: 0`, statuses/generations enumerate correctly from "$staged"/_memory/*/*.md |
| C-15 | pass | The check runs today, returns exactly the one hit the clause predicts, and the cited facts are correct: eval-scope.sh:6 points at smoke.sh:1226, which is now the '# v1 link checking (#32)' section header rather than the never-migrate rule, and that rule lives at :1296. | ran the grep -> `_maintenance//inbox-to-memory/eval-scope.sh:6:inbox-to-memory-smoke.sh:1226`; smoke.sh:1226 is '# ---------------------------------------------------------------------------' opening the v1 link-checking section at :1227; the rule's comment is at :1296-1298 |
| C-16 | pass | Both halves run clean today and the allowlist is correctly built: check-diff-scope.py unions `git status --porcelain --untracked-files=all` with `git diff --name-only`, so the new untracked script is genuinely covered, and omitting inbox-to-memory/scripts/lint-scope.sh really is a second lock on C-12. | all three suites exit 0; `.agent-guild/scripts/check-diff-scope.py <allowlist>` -> 'OK: 0 path(s) in scope', exit 0; check-diff-scope.py:96-97 |
| BASELINE | major | The claim 'No record fixture anywhere is `schema: 2` and missing `last_confirmed`' is false, and it is the stated justification for C-6 building its own input: two shipped fixtures are exactly that shape, both `schema: 2`, `status: accepted`, no `last_confirmed`. | constitution.md:22; tests/fixtures/inbox-to-memory/evals/unacknowledged-tension/_memory/context/bexley-dock-capacity-MH-2ub4mwK.md:1-12 and .../decisions/bexley-pilots-first-LvJedcverX.md:1-12 |
| BASELINE | major | The frontmatter-budget arithmetic is wrong in a way that misdirects C-6: `last_confirmed` is one of the 18 keys a full record carries, so a record 'already at the budget' by definition already has the key, and a record missing it can carry at most 17 keys and close on line 19. | constitution.md:23; machine-contracts.md:29 ('a record carries at most 18 and closes on exactly line 20') and :42 (the 19-name record order, with last_confirmed tenth); empirically the maximal key-only record without last_confirmed closes on line 19 and stamping it closes on line 20 with `failures: 0` |
| BASELINE | minor | The shell-shadowing note is wrong about `stat`, which is not shadowed; `grep`, `find`, and `cat` are, so the rest of the paragraph holds. | constitution.md:25; `type stat` -> `/usr/bin/stat`, and `stat -f '%z %N' inbox-to-memory/SKILL.md` returns cleanly. `type grep`/`type find` -> shell functions, `type cat` -> alias for ccat |
| BASELINE | minor | The hash guard covers `tests/fixtures/inbox-to-memory`, not 'every file under `tests/fixtures/`'; the two sets coincide today because that is the only subdirectory, so the claim is imprecise rather than wrong, but it is the sentence C-13 leans on. | constitution.md:20; tests/inbox-to-memory-smoke.sh:12 `fixtures=tests/fixtures/inbox-to-memory`; `ls tests/fixtures/` -> inbox-to-memory only |
| BASELINE | pass | Every other asserted fact checks out against the shipped tree: tool paths, all three suites green, the smoke suite's length and guard location, the mixed fixture's two records and its note date range, and both pre-existing prose defects. | yq at /opt/homebrew/bin/yq, nanoid at ~/.nvm/versions/node/v24.18.0/bin/nanoid; three suites exit 0; smoke.sh 1309 lines, guard at :1296-1302; mixed record :2 schema: 2, :6 status: accepted, :8 last_confirmed: 2026-02-10; note dates 2025-12-02 through 2026-02-17; EVALS.md:9 'Four'; eval-scope.sh:6 cites :1226; tests/fixtures/inbox-to-memory/README.md:3 'Three scopes' above four bullets |
| COVERAGE | blocker | Nothing in the constitution locates the write in the six-phase flow or requires SKILL.md to name and invoke the stamper, so AC1 and AC4 are not closed: C-8 pins detection at phase 2.5 and says the stamp skips phase 5, C-9 pins reporting at phase 6, and the write itself is unlocated, meaning a worker can ship a script that passes C-1 through C-7 and a SKILL.md that never calls it. | constitution.md:203 (phase 2.5 detection, 'the resulting stamp does not pass through phase 5') and :210 (phase 6 reporting) are the only placement statements; no clause requires the string `scripts/stamp-confirmed.sh` in SKILL.md, unlike the existing migrate wiring asserted at tests/inbox-to-memory-smoke.sh:600. The script contract's `--note <groomed-note-path>` (constitution.md:35) implies phase 3 or later, since a groomed note does not exist at phase 2.5, but that inference is stated nowhere |
| COVERAGE | pass | All seven acceptance criteria otherwise map to clauses that would catch their violation: AC1 to C-1/C-6/C-8/C-13, AC2 to C-2/C-13, AC3 to C-9, AC4 to C-8/C-10/C-14, AC5 to C-10, AC6 to C-5/C-13, AC7 to C-13 with C-2 supplying the assertion's content. | spec.md:27-33 walked against constitution.md:47-315 |
| NON-GOALS | pass | None of the six silently excludes something an acceptance criterion requires, and the fixtures-README exclusion is safe on inspection because that README does not enumerate eval fixtures at all, so the new one does not make it stale. | constitution.md:323-328 walked against spec.md:27-33; tests/fixtures/inbox-to-memory/README.md:5-8 lists only old-only/, mixed/, broken/, journal-v1/ — evals/ is already absent |
| PROTECTED | pass | 'Nothing in this job ships verbatim author words. No manifest.' is correct for this job's surface, so there is no manifest to parse and no protected-passage check to route. | constitution.md:317-319; the job's artifacts are a shell script, SKILL.md/README.md prose, a test section, and an eval fixture, none of which carries third-party verbatim text |

## Diagnosis

- **C-1** (pass): Falsifiable and the harness is sound: the interface is pinned, the check names real fixture paths, and the only reason it goes red today is the absent script, which is expected; the all-or-nothing validation half is discriminating because a partial-write implementation leaves the scope half stamped.
  evidence: constitution.md:52-64; ran verbatim: `bash inbox-to-memory/scripts/stamp-confirmed.sh ...` -> 'No such file or directory', exit 127; record under test reads last_confirmed: 2026-02-10 (mixed/_memory/context/atlas-region-topology-mPmy8XBe5H.md:8)
- **C-2** (pass): Falsifiable against a real fixture: freeze-window-owned-by-ops-ocPwdpeY0a.md genuinely carries no `schema` key, is `status: accepted`, and has no `last_confirmed`, so the shasum comparison would go red on exactly the failing example the clause names.
  evidence: tests/fixtures/inbox-to-memory/mixed/_memory/decisions/freeze-window-owned-by-ops-ocPwdpeY0a.md:1-16 (keys: id, memory_type, title, status, date, source_refs, applies_to, owners, tags, related; no schema, no last_confirmed)
- **C-3** (pass): The fixture supplies both halves the clause needs: note 2025-12-02-atlas-steerco is strictly older than the record's last_confirmed: 2026-02-10 and note 2026-02-10-atlas-runbook-review is exactly equal, so the check discriminates 'earlier' and 'equal' separately rather than collapsing them.
  evidence: mixed/notes/2025-12-02-atlas-steerco-ZGulgExW0q.md `date: 2025-12-02`; mixed/notes/2026-02-10-atlas-runbook-review-j5jLCGc5il.md `date: 2026-02-10`; record :8 `last_confirmed: 2026-02-10`
- **C-4** (pass): Falsifiable, and the check correctly builds its own input rather than trusting the fixtures, since no shipped record fixture is non-accepted with the key present; the python mutation targets `status: accepted` at line 6 of the record and count=1 confines it to that record.
  evidence: constitution.md:116-131; swept all of tests/fixtures/ for records with status != accepted carrying last_confirmed: none
- **C-5** (pass): Discriminating, and stronger than it looks: mktemp -d returns /var/folders/... while the real path is /private/var/folders/..., so a string-equality dedupe fails the absolute-vs-relative case while a realpath dedupe passes it; the grep pattern correctly excludes the closing summary line, though the check leaks its tempdir (no `rm -rf "$S"`).
  evidence: constitution.md:140-150; observed mktemp -d -> /var/folders/2b/.../T/tmp.RJIJBsJPsS resolving to /private/var/...
- **C-6** (major): The second half is not provable by the check the clause gives, because the failing case it describes cannot be built the way it says: a lint-clean record missing `last_confirmed` carries at most 17 contract keys and closes on line 19, so inserting the key closes on 20 and passes the budget; overflow requires a comment line in the block, which the clause never mentions.
  evidence: constitution.md:174 ('padding a record to exactly 20 lines with optional keys before removing last_confirmed'); built the maximal 17-key record, closing --- on line 19, inserted last_confirmed -> closing on line 20, lint-scope.sh reported `failures: 0`; only after adding one `# ...` comment line did insertion close on 21 and report `failures: 1` frontmatter-budget
- **C-7** (pass): Falsifiable and correctly baselined: the mixed fixture lints at `failures: 0` today, so the before/after comparison has a real fixed point, and the yq round-trip failing example is concrete against the record's actual single-quoted title and inline `related` list.
  evidence: ran `bash inbox-to-memory/scripts/lint-scope.sh tests/fixtures/inbox-to-memory/mixed` -> `v2 files: 5`, `total files: 8`, `failures: 0`; record :5 `title: 'Billing region...'`, :13 `related: [clarifies::G2k65qG3Nc]`
- **C-8** (pass): The rubric is specific enough to converge: it names four independently checkable propositions (detection rides the contradiction pass, eligibility is `accepted`, the five-read budget covers both unraised, the stamp skips phase 5) and a concrete structural failure mode (a separate numbered step with its own reads).
  evidence: SKILL.md:67 is the budget sentence the clause cites, verbatim '**Five body reads per input, and the budget binds.**'; SKILL.md:69 is the existing accepted-only rule the clause says it matches
- **C-9** (minor): The rubric is ambiguous on the scope of 'every record the run considered and skipped', which two checkers would resolve differently: the failing example implies the stamper's own skip list, while C-14's fixture requires grading a record 'read at phase 2.5 and neither confirmed nor contradicted', which implies the wider phase-2.5 read set.
  evidence: constitution.md:210 ('every record the run considered and skipped along with the reason') vs constitution.md:213 ('stamps four records and skips six') vs constitution.md:266 ('an accepted v2 record that is read at phase 2.5 and neither confirmed nor contradicted')
- **C-10** (pass): The strongest rubric in the document: the contradiction test is anchored to a verbatim line that really exists and really is the last bullet in the list, so a checker can settle 'do two rules both claim to be the one sanctioned exception' by reading two adjacent bullets.
  evidence: SKILL.md:459 '**VTT raw content is collapsed, not verbatim.** ... This is the one sanctioned exception to preserving raw content exactly as captured.'; Operating Rules heading at SKILL.md:450, list runs :452-459
- **C-11** (major): The clause's discriminating test contradicts its own failing example, so it cannot settle the case it was written for: the rubric says a passage scoped to record creation 'is still true and needs nothing', but README.md:74 is scoped to promotion and record creation in both sentences and remains literally true after this change, yet the failing example declares shipping it unchanged a defect.
  evidence: constitution.md:225 ('A passage scoped to record creation ("only create record files after sign-off") is still true and needs nothing') vs constitution.md:229; README.md:74 'Nothing is ever promoted to memory automatically. Every record requires your explicit per-candidate approval, and unpromoted candidates keep their `[memory candidate: ...]` flag as a record of what was considered.'
- **C-11** (major): The census scope collides with C-16 and C-12: the sweep is told to cover all of `inbox-to-memory/assets/` and is explicitly a floor rather than a boundary, but C-16's allowlist admits only `inbox-to-memory/assets/claude-md/`, and C-12 requires `assets/note.template.md` unchanged, so a hit the worker is required to fix outside claude-md/ cannot be fixed without failing another blocker clause.
  evidence: constitution.md:225 (sweep scope) vs constitution.md:300-310 (allowlist: only inbox-to-memory/assets/claude-md/) and constitution.md:233 (note.template.md unchanged); ran the prescribed sweep, hits outside claude-md/ include assets/records/*.template.md:8-10, assets/readme/client.template.md:34, assets/readme/project.template.md:37
- **C-11** (pass): The nine cited candidate locations all resolve to what the clause says they do, and the sweep command runs and returns them, so the floor is real; the sweep also surfaces at least one obvious omission (SKILL.md:361, the migrator's own account of how last_confirmed comes to hold its value), which is consistent with the clause's own 'floor rather than boundary' framing.
  evidence: verified SKILL.md:26, :138, :168, :455; README.md:74; assets/claude-md/_memory.template.md:65, client.template.md:71, project.template.md:55; references/migration.md:37 — all nine present and on-topic. Additional un-cited hit: SKILL.md:361 'Sets `last_confirmed` on a record to the record's own date.'
- **C-12** (major): The check cannot detect the thing the clause forbids: it diffs only lint-scope.sh, note.template.md, and machine-contracts.md, but the skill's own token inventory is at SKILL.md:96-100 and SKILL.md is inside C-16's allowlist, so a `[confirms accepted: ...]` line added there passes both greps and the clause's text ('This job registers no new inline token') goes unenforced.
  evidence: constitution.md:237-239 (check diffs three files, none of them SKILL.md); SKILL.md:96-100 is the token block, SKILL.md:100 '[contradicts accepted: [[<filename>|<short-name>]]] <statement> | claims: ...'; both greps ran clean on the clean tree (first empty, second exit 1)
- **C-13** (major): The clause asserts the whole-fixture hash guard 'now also covers stamping tests' but nothing makes that true: the snapshot is taken at smoke.sh:586 and compared at :1299, so only writes between those lines are covered, and a write-through section appended after :1302 (the natural place, the file ends at :1309) satisfies the clause literally while the guard covers none of it.
  evidence: tests/inbox-to-memory-smoke.sh:586 `fixtures_before="$(find "$fixtures" -type f -exec shasum {} + | sort)"`; :1299 the comparison; :1302 the closing brace; :1303-1309 two require_text calls and the success echo; wc -l = 1309
- **C-13** (minor): The falsification step is a comment placeholder rather than a runnable procedure ('# remove the v1 guard, run the suite, expect nonzero, then restore'), so the one step that would catch a green-but-vacuous suite is the one step a checker can skip without leaving a trace; it also mutates a tracked working-tree file mid-check, which dirties the tree C-16 inspects if the restore is missed.
  evidence: constitution.md:256-260
- **C-14** (minor): The mechanical half is not runnable as written because the clause never names the new fixture, leaving `<fixture-name>` unbound and giving the smoke suite, EVALS.md, and the checker no pinned string to agree on; the rubric half is otherwise specific enough to converge, and the four-record composition is a real teeth test.
  evidence: constitution.md:270 `staged="$(bash _maintenance/inbox-to-memory/eval-scope.sh <fixture-name>)"`; ran the same shape against `contradiction-amend` and it works: `failures: 0`, statuses/generations enumerate correctly from "$staged"/_memory/*/*.md
- **C-15** (pass): The check runs today, returns exactly the one hit the clause predicts, and the cited facts are correct: eval-scope.sh:6 points at smoke.sh:1226, which is now the '# v1 link checking (#32)' section header rather than the never-migrate rule, and that rule lives at :1296.
  evidence: ran the grep -> `_maintenance//inbox-to-memory/eval-scope.sh:6:inbox-to-memory-smoke.sh:1226`; smoke.sh:1226 is '# ---------------------------------------------------------------------------' opening the v1 link-checking section at :1227; the rule's comment is at :1296-1298
- **C-16** (pass): Both halves run clean today and the allowlist is correctly built: check-diff-scope.py unions `git status --porcelain --untracked-files=all` with `git diff --name-only`, so the new untracked script is genuinely covered, and omitting inbox-to-memory/scripts/lint-scope.sh really is a second lock on C-12.
  evidence: all three suites exit 0; `.agent-guild/scripts/check-diff-scope.py <allowlist>` -> 'OK: 0 path(s) in scope', exit 0; check-diff-scope.py:96-97
- **BASELINE** (major): The claim 'No record fixture anywhere is `schema: 2` and missing `last_confirmed`' is false, and it is the stated justification for C-6 building its own input: two shipped fixtures are exactly that shape, both `schema: 2`, `status: accepted`, no `last_confirmed`.
  evidence: constitution.md:22; tests/fixtures/inbox-to-memory/evals/unacknowledged-tension/_memory/context/bexley-dock-capacity-MH-2ub4mwK.md:1-12 and .../decisions/bexley-pilots-first-LvJedcverX.md:1-12
- **BASELINE** (major): The frontmatter-budget arithmetic is wrong in a way that misdirects C-6: `last_confirmed` is one of the 18 keys a full record carries, so a record 'already at the budget' by definition already has the key, and a record missing it can carry at most 17 keys and close on line 19.
  evidence: constitution.md:23; machine-contracts.md:29 ('a record carries at most 18 and closes on exactly line 20') and :42 (the 19-name record order, with last_confirmed tenth); empirically the maximal key-only record without last_confirmed closes on line 19 and stamping it closes on line 20 with `failures: 0`
- **BASELINE** (minor): The shell-shadowing note is wrong about `stat`, which is not shadowed; `grep`, `find`, and `cat` are, so the rest of the paragraph holds.
  evidence: constitution.md:25; `type stat` -> `/usr/bin/stat`, and `stat -f '%z %N' inbox-to-memory/SKILL.md` returns cleanly. `type grep`/`type find` -> shell functions, `type cat` -> alias for ccat
- **BASELINE** (minor): The hash guard covers `tests/fixtures/inbox-to-memory`, not 'every file under `tests/fixtures/`'; the two sets coincide today because that is the only subdirectory, so the claim is imprecise rather than wrong, but it is the sentence C-13 leans on.
  evidence: constitution.md:20; tests/inbox-to-memory-smoke.sh:12 `fixtures=tests/fixtures/inbox-to-memory`; `ls tests/fixtures/` -> inbox-to-memory only
- **BASELINE** (pass): Every other asserted fact checks out against the shipped tree: tool paths, all three suites green, the smoke suite's length and guard location, the mixed fixture's two records and its note date range, and both pre-existing prose defects.
  evidence: yq at /opt/homebrew/bin/yq, nanoid at ~/.nvm/versions/node/v24.18.0/bin/nanoid; three suites exit 0; smoke.sh 1309 lines, guard at :1296-1302; mixed record :2 schema: 2, :6 status: accepted, :8 last_confirmed: 2026-02-10; note dates 2025-12-02 through 2026-02-17; EVALS.md:9 'Four'; eval-scope.sh:6 cites :1226; tests/fixtures/inbox-to-memory/README.md:3 'Three scopes' above four bullets
- **COVERAGE** (blocker): Nothing in the constitution locates the write in the six-phase flow or requires SKILL.md to name and invoke the stamper, so AC1 and AC4 are not closed: C-8 pins detection at phase 2.5 and says the stamp skips phase 5, C-9 pins reporting at phase 6, and the write itself is unlocated, meaning a worker can ship a script that passes C-1 through C-7 and a SKILL.md that never calls it.
  evidence: constitution.md:203 (phase 2.5 detection, 'the resulting stamp does not pass through phase 5') and :210 (phase 6 reporting) are the only placement statements; no clause requires the string `scripts/stamp-confirmed.sh` in SKILL.md, unlike the existing migrate wiring asserted at tests/inbox-to-memory-smoke.sh:600. The script contract's `--note <groomed-note-path>` (constitution.md:35) implies phase 3 or later, since a groomed note does not exist at phase 2.5, but that inference is stated nowhere
- **COVERAGE** (pass): All seven acceptance criteria otherwise map to clauses that would catch their violation: AC1 to C-1/C-6/C-8/C-13, AC2 to C-2/C-13, AC3 to C-9, AC4 to C-8/C-10/C-14, AC5 to C-10, AC6 to C-5/C-13, AC7 to C-13 with C-2 supplying the assertion's content.
  evidence: spec.md:27-33 walked against constitution.md:47-315
- **NON-GOALS** (pass): None of the six silently excludes something an acceptance criterion requires, and the fixtures-README exclusion is safe on inspection because that README does not enumerate eval fixtures at all, so the new one does not make it stale.
  evidence: constitution.md:323-328 walked against spec.md:27-33; tests/fixtures/inbox-to-memory/README.md:5-8 lists only old-only/, mixed/, broken/, journal-v1/ — evals/ is already absent
- **PROTECTED** (pass): 'Nothing in this job ships verbatim author words. No manifest.' is correct for this job's surface, so there is no manifest to parse and no protected-passage check to route.
  evidence: constitution.md:317-319; the job's artifacts are a shell script, SKILL.md/README.md prose, a test section, and an eval fixture, none of which carries third-party verbatim text
