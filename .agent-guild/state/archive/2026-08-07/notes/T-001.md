# T-001 self-report

## What I did

1. **`frontmatter-key-domain` check, sited ahead of the budget guard** (`inbox-to-memory/scripts/lint-scope.sh:159-168`). It runs right after the frontmatter block is extracted (`$block`) and before the `FRONTMATTER_LINE_BUDGET` check, so it fires and `return`s on a file that also overruns the budget — the exact 21-line record the issue is about. It greps the block for `^tags:` and `^themes:` (counted, not `grep -q`, per the RTK gotcha) and fails `frontmatter-key-domain` only when both are present, regardless of `memory_type`. Message: `` carries both `tags` and `themes`, a record's key and a journal entry's `` — names both keys; the file name comes from `fail()`'s existing `$current_file` prefix.

2. **New `broken/` fixture** (`tests/fixtures/inbox-to-memory/broken/_memory/decisions/tags-themes-mixup-9YpQ2xLmZk.md`). A `memory_type: Decision` record (routes to `RECORD_KEY_ORDER`, never `NOTE_KEY_ORDER`) carrying only `tags` and `themes` beyond the minimum, closing well under line 20. Verified in isolation it produces exactly one failure (`frontmatter-key-domain`) and nothing else — no masked second defect.

3. **Suite updates** (`tests/inbox-to-memory-smoke.sh`):
   - Rewrote the `:151-156` comment (was: "the lone record ... carries no defect") since there are now two records in `broken/`, one clean (vendor-lock-window, the contradiction's link bait) and one carrying the new fixture's defect.
   - Updated the `v2 files:`/`failures:` counts in place: 19→20, 18→19 (not deleted — same call site, new numbers).
   - Added `require_failure` for the new fixture and touched the "eighteenth defect" comment to "twentieth" (same reasoning, new count).
   - Added an inline C-2 scope (`overrun_scope`, its own file, its own `trap`) reproducing the issue's exact case: all 19 `RECORD_KEY_ORDER` keys populated, frontmatter closing on line 21. Asserts `frontmatter-key-domain` fires and `frontmatter-budget` does not, with `v2 files: 1` / `failures: 1`.
   - Left C-3's two passing-case assertions untouched: the existing `journal-migrated` assertion at (now) line ~715 already exercises a migrated journal entry with `themes`/no `tags`, and the existing `refute_failure ... "vendor-lock-window-WJicoHVdFw.md"` (broken scope) already exercises a record with `tags`/no `themes`. Both are non-vacuous (v2, schema-bearing files), so I added no new fixture for C-3 — just confirmed both still pass under the new check.
   - Ran humanizer formally over every string added/rewritten in both files (fail message, both new/rewritten comments, the heredoc's title/body). Caught and fixed: a stray `-ing` construction in the fail message, a double-hyphen (`--`) used as an em-dash substitute in a new comment, the word "actually" (AI-vocabulary list) in another new comment, and a listy staccato line in the heredoc body — all revised in place.

## Mutation-testing proof (per the "prove your assertions discriminate" instruction)

Copied the repo to a scratch dir, moved the `tags_lines`/`themes_lines` check block in the copy's `lint-scope.sh` to sit *after* the `FRONTMATTER_LINE_BUDGET` guard (same code, new position — this is the exact defect C-2 exists to catch). Ran `tests/inbox-to-memory-smoke.sh` there: it failed, specifically on `require_failure ... frontmatter-key-domain` for the new `overrun_scope` fixture, because the mutated lint now reports `frontmatter-budget` instead. Restored/discarded the scratch copy afterward (never touched the real tree). Did this twice — once mid-work, once again after the humanizer pass on the final code — both times the suite failed as expected under the mutation and passed on the unmutated tree.

## Verification run against the real tree

`bash tests/inbox-to-memory-smoke.sh && bash tests/file-issue-smoke.sh && bash tests/handoff-smoke.sh` — all three exit 0.

Also ran `.agent-guild/scripts/check-diff-scope.py inbox-to-memory/ tests/ --ignore .agent-guild/` — OK, 3 paths in scope (matches the 3 files under `artifacts`).

## C-7 note for the checker

Ran the same `comm -23` check the checker will run (extracted `require_*`/`refute_*` lines from `d4ce6d2` and the working copy, sorted both). Two lines show up in the diff: the old `v2 files: 19` / `failures: 18` literals. These are the C-6-mandated arithmetic update at the same call site (now `v2 files: 20` / `failures: 19`), not a deletion — the constitution's own C-7 failing example distinguishes "deleted" from "updated when the fixture count changes." Flagging this up front so it doesn't read as a surprise.

## Judgment calls

- Did not touch `references/machine-contracts.md` (C-4/C-5, explicitly T-002's scope per the task's house constraints and clause list).
- Sited the check inline in `check_frontmatter` rather than as a separate function — the spec said either was fine; inline kept the diff smaller and put the reachability comment right where the ordering matters.
- For C-3, relied on pre-existing suite assertions rather than adding new ones, since the constitution's own check_method for C-3 names the existing `journal-migrated` line as the live discriminating assertion, and the existing `vendor-lock-window` refute already non-vacuously covers tags-only records. Happy to add explicit new assertions if the checker wants belt-and-suspenders here, but reading the clause text, I don't think it's required.
