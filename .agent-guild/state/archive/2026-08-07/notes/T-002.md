# T-002 self-report

## What I changed

- `inbox-to-memory/references/machine-contracts.md:29` — replaced the false headroom sentence.
- `inbox-to-memory/references/machine-contracts.md:152` — added the `frontmatter-key-domain` table row.
- `inbox-to-memory/references/machine-contracts.md:156` — added a prose note on why the check runs ahead of the budget.
- `tests/inbox-to-memory-smoke.sh` — one `require_text "$contracts" "frontmatter-key-domain"` pin plus a two-line comment.
- `.agent-guild/state/commit-message.md` — the commit message.

## Numbers, recounted

Sourced the two key-order variables straight out of `lint-scope.sh:20-21` and counted with `wc -w` rather than by eye. `NOTE_KEY_ORDER` = 17 names, so with the opening fence on line 1 and keys on 2-18, the closing fence lands on 19: one spare line. `RECORD_KEY_ORDER` = 19 names, closing on line 21, already over. With `tags` and `themes` mutually exclusive the ceiling is 18, closing on exactly line 20. Matches the brief and the constitution, but derived independently.

## Taste calls

**The table row stayed short; the precedence fact moved to prose.** My first draft crammed "checked ahead of the budget, so an overlong block still reports the mixup" into the "Fails when" cell. It ran 150 characters against a 69-character column and read as two facts wearing one hat. Every other row in that table is a single trigger condition, so the row is now just the trigger and the ordering fact sits in a sentence below the table, where the doc already keeps this kind of explanation.

**An accuracy slip the humanizer pass caught.** The precedence note first read "a record carrying both keys is over the budget by construction." That is false for a sparse record: five keys including both `tags` and `themes` fits fine. The overrun only follows when most of the order is populated. Now says so. Same fix applied to the commit message's opening sentence, which had the same overreach.

**Headroom paragraph.** Dropped my opening flourish ("Headroom is thinner than that sounds") — it editorialized before saying anything. Added the counterfactual "would close on line 21" so a reader learns why 19 names is not reachable rather than just being told the ceiling is 18.

**Markdown lint.** MD060 table-alignment warnings fire on my new row. They fire on essentially every table row in this file already (lines 17, 69, and the whole lint table): the body rows are 102 characters against a 101-character header. My row is byte-for-byte the same width as its siblings. Reflowing the table to satisfy the linter would have meant rewriting rows this task does not own, so I matched the file instead.

## C-7, one thing worth flagging

`comm -23` of base-against-current is **not** empty for `tests/inbox-to-memory-smoke.sh`. Two lines differ:

```
require_line "$broken_out" "failures: 18" broken   ->  "failures: 19"
require_line "$broken_out" "v2 files: 19" broken   ->  "v2 files: 20"
```

Both are T-001's, not mine — the arithmetic updates its new broken-scope fixture forced. Neither assertion was deleted or weakened; both still run and still assert the broken scope's arithmetic against the corrected count. C-7's own failing example names the opposite case as the defect ("deleted rather than updated when the new fixture changes the count"), so the clause text is satisfied. Only the mechanical `comm` shortcut in the check method overshoots, since it cannot tell an update from a deletion.

I did not file a dispute over this. The clause is satisfiable as written and no verdict exists yet; raising it preemptively would stall the task. Recording it here so the trail exists if a FAIL cites it.

## Verification run

All three suites exit 0:

```
i2m: 0   file-issue: 0   handoff: 0
```

`python3 .agent-guild/scripts/check-diff-scope.py inbox-to-memory/ tests/ --ignore .agent-guild/` reports `OK: 4 path(s) in scope`.

Did not touch `lint-scope.sh` or any fixture.
