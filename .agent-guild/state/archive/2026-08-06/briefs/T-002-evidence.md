# Locally collected evidence for T-002's deterministic clauses

Collected by the orchestrator on the project host at 2026-08-06. The far side
executes nothing; it judges these results as given.

## C-12 — no assertion deleted or weakened

`git diff 1f17478 -- tests/` has exactly three `-` lines. Two are diff headers
(`--- a/...`). The one removed content line is fixture prose, not an assertion:

```
-Verbatim transcript omitted from the fixture. Line refs above are illustrative.
```

No `require_*` or `refute_*` line appears among the removals.

All three suites, run verbatim:

```
bash tests/inbox-to-memory-smoke.sh   -> exit 0   ("inbox-to-memory smoke: ok")
bash tests/file-issue-smoke.sh        -> exit 0
bash tests/handoff-smoke.sh           -> exit 0   ("Generated skill is current")
```

## C-13 — the diff stays in scope

The constitution's command, run verbatim:

```
test -z "$( { git diff --name-only $(git merge-base main HEAD) -- ':(exclude)inbox-to-memory/' ':(exclude)tests/' ':(exclude).agent-guild/' ':(exclude)CLAUDE.md' ':(exclude).gitignore'; git ls-files --others --exclude-standard -- ':(exclude)inbox-to-memory/' ':(exclude)tests/' ':(exclude).agent-guild/' ':(exclude)CLAUDE.md' ':(exclude).gitignore'; } )"
```

exit 0 — nothing outside the allowed paths has changed since the branch point,
tracked or untracked.

## C-15 — Tier 2 cannot reach the budget

The constitution's command, run verbatim:

```
test $(( $(sed -n 's/^NOTE_KEY_ORDER="\(.*\)"$/\1/p' inbox-to-memory/scripts/migrate-scope.sh | wc -w) + 2 )) -le 20
```

exit 0. `NOTE_KEY_ORDER` holds 17 names, unchanged from `1f17478`, so a rendered
note closes on line 19 at worst.

---

# Addendum: the emitted Tier 2 extracts

The first crossing blocked because C-3 asks the checker to read each note's
emitted extract beside its written summary, and the extracts are generated
output rather than files in the repo — so nothing in the brief carried them.
They are reproduced below in full.

Generated by the orchestrator with the shipped script, against a throwaway git
repo built from `tests/fixtures/inbox-to-memory/old-only/` the same way the
suite builds its scope:

```
bash inbox-to-memory/scripts/migrate-scope.sh <scope> --tier2-extract <dir>
```

The run reported `migrated: 4`, wrote nothing to the scope, and emitted two
note extracts plus a proposals skeleton.

## The seam, checked directly

`Cascade Analytics` is the raw-content-only name T-001 planted.

```
grep -c Cascade tests/fixtures/.../2025-11-04-atlas-scoping-call-3iMu15QJ_x.md  -> 1
grep -c Cascade <emitted extract for 3iMu15QJ_x>                               -> 0
```

The name is in the note and absent from the extract, which is what C-1 asserts
the fence does. `Marcus` appears in both, which is why C-1 and C-2 must key on
`Cascade Analytics` rather than on any attendee name.

## Extract: `3iMu15QJ_x.extract.md`

```
## Notable Quotes

1. "We are not doing two vendors in one region again." — Priya Raghavan (raw: L12)
2. "Billing is frozen until Q1, and nobody wants to be the one who unfreezes it." — Marcus Dell (raw: L27)

## Tensions

- Priya wants a single vendor per region; Marcus reads the existing contracts as already committing two in EMEA. — Priya Raghavan, Marcus Dell (raw: L34)

## Stated Assumptions

- The EMEA contract renewal lands before the rollout starts.

## Unstated Assumptions

- Everyone in the room treated "region" as the billing region, not the deployment region. Nobody said so.

## Open Questions

- Who owns the EMEA contract renewal?
- Does the billing freeze cover configuration changes or only schema changes?

## Action Items / Memory Candidates

- [ ] @Marcus (priority: high) — pull the EMEA vendor contracts before next week (raw: L41)
- [memory candidate: project] One vendor per region is the default; two requires an explicit exception.
- [memory candidate: client] Billing schema is frozen until Q1 and unfreezing needs Finance sign-off.
```

## Extract: `P5spzLt4Bz.extract.md`

```
## Notable Quotes

1. "The exception process is the whole reason the last rollout took nine months." — Priya Raghavan (raw: L8)

## Tensions

- Priya wants the exception path documented; nobody in the room owns documenting it. — Priya Raghavan (raw: L19)

## Stated Assumptions

- The rollout starts in EMEA regardless of the contract situation.

## Unstated Assumptions

- The exception process is assumed to already exist somewhere.

## Open Questions

- Who owns the EMEA contract renewal?

## Action Items / Memory Candidates

- [ ] @Kendrick (priority: medium) — find out whether an exception process is written down anywhere (raw: L22)
```

## The proposals skeleton the extract run emits

```yaml
notes:
  3iMu15QJ_x:
    file: <scope>/notes/2025-11-04-atlas-scoping-call-3iMu15QJ_x.md
    summary: ''
    entities: []
  P5spzLt4Bz:
    file: <scope>/notes/2025-11-18-atlas-working-session-P5spzLt4Bz.md
    summary: ''
    entities: []
```

## The two summaries the suite author wrote

These are the strings C-3's judgment half is about. They are defined at
`tests/inbox-to-memory-smoke.sh:716-717` and used in every Tier 2 proposals
fixture the suite builds.

- `seam_summary` (for `3iMu15QJ_x`): "Priya and Marcus disagreed over one-vendor-per-region in EMEA, with the billing freeze and contract renewal left as open questions."
- `other_summary` (for `P5spzLt4Bz`): "Priya pushed to have the exception process documented, and nobody in the room owns writing it down."

Read each against its extract above. C-3 fails on any claim that does not trace.
