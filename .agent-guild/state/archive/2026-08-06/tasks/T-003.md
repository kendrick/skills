---
id: T-003
title: verify-migration.sh, with suite assertions
spec: .agent-guild/state/spec.md#what-to-build
clauses: [C-6, C-7, C-8, C-9, C-10, C-12, C-13, C-16]
executor: worker-standard
executor_model: sonnet
checker: checker-judgment
check_method: >-
  C-6: checker-judgment: run the suite (exit 0) and confirm both cases are
  asserted: a planted lint defect makes verify-migration.sh exit nonzero,
  report the lint's failure count, and name the failure; and an aborting lint —
  the suite runs verify-migration.sh from a copy of inbox-to-memory/scripts/
  whose lint-scope.sh is a stub writing one stderr line and exiting 2 with no
  summary — also makes verification exit nonzero with a report attributing the
  failure to the lint's exit status. Confirm the script reads the lint's exit
  status, resolves the lint as a sibling via dirname "${BASH_SOURCE[0]}", and
  carries its own yq preflight. C-7: checker-judgment: confirm the suite asserts
  the link sweep counts targets from the pre-migration --since ref, reports the
  id-fallback count, and fails with verify-link when a target resolves neither
  way. C-8: checker-judgment: confirm the suite asserts verify-migration.sh
  fails with verify-rename for a rename COMMITTED since the --since ref (an
  implementation reading git status --porcelain must fail this), and for a
  committed deletion, with renames and deletions named separately. C-9:
  checker-judgment: confirm the suite asserts a scope with a planted defect is
  byte-identical before and after a failing run, and a run with two planted
  defects reports both. C-10: checker-judgment: confirm the suite asserts the
  record paragraph appears on stdout on a passing run with that run's real
  counts, that no file under the scope contains it afterward, and that a
  failing run emits no record. C-12: run `git diff 1f17478 -- tests/` (no
  assertion removed or loosened) and all three suites (exit 0). C-13: run the
  constitution C-13 script check verbatim (exit 0). C-16: checker-judgment: read
  verify-migration.sh's header comment, its inline comments, and its failure and
  report strings against the humanizer skill's pattern list and the voice of
  lint-scope.sh and migrate-scope.sh; flag comments that restate what the next
  line does rather than why it's there, rule-of-three padding, and promotional
  framing. The record paragraph's final prose is T-004's, not this task's.
status: complete
retries: 1
max_retries: 2
deps: [T-002]
escalations: []
artifacts: [inbox-to-memory/scripts/verify-migration.sh, tests/inbox-to-memory-smoke.sh]
---

## Spec excerpt

Create `inbox-to-memory/scripts/verify-migration.sh` — it does not exist — plus the smoke-suite assertions that pin it. It is a standalone verification sweep: `verify-migration.sh <scope> --since <ref>`, runnable after the fact against a migration someone applied and committed yesterday. That standalone-ness dictates several shapes below.

What a run does:

1. **Refuse bad input first**: not-a-directory and not-an-opted-in-scope (needs `_inbox/` plus `_memory/` or `entries/`) exit 2 with one stderr line, same idiom as the neighbors. Carry your own `yq` preflight exactly like `lint-scope.sh:63` and `migrate-scope.sh:76` do — verification must fail on its own terms when the tool is missing, not inside the lint.
2. **Lint sweep (C-6)**: run `lint-scope.sh` over the scope and read its EXIT STATUS — never grep its output for a summary line an aborted run never prints. Report the lint's failure count when it ran, or attribute the failure to the lint's exit status when it aborted (exit 2 before any summary). Resolve the lint as a sibling of this script — `"$(dirname "${BASH_SOURCE[0]}")/lint-scope.sh"`, the way `tests/inbox-to-memory-smoke.sh:8` resolves the repo root — so a copy of `scripts/` runs that copy's lint. The suite tests the abort case through exactly that seam: a copied `scripts/` dir whose `lint-scope.sh` is a stub exiting 2.
3. **Link sweep (C-7)**: collect every wiki-link target present in the scope at `--since <ref>` (from git history, NOT the migrated tree — a link deleted during migration must still be checked) and confirm each still resolves after: by filename first, by the trailing ten-character id second. Resolving by id where it used to resolve by name is a pass; report how many took the fallback. Unresolvable targets fail with diagnostic `verify-link`.
4. **Rename sweep (C-8)**: read file statuses from `git diff --name-status -M <since-ref>` and fail on any status other than `M`, naming renames and deletions separately, diagnostic `verify-rename`. Never `git status --porcelain` — it reports a clean tree for exactly the committed-migration case this script exists for.
5. **Report, never repair (C-9)**: a failing run prints EVERY failure it found, changes nothing on disk, and exits nonzero. No path edits, deletes, restores, or re-runs anything.
6. **The record (C-10)**: a passing run ends by printing one paragraph to stdout — scope, date, the run's real counts — for the user to paste into the scope's patterns journal. A failing run prints no record. Nothing writes the paragraph to any file; `patterns-journal/` is never touched. (T-004 will own the paragraph's prose; write it plainly here and expect T-004 to polish the template.)

Suite assertions the checker will look for (build scopes with the throwaway-repo harness at `tests/inbox-to-memory-smoke.sh:566-575`; migrate with the existing Tier 1 `--apply`):

- Planted lint defect → nonzero exit, failure count reported, failure named (C-6 first case).
- Stub-lint abort → nonzero exit, report attributes the failure to the lint's status (C-6 second case).
- Link deleted during migration → `verify-link` failure; fallback count reported (C-7).
- COMMITTED rename since `--since` → `verify-rename`; committed deletion → named separately (C-8). Commit the rename in the throwaway repo — a working-tree rename doesn't discriminate porcelain from diff.
- Failing run leaves the scope byte-identical; two planted defects both reported (C-9).
- Passing run: record paragraph on stdout with matching counts; no file under the scope contains it; failing run emits none (C-10).

House constraints:

- Only `inbox-to-memory/` and `tests/` may change (C-13). Do not touch `SKILL.md` or `references/` — docs are T-004.
- Suites gain assertions, never lose them (C-12); all three suites exit 0 before and after.
- Match the house idiom: bash, `set -euo pipefail` semantics as the neighbors have them, `fail`-style diagnostics, comments explain why.
- Environment gotcha: `grep -q` is unreliable here (an RTK hook rewrites commands). Prefer capturing output and testing with `test -z`/`test -n` or counting, in both the script and the suite.

When done: set this task's `status` to `needs-check` and list changed files under `artifacts`.

## Rework diagnosis

<!-- ORCHESTRATOR appends here on each FAIL, copied verbatim from the checker's
verdict Diagnosis. Newest at the bottom, headed with the attempt it addresses
(e.g. "### sonnet r1"). Empty until the first failure. -->

### sonnet r0

Two blockers. Six clauses (C-6, C-8, C-9, C-12, C-13, C-16) came back SATISFIED and their diagnosis text is in `verdicts/T-003-sonnet-r0.md` — do not undo any of that work. The two failures are copied verbatim below.

**Both failures have the same shape, and it is worth naming before you read them: the script is correct in each case, and the suite fails to pin it.** A checker proved each one by mutation — swapping in a deliberately wrong implementation and watching the suite still pass. Your job is to make those mutants fail, not to change the script's behavior. The codex-lane second opinion reached both findings independently.

> **C-7** (blocker): FAILS the 'counts targets from the pre-migration ref rather than the post-migration tree' half of the check. The script itself is correct (verify-migration.sh:129-137 enumerates via git ls-tree/git show at $since_ref), but NO suite assertion pins it, so the clause's own failing example is untested. Proof by mutation: I replaced since_tree_targets' body with a post-migration-tree read (find "$scope" -name '*.md' -exec cat {} + | grep -oE '\[\[[^]|]*' ...) in a copy of the repo and the whole suite still exited 0. Both link scenarios are blind to the difference: in the passing scope the link lives in note A, which survives migration, and in the combo scope the DELETED file is the link's target (note B), not its source -- note A is still in the tree carrying the link either way. Fix: give the suite a scope where the link is only observable at the --since ref, e.g. commit the deletion of the note that CONTAINS a wiki link (or drop the link line in the post-migration commit) and assert both the 'links checked: N' count and the verify-link failure; a tree-based sweep then reports 'links checked: 0' and passes, while the shipped implementation fails. The other two halves of the check are satisfied: id-fallback count asserted at 978 ('links checked: 1 (id fallback: 1)', which I reproduced), verify-link failure asserted at 1060 (reproduced).
>
> evidence: inbox-to-memory/scripts/verify-migration.sh:129-147 vs tests/inbox-to-memory-smoke.sh:978,1060. Mutation run (tree-reading since_tree_targets, repo copied to scratch): 'MUTANT-A (tree-based links) suite exit=0 / inbox-to-memory smoke: ok'. Contrast: the porcelain mutant for C-8 exits 1 on the same suite, so the harness does catch implementation swaps it actually asserts.

> **C-10** (blocker): FAILS one of three required assertions: the suite never pins the record paragraph to that run's real counts. Suite 982-983 asserts only the prefix 'Verified $v_pass against $v_pass_since' and the trailing 'Paste this paragraph into the scope's patterns journal.'; the counts asserted at 977-981 come from the summary block (which prints on failing runs too), not from the paragraph. Proof by mutation: hard-coding the paragraph's counts to '0 lint failures, 0 links checked (0 by id fallback), 0 renames, 0 deletions' while the run really checked 1 link with 1 id fallback leaves the suite at exit 0 -- exactly the shape of the clause's failing example, a record certifying numbers the run didn't produce. Fix: add one assertion on the paragraph's interior, e.g. require_output "$pass_out" "0 lint failures, 1 links checked (1 by id fallback), 0 renames, 0 deletions". The other two halves ARE satisfied and I reproduced both: no file under the scope contains the paragraph after a passing run (suite 987-994, and my own grep -rlF over the scope returned nothing, with the patterns-journal/ sentinel intact), and a failing run emits no record (suite 1064-1068; my combo run's output ends at 'failures: 3' with no 'Verified ... against' line).
>
> evidence: tests/inbox-to-memory-smoke.sh:977-994,1064-1068; inbox-to-memory/scripts/verify-migration.sh:204-207. Mutation run (paragraph counts hard-coded to zeros): 'MUTANT-F (hardcoded record counts) suite exit=0 / inbox-to-memory smoke: ok'. My passing run printed 'links checked: 1 (id fallback: 1)' and a paragraph reading '... 0 lint failures, 1 links checked (1 by id fallback), 0 renames, 0 deletions', so the implementation is right and only the assertion is missing.

### sonnet r0

Six of eight clauses SATISFIED (C-6, C-8, C-9, C-12, C-13, C-16). The script itself is correct in both failing cases — what fails is that no suite assertion pins the correct behavior, so a broken implementation passes. Both were proven by mutation: the checker swapped in a deliberately wrong implementation and the whole suite still exited 0.

- **C-7** (blocker): FAILS the "counts targets from the pre-migration ref rather than the post-migration tree" half of the check. The script itself is correct (verify-migration.sh:129-137 enumerates via git ls-tree/git show at $since_ref), but NO suite assertion pins it, so the clause's own failing example is untested. Proof by mutation: I replaced since_tree_targets' body with a post-migration-tree read (find "$scope" -name '*.md' -exec cat {} + | grep -oE '\[\[[^]|]*' ...) in a copy of the repo and the whole suite still exited 0. Both link scenarios are blind to the difference: in the passing scope the link lives in note A, which survives migration, and in the combo scope the DELETED file is the link's target (note B), not its source — note A is still in the tree carrying the link either way. Fix: give the suite a scope where the link is only observable at the --since ref, e.g. commit the deletion of the note that CONTAINS a wiki link (or drop the link line in the post-migration commit) and assert both the 'links checked: N' count and the verify-link failure; a tree-based sweep then reports 'links checked: 0' and passes, while the shipped implementation fails. The other two halves of the check are satisfied: id-fallback count asserted at 978 ('links checked: 1 (id fallback: 1)', which I reproduced), verify-link failure asserted at 1060 (reproduced).

  evidence: inbox-to-memory/scripts/verify-migration.sh:129-147 vs tests/inbox-to-memory-smoke.sh:978,1060. Mutation run (tree-reading since_tree_targets, repo copied to scratch): 'MUTANT-A (tree-based links) suite exit=0 / inbox-to-memory smoke: ok'. Contrast: the porcelain mutant for C-8 exits 1 on the same suite, so the harness does catch implementation swaps it actually asserts.

- **C-10** (blocker): FAILS one of three required assertions: the suite never pins the record paragraph to that run's real counts. Suite 982-983 asserts only the prefix 'Verified $v_pass against $v_pass_since' and the trailing 'Paste this paragraph into the scope's patterns journal.'; the counts asserted at 977-981 come from the summary block (which prints on failing runs too), not from the paragraph. Proof by mutation: hard-coding the paragraph's counts to '0 lint failures, 0 links checked (0 by id fallback), 0 renames, 0 deletions' while the run really checked 1 link with 1 id fallback leaves the suite at exit 0 — exactly the shape of the clause's failing example, a record certifying numbers the run didn't produce. Fix: add one assertion on the paragraph's interior, e.g. require_output "$pass_out" "0 lint failures, 1 links checked (1 by id fallback), 0 renames, 0 deletions". The other two halves ARE satisfied and I reproduced both: no file under the scope contains the paragraph after a passing run (suite 987-994, and my own grep -rlF over the scope returned nothing, with the patterns-journal/ sentinel intact), and a failing run emits no record (suite 1064-1068; my combo run's output ends at 'failures: 3' with no 'Verified ... against' line).

  evidence: tests/inbox-to-memory-smoke.sh:977-994,1064-1068; inbox-to-memory/scripts/verify-migration.sh:204-207. Mutation run (paragraph counts hard-coded to zeros): 'MUTANT-F (hardcoded record counts) suite exit=0 / inbox-to-memory smoke: ok'. My passing run printed 'links checked: 1 (id fallback: 1)' and a paragraph reading '... 0 lint failures, 1 links checked (1 by id fallback), 0 renames, 0 deletions', so the implementation is right and only the assertion is missing.

RULING carried forward from r0, do not revisit: the `yq` preflight needs no suite assertion. The check_method switches subject mid-sentence, and constitution C-6 lists removing yq from PATH among "two triggers that look like they would work and don't" — a trap for the checker, not an assertion the suite owes. Confirmed by source read and by execution.

## Courier comparison

```yaml
repo: skills
job: 2026-08-06
task: T-003
verdict_pair: [{of_record: T-003-sonnet-r0.json, lane: T-003-sonnet-r0-codex.json}, {of_record: T-003-sonnet-r1.json, lane: T-003-sonnet-r1-codex.json}]
clause_types_sampled: mixed  # C-6,7,8,9,10,16 judgment; C-12,13 deterministic per constitution.md
brief_framing: unknown
courier_outcome: fail
agreement_on_outcome: agree
disagreement_kind: null   # substantive | evidence_packet | null
unique_courier:
  defect: 0
  inference: 0
  evidence_quality: 0
  coverage: 0
unique_checker: 0
unique_checker_access_derived: 0   # of the above, needing repo access the lane cannot have
overlap: 2  # shared blocker findings (C-7, C-10), not total clauses checked — the prose's own "2 blockers" framing, not a full-clause tally
changed_verdict: no
cost:
  wall_s: null
  tokens_in: 27854
  tokens_cached: null
  tokens_out: 2348
  brief_tokens: 4406
  vendor_calls: 3
retrofit_note: >-
  wall_s left null — the prose below states ledger line 8's started_at
  precedes the dispatch and its round duration_ms (60000) is unreliable
  against a crossing that took ~275s to appear; tokens_in/tokens_out are
  called "the vendor's own" in that same prose and are kept. The verdict
  JSON this crossing produced was malformed (unescaped quotes) and had to be
  read tolerantly for the comparison in the prose below — a data-quality
  caveat on top of the null'd cost field. brief_framing unknown — briefs/
  T-003.md is a clause/spec dump with no confirm/attack/open instruction
  surviving.
```

Of record: `verdicts/T-003-sonnet-r0.json` (checker-judgment, anthropic/claude-opus-5) — **fail**, two blockers among eight clause findings. Second opinion: `verdicts/T-003-sonnet-r0-codex.json` (openai/gpt-5.6-terra) — **fail**, the same two blockers. Ledger line 8.

**Unique to courier: 0. Unique to checker of record: 0. Overlap: 2 blockers, both on judgment clauses.**

**This is the first crossing on this job that is worth anything, and it agrees.** `tokens_in: 27854` confirms the artifacts actually crossed — the gap that made T-002's concurrence unverifiable. Working from the same material and blind to the verdict of record, the vendor reached both failures independently:

- **C-7**: "although the script reads targets from the pre-migration ref, the suite has no scenario where the link source exists only at that ref, so a post-migration-tree mutation would" pass. The checker of record proved that same claim by actually building the mutant and running it (suite still exited 0). Same defect, same reasoning, one derived and one proven.
- **C-10**: "the suite asserts the record's prefix and suffix and separately asserts summary counts, but never asserts that the record paragraph itself carries this run's real counts." Again identical to the verdict of record, which proved it by hard-coding the paragraph's counts to zeros and watching the suite pass.

Both agreed on the six satisfied clauses too, including C-16.

**What that buys #34.** A genuine convergence on judgment rubrics, not construction. C-7 and C-10 are exactly the kind of clause a single provider could plausibly miss — both are "the implementation is right but nothing pins it" defects, invisible to anyone reading the script rather than the assertions. Two providers found them independently. The unique-finding rate on this crossing is zero in both directions, and unlike T-002's it is measured over material both sides demonstrably received.

The one asymmetry worth naming: the verdict of record proved its two failures by mutation testing and the courier only reasoned to them. Same conclusions, different strength of evidence. A crossing that agrees is not the same as a crossing that would have caught it alone.

**Deterministic clauses**: C-12 and C-13 crossed as pre-run output and agree by construction. Discount them; the signal here is C-6 through C-10 and C-16.

**The independence claim carries a caveat, and it is the important one.** The courier's return states of each failure: "Same finding as the in-family checker produced on r0." It was dispatched with an explicit instruction not to read the verdict of record, and that sentence proves it read it. So the convergence above cannot be certified blind.

Two things argue the *vendor* was still uncontaminated even though the courier was. The vendor's C-7 reasoning is conditional — a post-migration-tree mutation "would" pass — where the checker of record had actually built that mutant and recorded the run. Had the checker's verdict been inlined, the vendor would most likely have echoed the mutation evidence rather than reasoning to it. The same asymmetry shows on C-10. That is suggestive, not conclusive.

Read this result, then, as: two providers reached the same two blockers on the same material, with the courier's own comparison happening after the fact against a verdict it should not have opened. For #34 the finding still stands, at reduced confidence. The fix is structural rather than a stronger prompt — the courier already had the instruction and ignored it; the verdict of record should be unreadable to a courier, not merely off-limits.

**Two further defects in the crossing, both the courier's, neither the vendor's:**

1. The verdict JSON on disk is **malformed** and does not parse — the vendor emitted unescaped inner double quotes inside its `evidence` strings (`sections "An aborting lint still fails verification" and ...`). The courier's contract is to retry the lane once and then write a schema-conforming `blocked` verdict carrying the raw response as evidence; instead it persisted the invalid JSON. The findings above were recovered by tolerant read for this comparison only. The file is deliberately left unrepaired — repairing a vendor's JSON would make the orchestrator the author of a check it commissioned.
2. Ledger line 8 reports `started_at: 2026-08-06T16:59:00Z`, which precedes the dispatch, and a round `duration_ms: 60000` against a crossing whose artifact took ~275s to appear. Treat the timing as unreliable; `tokens_in`/`tokens_out` are the vendor's own and are the useful part of the line.

### Retry 1

```yaml
clause_types_sampled: mixed  # C-6,7,8,9,10,16 judgment; C-12,13 deterministic per constitution.md
brief_framing: unknown
courier_outcome: pass
agreement_on_outcome: agree
unique_courier:
  defect: 0
  inference: 0
  evidence_quality: 0
  coverage: 0
unique_checker: 0
overlap: 8  # 6 judgment, 2 deterministic — per the prose's own breakdown
changed_verdict: no
cost:
  wall_s: null
  tokens_in: 279634
  tokens_cached: null
  tokens_out: 6186
  brief_tokens: 5448
  vendor_calls: unknown
retrofit_note: >-
  wall_s left null — the prose below calls ledger line 9's duration_ms
  (1905466) "worthless," self-reported by the courier as "estimated from
  token throughput" and matching neither the ~330s the verdict file took to
  appear nor the ~626s the courier agent itself ran. tokens_in/tokens_out/
  brief_tokens are treated as reliable in that same prose (proof the script
  and suite actually crossed) and are kept. vendor_calls left unknown rather
  than folding in vendor-calls.jsonl line 10 (task_id T-003, blocked,
  exit 143) — that line appears chronologically AFTER line 9's successful
  crossing and is never referenced anywhere in the archived prose, so its
  relationship to this retry (if any) is unrecoverable; flagging it here
  rather than silently dropping it or silently attributing it. brief_framing
  unknown — briefs/T-003-evidence.md (retry 1) is locally collected evidence
  with no confirm/attack/open instruction surviving.
```

Of record: `verdicts/T-003-sonnet-r1.json` (checker-judgment, anthropic/claude-opus-5) — **pass**, all eight clauses. Second opinion: `verdicts/T-003-sonnet-r1-codex.json` (openai/gpt-5.6-terra) — **pass**, all eight. Ledger line 9.

**Unique to courier: 0. Unique to checker of record: 0. Overlap: 8 (6 judgment, 2 deterministic).**

This crossing was dispatched with an explicit prohibition on opening any verdict file, which the r0 crossing had violated. Its verdict text references no other verdict and its report drew no comparison, so unlike r0 this convergence is plausibly blind. Ledger line 9 is also the first honest one on this job: real UTC `started_at`, and `tokens_in: 279634` against `brief_tokens: 5448` — proof the script and the 1,100-line suite actually crossed rather than being referenced by path.

Both sides agree the two r0 blockers are closed, and on the same mechanism: C-7 because `v_linkdrop` (suite 1074-1106) finally separates a since-ref sweep from a post-migration-tree read, and C-10 because the assertion at suite:986 pins the record paragraph's own interior rather than the summary block that also prints on failing runs.

**But read this agreement for much less than r0's.** Two checkers agreeing that nothing is wrong is weak evidence; they can miss the same thing in the same way, and nothing in a `pass` proves either looked hard. The r0 crossing is the one that carries weight for #34 — there, both providers independently named the same two defects, and defects are falsifiable in a way clean bills of health are not. The asymmetry from r0 also persists: the checker of record closed each blocker by rebuilding the mutant and watching the suite kill it, plus two extra mutants nobody asked for, while the courier reasoned from the assertion text. Same verdict, different depth.

**Deterministic clauses**: C-12 and C-13 crossed as pre-run output and agree by construction. Discount them — the sample is C-6 through C-10 and C-16.

**Courier defects this round: one.** No ledger line was appended until well after the verdict landed, and only after the orchestrator went looking. Four of the five crossings on this job have had a ledger problem (two invented timestamps, one placeholder preceding its own dispatch, one absent). `duration_ms: 1905466` on line 9 is worthless: the courier's own report says it was "estimated from token throughput." It matches nothing real — the verdict file appeared ~330s after dispatch and the courier agent itself lived ~626s. So all five crossings on this job have a fabricated or absent duration, and the lane's true wall-clock cost is unmeasured across the board. Only the token counts on lines 8 and 9 are the vendor's own and can be costed. The verdict JSON parsed cleanly this time, unlike r0's.

<!-- ORCHESTRATOR writes this once the second opinion lands, while #34 is still
open. Read both verdicts directly and record three counts: findings only the
courier raised, findings only the checker of record raised, and the overlap.
Name the clause behind each unique finding — #34 rules on the unique-finding
rate, and a count with no clause attached can't be audited later.

Say which cited clauses were deterministic. Those cross as pre-run output for
the far side to judge, so they agree by construction and are worth nothing as
evidence either way.

A denied or blocked second opinion goes here too, with the reason. An absence
recorded is data; an absence unrecorded reads later as agreement.

This section never reaches the vendor: compose-brief.py extracts only the spec
excerpt and rework diagnosis. -->
