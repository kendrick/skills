---
task: T-002
checker: checker-judgment
vendor: anthropic
model: claude-opus-5
verdict: PASS
checked_at: 2026-08-06T18:25:00Z
duration_ms: None
cost_usd: None
---

<!-- GENERATED FILE—do not hand-edit. Rendered by render-verdict.py
from the verdict JSON, the record of record. Edit the JSON and
re-render instead. -->

## Per-clause results

| clause | severity | description | evidence |
| ------ | -------- | ------------ | -------- |
| C-1 | blocker | Satisfied. I resolved the raw-content-only name from the fixture myself as "Cascade Analytics", the suite's C-1 assertion is keyed on that exact string, and the Tier 2 extract is a copy of the same $work/body that extract_body produced for the Tier 1 counts, not a second copy of the fence rule. | Resolved from fixture, not from T-001's self-report: `awk '/^## Raw Content/{exit} {print}' tests/fixtures/inbox-to-memory/old-only/notes/2025-11-04-atlas-scoping-call-3iMu15QJ_x.md \| grep -n Cascade` -> empty; below-fence grep -> `2:"Cascade Analytics flagged some concerns about the EMEA timeline," Marcus said.` So the raw-content-only name is "Cascade Analytics" in note id 3iMu15QJ_x. Suite keys on it: tests/inbox-to-memory-smoke.sh:711-713 sets `seam_id="3iMu15QJ_x"` / `seam_planted_name="Cascade Analytics"`, and :728-731 asserts `require_file "$seam_extract"` / `require_text "$seam_extract" "## Notable Quotes"` / `refute_text "$seam_extract" "$seam_planted_name"` -- a positive control plus the refute, so it cannot pass on an empty file. Teeth demonstrated independently: `grep -Fc 'Cascade Analytics'` = 1 on the raw fixture note, 0 on the extract I generated myself with `migrate-scope.sh <scope> --tier2-extract <dir>`. Not vacuous, and not keyed on the Marcus trap: `grep -Fc 'Marcus'` = 3 in that same extract, so an assertion written against Marcus would not have held. Single copy of the rule: `grep -n 'Raw Content' inbox-to-memory/scripts/migrate-scope.sh` returns exactly one line, `178: f && /^## Raw Content/ { exit }`, inside extract_body. migrate-scope.sh:356 `extract_body "$file" >"$work/body"` feeds the counts at :357-361, and the Tier 2 path at :369 is `cp "$work/body" "$tier2_extract_dir/$note_id.extract.md"`. The entity check at :396 greps that same $work/body. No Tier 2 path re-reads the note file. |
| C-2 | blocker | Satisfied. The apply emits tier2-entity-unsourced naming the file and the entity, leaves the note byte-identical (verified by SHA-256 before and after, not by reading the code), and the suite asserts both against a proposals row naming "Cascade Analytics". | Re-derived on my own throwaway scope built from the old-only fixture with a proposals row `entities: [Cascade Analytics]`. Run output: `.../notes/2025-11-04-atlas-scoping-call-3iMu15QJ_x.md: tier2-entity-unsourced: "Cascade Analytics" does not appear in this note's Tier 2 extract; left alone` and `left alone: 1`. Byte identity by hash, not by intent: shasum -a 256 before = 0f20904d64586d602f69fdc092601b0db18aba4e5bd33db7fcba464fcdd88442, after = 0f20904d64586d602f69fdc092601b0db18aba4e5bd33db7fcba464fcdd88442 -- identical. `git status --porcelain` after the refusal lists three other files and NOT the seam note, so it was never partially written. Structurally this holds because the refusal at migrate-scope.sh:406-410 `continue`s before the only write in the loop, `[[ "$apply" -eq 1 ]] && cat "$work/candidate" >"$file"` at :463. Suite asserts both halves: tests/inbox-to-memory-smoke.sh:848-862 -- proposals `entities: [$seam_planted_name]` at :853, `require_output "$unsourced_out" "...tier2-entity-unsourced: \\"$seam_planted_name\\""` at :857, hash compare against the git baseline at :858-861, plus `refute_text "$lifecycle_scope/$seam_note" "schema:"` at :862. |
| C-3 | blocker | Satisfied on both halves. I read each of the two fixture summaries beside its note's own emitted extract; every claim traces, with no invented date, name, decision, or outcome. The multiline refusal fires with a named tier2-* diagnostic and leaves the note byte-identical, and lint-scope.sh exits 0 over a Tier-2-applied scope. | Judgment half. Only two summaries are written by any Tier 2 run in the suite (subset :753-758, batched :794-802, lifecycle :884-890 -- all draw from `seam_summary` :716 and `other_summary` :717). I generated both extracts myself with `--tier2-extract` and read them side by side. (1) seam_summary = 'Priya and Marcus disagreed over one-vendor-per-region in EMEA, with the billing freeze and contract renewal left as open questions.' Extract 3iMu15QJ_x.extract.md line 9 (Tensions): 'Priya wants a single vendor per region; Marcus reads the existing contracts as already committing two in EMEA. -- Priya Raghavan, Marcus Dell' supports the disagreement, the parties, the subject, and EMEA. Lines 21-22 (Open Questions): 'Who owns the EMEA contract renewal?' and 'Does the billing freeze cover configuration changes or only schema changes?' support both items being open questions. Nothing asserted beyond that -- notably it does not claim the disagreement was resolved. (2) other_summary = 'Priya pushed to have the exception process documented, and nobody in the room owns writing it down.' Extract P5spzLt4Bz.extract.md line 8 (Tensions): 'Priya wants the exception path documented; nobody in the room owns documenting it. -- Priya Raghavan' supports both clauses near-verbatim; line 4 quote corroborates. Neither summary is the clause's failing shape (no 'Cutover approved for March' style unsourced outcome or date). Mechanical half, re-derived by hash. Proposals with `summary: \|` spanning two lines produced `tier2-summary-multiline: summary must be a single line; left alone` and `left alone: 1`; shasum -a 256 before and after both 0f20904d64586d602f69fdc092601b0db18aba4e5bd33db7fcba464fcdd88442. Implementation at migrate-scope.sh:387-391 `continue`s before the :463 write. Suite asserts it at :864-880 (require_output :875, hash compare :876-879, refute schema: :880). Lint over a Tier-2-applied scope: I ran `lint-scope.sh` on my own batched-apply scope -> `v2 files: 4` / `failures: 0`; suite asserts the same at :772-773, :834-835, and :896-897. |
| C-4 | blocker | Satisfied. The suite asserts a Tier-1-only apply writes no summary:/entities: line anywhere, that the dry-run report nests each Tier 2 proposal inside its own file's diff block, and that approval works both per-file (subset) and batched -- all three, not just one. | Tier-1-only: tests/inbox-to-memory-smoke.sh:671-680 loops every tracked .md in $mig (a plain `--apply` at :604, no --tier2) counting `^summary:`/`^entities:` and exits 1 on any hit. I reproduced it: after `migrate-scope.sh <scope> --apply`, `grep -rn '^summary:\\\|^entities:' --include='*.md'` returned empty. Per-file subset: :752-770 -- proposals file names only $seam_id; asserts `require_text "$subset_scope/$seam_note" "summary: '$seam_summary'"` and `entities: [Priya Raghavan, Marcus Dell]`, then `refute_text "$subset_scope/$other_note" "summary:"` and `"entities:"`. Reproduced independently: seam note frontmatter carried both keys, other note carried neither, `left alone: 0` / `migrated: 4`. Batched: :793-832 -- a proposals file naming both ids; asserts summary on the seam note AND on the other note plus `entities: [Priya Raghavan]`. Reproduced: `migrated: 4`, both notes carry their own summary line. Grouping: :807-821 slices the dry-run output per `--- <file>` header with awk and requires a `+summary:` line inside each of the two note blocks, then :822-825 asserts `git status --porcelain` is empty. Reproduced: my `--tier2 <file>` dry run printed `+summary: 'Priya and Marcus...'` under the 3iMu15QJ_x header and `+summary: 'Priya pushed...'` under the P5spzLt4Bz header, the two _memory records got no summary line, and porcelain was empty. |
| C-5 | blocker | Satisfied. The suite hashes a note omitted from the proposals file against the same note after a Tier-1-only apply and asserts byte identity; I reproduced the comparison independently. | tests/inbox-to-memory-smoke.sh:775-782 compares `shasum "$subset_scope/$other_note"` against `shasum "$mig/$other_note"`. $mig is the right control: built from the same fixture at :569-571 (`cp -R "$fixtures/old-only/."`) and applied Tier-1-only at :604 (`--apply`, no --tier2 anywhere in its lifetime). Reproduced on my own pair of scopes from the same fixture -- subset apply omitting P5spzLt4Bz vs plain --apply: shasum -a 256 both 820d53c61fb420a593d200f19c5ecb8abba86fd234a718589c6ea9c92e136b43. A partial proposals file re-rendered nothing. |
| C-11 | major | Satisfied. The suite asserts a second run over a Tier-2-applied scope reports migrated: 0 and produces no proposals skeleton at all. | tests/inbox-to-memory-smoke.sh:899-909: `idem_out="$(bash "$migrator" "$lifecycle_scope" --tier2-extract "$idem_dir" 2>&1)"`, then `require_output "$idem_out" "migrated: 0"`, `require_output "$idem_out" "already v2: 4"`, and `[[ ! -e "$idem_dir/proposals.yaml" ]]` with an explicit failure message. $lifecycle_scope is Tier-2-applied: the seam note received summary+entities at :892-894. Reproduced on my own batched-apply scope (both notes carrying summary and entities): re-running `--tier2-extract` into a fresh dir gave `migrated: 0` / `already v2: 4` / `left alone: 0`, and the extract dir was empty -- no proposals.yaml, no .extract.md files. Keying is off the schema key via has_schema_key (migrate-scope.sh:324-327), which gates the whole Tier 2 block, not off the absence of a proposals file. |
| C-12 | blocker | Satisfied. No require_*/refute_* line present at 1f17478 was removed or loosened -- every one survives verbatim -- and all three suites exit 0. | `git diff 1f17478 -- tests/` has exactly one removed line in the entire diff, and it is fixture prose, not an assertion: `-Verbatim transcript omitted from the fixture. Line refs above are illustrative.` (the old-only note's Raw Content placeholder). Stronger check, since 'loosened' means edited in place: I extracted each suite at 1f17478 with `git show` and diffed the sorted set of assertion lines. `comm -23 old new` is empty for all three files. Counts: inbox-to-memory-smoke 164 -> 189 (+25 added, 0 lost), file-issue-smoke 44 -> 44, handoff-smoke 31 -> 31. Because the diff removes no suite line, the surviving assertions also still run in place rather than being stranded in dead code. Suites: `bash tests/inbox-to-memory-smoke.sh` exit=0, `bash tests/file-issue-smoke.sh` exit=0, `bash tests/handoff-smoke.sh` exit=0 (`inbox-to-memory smoke: ok`). Noted per the constitution's preamble that a green suite proves nothing on its own here, which is why the assertion-level evidence above carries this clause. |
| C-13 | blocker | Satisfied. The constitution's C-13 command, run verbatim, exits 0: nothing outside inbox-to-memory/, tests/, and the allowed harness paths has changed since the branch point, tracked or untracked. | Ran verbatim: `test -z "$( { git diff --name-only $(git merge-base main HEAD) -- ':(exclude)inbox-to-memory/' ':(exclude)tests/' ':(exclude).agent-guild/' ':(exclude)CLAUDE.md' ':(exclude).gitignore'; git ls-files --others --exclude-standard -- <same excludes>; } )"` -> exit=0. Corroborating: `git status --short` lists only `.agent-guild/scripts/__pycache__/validate-verdict.cpython-314.pyc`, `inbox-to-memory/scripts/migrate-scope.sh`, `tests/fixtures/inbox-to-memory/old-only/notes/2025-11-04-atlas-scoping-call-3iMu15QJ_x.md`, and `tests/inbox-to-memory-smoke.sh`. |
| C-15 | major | Satisfied. The constitution's C-15 command exits 0, NOTE_KEY_ORDER was not grown (still 17 names, unchanged since 1f17478), and no tier2-budget refusal was added. | Ran verbatim: `test $(( $(sed -n 's/^NOTE_KEY_ORDER="\\(.*\\)"$/\\1/p' inbox-to-memory/scripts/migrate-scope.sh \| wc -w) + 2 )) -le 20` -> exit=0, with the inner word count = 17, so the worst-case rendered note closes on line 19. Not grown: `git diff 1f17478 -- inbox-to-memory/scripts/migrate-scope.sh \| grep -E '^[+-]NOTE_KEY_ORDER'` returns nothing, i.e. the line is untouched; it still reads `NOTE_KEY_ORDER="schema body_schema id date type summary attendees tags topics entities source_file transcript_corrections open_questions resolved_questions deferred_tensions unpromoted_candidates related"` at migrate-scope.sh:24, with summary and entities already among the 17. The two refusals the diff adds are `tier2-entity-unsourced` and `tier2-summary-multiline`; there is no `tier2-budget` anywhere in the file. |
| C-16 | major | Satisfied. I read every comment and user-facing string this task added to migrate-scope.sh. All six new comments explain why rather than what, the new diagnostics and usage lines match the file's existing terse voice and the house tier2-*/'; left alone' idiom, and I found no rule-of-three padding, promotional framing, bolded inline headers, or code-restating comments. | New comments read in full at migrate-scope.sh:10-14, 111-113, 366-368, 384-386, 403-405, 412-414. Each carries a constraint or a consequence, not a paraphrase of the next line. The strongest are :412-414 'Fed straight into the props file rather than through `extras`: a summary is free prose, and `extras` splits on `;`, which a summary is entitled to contain.' (names the exact hazard the code avoids) and :403-405 'An entity absent from the extract came from raw content or from the model's own memory, neither of which belongs in a field the skill later greps as fact.' :366-368 opens with an identification ('This is the same $work/body the counts above just read') but earns it -- the rest states the invariant the entity check depends on. New user-facing strings: diagnostics at :388 `tier2-summary-multiline: summary must be a single line; left alone` and :407 `tier2-entity-unsourced: "..." does not appear in this note's Tier 2 extract; left alone` both follow the Tier 1 pattern at :441 and :447 (`...; left alone`) and the required tier2-* prefix. Errors at :115, :121, :128 ('--tier2 and --tier2-extract do not mix in one run'; 'tier2 proposals file not found: ...'; '--tier2-extract only writes extracts; it never applies') match the existing lowercase terse register of :97 'not a directory: $scope' and :82 'migrate one scope at a time'. Usage text :44-47 and the closing report line :486 `tier2 extracts: $tier2_extract_dir` match the surrounding key: value report style. Comments added to the suite are the same register -- tests/inbox-to-memory-smoke.sh:706-710 explains why every assertion keys on 'Cascade Analytics' rather than 'Marcus', and :804-806 explains why grouping matters ('not float free at the end of the report where it could be approved against the wrong note'). One non-finding I checked and am recording rather than raising: the usage block's continuation lines at :45 and :47 sit one column off the description column of :44/:46. That is whitespace, not prose, and nothing in C-16's text or rubric reaches it. |
