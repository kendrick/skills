# T-003 rework (sonnet r1) self-report

## Starting state

When I picked this up, the working tree already contained fixes for both
blockers named in the rework diagnosis (`verify-migration.sh` untracked,
`tests/inbox-to-memory-smoke.sh` modified but uncommitted). I did not write
these from scratch — I verified them against the diagnosis's exact bar
("make the mutants fail") rather than assuming they were correct because
they were present.

## C-7 fix, verified

`tests/inbox-to-memory-smoke.sh:1074-1106` adds a `v_linkdrop` scenario: note
A (which carries the wiki link) has the link *line* edited out post-migration
(an ordinary `M`, so C-8 stays uninvolved), while note B (the link's target)
is renamed away. A tree-reading sweep would see zero links in the current
tree; only a sweep reading the `--since` ref still finds the link and then
has to fail because the target no longer resolves. This is the diagnosis's
suggested alternative ("drop the link line in the post-migration commit")
rather than its first suggestion (delete the note containing the link) —
functionally equivalent, and it lets the scenario isolate C-7 from C-8 (a
straight delete of note A would itself register as a `D` in the rename
sweep, muddying the assertion). The suite asserts both `links checked: 1 (id
fallback: 0)` and the `verify-link` failure (lines 1105-1106).

**Verified by mutation**: copied the repo to scratch, replaced
`since_tree_targets`'s body with a post-migration find+grep over the tree
(the exact mutant class the diagnosis specified). Suite exit code went from
0 to 1, failing on `expected output not reported: links checked: 1 (id
fallback: 0)` — confirms the new scenario, not some other assertion, is what
catches it.

## C-10 fix, verified

`tests/inbox-to-memory-smoke.sh:986` adds
`require_output "$pass_out" "0 lint failures, 1 links checked (1 by id
fallback), 0 renames, 0 deletions"` — the exact assertion the diagnosis
suggested, pinning the record paragraph's interior rather than the summary
block above it (which prints on failing runs too and was already asserted
separately).

**Verified by mutation**: hard-coded the paragraph's five count arguments to
`"0" "0" "0" "0" "0"` in a scratch copy. Suite exit code went from 0 to 1,
failing on `expected output not reported: 0 lint failures, 1 links checked
(1 by id fallback), 0 renames, 0 deletions` — confirms this new line, not an
incidental assertion, catches the hard-coded-zeros mutant.

## Regression check

- `git diff 1f17478 -- tests/` shows only added lines touching
  `require_*`/`refute_*` calls — no existing assertion line was removed or
  reworded (grep for removed `require_`/`refute_` lines returns nothing).
- All three suites (`tests/inbox-to-memory-smoke.sh`,
  `tests/file-issue-smoke.sh`, `tests/handoff-smoke.sh`) exit 0 in the
  current working tree.
- C-13's exact check-method command (`test -z "$( { git diff --name-only
  $(git merge-base main HEAD) -- ... } )"`) passes — the diff stays scoped
  to `inbox-to-memory/`, `tests/`, `.agent-guild/`, `CLAUDE.md`.
- `verify-migration.sh` itself is unchanged from r0 (still reads targets via
  `git ls-tree`/`git show` at `$since_ref`, still interpolates real counts
  into the record paragraph) — this rework only added suite assertions, per
  the diagnosis's instruction not to change script behavior.

## Judgment call

`inbox-to-memory/scripts/migrate-scope.sh` and
`tests/fixtures/inbox-to-memory/old-only/notes/2025-11-04-atlas-scoping-call-3iMu15QJ_x.md`
are also uncommitted in the working tree (visible in `git status`), but they
predate this task (T-002's territory) and I did not touch them. Left as-is.
