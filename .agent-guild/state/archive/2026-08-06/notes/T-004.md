# T-004 self-report

## What I did

Read `migrate-scope.sh` and `verify-migration.sh` end to end before writing anything, then wrote the docs against observed behavior rather than the spec's description of it.

- `inbox-to-memory/SKILL.md` — added two `###` subsections to Migrate Mode: "Tier 2: Summary and Entities" (the three-step sidecar flow, the generation instruction, both refusal diagnostics, the separate gating, rejection-as-absence) and "Verifying a Migration" (the three sweeps, the diagnostics, report-never-repair, the stdout-only record).
- `inbox-to-memory/references/migration.md` — replaced the "not implemented here" sentence in The Two Tiers, added "The Tier 2 Sidecar" with a "Gating" subsection, added a "Verification" section, updated "Running It" to show both tiers and the verification command, and added two Safety Properties bullets the suite actually asserts.
- `inbox-to-memory/scripts/verify-migration.sh` — polished the record paragraph template (:205). All eight substitution variables intact.
- `tests/inbox-to-memory-smoke.sh` — four assertions added next to the existing routing-row block. Nothing removed or loosened.
- `.agent-guild/state/commit-message.md` — drafted.

## Taste calls

**The one behavioral fact the spec didn't mention, and the docs now do.** Tier 2 merges into the Tier 1 apply; it does not follow one. `has_schema_key` skips any note carrying `schema: 2`, so a scope already migrated Tier-1-only cannot have summaries added later. Both docs say so explicitly, and `Running It` shows the two tiers as alternative paths rather than a Tier 1 apply followed by a Tier 2 apply, which is what a reader would otherwise infer from the flag names and which would silently do nothing.

**The record paragraph.** Kept all three strings the suite pins, so zero test churn: the counts interior, `Verified <scope> against <ref>`, and the paste instruction. Spent the polish on a sentence that adds what the counts alone can't tell a reader a year later, namely that the link count is the distinct targets the scope carried *at the ref*, resolved against the current tree. I considered lifting the paste instruction out of the paragraph (it gets pasted into the journal along with everything else, which is a real defect) but that would have meant rewriting the leak-check grep and a second assertion, and the task authorized updating only the counts assertion. Left it in rather than widening the test diff.

**Proved the pin still bites.** Temporarily hard-coded the counts in the template; the suite failed at the `:986` assertion with `expected output not reported: 0 lint failures, 1 links checked (1 by id fallback), 0 renames, 0 deletions`. Restored and re-ran green.

**Humanizer pass.** Invoked the skill and revised against its audit rather than from memory. Things I changed on the second pass: three back-to-back X-not-Y constructions in the Gating subsection (kept the one the spec names, "an absence, not a rollback", rewrote the other two); an `-ing` participle in SKILL.md's verify paragraph; "ASR pass" as unexplained jargon; two "rather than" clauses in one sentence; a "means... means" repetition; and "one clever invocation," which was cute. I also pulled the bolded inline headers out of the three-sweeps bullet list in migration.md, even though the file's existing "What It Refuses to Do" list uses that shape, because C-16's check names it.

**Em dashes.** SKILL.md's existing prose uses spaced em dashes throughout, and the house rule forbids spaced ones. Rather than pick a side inside someone else's paragraphs, my additions use none. The commit message has one, unspaced.

## Verification run

- `tests/inbox-to-memory-smoke.sh`, `tests/file-issue-smoke.sh`, `tests/handoff-smoke.sh` all exit 0.
- C-13's script check exits 0.
- `git diff 1f17478 -- tests/` removes exactly one line, and it is fixture prose from T-001, not an assertion.
