---
id: T-001
title: Fix the migration record's prose and strengthen the assertions that pin it
spec: .agent-guild/state/spec.md
clauses: [C-1, C-2, C-3, C-4, C-5, C-6, C-7, C-8, C-9, C-10, C-11]
executor: worker-craft
executor_model: opus
checker: checker-judgment
check_method: >-
  C-1 checker-judgment: run the constitution's recipe, read the count-1 case off real
  stdout, then derive the printf's output at links_checked 0 and 2; all three must agree
  count with noun.
  C-2 checker-judgment: run the constitution's recipe, isolate the record paragraph from
  stdout, and confirm it holds no directive to the reader and stays a single physical line.
  C-3 checker-judgment: run the recipe's split-stream form; confirm instruction, blank
  line, record, end-of-output all on stdout with stderr empty, that the instruction's
  reference points forward at the paragraph below it, and that a failing run prints
  neither line.
  C-4 checker-judgment: read `git diff HEAD -- tests/inbox-to-memory-smoke.sh` and pair
  every removed assertion with an equally specific replacement; the assertion count over
  the record must not drop.
  C-5 checker-judgment: confirm every needle aimed at the record is emittable by the
  changed script, then falsify both negative checks -- plant the record under the scope
  for :1046, feed a record-bearing output to :1123.
  C-6 checker-judgment: copy the repo out, confirm the copy's suite is green, then apply
  the count mutant and the placement mutant in turn; each must exit nonzero naming a
  record assertion. Never mutate the repo and never `git checkout --`.
  C-7 checker-judgment: read `git diff HEAD -- inbox-to-memory/scripts/verify-migration.sh`
  and confirm every hunk lands in the `failures -eq 0` record block or its comment.
  C-8 `bash tests/inbox-to-memory-smoke.sh && bash tests/file-issue-smoke.sh && bash tests/handoff-smoke.sh`
  C-9 `python3 .agent-guild/scripts/check-diff-scope.py inbox-to-memory/scripts/verify-migration.sh tests/inbox-to-memory-smoke.sh inbox-to-memory/references/migration.md inbox-to-memory/SKILL.md`
  C-10 checker-judgment: read migration.md:148 and SKILL.md:406 against a real passing
  run and confirm both still hold; any edit must be forced and confined.
  C-11 checker-judgment: confirm the three required assertions exist in the shapes the
  clause names, and that the first two die under C-6's placement mutant.
status: complete
retries: 0
max_retries: 2
deps: []
escalations: []
artifacts:
  - inbox-to-memory/scripts/verify-migration.sh
  - tests/inbox-to-memory-smoke.sh
---

## Spec excerpt

From `kendrick/skills#29`. A passing `verify-migration.sh` run ends by printing one
paragraph for the user to paste into the scope's patterns journal. Two things about
that paragraph are wrong.

Today, on a run that checked exactly one link, it prints:

```text
Verified <scope> against <ref> on <date>: 0 lint failures, 1 links checked (1 by id fallback), 0 renames, 0 deletions. The link count covers every distinct wiki-link target the scope carried at that ref, resolved against the tree as it stands now. Paste this paragraph into the scope's patterns journal.
```

1. `1 links checked` is ungrammatical at count 1. It is the only counter that shows the
   problem: `lint failures`, `renames`, and `deletions` each call `fail` when nonzero, and
   a failing run prints no record at all, so those three are only ever printed at zero.
2. The closing sentence is an instruction to the reader, and it sits inside the thing the
   reader is told to paste. Follow it literally and the journal entry ends with a
   direction to paste it into the journal.

**What to build.** The count agrees with its noun at every value it can print, and the
paste instruction moves out of the record. The user settled the placement: the
instruction goes on its own stdout line **before** the record, with a blank line between,
so the record is the last thing the run prints. Reword it so it reads correctly from
there — printed above the paragraph, a sentence saying "this paragraph" points backward
at nothing.

**Where.** The record `printf` at `inbox-to-memory/scripts/verify-migration.sh:205-206`,
inside the `failures -eq 0` block, and the assertions in
`tests/inbox-to-memory-smoke.sh` that pin it: the interior counts at `:1041`, the paste
sentence at `:1042`, the record-leak needle at `:1046`, and the failing-run absence
needle at `:1123`.

Those four existing sites are not the whole edit. **C-11 requires assertions that do not
exist yet** — three of them, one of which goes in the failing combo case. Writing new
assertions is part of this deliverable, not optional hardening.

Keep whatever the count agreement needs *inside* the `failures -eq 0` block. A plural-noun
variable hoisted up next to the counters is a perfectly reasonable-looking way to do it
and will fail C-7, whose check confines every hunk to that block. The clause carries a
narrow exception for a variable read nowhere else, but inside the block needs no
exception.

Two doc sentences describe this output and are expected to stay true rather than change:
`inbox-to-memory/references/migration.md:148` and `inbox-to-memory/SKILL.md:406` both say
a passing run *ends* by printing the paragraph, ready to paste. Printing the instruction
first keeps both true. Read them before you settle the wording — C-10 governs them, and a
reword that quietly falsifies a doc you never opened is the failure mode here.

**Why it shipped, and what that means for you.** A checker caught both defects during the
guild run for `#16` and deliberately did not fail them: both strings were pinned verbatim
by assertions that a separate clause forbade touching, so the only route to fixing the
prose ran through a protected assertion. That block is lifted. Changing the assertions
and the template together is the point of this task, not a side effect of it.

That history is also the warning. `#16`'s retrospective found that every real defect in
that run was "implementation right, test vacuous," and this task edits assertions on
purpose. The constitution weights that risk heavily — C-4, C-5, C-6, and C-11 exist for
it. In particular: every assertion over the record today is a substring search across the
whole captured output, which cannot tell an instruction on its own line from one welded
back onto the record's tail. Read C-11 before you touch the suite.

**Out of scope.** The sweep's actual checks — link resolution, the id fallback, the lint
pass-through, rename and deletion handling, every counter's computed value, and the exit
status all stay exactly as they are. The seven summary `echo` lines at `:193-199` stay
too: `links checked: N (id fallback: M)` is a label and a value, not a sentence, and the
user ruled it out of scope. Two assertions read it as machine output. Do not add a
`refute_output` helper to the suite; the idioms already in the file are enough.

**Done when** a passing run's pasteable paragraph reads correctly at count 1 and carries
no instruction to paste it, the suite still fails against both a zeroed-counts mutant and
a welded-instruction mutant, and all three suites under `tests/` exit 0.

`yq` must be on PATH; the scripts refuse without it.

## Rework diagnosis

<!-- ORCHESTRATOR appends here on each FAIL, copied verbatim from the checker's
verdict Diagnosis. Newest at the bottom, headed with the attempt it addresses
(e.g. "### sonnet r1"). Empty until the first failure. -->

## Courier comparison

```yaml
clause_types_sampled: judgment  # crossing was deliberately scoped to C-1,2,3,4,11 — all judgment rubrics per constitution.md; C-8/C-9 (deterministic) and C-5,6,7,10 were excluded from the sample entirely
brief_framing: unknown
courier_outcome: pass
agreement_on_outcome: agree
unique_courier:
  defect: 0
  inference: 0
  evidence_quality: 1  # C-11: courier self-declared it could not run the placement-mutant load-bearing check in a read-only context, so its C-11 pass covers structural presence only
  coverage: 0
unique_checker: 0  # 0 unique defects; the prose separately notes a reach asymmetry (6 clauses the courier never sampled, plus deeper mutation-based verification within C-1/C-5/C-6/C-11) that doesn't fit a finding count — see retrofit_note
overlap: 5
changed_verdict: no
cost:
  wall_s: 43.0
  tokens_in: null
  tokens_cached: null
  tokens_out: null
  brief_tokens: 4910
  vendor_calls: 2
retrofit_note: >-
  vendor_calls sourced positively: vendor-calls.jsonl has two T-001 lines —
  the first killed at the 120s Bash-tool ceiling (exit 143, empty artifacts),
  the retry completing in 43s on a same-size (4910-token) brief at a 600s
  ceiling. cost reflects the successful retry only. tokens_in/tokens_out are
  null in the ledger itself for both lines; the prose doesn't discuss them,
  only brief_tokens, so they're carried through as recorded rather than
  assumed unreliable. unique_checker is 0 defects by the prose's own count,
  but the same paragraph documents a "large asymmetry in reach" — six
  clauses (C-5 through C-10 minus C-9... precisely C-5,6,7,8,9,10) the
  courier never sampled, plus mutation-based depth within C-1, C-5, C-6, and
  C-11 that the courier could not perform — which the schema's single
  unique_checker count can't carry without conflating "found a defect" with
  "verified more thoroughly." brief_framing unknown — the persisted brief
  (log/T-001-r0-codex-brief.md) is a clause/spec dump with no confirm/attack/
  open instruction surviving.
```

Lane: codex from a Claude host. Checker of record: `checker-judgment`, PASS on all
eleven clauses. Courier: `gpt-5.6-terra`, PASS on the five sampled.

**Sample, and why these five.** C-1, C-2, C-3, C-4, C-11 — every one a judgment
rubric. C-8 and C-9 were deliberately excluded: they are deterministic scripts
whose output crosses pre-run for the far side to merely read, so they agree by
construction. Sampling them would have manufactured a unique-finding rate near
zero by method rather than by evidence, which is the failure mode #34 has to
avoid if its "close the multi-provider line" condition is to mean anything.
**No deterministic clause was crossed.** C-5, C-6, C-7, and C-10 were not sampled
either — the crossing was scoped to five, not to all eleven.

**Findings only the courier raised: 1, and it is not a finding against the artifact.**
On C-11 (minor), the courier recorded that it could not attempt the placement-mutant
load-bearing verification in a read-only context, so it confirmed only the three
assertions' structural presence. That is a self-declared limit on its own reach,
not a defect it found. Recorded here as unique because it appears in one verdict
and not the other, but it should not count toward a unique-finding rate for #34:
it argues the courier saw *less*, not that it saw *more*.

**Findings only the checker of record raised: 0 defects, but a large asymmetry in reach.**
The checker of record also passed everything, so neither side raised a defect
against the artifact. What it did that the courier structurally could not:

- Six clauses the courier never saw at all — C-5, C-6, C-7, C-8, C-9, C-10.
- Within C-11, the exact half the courier flagged as out of reach: it isolated
  each of the two pass-side assertions by neutralizing the other under the
  placement mutant, and confirmed each fires alone.
- Within C-6, both mutants on an rsync'd scratch copy, green at baseline and
  restored to green after.
- Within C-5, all four negative checks falsified by feeding each what it hunts for.
- Within C-1, derivation *through* the worker's new `links_noun` logic by executing
  the extracted block with substituted counters, rather than reading the format string.

**Overlap: 5 clauses, unanimous PASS.** C-1, C-2, C-3, C-4, C-11 — the courier
concurred on every one, including the two rulings the checker of record was asked
to make explicitly (C-1's derivation at 0 and 2; C-3's blank line preceding the
instruction). It concurred on C-3 having been given the failing-run output as
inlined evidence.

**The absence, recorded.** The first crossing on this task was `blocked`. It hung
and was killed at a 120-second ceiling with no vendor response, and its conforming
blocked verdict plus a ledger line (`exit_code: 143`, empty `artifacts`) stand as
the record of that attempt. The retry used a 600s ceiling on a same-size brief
(4910 tokens both times) and completed in **43 seconds**. That is the fifth
codex-lane hang recorded in this repo and the second that a retry cleared, and
again one where brief size predicted nothing. Both ledger lines are in
`vendor-calls.jsonl`.

One correction for anyone reading the courier's own narrative later: the retry
courier reported that the first attempt "produced a partial verdict with C-3
failing." The first attempt's artifacts say otherwise — timeout at 120s, exit 143,
no vendor response, empty artifact list. That narrative described the retry
courier's own internal iterations, not the first dispatch. The ledger is the record.

**Net read for #34.** On five judgment clauses, a second vendor produced zero
substantive unique findings against a PASS, and explicitly could not perform the
one check that carried the most weight in this job — the mutation that proves the
new assertions are load-bearing. The cost was two crossings for one opinion.
