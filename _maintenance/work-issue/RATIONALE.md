# work-issue—rationale

Evidence tiers: **[E]** measured or observed in a real run, **[P]** practitioner reasoning from prior art, **[C]** convention inherited from this repo or its neighbors.

## Where This Came From

Five sources:

- **Issue #101 and the session behind it.** The loop this skill writes down was run five times in one session against `kendrick/cambium`—issues #54, #38, #42 and #17, plus four external-review repair rounds. It caught two errors in the orchestrator's own plans (an invented incompatibility between structured outputs and thinking, a false claim that a provider would make a bundle-budget test meaningful), both built to spec *and* flagged, because the dispatch said the plan might be wrong. It also produced the two failure modes rows 7 and 18 exist to close. Everything the skill knows that a reader would not guess traces to that session; the issue is where the numbers live.
- **This repo's `divvy-up`.** Wave derivation, the shape line as the user's correction point, the report contract, the wave gate, the one-rung-up retry, the failed-twice stop, and the workers-never-run-git rule. It stops at its Step 7 with a green verify and an assembled diff, which is exactly where this skill starts.
- **This repo's `adversarial-review`.** The rule a finding is a hypothesis until something that did not author it reproduces it, quoted rather than restated, and the trigger table Step 4's trigger matches the diff against, through the `match-triggers.py` script that ships beside it. Its roster is heavier than most diffs earn, so it became the last line of defense rather than the default (row 9).
- **This repo's `file-issue`.** The `gh auth status` preflight that degrades to a rendered draft instead of erroring, the rule that a rendered draft must never read as a filed issue, and the depth-governor idea that an input earns its own amount of process—which is what the Step 4 trigger does for the red-team phase.
- **This repo's `handoff`.** State that would otherwise live in a conversation is written to an artifact, a later session rebuilds from that artifact rather than from recollection, and it stops before acting. RUN_DIR is that artifact for a run, and the resume rule extends the precedent one step: git, `gh`, and herdr outrank RUN_DIR, because a run's own notes are what the crash that stranded it leaves stale.

## Decision Ledger

| # | Decision | Why | Tier |
|---|---|---|---|
| 1 | Model-invocable (no `disable-model-invocation`) | A skill running a wave of issues reaches one lane through this one, and a user-invoked skill strips its description from siblings as well as from the agent, so no other skill can call one. That condition outranks the misfire asymmetry this row first weighed. Model-invocation adds agent reach without taking the human's away, so `/work-issue` still works, and Step 0's confirmation is what stops a misfire before anything is dispatched—with the push-and-pull-request half of its grant answerable separately from the build half. | [P] |
| 2 | An approved plan is required, and a thin one is refused rather than planned around | Every good outcome in the source session traced to a decision made before dispatch, and dispatching a thin plan buys a confidently-built wrong thing faster. Planning is `writing-plans`' job, and a gate that fills the plan's gaps itself has removed the last correction point before dispatch. | [P] |
| 3 | One confirmation up front, then unattended: fan-out shape and push authorization settled together | Confirming at first push instead parks every walk-away run at the gate, which is the property the whole design exists for. The grant covers the issue's own branch and nothing else, so what it buys is bounded. | [P] |
| 4 | Fan-out is proposed from the plan and confirmed, never chosen by a threshold | In the source session one of four issues was better as a single agent. A task-count threshold would have got that wrong silently; `divvy-up`'s shape line put it in front of the user as one sentence. | [P] |
| 5 | A scope-expanding finding is queued with its reasoning, not implemented and not treated as a blocker | Otherwise a walk-away run comes back as work nobody asked for. In the source session this fired once, on a reviewer asking for `navigator.storage.persist()` against a ticket whose criteria never mentioned it. | [P] |
| 6 | Everything derives from N—branch `issue-<N>`, flat, no `feat/` prefix; worker and herdr agent name `issue-<N>`; RUN_DIR path | Three runs went at once in the source session, and a derived name is a name two runs cannot collide on. A prefix would carry a change type this skill cannot know at Step 0, since the diff does not exist yet. | [P] |
| 7 | **Prove the mutation, then read the result.** Every edit shows its changed bytes before anything runs the command whose outcome depends on it | Measured in the source session recorded in issue #101: five separate mutations during verification silently failed to apply—BSD `sed` rejecting a `0,/re/` address, a regex matching `.close()` but not `closeIndexedDbRecordStore`, a `perl` pattern missing a guard's real shape. Each produced a passing suite and a clean bill of health, which is the exact failure verification exists to prevent. The phrase is load-bearing, pinned by the smoke test, and restated for the reproducer in `references/redteam.md`. | [E] |
| 8 | **Never the default branch.** Every push names its refspec in full after checking BRANCH is not DEFAULT; `--force-with-lease=issue-<N>` only, only after the Step 5 rebase; `gh pr create` always carries `--base` and `--head` | An unattended run holds a push grant, so the one thing it must not be able to do is write the branch everybody else builds on. A bare `git push` inherits whatever upstream the tree happens to carry, and a full refspec cannot. | [P] |
| 9 | `adversarial-review` fires only on trigger-table rows 1, 2, or 4 (money, authz, schema) or on `--deep`; the default red-team is one reproducer subagent | Settled with the user on 2026-09-17. This amends the issue's Non-Goals sentence "the red-team phase should invoke it rather than reimplement it": the invocation stays, the always-on part goes. Its full roster on every docs-and-scripts diff spends more than the diff can return, and reproducing a claim before it blocks takes one subagent. | [P] |
| 10 | The reproducer receives the claims, BASE_SHA, HEAD, the tree path, and `baseline.txt`, and never the worker's reasoning, summary, or proposed fixes | Straight from `adversarial-review`: a verifier that has read the author's argument is reproducing the argument. Its stance is refutation for the same reason. | [C] |
| 11 | Two red-team rounds over failed claims, then stop with the evidence | `divvy-up`'s failed-twice rule, pointed at claims instead of tasks. A third round spends the budget to learn what the second one already said. | [C] |
| 12 | RUN_DIR is `<git common dir>/work-issue/issue-<N>/` | Departs from `divvy-up`'s decision 9, "no run directory and no ledger," because resumability across sessions needs phase outputs on disk and `divvy-up` has no phase to resume. The common dir is one location visible from every worktree, invisible to `git status` without an exclude entry, and it survives `git worktree remove`—which a directory inside the worktree does not. | [P] |
| 13 | RUN_DIR holds phase *outputs* and never a phase flag | A note saying "phase 4" outlives the crash that stranded the run at 3, and then the resume reads the note instead of the world. Outputs cannot lie about themselves: a wave with no report is a wave that did not finish. | [P] |
| 14 | The worktree is a sibling of the repo root, `<ROOT>/../<PROJECT>-issue-<N>/` | Inside the root it lands in the tree the run is about, where `git status`, every glob, and the verification command all find it. A sibling is outside every one of those and still one `..` from the repo the user is looking at. | [P] |
| 15 | Isolation is decided by contention, not by issue count: dirty tree or another run in flight takes a worktree, a clean tree with nothing in flight skips it, ambiguity asks | From the issue's load-bearing properties. A worktree always costs an install and a cold verify; what makes it worth that is another writer, not the number of issues open. | [P] |
| 16 | `code-review` runs in session on the session model, never as a dispatch | A worker reviewing its own change is the shape this skill was written against, and the review is where delegation savings stop holding up. Same call as `divvy-up`'s decision 6. | [P] |
| 17 | The plan is copied to `RUN_DIR/plan.md`, and every later step reads the copy | `divvy-up` writes its `## Waves` table into whatever it is handed. A tracked plan file would gain that table in the pull-request diff, which makes the run's scratch state part of the change under review. | [P] |
| 18 | Waiting on review is a bounded poll inside Step 6—every 60 seconds for at most 10 minutes per invocation—and then a resume note, not a longer wait | Measured in the source session: one pull request drew review in minutes and another never drew any. The wait is unbounded, so no fixed timeout can tell a cleared pull request from one nobody has looked at; only a cutoff and a re-invocation can. Ten minutes covers the fast case without holding a session hostage to the slow one. | [E] |
| 19 | "Cleared" is scored against SINCE—the last push—and an approving reaction counts as a signal | Observed on this repo's PR #100: `gh api repos/{owner}/{repo}/issues/100/reactions` returns `+1 chatgpt-codex-connector[bot] 2026-09-15T19:06:03Z`, while the same pull request's `reviewThreads` hold five threads from `chatgpt-codex-connector`, opened earlier that day from 2026-09-15T14:44:42Z and since resolved. The reaction and the findings coexist, so the reaction is a verdict on a push rather than on the pull request, and the cutoff is what makes it usable. The two calls also spell one reviewer two ways—REST `chatgpt-codex-connector[bot]`, GraphQL `chatgpt-codex-connector`—so `[bot]` is stripped before logins are compared; unstripped, one reviewer reads as two depending on which API answered. | [E] |
| 20 | An approving reaction or review from any login other than the pull-request author clears the run; no bot allowlist | An allowlist is a list of every reviewer anyone might ever configure, maintained inside a skill that cannot see the repo's settings. "Not the author" is the property that actually matters, and it needs no maintenance. The cost is that a passer-by's stray `+1` reads as clearance, which is bounded by the SINCE cutoff. | [P] |
| 21 | **Answer, never resolve.** Every finding thread gets a reply with what changed and the commit SHA; marking it resolved stays the reviewer's act | Resolving a thread on the reviewer's behalf destroys the only signal they have that anyone read the finding, and it does it silently. A reply carries the same information and leaves the verdict where it belongs. | [P] |
| 22 | A rebase conflict at Step 5 writes `RUN_DIR/conflict.txt`, leaves the rebase in progress, and stops | Resolving somebody else's concurrent change is a judgment call. An unattended run guessing at it writes a merge nobody reviewed into a branch it is about to push. Leaving the rebase in progress keeps the evidence in the tree where the human resolves it. | [P] |
| 23 | The plan gate is split: `check-plan.py` for the mechanical half, prose for the judgment half, with no D5 "alternative left standing" heuristic | This repo's rule is that a script exists where a job wants a diff tool, and judgment stays in the prose. D1 through D4 and D6 are greps over shapes—a missing citation, a task with no files, an open marker, a decision box, a criterion whose identifier appears nowhere. "The plan names two options and picks neither" is a reading, and a script that guesses at it fails plans that are fine and passes plans that are not. | [C] |
| 24 | `check-plan.py`'s D3 marker scan ignores anything inside a backtick span | Measured while building it. The first version, run against this skill's own plan—which documents the D3 marker words inside backticks—exited 1 and flagged the plan's own documentation of `TBD`, `TODO`, and the rest as live markers. With backticked spans excluded, the same file prints `OK: plan cites #101, 9 tasks with files, 0 open markers, 0/0 criteria covered`, and a scratch plan carrying one bare `TODO` outside backticks still exits 1 with `check-plan: line <n>: D3 open marker 'TODO'`. | [E] |
| 25 | `check-inflight.py` vendors `paths_overlap`, `owns_entry_problem`, `path_within`, and the table parser byte-identically from `divvy-up/scripts/check-waves.py`, with their docstrings | A skill lands alone, so it imports nothing from a sibling. The docstrings record the incidents that set each rule, and a reader who trims them re-introduces the bug they describe. Upstream stays authoritative: fix it there, then re-copy. | [C] |
| 26 | `run-state.py review` lists every item past the SINCE cutoff, not only the items that set the state, and prints `comment by <login> <ts>` for a comment-driven finding | The design contract fixed the deciding-item line for threads, reactions, and reviews and left comments unshaped, so the comment line follows the same shape as its neighbors. Listing everything past the cutoff means a `findings` answer still shows the `+1` sitting beside the threads—the PR #100 situation from row 19, which is the one a reader most needs to see rather than infer. | [C] |
| 27 | `review` never exits 1; `findings` is an answer on stdout | It is an artifact producer, not a gate. A non-zero exit for "the reviewer found something" makes every caller treat a working poll as a broken command, and this repo's validator convention reserves 1 for a semantic failure. | [C] |
| 28 | Two sibling edits land in this branch as their own tasks rather than as workarounds inside `work-issue` | Settled with the user on 2026-09-17. `divvy-up` gains a `{{CALLER_NOTES}}` placeholder because a wrapping skill has to hand every worker a preamble its template had no slot for, and `adversarial-review` resolves `info/exclude` through `git rev-parse --git-path` because in a linked worktree `.git` is a file and the literal path does not exist. Both are defects this skill's shape exposed; routing around them in the wrapper would have left the sibling broken for everyone else. | [P] |
| 29 | The cross-run race is recorded as a Known Limitation rather than closed with a lock file | `check-inflight.py` closes the case that actually happens—a second run started while the first is working—and a lock file would add a stale-lock story, a timeout story, and a "remove this file to continue" instruction to close a window two invocations have to hit within the same second. See the Known Limitations below and the Deliberately Not Built row. | [P] |
| 30 | herdr is used when present, never hosts a wave, and its flag names are checked against `herdr worktree` help at run time | A herdr agent takes one prompt at a time, so a wave dispatched through herdr is a wave serialized, which is the one property a wave exists to provide. The flag names in `SKILL.md` are a cache of what the binary printed once; the installed binary is the authority. | [P] |
| 31 | `gh` degrades by phase: unauthenticated at Step 0 stops, at Step 5 or later renders the body to `RUN_DIR/pr-body.md` and pushes nothing | `file-issue`'s precedent, split by what the phase needs. At Step 0 the issue cannot be read and CRITERIA would be invented; at Step 5 the work exists and only the publish is blocked. The rendered body says on its face that it is rendered, because a draft that reads like an opened pull request is a draft somebody goes looking for on GitHub. | [C] |
| 32 | EVALS.md ships with its scenarios unrun | Settled with the user on 2026-09-17: the live end-to-end run happens after merge, on a real `cambium` issue, and the user records the result. A scenario file written after the run is a transcript; written before it, it is the procedure somebody can actually follow. | [P] |
| 33 | A thread's deciding line gains `reply-to <root_comment_id> <root_url>`, and its bundle entry gains the optional `root_comment_id` and `root_url` keys | Codex's review on PR #102 caught that Step 8's reply call needs the root comment's REST id. The bundle carried only the GraphQL thread node id, so no run ever had the id to reply with. The design contract fixed the bundle shape and the deciding-line prefix; this amends both. It keeps the existing prefix byte-identical, and prints `unknown` where a bundle predates the new keys. | [P] |
| 34 | A review's deciding line gains its URL, its bundle entry gains `id`, `body`, and `url`, and `review` gains `--save PATH` to write the gathered bundle to disk | Same Codex review on PR #102: a `CHANGES_REQUESTED` review's findings live in its body. The old deciding line named only the state and the timestamp, leaving Step 6 nothing to quote in a triage row. `--save` hands Step 6 that body without a second `gh` round trip, amending the design contract's bundle shape the same way row 33 does. | [P] |
| 35 | `reviews` and `reviewThreads` are each paged on their own query and cursor, with `pageInfo { hasNextPage endCursor }`, and the page size lives in one module constant | Codex's review on PR #102: both connections were fetched with `first: 100` and no `pageInfo`. A pull request that had drawn more than 100 of either would silently lose the tail, and could score `cleared` over a finding it never read. Proven live against PR #102, which carried 4 review threads and 1 review at the time: a scratch copy with `PAGE_SIZE = 2` printed all four thread lines with their reply targets and scored `findings`. | [E] |
| 36 | Row 17 matches a repair report on its own, or a triage round with no in-scope rows; amended in the script and in both copies of the Resume table | Codex's review on PR #102: an all-queued triage round writes no `repair-<k>.json`. The old `repair_reports > 0` test never matched it, and no other row did either, stranding the run at `stop`. Reproduced against `row-17-queued.json` with `triage_inscope_rows` 0, `repair_reports` 0, and `triage_rows_unanswered` 2: the pre-fix script printed `phase: stop reason: no resume row matches this probe; gather it again per references/resume.md`. The first fix gated both legs on a triage round, and the in-session review caught what that cost: a red-team repair, committed but unpushed, has a repair report and no triage round, and the gate sent it to row 18's `done`. The repair-report leg stands alone again; row 58 later confined it to an open pull request and moved that fixture to row 10 as `row-10-repair-unverified.json`. | [E] |
| 37 | `triage_rows_unanswered` counts every round, not only rounds newer than SINCE, and row 14 matches only when it is zero | Codex's second review on PR #102: Step 8 pushes and writes `pushed_at` before it replies, so a stop between the two leaves the round it owes replies to older than SINCE. Under the old definition the probe lost those rows, review read `pending`, and row 14 sent the run to the poll for good. Reproduced with `row-17-postpush.json` (review `pending`, `repair_reports` 1, `triage_rows_unanswered` 3, `triage_newer_than_since` false): the pre-fix script printed `phase: 6 reason: row 14`. Counting every round and guarding row 14 puts it on row 17, at Step 8. | [E] |
| 38 | A pull-request-level comment carries `id` and `url` in the bundle, and its deciding line ends with the URL | Same review: reviews and thread roots gained URLs in row 33 and row 34, and the issue-comment branch of `gather` still saved author, timestamp, and body. A finding that arrives that way had no `Source` URL for its triage row and nothing for Step 7 to quote it by. Optional on input like the other new keys. | [P] |
| 39 | D4 reads every heading still open above a box, not only the nearest one | Same review: `## Open Questions` / `### Storage` / `- [ ] IndexedDB or localStorage?` passed clean, because the subheading replaced the section that made the box a decision. Reproduced before the fix: a plan of that shape printed `OK: plan cites #101, 1 tasks with files, 0 open markers, 0/0 criteria covered` and exited 0. The scan now keeps a heading stack and names the nearest ancestor that matched, and `plan-thin.md` carries the nested shape. | [E] |
| 40 | The nine-field report contract is inside the preamble, not only in the section after it | Same review: the preamble is the only part of `references/worker-prompt.md` a dispatch carries, and `divvy-up`'s template names a six-field block. A worker handed the two reported six fields, and Step 3 refused its empty `claims` for a reason the worker was never told. The preamble now shows the nine fields and says they replace the six. | [P] |
| 41 | Step 0 probes before it writes `issue.md` | Codex's third review on PR #102: item 1 wrote `RUN_DIR/issue.md` and item 2 probed, so every fresh issue probed as a run dir with no branch, which is row 5's mark of a dead run, and `row-05.json` shows where that lands: `phase: 0 reason: row 5: a run dir with no branch anywhere; move it to closed/ first`. Restarting Step 0 rebuilt the same state. The probe moved ahead of the first write. | [P] |
| 42 | A null in any probe field but `pr_state` and `review_state` answers `stop` naming the field, after row 1 | Same review: `flag` and `count` read null as false and zero, so a transient failed `git ls-remote` made an existing run look fresh. Reproduced: `row-04.json` with `branch_remote` null printed `phase: 0 reason: row 4: no branch anywhere and no run dir` before the fix. Null means "no pull request" for the two exempt fields by contract. Row 1 runs first because a merged or closed pull request ends the run whatever else the probe missed. | [E] |
| 43 | Row 16 reads `newest_repair_report`, a new probe field, instead of comparing `repair_reports` to `triage_rounds` | Same review, and a defect row 36 introduced: an all-queued round writes no repair report on purpose, so the counts drift apart. Reproduced: two rounds with the first all-queued and the second repaired (`triage_rounds` 2, `repair_reports` 1) printed `phase: 7 reason: row 16` and would have re-dispatched a repair that had already landed. The new field is `test -f reports/repair-<k>.json` for the newest round, and it amends the design contract's probe field list. | [E] |
| 44 | Every claim carries a `path` | Same review: the reproducer's change-exists grep and its `opus` routing both read a claim's path, and the reproducer never sees `files_changed`, so a contract with no path field left it nothing to grep. A claim without one is `NOT_REPRODUCED` before anything runs. Amends the report contract. | [P] |
| 45 | D4's checkbox pattern accepts any list marker at any indent | Same review: `^-\s*\[\s\]` matched only a column-1 dash, so an indented box under a decision heading passed the gate. Reproduced before the fix: the nested shape with two leading spaces exited 0 with `0 open markers`. `plan-thin.md`'s nested box is now indented and D4 still fires twice. | [E] |
| 46 | Step 5 item 2 removes `conflict.txt` once the rebase is proven | Codex's fourth review on PR #102: nothing ever cleared the marker item 1 writes, and row 12 reads it first, so every invocation after a resolved conflict went back to item 1 and refreshed `pushed_at` instead of reaching review. Item 2 is where the rebase is proven, so it is where the stop signal comes down. | [P] |
| 47 | `ar_complete` replaces `ar_run_dir`, read from `redteam/ar-state.txt`, which Step 4 writes from `ledger.py state` after reading the report | Same review: adversarial-review's run directory exists from its preflight onward, so a review interrupted after preflight looked finished. Reproduced: `row-11.json` with the directory flag true printed `phase: 5 reason: row 13`, a publish of an unreviewed diff. adversarial-review writes no terminal report file; its `ledger.py state` line is the closest thing to one, and work-issue records it as a phase output the way it records reproducer verdicts. | [E] |
| 48 | Row 13 matches only before any triage round | Same review: a review repair committed at Step 7 is ahead of origin with a clean build red-team behind it, and row 13 sits ahead of row 17. Reproduced: `row-17.json` with `ahead_of_origin` true printed `phase: 5 reason: row 13`, skipping the repair's own red-team and the replies. `row-17-review-repair-unpushed.json` pins it, and Step 5 item 5 now creates a pull request only where none exists. | [E] |
| 49 | `trigger.txt`'s first line is exactly `fired: yes` or `fired: no`, and the probe reads that line alone | Same review: the probe was `grep -qi fired`, and `printf 'not fired: docs-only diff' \| grep -qi fired` exits 0, so a docs diff would have invoked adversarial-review, which row 9 reserves for triggered diffs. | [E] |
| 50 | `wave_tasks` counts rows with a whitespace-tolerant pattern | Same review: `\| [0-9]+ \|` missed a compact row such as `\|0\|one\|a.py\|sonnet\|done\|\|`, which the vendored parser accepts; `check-waves.py validate` printed `OK: 1 task in 1 wave, disjoint` while the grep printed 0, leaving `has_waves` true and `wave_tasks` 0, which no row matches. | [E] |
| 51 | D6 reads every heading still open above a criterion, the way D4 reads decision sections | Same review: a `### Storage` inside `## Acceptance Criteria` reset the scan and dropped its boxes. Reproduced: an issue of that shape against a plan mentioning nothing printed `0/0 criteria covered` and exited 0. `issue.md` carries a nested criterion and the OK line counted it (`3/3` before row 53 added a fourth criterion). | [E] |
| 52 | D4's heading test matches whole words | Same review: the substring `open` inside `OpenAPI changes` refused a valid plan with `D4 unchecked box under heading 'OpenAPI changes'`. `plan-good.md` carries that section and still passes. | [E] |
| 53 | D6's checkbox pattern accepts any list marker at any indent, the same widening row 45 gave D4 | Fixing row 51's ancestor-stack bug touched the same line as this pattern, and code review on this branch caught that the two had drifted: D4 reads `CHECKBOX_RE` (`^\s*[-*+]\s*\[\s\]`) while D6 still matched only a column-1 dash. A criterion indented under its heading, or written with `*`/`+`, is as real as one at column 1, and a reader who nests one under Acceptance the way `plan-thin.md` nests a decision box would have had it silently uncounted. `issue.md`'s new criterion is indented with a `*` marker and still lands in the `4/4` count. | [P] |
| 54 | `trigger_fired` is `yes`, `no`, or `absent`; row 11 sends an absent verdict back to the trigger, and row 13 requires a recorded one | Codex's sixth review on PR #102: a bool made a missing `trigger.txt` read as `no`. Reproduced: `row-11.json` with the flag false printed `phase: 5 reason: row 13`, a publish of a diff whose trigger was never evaluated. `row-11-trigger-unrecorded.json` pins the absent case at row 11, which is where every clean red-team with no verdict lands; row 13's own "trigger recorded" clause is unreachable behind it and stays as a statement of the invariant rather than a check a fixture can fail. | [E] |
| 55 | `base_sha` and `baseline` are probed, and row 7 resumes Step 1 where either is missing | Same review: nothing in the probe covered Step 1's closing writes, so a run stopped inside Step 1 had the branch, the Waves table, and no reports, which row 7 read as "dispatch wave 0". Reproduced: `row-07.json` with zero reports printed `phase: 2`, workers away with no fixed point and no baseline. Row 7 keeps its number and gains the branch; `row-07-isolation-incomplete.json` pins it. | [E] |
| 56 | `pushed_at` is written just before the push, and the cutoff is inclusive | Same review: both GitHub and the marker carry one-second precision, the marker was written after the push, and `newer` used strict `>`, so a review stamped in the push's own second was rejected for good. Reproduced with `changes-requested.json` and `--since` equal to its review's timestamp: `pending`. Writing the marker first and comparing with `>=` keeps that event; the cost is that a reaction to the previous push landing in the same second reads as a verdict on the new one, which is rarer than a reviewer that answers within a second of a push and errs toward `findings`. | [E] |
| 57 | `wave_tasks` counts rows inside the `## Waves` section only | Same review: the whole-file grep counted numbered rows in any later table. Reproduced: a decision table appended to `plan-good.md` made the grep say 4 where the parser says 3, and `phase` printed `row 7: 3 of 4 wave reports on disk`, waiting on a report no task would write. The recipe now cuts the section with `awk` before counting. | [E] |
| 58 | Row 17 requires an open pull request, and row 10 keeps a failed round until a later round is clean, whether or not a repair report followed | Codex's seventh review on PR #102, and a defect row 36 introduced: with the repair leg standing alone, a red-team repair made before any pull request existed matched row 17, and Step 8 runs Step 5 items 1 through 4 only, so the run pushed and finished without ever opening the PR. Reproduced: `row-17-redteam-repair.json` with `pr_state` null printed `phase: 8 reason: row 17`. A repair with no PR behind it now lands on row 10 while unverified (run round k+1) and on row 13 once clean (Step 5, PR create included); `row-10-repair-unverified.json` and `row-13-prepr-repair-clean.json` pin both, at rows 10 and 13. Row 17's own "PR open" clause is unreachable behind them, the way row 13's trigger clause is behind row 11 (row 54), and stays as a statement of the invariant rather than a check a fixture can fail; the row-10 change is the one that moves the answer. | [E] |
| 59 | An unresolved thread counts on its root or on its latest non-author comment, and the bundle carries `last_comment_at` and `last_comment_author` | Same review: the thread query fetched only the root comment, so a reviewer who answered "still wrong" inside an old thread after a repair push wrote no new root, and the poll scored the thread against the old date. Reproduced with a bundle whose root predates SINCE and whose reply postdates it: `pending`. The query aliases `comments(last: 1)` beside the root; `thread-followup.json` scores `findings` and `thread-author-reply.json`, where the only reply is the author's, stays `pending`. Amends the design contract's bundle shape and findings rule. | [E] |
| 60 | `redteam_last_failed` reads a false `left_checks` entry as a failure | Same review: `references/redteam.md` routes `holds: false` like a `NOT_REPRODUCED` claim, and the probe grepped only for the claim verdict. Reproduced on a round holding one false left check and no failed claim: the grep said clean, and `phase` published. The grep now matches either. | [E] |
| 61 | `wave_tasks` ends the table at the first non-table line, the way the vendored parser does | Same review, and a gap row 57's section cut left open: `f&&/^## /` stops only at a level-two heading, so a numbered row under a `### ` heading inside the Waves section still counted. Reproduced: a `### Decision log` table appended to `plan-good.md` gave 4 where the parser gives 3. | [E] |
| 62 | The latest thread comment carries its body, id, and URL in the bundle, and the deciding line names the reply's URL | Codex's eighth review on PR #102, on row 59's own gap: the aliased `latest` selection took only author and timestamp, so a thread scored `findings` on a reply whose text the saved file did not hold, and Step 6 had only the stale root to quote. Reproduced: `--save` on `thread-followup.json` wrote a thread with no `last_comment_body`. The reply still goes to the root's id; only the quote and the Source change. | [E] |
| 63 | `repair_after_last_round` orders the newest repair report against the newest round, and row 10 reads it instead of counting reports | Same review, on row 58's gap: Step 8's reproducer writes a failed round over `repair-<k>.json`, so a resume there sees a report and a failed round and cannot tell whether the report came before or after. Counting sent it to "run round k+1", which re-tests the code the round just refuted. Reproduced: `row-10-repair-needed.json`, with two rounds, the newest failed, and one report, printed `run round k+1` under the old script. Order settles it: a report newer than the round is unverified, a report older than it is what failed. | [E] |
| 64 | `redteam_failed_twice` is probed, and row 10 answers `stop` on it | Same review: Step 4 stops after two failed rounds, and row 10 ignored `redteam_rounds`, so every later invocation started another cycle. Reproduced: `row-10-failed-twice.json`, with two rounds, printed `run round k+1` under the old script. The probe reads the two newest round files; the stop is the same one Step 4 names, reached on resume. | [E] |
| 65 | The `wave_tasks` recipe accepts leading whitespace before the pipe | Same review, on row 61's gap: the vendored parser strips leading whitespace, so an indented table validates, and the recipe counted zero rows of it and stranded the run. Reproduced: `check-waves.py validate` printed `OK: 1 task in 1 wave, disjoint` while the recipe printed 0. | [E] |
| 66 | `deferred_comment_needed` is probed, row 17 resumes Step 8 on it, and row 14 yields to it | Codex's ninth review on PR #102: Step 8 posts the `Deferred findings` comment after its replies, so a stop between the two left every row answered, the review `pending`, and nothing in the probe able to see the unpublished queue. Reproduced: `row-17-queued.json` with review `pending`, no unanswered rows, and nothing unpushed printed `phase: 6 reason: row 14`, and a later invocation could finish without the queue ever reaching the pull request. The field reads the author's comments on the pull request for the heading. | [E] |
| 67 | D2's task-checkbox pattern accepts any list marker at any indent, the same widening rows 45 and 53 gave D4 and D6 | Same review: a plan whose tasks were indented checkboxes had no task D2 could see, and printed `OK: plan cites #101, 0 tasks with files` on a plan with no files anywhere. `plan-nofiles.md` carries an indented `* [ ] Task` above its heading task and D2 fires on both. A checkbox task nested inside a heading's section still belongs to that section, as before. | [E] |
| 68 | `deferred_comment_needed` compares every queue row's Source against the `Deferred findings` comment, not the heading's existence | Codex's tenth review on PR #102, on row 66's gap: a comment posted in an earlier round satisfied the heading test while a later round's rows had never reached it, and a stop after the replies but before the edit went to row 14's poll with the queue on the pull request behind the queue on disk. Measured on scratch inputs: a two-row queue against a comment carrying one row reads `true`, against a comment carrying both reads `false`, against no comment `true`, and an empty queue `false`. Sources are compared as the `\| <src> \|` cell so a bare `worker` cannot match prose. | [E] |
| 69 | The `Deferred findings` comment carries the queue table copied byte-for-byte, and Step 8 and `references/triage.md` both say so | In-session review of row 68's own fix: the new probe compares Source cells as literal text, and nothing said the comment had to be a verbatim copy of `queue.md`'s table rather than a readable rewrite. Reproduced: a comment whose row 1 Source was the queue's own URL rewritten as `[thread](<url>)` — the row plainly present to a reader — made the probe read `deferred_comment_needed=true` forever, the same disjoint-twin failure `divvy-up`'s owns entries hit before #97 (fixed in 19870bb). | [E] |
| 70 | Row 17 matches an owed `Deferred findings` comment on its own, with no repair report or triage round required | Codex's eleventh review on PR #102, on row 66's gap: a worker's `plan_concerns` row reaches the queue at Step 4, before any review, so after publishing the comment is owed while row 17's first conjunct still wanted a repair report or an all-queued round. Reproduced: `row-14.json` with `deferred_comment_needed` true printed `phase: stop reason: no resume row matches this probe`. `row-17-worker-queue.json` pins it. | [E] |
| 71 | `deferred_comment_needed` compares whole queue rows to the comment, not Source cells | Same review, on row 68's gap: a row changes without its Source changing, most plainly a Status moved from `queued` to `filed #M`, and a Source-only compare read that comment as current. Measured on scratch inputs: a `filed #123` row against a `queued` comment read `false` under the Source compare and reads `true` under the whole-row compare, while the earlier four cases keep their answers. | [E] |
| 72 | Step 4's repair reports are `redteam-repair-<k>.json`; `repair-<k>.json` is Step 7's alone | Codex's twelfth review on PR #102: `newest_repair_report` matches a triage round to its repair by number, and a build repair from Step 4 named `repair-1.json` read as triage round 1's repair, so a resume skipped Step 7 with the findings unfixed. Measured on a scratch run dir holding a Step 4 repair beside `triage/round-1.md`: under one name the probes read `repair_reports=1 newest_repair_report=true`; under the split they read `0` and `false`, and `repair_after_last_round`, which lists both kinds, still reads `true`. The four pre-PR red-team fixtures drop to `repair_reports` 0 accordingly. | [E] |
| 73 | Step 8 items 1 and 2 run only where there is a repair report and only where local HEAD is ahead of origin | Same review: an all-queued round or a worker row owed its comment reaches Step 8 with no repair and nothing to push, and the unconditional items named a report that did not exist and then pushed a no-op, moving SINCE past the review just triaged so it read as stale. The items are now guarded, and a queue-only entry goes straight to its replies and the comment. | [P] |
| 74 | Row 5 resumes a RUN_DIR that carries a `## Waves` table and no branch at the confirmation, and archives only one without the table | Same review: Step 0 writes the table before Step 1 makes the branch, so a stop between the two, including right after the user's yes, was archived and re-gated. Reproduced: `row-05.json` with `has_waves` true printed `move it to closed/ first`. The table is the gate's own artifact and nothing else produces it, so its presence is the durable mark of a live run. `row-05-gated.json` pins it; the confirmation is asked again, since a yes that was never acted on is not one to carry across a crash. | [E] |
| 75 | Row 5's confirmation resume re-runs Step 0 items 4, 6, and 7, not just item 8 | In-session review of row 74's own fix: "Step 0 at the confirmation" named only the last of four items and none of the isolation decision, the cross-run check, or the red-team-mode text is written to RUN_DIR, so a literal reading could jump straight to item 8 on stale or absent values. Item 6 is the one that matters: it is the concurrency guard that stops the run "whatever the isolation decision was," and the crash that stranded this run is not the only thing that could have started in the tree while nobody was watching. Both copies of the Resume table now name items 4, 6, and 7 explicitly, and the prose paragraph says why none of the three survive a crash. | [P] |
| 76 | Row 18 also answers `done` on a `findings` review whose round is triaged, answered, and published | Codex's thirteenth review on PR #102: `findings` never clears itself, since resolving is the reviewer's act, and a queue-only round pushes nothing, so SINCE never moves past the threads it answered. Once rows 15 through 17 found nothing left to do, no row matched at all. Reproduced: `row-17-queued.json` with nothing unpushed, unanswered, or owed printed `phase: stop reason: no resume row matches this probe`, and a crash before the final report could never resume to print it. `row-18-answered.json` pins the new reading; the final report then says the run waits on the reviewer. | [E] |
| 77 | The thread query carries the last twenty comments, the bundle keeps them as `comments`, and the reply that counts is the newest one not by the author | Same review, on row 59's gap: `comments(last: 1)` returned the author's own answer when it came after the reviewer's follow-up, and `classify` read an old root and an author-authored last comment as `pending`, hiding the follow-up for good. Reproduced: a bundle with the reviewer at 16:20 and the author at 16:25 scored `pending` under the last-comment rule. `thread-followup-then-author.json` scores `findings` and names the reviewer's URL; a bundle from before the tail existed still reads its `last_comment_*` keys. | [E] |
| 78 | The final report's closing line branches on `review_state`: "a human merges" only where `cleared`; "waiting on the reviewer" where row 18's second reading fired | In-session review of row 76's own fix: the two Resume tables started promising a `findings`-round ending in "wait on the reviewer," but Step 8 item 5 and the top-of-file phase summary still printed the unconditional `cleared`-only line, so the documents disagreed with each other inside the same skill. Found by re-reading Step 8 item 5 against the new row 18 text: `grep -n 'a human merges' work-issue/SKILL.md` showed the line unchanged after row 76 landed. Both sentences now name the second ending explicitly. | [P] |
| 79 | The plan gate's heading patterns accept up to three leading spaces | Codex's fourteenth review on PR #102: CommonMark allows three, and column-1 patterns read an indented `## Open Questions` as prose, so D4 lost the section and the box beneath it passed. Reproduced: a plan with the heading and box each indented two spaces printed `0 open markers` and exited 0. `plan-thin.md`'s decision heading is now indented and D4 still fires twice. Four spaces is a code block and stays out. | [E] |
| 80 | `has_waves` and the `wave_tasks` cut match the Waves heading with whitespace on either side | Same review: the vendored parser strips the line before comparing, so `   ## Waves` is a valid table to it, and the column-1 grep read the same plan as having no table, sending an existing branch to row 6's plan gate on every invocation. Measured: `check-waves.py validate` printed `OK: 1 task in 1 wave, disjoint` on that plan while `grep -q '^## Waves'` exited 1. | [E] |
| 81 | A worker's queue published with no review yet resumes at the poll, and the poll's stop names the pull request | Same review asked that this state resume at the final report instead. Declined: with the comment posted and nothing else owed, waiting on a review is the state Step 6's bounded poll exists for, and its ten-minute stop is a resume note, not a stranding. What a crash between the comment and the report costs is one informational report, so the poll's stop now carries the pull-request URL, which is the one thing that report would have told the user that nothing else prints. | [P] |
| 82 | Route 2's search path is documented rather than made configurable, and the `PLAN` bullet is a numbered ladder so the prose can name a route by number | Issue #112. The glob is `<ROOT>/docs/plans/` and nothing else, so a setup that keeps plans outside the working tree missed route 2, and—where the issue carried no link either—route 3 with it, landing on route 4, the conversation copy—reintroducing, without saying so, the dependency this skill exists to remove. The failure surfaces at resume: `RUN_DIR/plan.md` is the only copy by then, and Resume row 5's no-Waves leg moves that RUN_DIR to `closed/` and restarts at Step 0, where PLAN re-resolves in a session that no longer holds the plan. An ordinary resume is safe—row 6 re-reads the surviving copy—so the window is a run that stopped before `divvy-up` wrote its Waves table with no branch made yet. Documenting it costs two paragraphs and moves the case onto routes 1 and 3, which already work. A configurable search path would buy a precedence question, an unset-variable story, and a second place for the plan gate to disagree with itself, to fix what passing the path fixes. No earlier row covered the `docs/plans` location at all—row 17 covers only the copy to RUN_DIR. | [P] |
| 83 | Where a claim concerns a seam with a production caller at HEAD, the reproducer's last reproduction drives that caller, and the seam reproduction stays ahead of it | Issue #109's storage seam. Its author verified a comparison in isolation with five mutations, the orchestrator verified it again with a five-case probe, and it shipped with its boundary unexamined. Both built their fixtures by hand, so both constructed every version consistently. The production caller does not: it builds histories from an object it never re-parses, which is the one input no hand-built fixture supplies. The only thing in that wave that looked at the caller was an external bot, and its finding was wrong, which cost a round of scope argument and left the boundary question unasked. The seam reproduction comes first and stays, per the issue's first non-goal. It is what separates a seam that is wrong everywhere from a seam that is wrong only at its caller, and cutting it would trade one blind spot for another. Reasoned from that one incident; nothing here was measured. | [P] |
| 84 | `reproduced_at` is a field beside `verdict`, and `verdict` stays exactly `REPRODUCED`, `NOT_REPRODUCED`, `UNVERIFIABLE` | Three readers already split on those three values: Step 4 items 4 and 5 route the repair dispatch and the "Not independently verified" section, `references/resume.md`'s `redteam_last_failed` probe greps `NOT_REPRODUCED\|"holds": *false`, and `run-state.py`'s row 10 branches on the boolean that grep yields. A fourth value means nothing until all three learn it. A compound spelling would be worse than a distinct one, because that alternation is unanchored: `NOT_REPRODUCED_AT_CALLER` scores as a plain failure to a probe that never learned it, and `REPRODUCED_AT_CALLER` scores as nothing at all. A field carries the new distinction past all three without changing any of them, the way `left_checks` already carries `holds`. | [P] |
| 85 | No production caller among the hits goes in the verdict—`caller.path` null, the search grep and whatever it returned as the evidence, `reproduced_at: seam`—and the verdict stays whatever the seam said, rather than stopping the run or falling to `UNVERIFIABLE` | An unexercised seam is information for whoever reads the pull request, and the claim itself is intact: the worker built what the plan asked for, and failing the claim would send a repair dispatch with nothing to repair. `UNVERIFIABLE` is also spoken for. It means nobody could check the claim, and Step 4 item 5 turns it into the pull request's "Not independently verified" section, so spending it on a claim that was checked at the seam and held would empty the one word that section has for work nobody could check. A caller found but not drivable—a live service, credentials, hardware—records the same way, with `caller.path` set, `caller.command` null and the reason in `caller.output`. The pull request's verification section marks either case seam only, which is what makes the weaker verdict visible. | [P] |
| 86 | The caller search keeps its test, spec, and fixture exclusions and nothing more, and the reproducer reads the hits | In-session review of this change, which found the search returning non-callers. Run here with `<symbol>` = `paths_overlap`—`git grep -n -w paths_overlap HEAD -- . ':!*test*' ':!*spec*' ':!*fixture*'`—it answers with `AGENTS.md`, `divvy-up/README.md`, and four `RATIONALE.md` files beside the three scripts that do call it, `adversarial-review/scripts/check-territories.py`, `divvy-up/scripts/check-waves.py`, and `work-issue/scripts/check-inflight.py`. Every hit outside those three spells the symbol out and runs nothing. Excluding prose would cost the case this skill cares most about. `references/redteam.md` says that for a script or a CLI the production caller is the command line production runs it with, and in a skills repo that line lives in a `SKILL.md`. Run with `<symbol>` = `run-state.py`, every hit that drives the script is markdown—command lines in `work-issue/SKILL.md`, in `references/resume.md`, and in this skill's own `EVALS.md`—so a `':!*.md'` would report it as called by nothing. Every exclusion past the test paths is a guess about what language the repo under test is written in, and the reproducer runs on repos nobody here has seen. The definition already filters—a production caller reaches the symbol rather than naming it—so the change is to say the search is wide on purpose and the hits need reading, in the prose and in the prompt block the reproducer sees. Reasoned from the shape of the repos this runs on; the two searches above illustrate it rather than measure it. | [P] |
| 87 | The caller search excludes test, spec, and fixture paths only, and keeps the claim's own file in range | Codex's review of PR #121, on the `':!<claim path>'` this change had shipped. A seam and the production caller above it often sit in one file, and a pathspec exclusion's unit is the whole file, so both vanished from the hits and the reproducer recorded "No production caller at HEAD" for a seam that has one—the false negative the caller rule exists to catch, arriving through the search. Reproduced in a scratch repo holding `normalize` and `production_handler` in one `app.py`: `git grep -n -w normalize HEAD -- . ':!app.py' ':!*test*' ':!*spec*' ':!*fixture*'` exited 1 with no output, while the same command without the claim-path exclusion printed `HEAD:app.py:1:def normalize(raw):` and `HEAD:app.py:5:    return normalize(raw)`. What the exclusion meant to skip was the definition line, which is one hit and not one file, and row 86 already has the reproducer reading the hits rather than taking the first. Row 86's headline now reads true of the search it describes. | [E] |
| 88 | `reproduced_at` is `caller`, `seam`, or `null`, and an `UNVERIFIABLE` verdict carries null | Same review. `references/worker-prompt.md` routes a claim that arrives with no command to `UNVERIFIABLE`, and both of the field's values were false of such a claim: nothing ran at a caller and nothing ran at a seam. Step 5 would have published a reproduction site and an output tail for a claim nobody reproduced. Step 4 item 5 already sends `UNVERIFIABLE` to the pull request's "Not independently verified" section, so the verification section never had cause to mark it. Null keeps the distinction in a field rather than in a fourth `verdict` value, which row 84 refuses and the Deliberately Not Built table pins. Null reaches no probe either: `references/resume.md`'s `redteam_last_failed` grep matches `NOT_REPRODUCED` and `"holds": *false`, and `"reproduced_at": null` carries neither, so `run-state.py`'s row 10 reads the same boolean it read before. | [P] |
| 89 | The no-caller route is "no production caller among the hits", and `caller.output` carries whatever the search returned rather than an empty result | Same review. Row 86 keeps the pathspec wide on purpose, so the ordinary answer is a full hit list of non-callers: the symbol's own definition line, plus every ledger row, agent doc, and changelog that spells it out. Wording that promised `caller.output` an empty result described the rare case and left the common one with no shape at all, and a reproducer holding hits and no caller had nothing the contract told it to write. The evidence a reader needs is the same either way—the search, and what came back—so the field carries that. Row 85's evidence clause now reads true of the route it describes. Reasoned from row 86's own search; the wording was never measured against a live reproducer. | [P] |
| 90 | `reproduced_at` is null wherever no command ran, and two verdicts reach it: an `UNVERIFIABLE` claim that arrived with no command, and a `NOT_REPRODUCED` claim the change-exists grep refuted before its command ran | Same review, on row 88's scope. Row 88 tied null to `UNVERIFIABLE` alone, and the change-exists refutation—written into Step 4 item 2 and into the reproducer's prompt since this change began—runs no command either: `references/redteam.md` stops the run before the claim's command on an empty grep. Under row 88's wording that verdict had to pick `caller` or `seam`, both false of it, and Step 4's Done-when could not be satisfied by a claim that correctly ran nothing. Step 4 item 4 gains the case: the top-level `command` and `output` on a null verdict are the grep and its empty result, which is the whole of the repair evidence. Null still reaches no probe. `references/resume.md`'s `redteam_last_failed` grep matches `NOT_REPRODUCED` and `"holds": *false`, and a change-exists refutation is a `NOT_REPRODUCED` that should route to repair, which is what that grep already does; `"reproduced_at": null` carries neither token. Row 88's headline now reads true of the field it describes, and `references/redteam.md`'s `verdict` gloss branches the repair dispatch on `caller` against everything else rather than against `seam` alone. | [P] |
| 91 | D3's `open question` fires wherever an occurrence is not the object of its own `settl…`, `resolv…`, or `answer…` word, with at most one determiner between the two, and that covers a label (`Open question: …`, `- **Open question** …`) because no resolving word comes before the phrase there to take it as its object; the other six markers stay unconditional | Issue #129. The phrase names a category, so prose that closes questions uses it too. The pre-change script (`05c0458`) refused #113's plan at line 417, "The four that settle an open question:", which introduces the questions the plan settles: `check-plan: line 417: D3 open marker 'open question'`, exit 1. The issue's reproduction failed at line 7 the same way. Its first seven lines hold that sentence and nothing else a gate could catch, and they now print `OK: plan cites #999, 1 tasks with files, 0 open markers, 0/0 criteria covered` and exit 0. #113's plan no longer names line 417. Only this phrase was reproduced misfiring, and the other six leave something open wherever they appear, so only this one got a gate. On scratch plans with `Settled:`, `Resolved:`, or `Answered:` in front of each, all six still fire. The first version of the gate passed any sentence that held a resolving word anywhere. The in-session review found that "Whether the key is resolved remains an open question." exited 0 under it, and that sentence leaves the question open. The verb now has to take the phrase as its object, and that sentence fails at `plan-open-section.md` line 22. A label fires through the same sentence test. Only a list marker or `**` comes before the phrase in a label, so no resolving word can take it as its object. `Open question: how do we resolve conflicts?` fails at `plan-open-section.md` line 18. `**Open question** who gets to answer the open question of retries?` fails at line 20, because the verb clears only the second occurrence. An earlier revision of this branch also carried a separate label regex, `OPEN_QUESTION_LABEL_RE`, and PR #132's review found it redundant. With the `if` that checked it deleted, the smoke suite and every file in `tests/fixtures/work-issue/plans/` printed byte-identical output. A probe builds 3,024 label lines from list markers, `*` or `**` emphasis, three casings, separators, and tails. Three of the tails hold a resolving verb. Run from the repo root, `python3 -c 'import importlib.util as u,itertools as it;s=u.spec_from_file_location("cp","work-issue/scripts/check-plan.py");cp=u.module_from_spec(s);s.loader.exec_module(cp);L=[m+e+p+e+x+t for m,e,p,x,t in it.product(["","- ","* ","+ ","1. ","10) ","  - ","- [ ] "],["","*","**"],["Open question","open question","OPEN QUESTION"],["",":"," -"," —","."," (",";"],[""," retries"," how do we resolve conflicts?"," settled by a live run"," who gets to answer the open question of retries?"," this settles the open question."])];print(len(L),"lines,",sum(not cp.open_question_fires(l) for l in L),"missed")'` prints `3024 lines, 0 missed`, so the regex came out. No version of the gate catches `_Open question_`, because `_` is a word character and the whole-word match finds no boundary before `Open`. A scratch plan holding `_Open question_: retries` exits 0 at `05c0458`, at `a5dcb30`, and at this change. The adversarial review of this change (F-r1-authz-03) found the test ran per sentence, so one resolved occurrence cleared every other one beside it. "This settles the open question of retries, but the open question of caching remains." exited 0 at `59ef735`. The gate now removes each resolved span before it searches, and that sentence fails at `plan-open-section.md` line 44. `plan-settled.md` line 30 settles one occurrence and answers the other, and it still passes, so a second occurrence alone does not fire. The adjacency test is still a heuristic, and Known Limitations records where it errs. `plan-settled.md` line 9 pins the settled sentence. | [E] |
| 92 | D3 fails any list item under a non-task heading that contains `open question(s)`, `open uncertainty` or `open uncertainties`, `open decision(s)`, or `open item(s)`, or that opens with `unresolved` or `undecided` inside any `*` or `_` emphasis, with backtick spans ignored, unless the item opens with a `~~struck~~` span or a ticked `[x]` box or is nested at or past that item's content column; an unchecked box under a heading D4 reads stays D4's | Issue #129's other half. #113's plan passed the old gate with a list of genuinely open items under `## Open uncertainties`, because none of them used a marker phrase, and a phrase list cannot see an item that names no phrase. The heading says the items are open, so the gate reads the heading. #113's plan now prints `D3 unresolved item under heading 'Open uncertainties'` for lines 555, 557, and 558 and exits 1. It skips line 556, which opens with `~~**Resume at Step 5 after a build-only invocation.**~~`. Line 555 reads "Settled by EVALS Scenario 1" and still fires on purpose. Words like "settled" in an open item say how it will close, so only striking or ticking closes it. The issue's reproduction fails at line 11 alone, on its one open item. The first version also read any heading that named uncertainties. The in-session review ran `## Risks and uncertainties` over `- The API may rate-limit us; we back off.` and got a D3 failure, where `05c0458` exits 0. A risks list says how the plan handles each risk, so `uncertainties` now counts only with `open` in front, and `plan-settled.md` line 13 pins the pass. `open decisions`, `open items`, `unresolved`, and `undecided` stay in the set because each one says the items are open. The adversarial review (F-r1-authz-01) then found that `unresolved` counted anywhere in any heading, task headings and backtick spans included. At `59ef735`, a scratch plan with `### T1 Count unresolved threads` over two bullets printed `D3 unresolved item under heading 'T1 Count unresolved threads'` for both, where `05c0458` exits 0. Three rules now narrow the heading. A task heading never marks its items open. `unresolved` and `undecided` count only when they open the heading, optionally after a number such as `3.`. The heading test skips backtick spans, as the marker scan does. That scratch plan and its `` `unresolved` `` twin now exit 0, and `## Unresolved`, `## Undecided items`, `## 3. Unresolved`, and `## Open uncertainties` still fire. `plan-settled.md` lines 17 and 21 pin the topic heading and the backticked one. `plan-open-section.md` lines 36 to 42 pin the finder's two task headings, and lines 61 and 62 pin a task about parsing open questions, which only the task rule passes. The same review (F-r1-authz-02) found that a sub-bullet recording the answer under a ticked or struck item failed as an open item of its own. The finder's two scratch plans exited 1 at line 10 then and exit 0 now. Nested items now close with their parent until the next list item short of the parent's content column, the next heading, or the next non-list line no deeper than the parent's marker. Blank lines do not end it, because a loose list puts them between an item and its sub-items. `plan-settled.md` lines 26 and 28 pin the pass. `plan-open-section.md` pins the edges: lines 49 and 53 pass, line 50 fires as a sibling that ends the exemption, line 51 fires under that open sibling, and line 57 fires after the paragraph at line 55 ended the list. The second adversarial round found three more holes and one untested line. The lead test matched from the heading's first character (F-r2-authz-01), so `## **Unresolved**` stopped marking its section open. A scratch plan with that heading over one bullet fails at `59ef735` and exits 0 at `a8fc0dc`. The lead test now skips `*` and `_` around the word, and `plan-open-section.md` lines 66, 70, and 74 fail under `**Unresolved**`, `_Undecided_`, and `3. **Unresolved**`. Deleting backtick spans before the lead test moved a topic word to the front (F-r2-authz-02), so at `a8fc0dc` `` ## `gh api` unresolved thread counts `` failed the bullet under it. The lead test now swaps each span for a placeholder word, and the `open questions` search still deletes spans. `plan-settled.md` line 34 pins the pass. The exemption covered any item deeper than the closed item's marker (F-r2-authz-03), but CommonMark nests an item only at its parent's content column, 2 columns past `- ` and 4 past `10. `. pandoc renders ` - x` after a struck `- ` item, and `  - x` after a struck `10.` item, as new open items, and `a8fc0dc` passed both. The exemption now starts at the content column. `plan-open-section.md` lines 79 and 82 fail, and `plan-settled.md` line 39, an answer four spaces under a struck `10.` item, passes. No fixture needed the heading's reset of the closed-item state, so deleting that line left the suite green (F-r2-state-01). `plan-open-section.md` line 88 is an indented item under `## Still open items`, right after the ticked item at line 84 ends the section above. With the reset deleted, line 88 passes, at `a8fc0dc` and at this change alike, and the suite fails. `plan-open-section.md` pins both directions: line 13, "Unresolved; settled by a live run.", fires, and struck line 14 does not. The same fixture pins the handoff. The `- [ ]` at line 26, under `## Open questions`, prints only D4's line. The one at line 30, under `## Unresolved`, a heading D4 does not read, prints only D3's. No box is reported twice or missed. The fixture's title leaves out the word "open", because D4 reads the H1 like any other heading and would take line 30 from D3. | [E] |
| 93 | Step 4 reaches `adversarial-review` by invoking it, and Step 0's yes answers that skill's own Step 2 question | Row 9 keeps the invocation, and #124 found the Skill tool refusing it because `adversarial-review` was user-invoked, so it flipped to model-invocable (its ledger's row 34) instead of this skill working around the refusal. Reading its `SKILL.md` and following the steps inline was the other route, and it was not built. It would run a workflow the host reserves for explicit invocation by going around the host's own gate, and #124 rules out any wording that treats a refusal as something to route past. It would also fork the review: a copy of the steps runs whatever this skill's reader made of them, not the skill as installed, and loses the fan-out question the invoked skill asks. The confirmation folds in the same way `divvy-up`'s Step 4 question already does (row 1): the red-team mode line in Step 0 names the review and its forecast depth, so the user's yes there is the answer up to that depth (row 95), and Step 4 says so when it invokes. Step 0 item 7 also names the review when `--deep` is set, which the plan did not ask for: `--deep` fires the trigger on its own and is known at Step 0, so without that clause a `--deep` run whose forecast grep misses would reach the review's question with no yes covering it. Where `adversarial-review` is not installed, the step stops and names it, as the Known Limitation on the trigger already says. | [P] |
| 94 | Step 0 item 8 writes the red-team mode the user said yes to into `RUN_DIR/redteam/mode.txt`, and Step 4 item 7 tests that file, not Step 0's memory, before it carries the yes into `adversarial-review`'s Step 2 | The adversarial review of this change (F-r1-authz-01) found row 93's carry sentence testing Step 0's red-team mode, which a resumed run cannot see. Resume rows 10 and 11 jump straight to Step 4, nothing under RUN_DIR recorded the mode, and `references/resume.md` says memory answers nothing. Re-deriving the mode at Step 4 was the other route. Item 7's forecast grep runs over `plan.md`'s owned paths, which RUN_DIR keeps, and `--deep` comes back when the user retypes the invocation. But that grep is built at run time from `adversarial-review/references/trigger-table.md`, and the table can gain rows between invocations, so a re-derived forecast can name a review the user never saw. The file records what the confirmation actually carried. Step 4 reads it and the resume probe does not, so `run-state.py` and the probe fields are unchanged: whether the yes covers the review decides nothing about where a run resumes. Row 5's resume re-asks the confirmation, and that yes rewrites the file, since item 7's re-derived mode can differ from the one the first yes answered. A missing file answers nothing, so a run with no record of the yes gets the review's own question. PR #134's second in-session review found nothing carrying the file's depth to the review: its Step 2 waiver checks the wrapper's confirmation, and a resumed run's review saw none and asked. Step 4 now hands the review the file's line verbatim in the invocation, as the confirmation its Step 2 checks the derived depth against. It travels as an argument rather than the ambient conversation, because a resumed run has no conversation to find the yes in (`adversarial-review` row 35). Step 4 sends the line last, after the fixed point and any flags, wrapped in double quotes: a `--deep` run's line names `--deep`, and the review reads flags from the arguments around the line and the line itself as quoted text, so it needs to know where the line starts. Pinned by `require_text` on that sentence in `SKILL.md` and `references/redteam.md`. | [P] |
| 95 | Step 0 item 7 forecasts `adversarial-review`'s depth from its Step 2 Depth table, `--deep` forecasting depth 2, and the yes carries into that skill's Step 2 only where the depth it derives is at or below the forecast in `mode.txt` | PR #134's review (P0) found row 93's carry spending a yes the user never gave on what it costs. The mode line named only `adversarial-review (<row names>)`, while the question it stood in for carries the depth, territory count, verifier model, and out-of-scope list, and a real diff can derive a costlier run than the plan's owned paths forecast. A yes to a forecast is not consent to a costlier real run. The forecast now names a depth, taken from the same Depth table the review applies, so the user sees the cost they are agreeing to, and a deeper derived depth answers nothing and gets the review's own question. `--deep` forecasts 2 because it is the top of the table, which is what a user typing the flag asked for, and Step 4 passes `--deep` on to the review, whose own `--deep` pins Depth 2, so the forecast is the depth that runs. PR #134's second in-session review found the forecast resting on a pass-through Step 4 lacked: it invoked the review with the fixed point alone, so a `--deep` run could show depth 2 and run depth 1. Step 4 keys the pass-through on the run's own flag rather than on `mode.txt`, since the flag is what fires the trigger, and a resume whose flags differ from the first run's still reaches the depth bound, which asks where the two disagree upward. The review, not this skill, prints the depth line marked as answered (`adversarial-review` row 35), since only its Step 2 knows the derived depth. The cost is a second prompt on a run whose diff outgrew its plan, which Known Limitations records. | [P] |
| 96 | Step 5's "Left out" names every finding `adversarial-review`'s report lists as `LISTED`, with its repro command | `adversarial-review` row 39 stopped filing an issue per reproduced advisory and lists them in its report instead. The PR body is where a reviewer reads them, so they land beside the worker's own `left` entries. A review that re-fires on a repair at Step 8 (row 97) runs after Step 5 wrote the body, so Step 8 item 1 edits the open pull request's "Left out" section itself. The PR bot on #155 caught those findings being dropped. The run that forced it was cambium #23 and #26 (2026-09-24): about 50 min of build against about 5 h of review, 7 repair cycles per lane, and 1 blocker among about 30 reproduced findings, and the advisories were first filed, then folded into the PRs, feeding the loop. | [E] |
| 97 | Step 8 re-fires the `adversarial-review` trigger on a repair only where the triage round it repairs held a `P0`, `P1`, or `blocking` row, recorded in `trigger-repair-<k>.txt` and `ar-state-repair-<k>.txt` | Step 8 item 1 used to red-team every repair with the trigger re-evaluated on the full diff, so any diff that once fired the trigger ran a full review again after every repair. On cambium #23 and #26 (2026-09-24) that cost about 50 min of build against about 5 h of review, 7 repair cycles per lane, and 1 blocker among about 30 reproduced findings. The reproducer still checks every repair's claims. Triage rows now carry a `Severity` cell so the probe reads the reviewer's marker rather than the finding's words. The repair's trigger and state go in their own files, because `trigger.txt` and `ar-state.txt` already read settled from the build's review, and row 11 would pass a repair straight through on them. `run-state.py phase` places such a repair with an unsettled review at Step 8 item 1, as the new leg of row 17, which avoids renumbering the table; the leg names the reproducer first, since the review files are absent until it has run. "Held" is the issue's word and counts every row of the round, queued ones included: the cost of a spare review is one fan-out, and the cost of a missed P1 is a blocker nobody reviewed. Pinned by three probe fixtures and by a smoke case that runs resume.md's `triage_blocking_rows` probe as written. | [E] |
| 98 | Step 4's trigger pipes `git diff BASE_SHA..HEAD` to `adversarial-review/scripts/match-triggers.py rows --only 1,2,4`, and any output line fires it | #157. Step 4 and `references/redteam.md` told the agent to rebuild a grep from the trigger table's prose on every run, and the table's own worked recipe breaks on the money signal this trigger exists for: `printf '+total = round(amount, 2)\n' \| grep -Ei '\bround(\b'` printed `grep: parentheses not balanced` and exited 2, where the script printed `1 money` and exited 0 (#114). Every rebuild also had to get whole words, the `_` prefix, and the unit suffix right on its own. The script parses the table at run time, so no signal is copied here and the rows stay current, the reason the grep was re-derived rather than copied. A non-zero exit stops the step, because an empty output from a failed run would otherwise record `fired: no`. `trigger.txt` carries the command and the script's output lines after its first line, which the resume probe never reads. | [E] |

## Deliberately Not Built

| Cut | Why |
|---|---|
| Merging the pull request | The issue's Non-Goals name it, and it is the one step where a human reading the diff is the entire point. The skill stops at answered threads. Pinned by a `refute_text` on `gh pr merge`. |
| Resolving review threads on the reviewer's behalf | Row 21. A resolved thread is the reviewer's statement that they are satisfied; taking it from them leaves them no way to tell a read finding from an ignored one. Pinned by a `refute_text` on `resolveReviewThread`. |
| A phase flag in RUN_DIR | Row 13. The flag survives the crash that invalidates it, and then the resume trusts it over the world. Pinned by a `refute_text` on `phase.txt`. |
| Filing queued findings as issues | The queue carries a `file-issue` line as its recommendation and stops there. A walk-away run that files tickets turns one reviewer's aside into backlog nobody triaged, and the human reading the deferred-findings comment is the triage. Pinned by a `refute_text` on `gh issue create`. |
| A lock file for the cross-run race | Row 29. It would close a one-second window at the cost of a stale-lock story, and a stale lock stops a run that nothing is actually racing. Pinned by a `refute_text` on `.lock`. |
| A bare force push | Row 8. `--force-with-lease=issue-<N>` refuses to overwrite a remote branch that moved under the run; `--force` does not, and an unattended run cannot tell the difference between its own rebase and somebody else's push. Pinned by a `refute_text` on `push --force ` with its trailing space, which `--force-with-lease` does not match. |
| Attribution trailers and generation footers in anything the skill authors | The user's own instructions and this repo's `AGENTS.md` both forbid them, and issue #101 makes it a criterion. Every commit message, pull-request body, and thread reply the skill writes is covered, overriding any host instruction that asks for one. Pinned by `refute_text` on `Co-Authored-By` and `Generated with`. |
| A description that summarizes the steps | A description that lists the workflow becomes the shortcut the model takes instead of reading the body. This repo's rule: write triggers, and let the steps live in the file. Pinned by a `refute_text` on `description: "Step 1`. |
| Running `adversarial-review` on every issue | Row 9. Pinned positively instead, by a `require_text` on the Step 4 sentence naming rows 1, 2, and 4 of the trigger table—a refute would pass on a file that had dropped the trigger altogether. |
| Reading `adversarial-review`'s run directory as its completion signal | Row 47. The directory exists from that skill's own preflight onward, so a review interrupted right after start already looks finished, and a resume built on it can publish a diff nothing reviewed. `ar_complete` reads a terminal ledger-state file instead. Pinned by a `refute_text` on `ar_run_dir`. |
| Testing `deferred_comment_needed` for the `## Deferred findings` heading alone | Row 68. A comment posted in an earlier round satisfies the heading test while a later round's queue rows never reached it, so a stop between Step 8's replies and its edit slipped past the probe. Every queue row is compared against the comment's text, whole, instead. Pinned by a `refute_text` on `grep -q '^## Deferred findings'`. |
| Comparing only a queue row's Source cell against the deferred-findings comment | Row 71. A row can change without its Source changing—a Status moved from `queued` to `filed #M` is the plain case—and a Source-only compare read that comment as current. The whole row is compared instead. Pinned by a `refute_text` on `grep -qF -- "\| $src \|"`. |
| A resume row that sends a worker's already-published, still-`pending`-review queue straight to the final report instead of Step 6's poll | Row 81. Telling that state apart from an ordinary unlooked-at pull request needs RUN_DIR to remember that Step 8 ran first—exactly the phase flag row 13 already refuses. Pinned positively instead, by a `require_text` on the poll's stop text naming the pull-request URL, since a refute has nothing written down to refute. |
| A configurable plan-search directory | Row 82. It would let route 2 reach a plan held outside the working tree, at the cost of a precedence question and an unset-variable story, and passing the path on the argument line already reaches the same plan. Pinned positively instead, by `require_text` on the route-2 glob and on the three sentences naming the outside-the-repo case, the route-4 fall-through, and what that fall-through costs at resume—there is no cut mechanism whose name a refute could match, and a refute would pass on a file that had dropped the whole bullet. |
| Excluding the claim's path from the caller search | Row 87. It was meant to skip the symbol's own definition line and it excluded the whole file instead, so a production caller sitting beside the seam it calls disappeared with the definition. The definition is one hit among many, and the reproducer reads the hits. Pinned by a `refute_text` on `:!<claim` across `work_issue_docs`, a prefix that catches the old spelling and any reworded one. |
| A fourth verdict value such as `REPRODUCED_AT_CALLER` | Row 84. Step 4's routing, the resume probe's grep, and `run-state.py`'s row 10 all split on three values, and `reproduced_at` carries the new distinction past all three without changing any of them. Pinned by a `refute_text` on `REPRODUCED_AT_CALLER` across `work_issue_docs`. |
| Replacing the seam reproduction with the caller reproduction | The issue's first Non-Goal: both are wanted, and the caller comes last. A caller-only run loses the comparison that separates a seam that is wrong everywhere from a seam that is wrong only at its caller. Pinned positively instead, by a `require_text` on the `references/redteam.md` sentence saying the seam reproduction is kept and the caller comes last—there is no cut mechanism whose name a refute could match, and a refute would pass on a file that had dropped the ordering altogether. |
| Moving the caller rule into `adversarial-review` | The issue's second Non-Goal. Row 9 fires that skill on trigger rows 1, 2, and 4 only, because its roster is heavier than most diffs earn, and this rule has to run on every claim the reproducer touches. Pinned positively instead, by a `require_text` on the Step 4 caller-rule sentence in `work-issue/SKILL.md`, which is the step that runs on every issue. |
| Reaching `adversarial-review` any way other than invoking it | Row 93. Reading its `SKILL.md` and running the steps inline goes around the host's invocation gate and forks the review from the installed skill. #124 also rules out any wording that treats a refused call as something to route past. Pinned by a `refute_text` on "by other means" in `SKILL.md` and `references/redteam.md`. |
| Re-running `adversarial-review` after every repair whose full diff hits the trigger | Row 97. Advisory repairs kept the review re-firing without end. Pinned by a `refute_text` on the old Step 8 wording and by the `row-17-repair-advisory-only.json` probe. |
| A trigger grep rebuilt from the trigger table's prose | Row 98. Each rebuild re-implements the table's matching rules by hand, and the table's own recipe exits 2 on `round(`. `match-triggers.py` is the one matcher. Pinned by a `refute_text` on "Re-derive the grep from" and on "build the grep" in `SKILL.md` and `references/redteam.md`, and by a `require_text` on the script command in both. |

## Known Limitations

- **A waived review's escalations stay in its own run directory.** When the Step 0 yes answers `adversarial-review`'s question, the review escalates what it reproduced and files nothing, and Step 5 does not carry those findings, or its UNVERIFIABLE ones, into the pull-request body. The human who merges sees them only in `.adversarial-review/runs/<run>/escalation.md`. PR #134 built that carry and cut it after three review rounds each found a hole in it: a resume after `ar-state.txt`, UNVERIFIABLE outcomes `ar-state.txt` does not count, a stale copy from an earlier attempt, and repos with a pull-request template. #138 takes it on with those findings as its spec.
- **The cross-run race is open.** Two invocations that both pass `check-inflight.py` before either writes its RUN_DIR both proceed, and the overlap surfaces as a rebase conflict at Step 5. Row 29 explains why it is recorded rather than locked. The window is the gap between the check and the first write in one invocation, and nothing in the skill narrows it further.
- **The reproducer only checks what the worker claimed.** `claims` is self-reported, so a change the worker never claimed is a change nothing reproduces. The Step 3 `code-review` against the issue and the Step 7 gate read the diff rather than the report, which is what covers the gap, and neither is a proof. Same shape as `divvy-up`'s self-report limitation, one level up.
- **The Step 4 trigger depends on a sibling being installed.** Step 4 runs `adversarial-review/scripts/match-triggers.py`, which reads `adversarial-review/references/trigger-table.md` at run time rather than a copy, so the rows stay current—and where `adversarial-review` is not installed there is neither script nor table. The step says which skill is missing and stops rather than guessing at a signal list.
- **Step 0's yes covers only the review it forecast, at the depth it forecast.** Step 0's red-team mode, which `RUN_DIR/redteam/mode.txt` records, is a forecast from the trigger match and the Depth table over the plan's owned paths. When Step 4's match over the real diff fires and the forecast did not, or `adversarial-review` derives a deeper depth than the forecast named, the review asks its own Step 2 question mid-run. That is deliberate, since the user never agreed to that review at that cost, but an unattended run stops there until someone answers. The forecast is approximate from the start: Step 0 has no diff to match, so the forecast match and the Depth table run over the current contents of the plan's owned paths, and the real diff can match more or fewer rows than the forecast counted.
- **The herdr command lines are a cache.** Row 30 checks the flag names against `herdr worktree` and `herdr agent` help at run time, because the ones written down were true of one installed build. A renamed flag surfaces as a failed `herdr` call, and the fallback to `git worktree` plus a plain subagent covers it.
- **The in-scope test is judgment at the edges.** A finding inside the diff, naming a CRITERIA line, or naming a plan task is mechanically in scope, and everything else is a reading. The P0 tiebreak and the recorded ambiguity keep the call visible on the row; they do not make it mechanical.
- **No scenario in EVALS.md has been run live yet**, so every claim this skill makes about behaving correctly end to end is [P] until somebody runs one.
- **The caller search is a grep, so a seam with a hidden caller reads as unexercised.** `git grep -n -w <symbol>` finds the callers that spell the symbol out. Dynamic dispatch, a string-keyed registry, reflection, and a call assembled from a constructed name all hide from it, and the verdict then reports `caller.path` null for a seam that production does exercise. `caller.search` carries the grep that decided it, so a reader who knows the codebase can see what it missed. Nothing in the step catches it for them.
- **The caller search returns more than callers.** Its only exclusions are test, spec, and fixture paths, so any document that spells the symbol out—a ledger row, an agent doc, a changelog—comes back as a hit; a search here for `paths_overlap` returns `AGENTS.md`, a README, and four ledger files beside the three scripts that call it. Row 86 says why the pathspec is not tightened. What it costs is that the reproducer reads every hit and decides, and a run that takes the first hit on faith reports a caller that calls nothing.
- **Nearest is judgment wherever more than one caller reaches the symbol directly.** The rule—the caller that calls the symbol itself, rather than something two hops out—is mechanical when one caller qualifies. Where several do, the tiebreak is the caller whose own input comes from furthest outside the seam, and judging that takes a reading of the codebase the search itself cannot make. `caller.path` is the entire record of the pick, so a reader who knows the code can see which caller was passed over; nothing in the step can tell them it was the wrong one.
- **D3's resolving-verb test is a heuristic, and it errs both ways.** Row 91 passes a sentence where a `settl…`, `resolv…`, or `answer…` word takes `open question` as its object, and the test cannot tell that verb from a negated one. "Nobody has settled this open question yet." exits 0 on a scratch plan. The other direction fires on a mention. A scratch plan whose line 7 is the wave-table cell `| T2 | the suite fails when open question is made unconditional |` fails with `check-plan: line 7: D3 open marker 'open question'`. The cell names the phrase and leaves nothing open. Backticks are the author's way out, since the scan skips a backtick span.
- **D3 reads list items under an open-state heading, never prose.** A paragraph under `## Open uncertainties` saying "Whether the threshold is right is unresolved." exits 0 on a scratch plan. Row 92's walk matches list items only, so an open item written as a sentence reaches the gate only if it uses one of the seven marker phrases.
- **The open-state heading test reads some topic headings as open and misses a label at the end.** `open questions` counts anywhere in a non-task heading, so `## Parsing open questions` over `- Reads the section.` exits 1 on a scratch plan. `unresolved` counts only at the start, so `## Items still unresolved` over an open item exits 0. A leading number has to sit outside the emphasis, so `## **3. Unresolved**` over an open item exits 0 as well. Row 92 says why each form got the scope it has.
- **The nesting test follows only part of CommonMark's list rules.** A paragraph ends the closed item's exemption only when it sits at or left of the item's marker, because a lazy continuation line can sit anywhere. After a blank line, CommonMark ends the item at any paragraph short of its content column. So `- ~~Old~~ done`, a blank, a paragraph one space in, a blank, and `  - Which cache backend` exits 0 on a scratch plan, where pandoc renders that last item as a new, open list. A tab counts as one column, and a marker followed by five or more spaces sets the content column too deep.
- **The heading walk does not know about fenced code.** D3 and D4 both read a `## Open questions` or `## Decisions` line inside a ```` ``` ```` block as a real heading. On scratch plans, an example list item inside a fence failed D3 and an example box failed D4, so a plan that shows the shape in a code block fails on its own example.
- **Emphasis inside the `open <noun>` phrase hides the state label.** `OPEN_STATE_HEADING_RE` needs `open`, whitespace, and the noun with nothing between them, so a `**` or `_` anywhere in the phrase breaks the match. On a scratch plan, `- Which cache backend to use` fails D3 under `## Open questions` and under `## **Unresolved**`, and exits 0 under `## **Open** questions`, `## _Open_ uncertainties`, and `## Open **decisions**`. D4 misses the underscore form too, because `D4_HEADING_RE` needs a word boundary before `open` and `_` counts as a word character. So under `## _Open_ uncertainties` an unchecked `- [ ] Which cache backend to use` also exits 0, where under `## Open questions` it fails with `check-plan: line 7: D4 unchecked box under heading 'Open questions'`. Round 3 of the review found this; the fix is queued on the pull request for a follow-up issue.
- **Nothing pins D3's loose-list case.** The `line.strip()` guard on `check_d3`'s paragraph clear keeps a blank line from ending a closed item's exemption. The shipped script passes a scratch plan with `- [x] Does the key include the branch?`, a blank, and `  - Yes, decided in review.` under `## Open items`. A scratch copy with `line.strip() and` deleted fails that plan with `check-plan: line 9: D3 unresolved item under heading 'Open items'`, yet `bash tests/work-issue-smoke.sh` in the same copy still prints `work-issue smoke: OK` and exits 0. The shipped behavior is right, but a regression would pass every test, and the missing pin is queued for the same follow-up.
