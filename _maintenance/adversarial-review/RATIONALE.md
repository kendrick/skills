# adversarial-review — rationale

Evidence tiers: **[E]** measured or observed in a real run, **[P]** practitioner reasoning from prior art, **[C]** convention inherited from this repo or its neighbors.

## Where This Came From

Four sources, one keeper mechanism each:

- **Pocock's `code-review`** — preflight discipline. Resolve the ref, confirm the diff is non-empty, fail in front of the user rather than inside subagents. Also its refusal to merge findings across axes.
- **`adversarial-reviewer` (alirezarezvani)** — names the self-review trap correctly. Its implementation, personas in one context, does not solve it.
- **A Claude Code Opus session** — genuine subagent isolation, a pre-declared out-of-scope list, escalating context across rounds, hand-verification of every blocker before acting on it.
- **The "MIL method"** — non-overlapping, domain-derived beats. The emotional frame is a confound rather than a mechanism.

All four stop at *report*. Findings arrive as results rather than hypotheses, and nobody reproduces them. That gap is what this skill exists to close.

## Decision Ledger

| # | Decision | Why | Tier |
|---|---|---|---|
| 1 | Verification gate is the confidence mechanism, not multi-agent agreement | Agreement between reviewers who saw the same code is correlation, not corroboration. Reproduction by an agent that never saw the finder's reasoning is independent evidence. This is also what makes disjoint territories coherent: with no overlap there is nothing to agree, so something else has to carry confidence. | [P] |
| 2 | Only REPRODUCED can block | An unreproduced finding is unproven, not merely minor. Letting it block means the reader does the verification the review skipped. | [P] |
| 3 | Territories are literal paths, not globs | The brief specified glob sets. The mechanism this repo actually has (`paths_overlap` / `owns_entry_problem`, agent-guild `check-diff-scope.py`) rejects `*` and `?` outright: "an entry is a literal path, and a pattern here would own nothing." That rejection is load-bearing for upstream issue #162, where a meaningless overlap answer put two agents on one tree. Amending the brief was cheaper than forking the predicate. | [E] |
| 4 | Predicate vendored byte-identical rather than imported | The canonical copy lives in a sibling repo and a version-pinned plugin cache; this skill installs standalone to `~/.claude/skills/`. Vendoring with a provenance comment is the only form that survives installation. Docstrings travel with the code because they record the incidents that set each rule. | [C] |
| 5 | Disjoint ownership, unioned lenses | Strict glob disjointness would make a file that is both authz and money lose one lens. Each file gets exactly one owning territory (so coverage accounting stays exact and overlap stays a hard error), and a territory carries the union of its files' suspicion classes. | [P] |
| 6 | No `disposition` field on a finding | The brief specified `disposition` starting at `UNVERIFIED`, alongside append-only JSONL. Those contradict: a field that later reads `REPRODUCED` is a mutation with extra steps. State is derived from appended events instead, which also preserves when each state landed and who landed it. | [C] |
| 7 | Merge-base SHA pinned in preflight | `git diff main...HEAD` re-resolves `main` on every invocation. Across a multi-day round loop, round 3 would review a different diff than round 1, silently. | [P] |
| 8 | Run dir excluded via `.git/info/exclude`, never `.gitignore` | `.gitignore` is tracked, so editing it adds a change to the diff under review and pollutes the next round's fix intersection. The path itself is resolved with `git rev-parse --git-path info/exclude` rather than hardcoded, because in a linked worktree `.git` is a file, not a directory, and the hardcoded spelling doesn't exist there. | [P] |
| 9 | Dirty tree stops the run | Verification evidence is only meaningful against the code the diff describes. An override is allowed and recorded as `dirty_tree_accepted`, so the report can say the evidence was gathered under that condition. | [P] |
| 10 | Verifier receives five fields, selected explicitly | `proposed_fix` is the finder's reasoning in another form; a verifier who reads it inherits the belief it was meant to test. Selecting five keys explicitly rather than deleting one means a field added to the schema later cannot leak in by default. | [P] |
| 11 | Unsafe repro commands are UNVERIFIABLE, not run | The verifier executes commands proposed by another agent. Read-only and test-runner commands only; anything that writes, migrates, deploys, or leaves the repo is refused with a recorded reason. | [P] |
| 12 | Failing test before the fix, for blocking findings | A test written after the fix is written by someone who already believes the fix works. Writing it from the repro command first is what makes the finding survive its own fix. | [P] |
| 13 | `file-issue`, looped once per advisory finding | `create-issue` does not exist; the rename to `file-issue` was deliberate (see that skill's ledger). It files exactly one issue per invocation, so N findings means N invocations rather than one call that quietly drops the rest. | [E] |
| 14 | UNVERIFIABLE back-pressure reports, never re-fans-out | A territory that is mostly UNVERIFIABLE is generating untestable claims, which is worth a human's attention and worth fixing in the trigger table. Making it trigger another round would make termination depend on a judgment call, and termination has to stay mechanical. | [P] |
| 15 | Depth governor (0/1/2)—announcement amended by row 34 | Row 34 turns the one-line announcement into Step 2's one question, asked at the same point; depth, its inputs, and the overrides stand as written below. A naive invocation should not cost what a full opus fan-out costs; the source session ran ~31 minutes. Depth is computed from trigger hits and diff size, announced in one line before any subagent spends a token, and overridable with `--fast` / `--deep`. Borrowed wholesale from `file-issue`'s depth ladder, which solves the same problem. | [E] |
| 16 | `disable-model-invocation: true`—superseded by row 34 | Row 34 reverses this once `work-issue` had to reach the skill; the reasoning below stands as the record of why it was first user-invoked. Three ambient skills (`code-review`, `security-review`, `grilling`) contend for review vocabulary. The failure modes are asymmetric: a misfire costs a long expensive fan-out on someone who wanted a quick read, a missed trigger costs one sentence naming the skill. Removing this one from ambient arbitration also makes the other three more reliable. Description is therefore human-facing, matching `handoff`. | [P] |
| 17 | Per-run ledger, not per-repo append-only | A per-repo ledger grows without bound and collides finding ids across runs. A run directory is self-contained and survives across sessions, which the round loop and human escalation both need. | [P] |
| 18 | Trigger signals match as whole words, never substrings | Found in the first live eval run. Substring matching filed a paging helper as a schema change because `page_index` contains `index`, and would file every function containing `generate` as a money change because it contains `rate`. A territory built on a false signal hunts the wrong thing with a straight face, and the derivation record makes it look deliberate. | [E] |
| 19 | The catch-all row is named `general`, not `tests` | Same eval run. It owns any file that matched nothing else, and those are rarely test files — a paging helper landing in a territory called "tests" reads as a mistake. Changed test files usually match their subject's row and land with the code they cover, which is the desirable outcome: one finder sees both the change and the test meant to catch it. | [E] |
| 20 | Preflight exclusions apply to the round loop's fix diff, not just round 1 | Found in the first live eval. `__pycache__` artifacts showed up in the fix diff and sent `intersect` to the scope-amendment branch over build output. An amendment rubber-stamped every round stops being a signal, which defeats the point of exiting non-zero on an unowned path. | [E] |
| 21 | Row order in the trigger table is the determinism mechanism | First-match by table order assigns each file its owner. Same diff, same assignment, on any machine and any model. Inserting a row therefore changes derivation for existing diffs, which is why insertion is a deliberate act that belongs in this ledger. | [P] |
| 22 | Fixes are committed before each round, and the cleanliness check re-runs at every fan-out | Step 7's `intersect` compares commits, so an uncommitted fix was invisible to it: the loop printed nothing and exited clean with the fix code never reviewed—a false-clean exit, not just contaminated evidence. The rounds are also exactly when the tree is most likely dirty, yet the porcelain check ran once at preflight. Surfaced by an external review of the skill. | [P] |
| 23 | Disposition routes on `claimed_severity`, bound at finding time | Step 6 routed on "REPRODUCED + blocking" without naming a source for blocking, which left the one gate that claims to be mechanical open to an ad hoc severity call at disposition. `claimed_severity` is the only severity in the system and is fixed before verification, so nobody re-litigates it with a stake in the outcome. The alternative—each trigger-table row declaring what blocks—trades finder miscalibration for class coarseness; it can layer on later as a floor if run data shows finders under-claiming. | [P] |
| 24 | A repro failing for a different reason than claimed is UNVERIFIABLE, never REPRODUCED | The repro command is finder-authored, so it is the one channel the finder still controls into the verifier: a command dying on a missing fixture exits non-zero just like the claimed defect would, and a refutation-stance verifier watching it fail could stamp REPRODUCED on a defect it never saw. The output has to show the named defect. The broken repro is not counter-evidence either—NOT_REPRODUCED needs evidence about the code, and a failed check is evidence about the check. Surfaced by an external review of the skill. | [P] |
| 25 | A failed territory reports UNREVIEWED, listed apart from undying blockers in escalation | The old text carried a failed finder "into Step 7's escalation," but `escalation.md` is only written at `--max-rounds`—in a run that otherwise exited clean, the failed territory surfaced nowhere and the report read like coverage. UNREVIEWED is a distinct verdict because nobody-looked must never read like looked-and-found-nothing, and a never-reported territory is a different kind of unknown than a blocker that survived three rounds. Surfaced by an external review of the skill. | [P] |
| 26 | Re-copied `owns_entry_problem`'s backtick and `](`-link refusals from upstream | Measured before the fix: `owns_entry_problem` returned None for a backticked entry and for a markdown-linked one alike, and `paths_overlap` answered False for the decorated form against the bare form of the same path, so decorated ownership passed as disjoint from the real thing it named. Fixed upstream in agent-guild commit ef20574, closing kendrick/agent-guild#232. The check belongs in the shared predicate rather than in either skill's own code because decision 4 already vendors this function byte-identical into both standalone installs — patching it in only one skill's copy would leave the other silently exposed to the same gap. | [E] |
| 27 | Method independence sits beside authorship independence, and a same-method reproduction lands UNVERIFIABLE | The gate said a finding must be reproduced by something that did not author it, which is authorship independence alone. In the wave behind issue #103 a shadow derivation shipped invisible for four rounds, every round green. The orchestrator red-teamed it and cleared it, having measured the composited result with the same linear OKLCH-lightness interpolation the implementation's own tests used. A browser composites per channel in sRGB, and the two diverge by up to 88% on a dark page, always in the flattering direction. The reproduction was independent of its author and identical in method, so it proved the code equals itself. An external reviewer caught it by questioning the measurement rather than the code. | [E] |
| 28 | Representation-boundary row, inserted at 6 above `general` | All five defects in that wave shared one structure: a value correct in the producing code's representation and wrong the moment something else evaluated it. Row 7's coverage lens is the nearest existing hunt and asks a different question—whether a test changed in the same direction as the code, not whether it measures the right quantity at all. A test can be written first, never change, and measure the wrong quantity for the life of the feature. Above `general` so a file whose only other match is row 7 gets hunted for it; per row 21 the placement is the deliberate act. Derivation run over a 72-file diff from this repo (a20b865) to price that placement: the row took ownership of no file, and the roster kept its shape. This row first reported that `general` kept all 46 it already had, which was wrong. That hand derivation matched `is_`, `has_`, and `max_` as whole words instead of as the prefixes the table defines one paragraph above the signals, which is the slip row 18 exists to prevent. Re-derived by a script built from the table's own rows: `general` 14, `state` 43, `representation` 0. Every legal reading of the rules gives representation nothing, so only the number moved. | [E] |
| 29 | The unit signals `px`, `rem`, and `em` get their own matching clause | Measured here. `grep -Ei '\bpx\b'` matches `const px = ratio * dpr` and never matches `padding: 12px`, because there is no word boundary between `2` and `p`. Under the table's existing whole-word rule the unit signals as the issue filed them would have gone blind to the stylesheets the fifth defect lived in. The clause gives them a form that fires. | [E] |
| 30 | `representation` is sonnet, and Depth 2 does not raise it | A representation bug is judged against what a consumer does with the value, which is stated behavior rather than intent, so it sits on the middle rung with schema and budget. Raising it at Depth 2 beside money, authz, and state would put an opus finder on any diff that touches a stylesheet. | [P] |
| 31 | The Depth 0 condition names `general` instead of a row number | Row 28 moved the catch-all row from 6 to 7 while the condition read "no row above 6 matched". Left literal it keeps firing Depth 0 on a diff whose only substantive hit is `representation`; bumped to 7 it reads correctly and goes stale at the next insertion, which is how row 19's number went stale. Naming the row states the intent directly: Depth 0 is for a diff that matched nothing but the coverage lens. The cost is real and belongs here — a stylesheet-only diff matching `representation` now runs Depth 1 behind an opus verifier where it ran Depth 0 behind a batched sonnet one. | [P] |
| 32 | The finder fills `proposed_repro` on every finding, including one with no independent route | Row 27's clause first told a finder with no route to say so in the claim "instead of proposing a command". No finder can obey that. `finding.schema.json` lists `proposed_repro` in `required`, and a findings file that fails the contract marks its territory failed, so the finding would have surfaced as UNREVIEWED—nobody looked—rather than as the UNVERIFIABLE the rule intends. Those two verdicts must never read alike. The finder now gives the closest command it has and names that command's route in the claim, which is the signal the verifier already acts on. Surfaced by an external review of this change. | [P] |
| 33 | Three further deviations from the issue's signal list: `parse(` over `parse`, consumer-side spellings over `render`, and no `export` | Split from row 29 because these were reasoned about and not measured, and a row claiming [E] for reasoning is the kind of thing this ledger exists to catch. All three over-match. `export` is the plain case: it is a whole word in every TS module, and row 6 sits above `general`, so it would have owned every changed file in a TS repo—a cap slot spent on a territory that tells a later reader nothing. `parse` and `render` are the same shape and narrower. Nobody has run the TS derivation that would make this [E]. | [P] |
| 34 | Model-invocable (no `disable-model-invocation`), superseding row 16 | `work-issue` reaches this skill at its Step 4 when a diff hits trigger-table rows 1, 2, or 4, and a user-invoked skill strips its description from siblings as well as from the agent, so the Skill tool refused that invocation outright (#124). That condition outranks the misfire asymmetry row 16 weighed, which was the right call while nothing but a human invoked the skill. Model-invocation adds agent reach without taking the human's away, so `/adversarial-review` still works, and the guard row 16 kept in the frontmatter moves to Step 2: one question before the fan-out, asked before any subagent spends a token. A wrapping skill whose own confirmation already put this review to the user, at this depth or deeper (row 35), answers that question on the user's behalf, which is how `work-issue`'s Step 0 yes covers it without a second prompt mid-run. The description carries triggers now rather than a human-facing summary, since agents read it too; for the contention row 16 named, this skill's description names `code-review`, `security-review`, and `grilling` as the ones to use instead. That naming runs one way. `code-review` and `grilling` are installed from outside this repo and their descriptions never name this skill, and `security-review` is a built-in with no `SKILL.md` to edit, so none of the three steers an agent away from here. Step 2's question is therefore the guard that actually stops a contention misfire. | [P] |
| 35 | A wrapping skill's confirmation answers Step 2's question only up to the depth it showed, and Step 2 prints the depth line itself, marked as answered | PR #134's review found row 34's waiver carrying a yes the user never gave. `work-issue`'s Step 0 put `adversarial-review (<row names>)` to the user with no depth, territory count, verifier, or out-of-scope list, yet that yes stood in for the question that carries all four, and the waiver said the wrapper printed a depth line that `work-issue` never printed. A forecast from a plan's owned paths can also come in under what the real diff derives, so a Depth 1 yes could fan out a Depth 2 run. A yes to a forecast is not consent to a costlier real run. The wrapper now names a forecast depth, and the waiver holds only where the depth derived here is at or below it; deeper, or no depth shown, and the question is asked. Where the waiver holds, this skill prints the depth line and the list itself and marks them answered, so the correction point lands in the run log rather than nowhere. Whoever prints it has to be this skill, since only Step 2 knows the derived depth. PR #134's second in-session review found the waiver naming no input: nothing said how the wrapper's depth reached Step 2, and a resumed `work-issue` run arrives with no conversation holding the yes, so the review would ask anyway. Step 2 now reads the depth from a confirmation line the wrapper hands over at invocation, listed beside FIXED_POINT under what each run resolves, and from that line alone. It is an argument rather than the ambient conversation, because an argument survives the resume and the conversation does not. `work-issue` is still named only as the example; any wrapper that hands over a line showing a depth gets the same waiver. A `--deep` run's line names `--deep`, and read as a flag that token would pin Depth 2 on a resumed run whose own flags no longer carry it, overriding the pass-through `work-issue` row 95 keys on the run's own flag. So the line is quoted text: flags come from the arguments around it, and `work-issue` sends it last, in double quotes, so the review can find where it starts. Pinned by `require_text` on the depth bound, on the named input, on the quoted-text rule, and on the Done-when that accepts the waiver only with the line printed. | [P] |
| 36 | A waived run escalates its blockers rather than fixing them | PR #134's Codex review found row 35's waiver fanning out on a scope the user never confirmed. `work-issue`'s Step 0 shows row names and a forecast depth, never the territories or the out-of-scope list, yet Step 2 called its own question the only chance to correct that list. The cost is worse than a noisy finding: Step 6 fixes a reproduced blocker, so a settled decision the list left out would be "fixed" unattended, undoing deliberate work. So the yes stays consent to the fan-out's cost, and Step 6 routes a waived run's blockers to `escalation.md` with `ESCALATED`, where the human merging rules on them; `work-issue` carries them into the pull request's "Left out" section. Two alternatives lost. Asking mid-run when the scope went unshown breaks `work-issue`'s one-confirmation promise, which round 1 of the same review made the PR keep. Having `work-issue` collect and show the list at Step 0 is the most faithful to consent, but it touches both skills and lengthens a confirmation that already carries four decisions. | [P] |
| 37 | A waived run takes all of Step 6 as a report: advisories escalate beside blockers, and an UNVERIFIABLE finding records `QUESTION_FILED` with a reason and no artifact | PR #134's third Codex round found row 36 covering blockers only, so a waived run still handed advisories to `file-issue`, which stops to confirm with nobody there to answer, and wrote inbox questions against a scope the user never confirmed. Every finding in a waived run can be a settled decision the list missed, so none of them leaves the run directory. `QUESTION_FILED` with a reason and no artifact is the path `--report-only` already takes, so the ledger needs no new disposition. | [P] |

## Deliberately Not Built

Each of these was considered and turned down: most came from the prior art, the rest were proposed by a change to this skill. A well-meaning edit is exactly how they come back, so the smoke test refutes the text of each.

| Cut | Why |
|---|---|
| "Each reviewer MUST find ≥1 issue" quota | Unterminatable in a multi-round loop, and it manufactures findings. The verification gate would then spend real time refuting them. |
| Severity promotion on multi-agent agreement | Meaningless once territories are disjoint — there is nobody to agree with. Verification replaces it. |
| Personas as identity (Saboteur, New Hire, Security Auditor) | A persona reviews a money change and an authz change with the same posture. A suspicion class carries the actual question. Their checklists were mined for trigger-table rows; their identities were not kept. |
| A single cross-territory verdict | One axis passing must not mask another failing, and a combined verdict is exactly the artifact that lets it. |
| Emotional or roleplay framing | Not portable to a client repo, and it optimizes for finding volume over calibration. |
| Cross-territory dedupe and re-ranking at ledger time | Two territories reporting one bug is information about the bug. Re-ranking reintroduces the single ordering per-territory verdicts exist to avoid. |
| An `independent_route` field on the event, or a second verifier pass proving independence | Row 27 is a rule about which verdict a verifier may record, and `UNVERIFIABLE` with a reason already carries the case it names. A field would hold the verifier's own account of its independence, with nothing to check that account against—the same tautology one level up. |
| A `NO_INDEPENDENT_ROUTE` sentinel in `proposed_repro` | Row 32 needed the field filled, and a reserved token is the obvious way to fill it. It tells the verifier what the claim already tells it, and it splits the field into two types for every later reader of the ledger, all to save one sentence. `proposed_repro` holds a command someone can run; a value that is not one has to be special-cased by everything that reads the field. |
| User-invoked frontmatter: `disable-model-invocation`, and a description that says to run only when the user types the name | Row 34. `work-issue` invokes this skill at its Step 4, and either one makes that call fail: the flag strips the description from siblings, and the description tells the agent to wait for a user who isn't there. The misfire guard is Step 2's question. Pinned by a `refute_text` on the bare `disable-model-invocation` key and on the old "Use ONLY when the user explicitly invokes" sentence. |

## Known Limitations

- **Territory derivation is grep-driven.** A money bug in a file whose hunks name no money signal lands in whatever territory its first matching row gives it, and gets that territory's lenses. Row 7 (`general`) catching every file limits the damage but does not eliminate it.
- **The verifier trusts its own environment.** A repo whose test suite cannot run in the agent's environment produces UNVERIFIABLE across the board. The calibration signal reports this; it cannot fix it.
- **`--report-only` runs the full cost.** It skips the actions in Step 6, not the fan-out or the verification. Use `--fast` to spend less.
- **The method rule is prose, and no script gates it.** `ledger.py` requires a command and its output before it will record REPRODUCED, and it cannot tell whether that command re-derived the quantity the way the code does; deciding that takes a reader. So the smoke test pins the rule's presence in the documents, which is a claim about the documents rather than about what an agent does with them. Scenario 15 is the only check that reaches the behavior, and it has not been run.
- **Three rounds is a guess.** The cap comes from one session where two of three blockers were fix-introduced. It has not been calibrated across repos.
