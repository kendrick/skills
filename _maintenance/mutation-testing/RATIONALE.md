# mutation-testing—rationale

Evidence tiers: **[E]** measured or observed in a real run, **[P]** practitioner reasoning from prior art, **[C]** convention inherited from this repo or its neighbors.

## Where This Came From

One observation and two rounds of evidence on top of it.

- **kendrick/skills#108**, the issue. Two `code-review` passes across four subagents returned nothing that changed a line on a ticket whose entire content was a new rejection rule. An unprompted mutation run on the same code produced four measurements in minutes, including the one that became the most useful sentence in the pull request: hoisting a read out of its write transaction fails exactly one test and leaves 62 green. Reading a diff cannot produce that, and nothing in the toolkit asked for it.
- **The three rules in that issue**, each learned by getting it wrong in that run: mutate the plausible refactor rather than an arbitrary break (21 meaningless failures versus 1 that mattered), restore by file rather than by directory (`git checkout -- app/storage/` reverted the new test alongside the mutation and the suite came back green without it), and re-measure after the suite grows (one mutation moved from 2 failures to 4 once a later test began exercising the same return value).
- **The issue's own comment**, offering two later measurements as evidence rather than as an edit to the criteria. Both were folded in: the plausible-refactor rule turns out to have a scope, and mutations running one at a time is a boundary worth writing down.
- **kendrick/cambium#84 and kendrick/cambium#95**, whose whole content is a guard on a `package.json` script, mutation-tested three times across two rounds by hand because no skill asked for it. The mutation that found the defect was an arbitrary break the plausible-refactor rule says not to try: `oxfmt --write . >/dev/null && oxfmt --write` rewrites the whole tree with the summary discarded, then formats the one file it was handed and reports `on 1 files`. The guard had been reading the file count oxfmt prints about itself, so it passed while the tree-wide write it exists to catch had already happened. A second round repeated the shape with a write scoped to a nested path.
- **kendrick/cambium#87 and cambium's `docs/agents/testing.md`**, which already require the check—mutate the branch, confirm a test fails—and are where the verdict question was already live before this skill existed.

## Decision Ledger

| # | Decision | Why | Tier |
|---|---|---|---|
| 1 | Model-invoked, with a trigger-bearing description and no `disable-model-invocation` | The issue's premise is that nothing in the toolkit asks for this measurement, so a missed trigger costs a guard its only check, while a misfire costs one mutation run and a restore. That is the inverse of the asymmetry behind the user-invoked `handoff`, `eli5`, and `jira-refine`, where a misfire costs more than the one word a miss costs the user. `adversarial-review`, `divvy-up`, and `work-issue` are model-invocable too, for a different reason: a sibling skill has to reach them. The body says so in one line, the way the user-invoked siblings justify themselves. | [P] |
| 2 | Two mutations per guard, one per question, rather than one rule carrying a caveat | The output shapes differ and neither mutation can answer the other's question: a plausible mutation measures how much of the suite notices a refactor, and an adversarial one returns a verdict on whether the guard watches the right quantity. Measured across three by-hand rounds on kendrick/cambium#84 and #95, where the plausible half stayed silent through both rounds that found the defect. Folded into one skill rather than split into two, per the user's call on the issue comment. | [E] |
| 3 | The refactor question takes the plausible mutation, never an arbitrary break | Measured in the run behind #108: mutating a read into a form that stalls under the test double produced 21 failures and meant only that the test double was load-bearing. Hoisting the read above its transaction—the version a reviewer would approve without comment—fails 1 test, and that one test is the answer. | [E] |
| 4 | Restore by name, one path at a time, with `git status --porcelain` diffed against a pre-mutation snapshot; row 19 moves the restore itself off `git checkout` and onto a copy-back, and row 16 adds the contents comparison | `git checkout -- app/storage/` reverted the new test alongside the mutation, and the suite came back green without it. That was caught on the test count rather than by noticing, so the honest description is a near miss. The snapshot comparison is what turns a near miss into a hard stop, because the failure is invisible in a passing suite and nothing else in the loop looks at it. | [E] |
| 5 | Every mutation re-runs against the final suite before any count leaves the run | One mutation moved from 2 failures to 4 once a later test began exercising the same return value. Extrapolating from the earlier run puts a wrong number in a pull request, where it is read as a measurement. The bound is any edit to a test file, not any edit at all, and the trigger is a number leaving the run rather than the run ending. | [E] |
| 6 | Counts are quoted from the runner's own summary line, verbatim, and nothing parses it | Runners disagree about what they report and about how they phrase it, so a parser would be wrong per project and silently. The line as printed is what a reader can check against their own run. This is the consumer's-units rule applied to a test runner: assert what the runner said, not a count reassembled from it. | [P] |
| 7 | A red baseline stops the run | A failure that was already there is indistinguishable from one a mutation caused, so every count downstream inherits the ambiguity while still reading as a measurement. | [P] |
| 8 | No script. Choosing a mutation, reading a verdict, and ruling on an equivalent mutant all stay in the prose | Choosing the plausible refactor is judgment, the suite is the project's own command, restore is a copy-back, and the restore check is a `cmp` against the backup beside a `git status` comparison. None of it wants a diff tool, and this repo's rule is that where judgment is the job it stays in the prose. | [C] |
| 9 | The name keeps "mutation" despite this repo's other sense of the word | `work-issue`'s "Prove the mutation, then read the result" means an edit that may have silently failed to apply. The issue title asks for `mutation-testing`, the term is the industry's, and both senses want the same discipline, so the preamble draws the line in one sentence rather than the skill taking a worse name to avoid it. | [C] |
| 10 | A `slipped` adversarial verdict that names a real gap is reported first, ahead of every measurement; row 17 carves out the equivalent mutant, which is reported unresolved instead | It is a defect in the guard rather than a measurement of the suite, and the two read differently: a measurement invites a judgment call about coverage, while a slipped verdict says the guard is watching the wrong quantity and the numbers beside it describe nothing. | [P] |
| 11 | The applied mutation is proved on disk before the suite runs; row 20 moves that proof off `git diff` and onto the backup | An edit that silently failed to apply leaves the suite green, and a green suite reads in this skill as "no test catches this"—the exact inverse of what happened, arriving as a confident measurement. `work-issue` carries the same rule for the same reason, after losing five edits that way in one session. Not in the issue or the plan; added while writing Step 3, because this skill's output is precisely the reading a silent failure corrupts. | [P] |
| 12 | Step 3's copy-aside goes outside the repository | A backup beside the file is untracked, so it appears in the `git status --porcelain` that item 5 compares against the snapshot, and item 5 halts the run on it. The skill's own scaffolding would have failed the skill's own check, on every mutation. Surfaced by the Standards axis of this branch's `code-review`, reproduced on a scratch repo as `?? app/storage.py.orig`. | [E] |
| 13 | BASELINE is re-taken at Step 4, from a tree that has just passed the restore check, and only there | Decision 5 requires a re-measure once the suite grows, and decision 4 stops the run whenever status differs from the snapshot. A grown suite *is* a status difference, so the two decisions deadlocked: the re-measure halted on its first mutation at exactly the moment it existed for. Reproduced by snapshotting a clean tree, adding one test file, and diffing. The ordering is load-bearing in the other direction: re-snapshotting a tree with a mutation still applied bakes it in, and decision 4's check then passes for the rest of the run with the mutation live on disk. Verified both halves on a scratch repo before writing the fix. Surfaced by the Spec axis of this branch's `code-review`. The first wording of the confirmation step read two ways, and as a check run at Step 4 time it fails by construction, since the grown suite is itself the difference it compares against; a red-team round caught that and it now says the confirmation is a fact about the finished cycle. | [E] |
| 14 | A guard with no pinning test gets no mutation, only its Step 1 row | Step 1 already reports it and says there is nothing to measure, but Step 2 asked for two mutations per guard and its completion criterion said *every* guard. Mutating an unpinned guard returns zero failures, and zero failures is the reading this skill assigns to a real gap, so the untested guard and the uncovered one arrived in the report indistinguishable. Surfaced by the Spec axis of this branch's `code-review`. | [E] |
| 15 | A dirty tree is snapshotted rather than refused, diverging from `adversarial-review` and `divvy-up` | Both siblings stop on a dirty tree, and both review or dispatch against committed work. This skill fires *during* the work, on a guard somebody just added and has not committed, so refusing a dirty tree would refuse every run the trigger describes. The snapshot is what replaces the clean-tree precondition: decision 4 compares against the tree as it stood, not against HEAD. Raised as a sibling divergence with no ledger row by the Standards axis of this branch's `code-review`, then again by `adversarial-review` as a load-bearing sentence no test pinned — measured: reverting it to the siblings' wording left the whole suite green. The smoke test now requires the sentence and refutes both sibling spellings of the precondition, since the reintroduction would arrive in their words rather than in one invented here. | [E] |
| 16 | The restore check compares file contents against the backups, alongside the `git status --porcelain` comparison | Decision 15 tolerates a dirty tree, and decision 4 verified restoration by status alone. Together they left a hole: `git status --porcelain` reports status codes rather than contents, so a tracked file the user had already modified reads ` M path` before the mutation and ` M path` again after a restore that silently failed. Measured on a scratch repo — the comparison printed no difference while the hoisted read was still on disk. Not an edge case: this skill fires on an uncommitted new guard, so the already-modified file is the normal path. Both checks stay, because neither sees what the other does — the contents check is blind to a casualty among files no mutation touched, which is the near miss decision 4 exists for. Surfaced by Codex on the pull request. | [E] |
| 17 | A `slipped` adversarial verdict is a defect only where the mutation changed observable behaviour; an equivalent mutant is reported unresolved | Decision 10 made every `slipped` verdict a defect, while this file's Known Limitations, the README's Gotchas, and EVALS scenario 6 all said an equivalent mutant needs a human ruling — and `SKILL.md`, the document that actually executes, contained no instruction to mark such a row. Three documents promised a report behaviour the fourth never implemented, so the run would have called a sound guard defective. Surfaced by Codex on the pull request. | [E] |
| 18 | The contents check compares mode as well as bytes, against the backup rather than against HEAD | `cmp` is blind to mode, and on a file already modified before the run the status comparison is too, so a restore that put the bytes back and dropped the executable bit cleared both checks. Measured on a scratch repo: `cmp` exit 0, status identical, `git diff --summary` reporting `mode change 100755 => 100644`. Comparing against HEAD rather than the backup fails in the same dirty case row 16 describes — where the user's own uncommitted work carries the mode, `git diff --summary` prints nothing while the restored mode is wrong, so all three offered checks clear a failed restore. Surfaced by an external review of the fix for row 16. | [E] |
| 19 | The restore is a copy-back from the external backup, and `git checkout` is named as forbidden rather than merely unmentioned | The step said "restore by name" and the only concrete command anywhere near it was `git checkout`. On this skill's own tree that is destructive: the index holds the state before the guard existed, so `git checkout -- guard.py` against an unstaged new guard replaces it with the committed version and the user's work is gone — the very work the run was invoked to measure, and the backup is the only other copy of it. Reproduced on a scratch repo before the fix. The README and row 8 both already called restore a copy-back, so the executing document was the one out of step. This is the lossy-on-a-user's-files case `AGENTS.md` rates P0. Surfaced by Codex on the pull request. | [E] |
| 20 | The mutation proof compares against the backup, never `git diff -- <path>` | Decision 11 proved the edit landed with `git diff`, which is non-empty on this tree because of the guard itself, so the proof passes for a mutation that never applied. The suite then runs unchanged and the row reads "no test catches this" — the false-coverage-gap reading decision 11 exists to prevent, arriving through the case decision 15 made normal. Measured: with the mutation a no-op, `test -n "$(git diff -- guard.py)"` passed while `cmp` against the backup reported the file identical. Surfaced by Codex on the pull request. | [E] |
| 21 | Each check in Step 3 names exactly one comparison, with no alternatives offered beside it | Three separate times an alternative offered as equivalent turned out to be weaker than the check it sat beside, and each one silently cleared the failure that check existed to catch. `git diff --summary` for the mode comparison reports against HEAD, so it saw nothing where the user's own work carried the mode. A grep for the inserted text proved a mutation landed whenever that text already appeared in the user's uncommitted work. `git diff -- <path>` proved it whenever the guard itself was uncommitted. All three were measured, and the pattern is the same: an alternative phrased as a convenience becomes the route a run actually takes, so the weakest option is the one that runs. Surfaced across two rounds of Codex review and an external pass. | [E] |
| 22 | Backups mirror each path's full position in the repo, are taken with `cp -p`, and are restored with the mode set explicitly; row 30 moves that to `cp -Pp` so a symlink survives | The backup became the sole authority for the mutation proof, the restore, and the mode comparison, while item 1 still said only "copy to a scratch location". Flattening to basenames collides: `app/storage.py` and `lib/storage.py` share one backup, the second copy destroys the first, the restore writes one file's contents into the other path, and both of item 5's checks pass — measured, with the user's uncommitted guard gone and nothing in git holding a copy. `cp` without `-p` also fabricates the backup's mode through the umask, so row 18's comparison runs against an authority the source never had; and `cp` onto an existing file keeps the destination's mode, so a correct-bytes restore leaves the mode wrong and row 18 halts a valid run. Surfaced by a reproducer after two Codex rounds and two external passes. | [E] |
| 23 | DIFF is resolved once, before Step 1, and the invoked-by-name case names a merge-base | Nothing in the skill said which diff it reads. On the dirty tree it resolved by luck to the working-tree diff, but the README advertises invoking the skill by name against work already on the branch, where the tree is clean and `git diff` is empty, and the agent would have to guess between HEAD~1, a merge-base, and the whole branch. The README promised a mode the executing document did not implement, which is row 17's shape exactly. | [P] |
| 24 | DIFF includes untracked files, read as wholly added | `git diff` and `git diff --cached` between them see nothing of a file that was never added, and a new guard with its test in new files is an ordinary shape — arguably the most ordinary one, since a guard often arrives with the module it guards. Measured: two new files, `git diff` zero bytes, `git status --porcelain` showing both. Step 1 would have found no guards and the run would have finished reporting success having tested nothing, which is the one failure mode worse than a wrong measurement. An untracked path also makes row 22's backup the only copy in existence, since the index holds no version to fall back on. Surfaced by Codex on the pull request. | [E] |
| 25 | The eval fixture commits a guardless base, and each scenario applies its guard as uncommitted work | The fixture described the repo *after* the work and told the tester to commit it clean before every scenario, so there was no diff adding a guard and Step 1 would find nothing. Every scenario would have passed vacuously while exercising none of the behaviour it names — the same defect as scenario 4's unfailable setup, one level up, at the fixture every scenario shares. Surfaced by Codex on the pull request. | [E] |
| 26 | The collateral snapshot is `git status --porcelain -uall` | Without `-uall`, git coalesces a wholly-untracked directory into one `?? dir/` line, so a file deleted inside it leaves the snapshot byte-identical and the collateral check sees nothing — precisely the casualty that check exists to catch, in the case row 24 had just brought into scope. Measured: `?? pkg/` before and after deleting `pkg/b.py`; `-uall` catches it. Surfaced by an external pass. | [E] |
| 27 | Each eval scenario's uncommitted work is the guard and its pinning test together | Row 25's guardless base strips the pinning tests, and row 14 gives an unpaired guard no mutation at all. Together they made every scenario vacuous while looking like a pass: Step 1 reports a guard with no test, Step 2 runs nothing, and no scenario's pass condition can occur. Two fixes from this branch colliding, which is the shape most of this branch's later defects have taken. Scenario 6 is the deliberate exception, since an unpaired guard is the condition it checks. Surfaced by an external pass. | [E] |
| 28 | Ignored files stay out of DIFF, and a guard found in one stops the run | `--exclude-standard` drops them, so row 24's "every untracked file" overstated what it delivered. Keeping them out is the right call rather than a gap to close: the restore snapshot cannot see an ignored path either, so including one would mean measuring a guard the run cannot verify it put back, which is worse than declining. The run says so and stops instead. | [P] |
| 29 | An empty GUARDS stops the run, naming what DIFF resolved to | Every defect found on this branch has been a different route to one outcome: the run finishes, reports success, and measured nothing. Untracked files omitted from DIFF, an eval fixture whose guards were already committed, a guardless base that stripped the pinning tests, a merge-base resolving somewhere unintended. Each was closed individually and the next one arrived. The routes are not enumerable, so the stop sits at the point they all converge, where GUARDS is empty and the run is about to produce a report with no rows — which is indistinguishable from a report of a run that measured something and found the suite adequate. Surfaced by a reproducer that asked what else could reach the symptom rather than what else could cause it. | [P] |
| 30 | Backups use `cp -Pp`, and link identity is checked in place of contents for a symlinked path | `cp -p` follows a symlink rather than copying it, so the backup holds the target's bytes and a restore writes a regular file where the link was. Both existing checks clear that: `cmp` reads through to the same bytes, and the two sides agree on mode because both are now ordinary files. Measured, including the restore destroying the link. `cmp` cannot be repaired into covering it either — on a surviving link it compares the target, and a relative link in the backup resolves against the backup directory where the target does not exist, so it errors rather than answering. Surfaced by Codex on the pull request. | [E] |
| 31 | A pinning test is a trigger, never an entry in GUARDS | Step 1's definition ended with "or a test that pins one of those", which is right for deciding whether to fire and wrong for deciding what to measure. Listed as a guard, a pinning test gets looked up for a test that pins *it*, reported as untested, and Step 2 then either mutates the test instead of the code under review or leaves a spurious untested-guard row in the report. Sharper under row 27, where every scenario's uncommitted work is a guard and its test, so every scenario would have carried the phantom row. Surfaced by Codex on the pull request. | [E] |
| 32 | A runner that reports no passing count gets `passed: not reported by <runner>`, never a reconstructed number | The report row required a passing count while the paragraph beside it forbade assembling one by hand, and `go test ./...` prints `ok example.test/count 0.392s` with no total — measured. That left an agent three bad options: fabricate, break the stated rule, or fail the completion criterion. An absent number is a fact about the runner and reads as one; a count reconstructed by counting test functions reads exactly like a measurement, which is the single thing this skill exists not to produce. Surfaced by Codex on the pull request. | [E] |
| 33 | The smoke suite counts `git checkout` occurrences in `SKILL.md` rather than refuting spellings of it | Row 19 forbids restoring with `git checkout`, and the suite pinned the prohibition's text plus one refute on `git checkout -- .`. Neither stops a reintroduction: adding `Restore each named file with git checkout -- "$path"` to Step 3 left the suite green, measured. Row 21's lesson applied to the test instead of the document — enumerating spellings of a mechanism is how a mechanism gets back in. The count is four, every one cautionary, and it goes red on a new mention in any wording *and* on the quiet deletion of an existing one, which a refute could never catch. | [E] |
| 34 | The README names `git status --porcelain -uall`, not the bare command | Row 26 made `-uall` load-bearing in the executing document and left the README naming the bare form, so a reader following the README's restore check would certify a lossy restore inside a wholly-untracked directory. Sixth instance on this branch of a fact stated in one of the four documents and not swept into the others, which is why row 33 prefers a derived check to a pinned string wherever one is available. | [E] |
| 35 | A symlinked path is refused rather than handled | Two handlings were tried and each was worse than the last. `cp -p` dereferences the link, so the backup held the target's bytes and the restore wrote a regular file where the link was, destroying it while every check passed. `cp -Pp` backs up the link instead — and a mutation is written *through* a link, so the target it lands on is never backed up at all: the restore puts the link back, the contents check was scoped away from symlinks, and the mutation stayed permanently on the user's file with all three checks green. The second was strictly worse than the first, and both were measured. Refusing is one line and is correct for every case; handling is correct only when every column of `references/what-each-check-sees.md` is filled in, and it was not. | [E] |
| 36 | Step 3's items are named rather than numbered, and the suite refuses a numbered cross-reference | The numbers shifted twice, and each time every "item 5" elsewhere in the four documents went stale without anything noticing — the sweep-failure family in its most mechanical form. Names do not shift when a step is inserted. The suite counts numbered item references in `SKILL.md` and requires zero, which is a derived check rather than a pinned string, per row 33. | [P] |
| 37 | `references/what-each-check-sees.md` states what each Step 3 comparison can and cannot see, per kind of path | Nearly every defect in this skill was a change to one comparison made without asking what the others still covered: mode fixed without asking about links, links fixed without asking what a write through a link touches, untracked brought into scope without asking what `porcelain` collapses. The rules existed only as prose, so each new one was checked against the paragraphs beside it instead of against the set. The table is the artifact a change gets checked against, and it makes "prefer refusing to handling" concrete: a row with empty columns is a row that gets refused. Contradicts the plan's "no `references/` unless the worked example runs long", deliberately. | [P] |

## Deliberately Not Built

Each row below is pinned by at least one `refute_text` assertion in `tests/mutation-testing-smoke.sh`, because every one of them is a reasonable-sounding idea and a well-meaning edit is exactly how it comes back. The assertions pin the mechanism's ordinary wording as well as its tooling names: the first version pinned only CLI tokens such as `--parallel`, and prose re-adding the same mechanism without them passed green.

| Cut | Why |
|---|---|
| Parallel mutations, or a worktree fan-out for speed | Restore is already the fragile step at concurrency one. Two mutations sharing a tree restore against the same snapshot and race over the same paths, which turns a fragile step into a correctness bug that reports a clean run. The only safe shape is a worktree per mutation, and the boundary is written down so the cheap version does not get built by accident. |
| One git worktree per mutation, so restore is free rather than checked | A different idea from the fan-out cut above: not parallelism, but using a throwaway worktree as the unit of isolation, which would delete the backup, the proof, the restore and both checks in one move — the layer where essentially every defect in this skill has been found. Not built here because it changes what the skill fundamentally is: it would measure a copy of the tree rather than the tree the user is working in, which breaks the untracked and uncommitted cases the trigger is built around, and a worktree does not carry uncommitted work across on its own. The defect history is real evidence for it, so it is filed rather than settled, and this row records that it is open rather than declined. |
| A mutation-operator engine in the `mutmut` or Stryker shape | Those generate arbitrary breaks exhaustively, which is the one thing decision 3 measured as worthless here: 21 failures that meant nothing. The mutations worth running are the plausible refactor and the adversarial route, and both are readings of the code rather than enumerations of it. |
| A persisted run directory | The report is the artifact and it goes in the reply. A run directory would hold counts that decision 5 already says go stale the moment a test file changes, which is a stored measurement with a shelf life nobody can see. |
| Parsing the runner's output into structured numbers | Decision 6. A parser is wrong per project and silently, and the summary line is quoted precisely so a reader can check it. |
| Mutating anything the diff did not add | The trigger is what the diff adds, and a run that wanders into surrounding code produces findings about work nobody is reviewing, on a budget the guard's own check was meant to spend. |
| Restoring with `git checkout` in any form, by directory or by name | Decisions 4 and 19. The directory form is the one that reverted the new test alongside the mutation. The by-name form is worse and less obvious: on this skill's own tree it replaces the user's uncommitted guard with the committed version, and the backup is the only other copy. |

## Known Limitations

- **A guard with no test yields a row, not a mutation.** There is nothing to measure, so the absence is reported and the run moves on. Whether the guard should have a test is a judgment for the reader.
- **Suite runtime bounds how many mutations a run can afford.** Two mutations per guard against a suite that takes ten minutes is twenty minutes per guard, serially, and Step 4 re-runs all of them. On a slow suite the honest move is fewer guards per run rather than fewer mutations per guard.
- **The snapshot sees tracked and untracked paths, never ignored ones.** It sees untracked paths only because of `-uall`, per row 26: the bare `git status --porcelain` coalesces a wholly-untracked directory into one line and names nothing inside it. Ignored files it cannot see at all, so a mutation that writes into an ignored build directory restores unverified — which is why row 28 keeps ignored paths out of DIFF rather than measuring what cannot be checked back.
- **An equivalent mutant reads as `slipped`.** A mutation no test could distinguish, because it does not change observable behavior, produces the same verdict as a real gap in the guard. Telling the two apart is a human's ruling, and the report says which rows are waiting on one.
